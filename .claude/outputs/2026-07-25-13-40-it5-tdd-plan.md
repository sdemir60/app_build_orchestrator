# It-5 Implementation Plan — Perf + dağıtım + docs (v7 Part C It-5, SON iterasyon)

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development` ile task-by-task uygula. Adımlar checkbox (`- [ ]`) ile izlenir.

**Goal:** v7 Part C It-5'i kapatmak: K11 perf modellerinin motor yarısını (process priority + inner Job CPU rate cap) gerçekten uygulamak, graf ve listeyi 500–1000 ölçeğinde akıcı hale getirmek (It-4 acceptance #5'i kapatmak), token sözlüğünde son drift geçişini yapmak, çalışan bir `dotnet publish` çıktısı üretmek, README + trust-boundary belgesini yazmak; ardından ertelenen tüm GÖZLE KONTROL borcunu tek yürünebilir listeye dönüştürmek.

**Baseline (bu plan yazılırken doğrulandı):** `main` @ `1b63720` (= `origin/main`), build **0 uyarı / 0 hata**, suite **1203 passed / 1 skipped / 0 failed** (tam koşum, canlı OSYS acceptance 3/3 dahil; non-acceptance 1200). Branch: **`it5-perf-dist`** (main'den).

**Keşif kaynağı:** 7-lens paralel kod taraması (2026-07-25) — bulgular aşağıdaki task'ların "Bugünkü durum" bloklarına dosya:satır olarak işlendi.

---

## Global Constraints

Her task'ın örtük gereksinimi. İhlal = plan ihlali.

- **v7 yasakları:** in-process MSBuild YOK · ortak OutDir okunmaz/yazılmaz · stdout YALNIZ NDJSON [D4] · **D8: sleep-poll / sabit `Thread.Sleep` YASAK** (deterministik seam + IOCP/TCS randevusu) · **K1: git salt-okur** (checkout/pull/reset ASLA) · §3 nested-Job cascade ≤2sn ve "torn DLL yok" garantisi bozulmaz.
- **MOTION SÖZLEŞMESİ (bağlayıcı):** kod-tarafı animasyonlar `MotionSettings.Effective(base)` / `AnimationsEnabled`'ı animasyon **BAŞLARKEN TAZE** okur (`Controls/MotionTokens.ResolveDuration/ResolveKeySpline` tek kapı). Saf-XAML Storyboard süreleri `{DynamicResource Duration.X}`, **`{StaticResource}` ASLA**. Hardcoded hex/ms **YASAK**.
- **A13.2 HARFİYEN:** koleksiyon reset YASAK · frozen/paylaşılan brush anime edilemez (template-lokal brush) · `ContainerVisual.Opacity` animate edilemez (katman host'ları UIElement) · akan dash **HER ZAMAN** UIElement `Path` · dash birimi = `StrokeThickness` çarpanı · graf etiketleri `TextFormattingMode=Ideal` · `DragDrop.DoDragDrop` YASAK · tooltip `InitialShowDelay=0` + `CustomPopupPlacementCallback` · Window'da `AllowsTransparency` **ASLA**.
- **REALIZE TESTİ ZORUNLU (It-4b dersi — `c6e9a21`):** yeni XAML kökü/şablonu ekleyen **veya mevcut bir kökün token tüketimini değiştiren** her task, `DsResources.NewHost()` + `Realize()` ile bir realize testi de ekler. Gerekçe: headless suite XAML runtime çözümlemesini görmez; kullanıcının görsel pası en sona ertelendiği için bu testler tek güvenlik ağıdır.
- **Görsel otorite = design-v1** README + prototip; davranış semantiğinde çelişkide v7 kazanır. Kopya metinleri BİREBİR.
- **Sayı/tarih formatlaması `CultureInfo.InvariantCulture`.**
- **Test kuralları:** metot adı `Snake_case_tam_cümle` · sınıf başında `[Tnn]` etiketli `<summary>` · WPF'e dokunan test `[StaFact]` · kaynak yükleme `DsResources` (pack:// headless'ta çözülmez) · bekleme `App/DispatcherPump.PumpUntil` · WPF çekişmeli sınıflar `[Collection("Console UI (serial)")]`.
- **Suite yeşil kalır:** her task sonunda `dotnet build BuildOrchestrator.slnx` → 0/0 **ve** `dotnet test --filter "Category!=Acceptance"` → 0 failed. Kırmızıyla bir sonraki task'a geçilmez.
- **Uygulama çalışırken build kilitlenir** — build/test öncesi `BuildOrchestrator.App` process'i kapalı olmalı.
- **Git:** `it5-perf-dist` branch'inde task başına tek commit; iş bitince main'e merge + push, merge doğrulandıktan sonra branch local+remote silinir, oturum main'de biter (CLAUDE.md 2026-07-21 kararı).

### PER-TASK METOD (ledger'da bağlayıcı)

Taze implementer (Opus) → `git diff BASE..HEAD > .superpowers/sdd/review-<BASE>..<HEAD>.diff` → **3-lens paralel review** (① spec/design-fidelity · ② WPF/threading + A13.2 · ③ tests/yapı) + her Critical/Important'a **3-açılı adversarial** (reproduce / code-reading / severity; **≥2 onay = hayatta kalır**) → hayatta kalanlar için **TEK fix wave** → re-review → ledger satırı (`.superpowers/sdd/progress.md`).

---

## Dosya Haritası (bu planın yarattığı/değiştirdiği ana yüzeyler)

| Yol | Sorumluluk | Task |
|---|---|---|
| `src/BuildOrchestrator.Core/ProcessControl/NativeMethods.cs` | `JOBOBJECT_CPU_RATE_CONTROL_INFORMATION` + info-class 15 + Query/Set overload'ları | P1 |
| `src/BuildOrchestrator.Core/ProcessControl/JobObject.cs` | `SetCpuRate` / `ClearCpuRate` / `QueryCpuRate` / `SetPriorityClass` | P1 |
| `src/BuildOrchestrator.Core/ProcessControl/PerfProfile.cs` | **YENİ** — saf `PerfMode → (parallelism, cpuCapPercent, priorityClass)` tablosu | P1 |
| `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs` | `StartRunCommand += PerfMode` · **YENİ** `SetPerfModeCommand` · `RunStartedEvent += CpuCapPercent` | P2 |
| `src/BuildOrchestrator.Supervisor/{Program,SupervisorHost,RunCoordinator}.cs` | cap/priority uygulama + canlı `setPerfMode` + konsol notu | P2/P3 |
| `src/BuildOrchestrator.Core/MsBuild/{RetryingMsBuildInvoker,CopyContention}.cs` | copy-fazı rate floor kancası + cap-farkındalı backoff | P3 |
| `src/BuildOrchestrator.App/Graph/{GraphLayout,GraphView.xaml.cs}` | dinamik tuval + cull + statü fast-path + GlyphRun cache | G1/G2 |
| `src/BuildOrchestrator.App/Views/ProjectRow.xaml(.cs)` | lazy hover ikonları / VS-chooser / tooltip (kart sadeleştirme) | L1 |
| `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs` + `Views/ProjectRow.xaml.cs` | `CurrentSha` besleme + `Short7` + TargetSha tazeleme | W1 |
| `src/BuildOrchestrator.Core/Workspace/SyncWorkspaceService.cs` · `Supervisor/RunCoordinator.cs` | `BuildPreviewItem += BuiltCommit` üretimi | W1 |
| `src/BuildOrchestrator.App/Controls/{MotionGate,RevealStagger}.cs` · `Views/PopoverBase.cs` | **YENİ** — seam fold | W2 |
| `src/BuildOrchestrator.App/Spikes/FontAbWindow.xaml.cs` | tek gerçek token drift'i (`#2a2a30` → `Brush.BorderStrong`) | T1 |
| `src/BuildOrchestrator.App/BuildOrchestrator.App.csproj` | publish-aware supervisor kopyalama + lisans + sürüm | D1 |
| `README.md` · `docs/TRUST-BOUNDARY.md` | **YENİ** — ürün README + T17 | D2/D3 |

---

# FAZ P — Perf motoru (T20 + K11'in eksik yarısı)

> **Gerekçe:** K11 "sabit 6/4/2 + **process priority** + **inner Job CPU rate cap (∞/%70/%40)**" diyor. Bugün yalnız paralellik uygulanıyor; cap ve priority **kodda hiç yok** (`grep CpuRate|SetPriorityClass` → src/tests'te 0 eşleşme). It-5 acceptance'ının ilk iki maddesi ("CPU cap tavanı tutar", "copy fazı starve olmaz") bu faz olmadan kanıtlanamaz.

---

### Task P1 (T20-a): Job Object CPU rate cap + priority altyapısı + ölçümle kanıt

**Bugünkü durum:** `NativeMethods.cs:12-14` yalnız iki info-class sabiti taşır (9 = ExtendedLimit, 7 = AssociateCompletionPort); `SetInformationJobObject` yalnız 2 overload'a sahip (`:108-114`); `QueryInformationJobObject` **hiç yok**. `JOBOBJECT_BASIC_LIMIT_INFORMATION.PriorityClass` alanı var (`:47`) ama hiçbir yerde doldurulmuyor. `JobObject.CreateKillOnClose` (`JobObject.cs:16-32`) **ORTAK fabrikadır** — App'in outer job'ı (`EngineHost.cs:9`), Supervisor'ın inner job'ı (`Program.cs:75`) ve testlerin job'ları ondan gelir.

**Files:**
- Modify: `src/BuildOrchestrator.Core/ProcessControl/NativeMethods.cs`
- Modify: `src/BuildOrchestrator.Core/ProcessControl/JobObject.cs`
- Create: `src/BuildOrchestrator.Core/ProcessControl/PerfProfile.cs`
- Test: **yeni** `tests/BuildOrchestrator.Tests/ProcessControl/JobCpuRateTests.cs`, **yeni** `tests/BuildOrchestrator.Tests/ProcessControl/PerfProfileTests.cs`

**Interfaces (üretilen — P2/P3 bunları tüketir):**
```csharp
public enum PerfMode { Full, Balanced, Light }
public readonly record struct PerfProfile(int Parallelism, int? CpuCapPercent, ProcessPriorityClassKind Priority)
{
    public static PerfProfile For(PerfMode mode);            // Full(6,null,Normal) · Balanced(4,70,BelowNormal) · Light(2,40,Idle)
    public static PerfProfile? TryParse(string perfModeText); // App'in string'i ile köprü
}
// JobObject:
public void SetCpuRate(int percent);   // 1..100 · HARD_CAP · KILL_ON_JOB_CLOSE'a DOKUNMAZ
public void ClearCpuRate();
public int? QueryCpuRate();            // doğrulama seam'i (test + acceptance kanıtı)
public void SetPriorityClass(ProcessPriorityClassKind kind);
```

- [ ] **Step 1: RED — cap ayarlanabildiğini ve geri okunabildiğini kanıtlayan test**

`JobCpuRateTests.cs`: taze bir `JobObject` üzerinde `QueryCpuRate()` başlangıçta `null`; `SetCpuRate(70)` sonrası `70`; `ClearCpuRate()` sonrası tekrar `null`. `SetCpuRate(0)` / `SetCpuRate(101)` → `ArgumentOutOfRangeException`. Bu adım P/Invoke imzasının doğruluğunu (yanlış `cbJobObjectInfoLength` → `ERROR_INVALID_PARAMETER`) yakalayan tek yerdir.

- [ ] **Step 2: RED — KILL_ON_JOB_CLOSE'un cap yazımından SAĞ ÇIKTIĞI testi**

⚠️ **Bu adım atlanamaz.** `SetInformationJobObject`'e `ExtendedLimitInformation` ile ikinci kez yazarken `LimitFlags` OR'lanmazsa `KILL_ON_JOB_CLOSE` **silinir** ve §3 cascade-kill garantisi sessizce kaybolur. Test: cap + priority uygulanmış bir job'a child spawn et, job'ı dispose et → child ≤2sn içinde ölmeli (mevcut `JobObjectTests.cs:16` deseni: IOCP `WaitNext` + `WaitForExitAsync`, **sleep yok**).
> Uygulama notu: CPU rate ayrı bir info-class'tır (15) ve `ExtendedLimitInformation`'a dokunmaz — güvenli. **Priority ise** `JOBOBJECT_BASIC_LIMIT_INFORMATION.PriorityClass` + `JOB_OBJECT_LIMIT_PRIORITY_CLASS (0x20)` üzerinden gider ve **aynı struct'ı** paylaşır: mevcut flag'leri **Query → OR → Set** ile korumak ZORUNLU.

- [ ] **Step 3: RED — cap TAVANININ gerçekten tuttuğunu ölçen test (acceptance kanıtı)**

`Cpu_hard_cap_holds_under_a_saturating_child`: job'a CPU'yu doyuran bir child (busy-loop; `TotalProcessorTime` okunabilen basit bir .NET/`cmd` döngüsü) assign et, cap=40 uygula, **duvar saati penceresi** boyunca child'ın `TotalProcessorTime` artışını ölç → `cpuTime / (wall × logicalCoreCount)` oranı **cap + tolerans** altında kalmalı. Tolerans cömert (ör. cap+15 puan) ve pencere ≥1.5sn; **sabit uyku değil**, `Process.WaitForExitAsync`/`Task.Delay` yerine ölçüm penceresi `Stopwatch` ile kapatılır ve child deterministik olarak sonlandırılır. Makineye duyarlılığı azaltmak için asıl assert **capsiz koşuya göre oransal düşüş** (capli oran < capsiz oranın belirgin altında) + mutlak tavan birlikte kullanılır.
> Bu test It-5 acceptance'ının "CPU cap tavanı tutar" maddesinin **tek otomatik kanıtıdır**; flake riskine karşı `[Trait("Category","Perf")]` ile etiketlenir ama **suite'te koşar**.

- [ ] **Step 4: RED — PerfProfile tablosu K11'e birebir**

`PerfProfileTests.cs`: `Full → (6, null, Normal)` · `Balanced → (4, 70, BelowNormal)` · `Light → (2, 40, Idle)`; `TryParse` App'in kullandığı üç string'i ("Full"/"Balanced"/"Light") çözer, bilinmeyen → `null`. Paralellik değerleri `RunViewModel.ParallelismFor` (`RunViewModel.cs:548-551`) ile **aynı** olmalı — test bunu da assert eder (tek doğruluk kaynağına geçiş P2'de).

- [ ] **Step 5: GREEN — implementasyon**

`NativeMethods`'a: `JobObjectCpuRateControlInformation = 15`, `JOB_OBJECT_CPU_RATE_CONTROL_ENABLE = 0x1`, `..._HARD_CAP = 0x4`, `JOB_OBJECT_LIMIT_PRIORITY_CLASS = 0x20`, `JOBOBJECT_CPU_RATE_CONTROL_INFORMATION` struct'ı (`ControlFlags` + union alanı; `CpuRate` = 1/100 yüzde → `percent × 100`), `QueryInformationJobObject` P/Invoke'u, iki yeni `SetInformationJobObject` overload'u. `JobObject`'e yukarıdaki 4 metot; priority yolu **Query → OR → Set**.

- [ ] **GÖZLE KONTROL:** yok (saf motor; kanıt testlerdedir).

---

### Task P2 (K11 wire): PerfMode'u uçtan uca bağla — canlı cap + priority + konsol notu

**Bugünkü durum:** `PerfMode` string'i **IPC'ye hiç gitmez**; Supervisor yalnız `int Parallelism` görür (`IpcMessages.cs:56-58`). `setPerfMode` komutu **kodda yok** (yalnız plan dokümanlarında geçer). `CyclePerf` (`RunViewModel.cs:555-560`) koşan run'a **sıfır etki** eder; konsol notu `parallelism: 2 (Light)` — K11'in istediği `parallelism: 4 · cpu cap 70%` **değil** (mevcut metin `RunViewModelStateTests.cs:220`'de pinli).

**Files:**
- Modify: `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs` (`StartRunCommand += PerfMode`; **yeni** `SetPerfModeCommand`; `RunStartedEvent += CpuCapPercent`)
- Modify: `src/BuildOrchestrator.Supervisor/Program.cs` (inner job'a başlangıç cap/priority), `SupervisorHost.cs` (dispatch case), `RunCoordinator.cs` (run başı uygulama + konsol/decision satırı + `ApplyPerfMode`)
- Modify: `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs` + `RunViewModel.ActionBar.cs` (tek kaynak = `PerfProfile`, `CyclePerf` → IPC komutu)
- Test: `tests/BuildOrchestrator.Tests/Ipc/IpcMessagesTests.cs`, `tests/BuildOrchestrator.Tests/Supervisor/RunCoordinatorTests.cs`, `tests/BuildOrchestrator.Tests/App/RunViewModelStateTests.cs`

- [ ] **Step 1: RED — `SetPerfModeCommand` NDJSON round-trip + bilinmeyen komut davranışı**

`IpcMessagesTests`: yeni discriminator round-trip'i; `StartRunCommand.PerfMode` (nullable, geriye dönük uyumlu) round-trip'i; `RunStartedEvent.CpuCapPercent`.

- [ ] **Step 2: RED — run başında cap/priority uygulanır ve run sonunda geri alınır**

`RunCoordinatorTests`: `StartRunCommand(PerfMode="Light")` → koordinatör inner job'a `SetCpuRate(40)` + `SetPriorityClass(Idle)` uygular (job seam'i test edilebilir olmalı — `RunCoordinator` bugün somut `JobObject` alıyor; **ince bir arayüz** (`ICpuGovernor`) ile enjekte edilir, `JobObject` onu implement eder). Run bittiğinde cap **geri alınır** (`ClearCpuRate`) — aksi halde Supervisor ömrü boyunca yaşayan tek job (`Program.cs:75`) sonraki Sync/git işlerini de kısıtlar.

- [ ] **Step 3: RED — canlı `setPerfMode` koşan run'ın cap'ini DEĞİŞTİRİR, paralelliği değiştirmez**

Koşarken `SetPerfModeCommand("Full")` → `ClearCpuRate` + priority Normal; **worker sayısı değişmez** (worker'lar run başında bir kez yaratılır — `RunCoordinator.cs:507-509`). Bu, K11'in "çalışırken değiştirilebilir" ifadesinin dürüst yorumudur ve **koda + konsol notuna yazılır**: canlı değişen = CPU cap + priority; paralellik bir sonraki run'da geçerli olur.
> ⚠️ **Karar (kayda geçer):** ETA modeli run'ın başlangıç parallelism'ini dondurur (`RunViewModel.cs:679,782`); canlı cap değişimi ETA'yı **bilinçli olarak** güncellemez (tahmin gürültüsü < karmaşıklık maliyeti).

- [ ] **Step 4: RED — konsol notu K11 formatında**

`RunViewModelStateTests.cs:220` pini güncellenir: koşarken perf değişiminde konsola **`parallelism: 4 · cpu cap 70%`**; Full modda **`parallelism: 6 · cpu cap off`**. Run başı satırı (`RunCoordinator.cs:467-472`) da cap'i yazar. Metinler `CultureInfo.InvariantCulture`.

- [ ] **Step 5: GREEN + tek kaynak**

`RunViewModel.ParallelismFor` kaldırılır; App `PerfProfile.TryParse` üzerinden okur (Core referansı App'te zaten var). `UiStateStore` şeması **değişmez** (PerfMode string'i yeterli) — toleran-converter riski açılmaz.

- [ ] **GÖZLE KONTROL:** Build başlat → perf chip'ine tıkla → konsolda `parallelism: N · cpu cap M%` satırı çıkar; Task Manager'da `MSBuild.exe` ağacının CPU'su Light'ta belirgin düşer, Full'de serbest kalır.

---

### Task P3 (T20-b): Copy fazı rate floor + git/IPC etkileşimi

**Bugünkü durum:** post-build copy **MSBuild child'ının içinde** olur; orchestrator hiçbir şey kopyalamaz. Copy'nin **"başlıyor" sinyali yok** — elde olan tek şey geriye dönük log regex'i (`CopyContention.cs:12-22`, MSB3021/3026/3027) ve tüm invoke'un retry'lanması (`RetryingMsBuildInvoker.cs:29-67`, sabit 200/600ms backoff). git/vswhere child'ları **inner job'ın DIŞINDA** (Supervisor'ın soyu olarak yalnız outer job üyesi — `ProcessRunner.cs:26-43`, `KillMidBuildTests.cs:84-89`), yani inner job'a konan cap git'i ve IPC'yi **boğmaz**.

**Files:**
- Modify: `src/BuildOrchestrator.Core/MsBuild/RetryingMsBuildInvoker.cs` (cap-farkındalı backoff + floor kancası)
- Modify: `src/BuildOrchestrator.Supervisor/RunCoordinator.cs` (graceful-stop drain'de floor)
- Create: `docs/` altına değil — bulgular `.claude/outputs/<ts>-it5-records.md`'ye (V3)
- Test: `tests/BuildOrchestrator.Tests/MsBuild/RetryingMsBuildInvokerTests.cs`, `tests/BuildOrchestrator.Tests/Supervisor/RunCoordinatorTests.cs`

- [ ] **Step 1: Karar — floor mekanizması (kodda gerekçeli yazılır)**

Copy fazı ayrı bir faz olarak işaretlenemez (child'ın içinde). Uygulanacak **iki ayaklı floor**:
1. **Kalıcı taban:** cap `HARD_CAP` olarak uygulanır ama **asla %40'ın altına inmez** (Light = %40 zaten taban). Ek olarak cap uygulanan job'da **priority Idle'a düşürülmez** eğer in-flight bir copy-retry penceresi varsa (aşağıdaki 2. ayak).
2. **Olay-tetikli floor:** `RetryingMsBuildInvoker` bir copy contention (MSB302x) gördüğünde **retry'dan ÖNCE** cap'i geçici olarak floor'a (%70) yükseltir ve retry bitince eski değere döner. Gerekçe: contention penceresi tam olarak copy'nin sıkıştığı andır; cap'i orada gevşetmek starve'ı kapatır ve kalıcı bir davranış değişikliği yaratmaz.
3. **Graceful-stop drain:** `TryRequestStop(Graceful)` in-flight child'ların post-build copy'lerini **tamamlamasına** dayanır (`RunCoordinator.cs:249-254`). Drain süresince cap **kaldırılır** — "ortak bin'de torn DLL yok" garantisi cap yüzünden uzayan bir pencereye bırakılamaz.

- [ ] **Step 2: RED — contention penceresinde floor uygulanır ve geri alınır**

`RetryingMsBuildInvokerTests` (mevcut `RecordingDelay` seam'i, `:77-87`): enjekte edilen bir `ICpuGovernor` fake'i ile — contention görülünce `SetCpuRate(≥70)`, retry sonrası **eski değere** (`SetCpuRate(40)` veya `ClearCpuRate`) dönüş; contention **görülmeyen** hatada governor'a **hiç dokunulmaz**. **Gerçek zaman beklenmez** (D8).

- [ ] **Step 3: RED — cap-farkındalı backoff**

Cap aktifken backoff dizisi uzar (ör. 200/600 → 300/900), cap yokken **aynen korunur** (mevcut test dizisi `:97+` kırılmaz). Gerekçe: cap altında copy'nin tamamlanma penceresi uzar; sabit backoff MSB3027'yi kalıcı Failed'a çevirir.

- [ ] **Step 4: RED — graceful drain'de cap kalkar**

`RunCoordinatorTests`: graceful stop isteği → governor'a `ClearCpuRate` gider; hard stop yolunda job zaten terminate edildiği için **çağrı yok**.

- [ ] **Step 5: Belge — git/IPC etkileşimi kanıtı**

`ICpuGovernor`'un yalnız **inner job**'a bağlandığı, git/vswhere'in inner job dışında kaldığı (dolayısıyla Sync'in cap'ten etkilenmediği) ve IPC pump'ın Supervisor process'inde (job dışı) koştuğu **testle değil kod-okumasıyla** sabittir — bu üçlü V3'te `it5-records.md`'ye yazılır ve trust-boundary/README ile çelişmediği doğrulanır.

- [ ] **GÖZLE KONTROL:** Light modda büyük bir Build koş → Sync/git adımları **yavaşlamaz** (fetch satırı normal hızda gelir), konsol akmaya devam eder, UI donmaz.

---

# FAZ G — Graf perf (T51 + T63-perf)

---

### Task G1 (T51-a): Sentetik büyük graf + ölçüm + layout'un ölçeklenmesi

**Bugünkü durum:** render yolu **tamamen UIElement/Shape**; düğüm başına **15 declared UIElement** (`GraphView.xaml.cs:274-383`), bunun ~5'i (rozet alt-ağacı) `HasDepIssue=false` olsa bile kuruluyor. Kenar başına 1 `Path` (geometri frozen). **Cull YOK**, **GlyphRun cache YOK**, `ShapesPathMaxNodes = 150` sabiti **tanımlı ama hiç okunmuyor** (`GraphView.xaml.cs:37` — ölü sabit). `GraphLayout.Compute` **O(n)** ama **`CanvasWidth = 880` SABİT** (`GraphLayout.cs:22`): 500–1000 düğümde katman içi spacing ≈0.8–1.6px olur ve 26px kareler **fiziksel olarak üst üste biner**; `FitScale` tabanı 0.68'de kilitli olduğu için uzaklaşarak da çözülemez. Graf için **perf/bütçe testi yok** (listede var).

**Files:**
- Create: `tests/BuildOrchestrator.Tests/App/SyntheticGraph.cs` (paylaşılan helper), `tests/BuildOrchestrator.Tests/App/GraphRealizationPerfTests.cs`
- Modify: `src/BuildOrchestrator.App/Graph/GraphLayout.cs`
- Test: `tests/BuildOrchestrator.Tests/App/GraphLayoutTests.cs`

- [ ] **Step 1: Sentetik graf helper**

`SyntheticGraph.Build(nodeCount, layerCount, avgFanIn)` → `GraphNode[]` + `GraphEdge[]`, deterministik (seed'li, `Random` **paylaşılmaz**), gerçek OSYS profiline yakın (177 proje / ~6 katman referansı: `OsysGraphIntegrationTests.cs:30`). 36 / 191 / 500 / 1000 boyutları üretilebilmeli.

- [ ] **Step 2: RED — graf realize perf testi (baseline kaydı)**

`ListRealizationPerfTests` desenini birebir kopyala (warmup + medyan + GC + yalnız `Measure/Arrange/UpdateLayout` penceresi): **36 için sert bütçe**, 500 ve 1000 için **kayıt + gevşek felaket tavanı**. İlk koşumda gerçek sayılar ölçülür ve rapora yazılır — G2'nin hedefi bu sayıları düşürmektir.

- [ ] **Step 3: RED — layout düğüm sayısıyla BÜYÜR**

`GraphLayoutTests`: 500 düğümlü tek katmanda **komşu düğüm merkezleri arası mesafe ≥ 34px** (26px düğüm + 8px boşluk) olmalı; 3 düğümlü mevcut testler (880px tuval pini `:68-75`) **kırılmamalı** → `CanvasWidth = max(880, gereken)`.

- [ ] **Step 4: GREEN**

`GraphLayout.cs:61` spacing'i `Math.Clamp(available/n, MinSpacing:34, MaxSpacing:96)`; tuval genişliği bu spacing'den türetilir. Kamera zaten pan/zoom ile frontier'i takip ediyor (`GraphCamera.cs`) — **tüm grafın aynı anda sığması hedef değildir** ve bu karar kodda gerekçelendirilir.

- [ ] **GÖZLE KONTROL:** (G2 sonrası) — G2'de birlikte.

---

### Task G2 (T51-b + T63-perf): Cull + statü fast-path + GlyphRun cache

**Bugünkü durum:** `UpdateStatuses` her tick (MainWindow 200ms — `MainWindow.xaml.cs:161`) **her düğümde** koşulsuz: 2× `SetResourceReference` + `IconPaint.Apply` (ağaç yukarı `TryFindResource` yürüyüşü) + **yeni `DoubleCollection` allocation** (`GraphView.xaml.cs:259-264`, `:434`). Kenarlarda "değişmediyse dokunma" fast-path'i **var** (`:558-565`), düğümlerde **yok**. Etiketler düz `TextBlock` (`:358-372`).

**Files:**
- Modify: `src/BuildOrchestrator.App/Graph/GraphView.xaml.cs`, `Graph/GraphNodeVisual.cs`
- Test: `tests/BuildOrchestrator.Tests/App/GraphRenderTests.cs`, `GraphRealizationPerfTests.cs`

- [ ] **Step 1: RED — düğüm statü fast-path'i**

`GraphNode` bir `record`; `visual.Model == node` ise `ApplyNodeStatus` **atlanır** (kenar tarafındaki `:558-565` deseniyle simetrik). Test: aynı statülerle iki kez `UpdateStatuses` → ikinci geçişte hiçbir düğümde `SetResourceReference`/`StrokeDashArray` ataması **olmaz** (sayaç seam'i veya `DoubleCollection` referans özdeşliği ile kanıtla).

- [ ] **Step 2: RED — statik dash koleksiyonları paylaşılır**

`:434`'teki `dashed ? [2.0,2.0] : []` her çağrıda yeni allocation üretir → **statik frozen** iki örnek (`EdgeStyleResolver.cs:45-52`'deki mevcut desen). Test: iki farklı düğümün `StrokeDashArray`'i `Assert.Same`.

- [ ] **Step 3: RED — viewport cull**

Kamera transform'undan görünür dünya dikdörtgeni hesaplanır (saf fonksiyon → `GraphCamera` veya yeni `GraphCulling`); dışarıdaki düğüm/kenar **`Visibility.Collapsed`** yapılır (element ağacından silinmez — hit-test/seçim/testler bozulmaz, ancak measure/arrange/render kısa devre eder). Cull **margin**'li (±1 satır aralığı = 96px) olmalı ki kamera animasyonu sırasında pop-in olmasın. Test: 500 düğümlü grafta kamera bir köşeye sabitlendiğinde görünür düğüm sayısı **toplamdan belirgin küçük**; seçili düğüm ve komşuları **asla cull edilmez**; cull kapalıyken (küçük graf) davranış birebir aynı.
> ⚠️ **Cull, `RevealRows`/`NodeVisuals` test yüzeyini ve reveal stagger'ı etkiler** — reveal, `Visibility` değil `Opacity` üzerinden çalışır; cull edilen düğümün reveal'i **atlanır ama gecikme indeksi kaymaz** (global indeks korunur).

- [ ] **Step 4: RED — GlyphRun cache**

Etiketler `TextBlock` kalır (A13.2'nin `Ideal` kararı ve mevcut testler `GraphRenderTests.cs:95-111` buna bağlı) **ANCAK** kısa-ad → `FormattedText`/`GlyphRun` üretimi cache'lenir: aynı metin+font+boyut için tek örnek. Ölçülebilir kazanç yoksa (G1 baseline'ı ile karşılaştır) **bu adım gerekçesiyle atlanır ve kayda geçer** — spekülatif optimizasyon eklenmez.

- [ ] **Step 5: Karar kapısı — DrawingVisual katmanlarına geçilecek mi?**

G1 baseline'ı + Step 1-4 sonrası 1000 düğüm ölçümü hâlâ kabul edilemezse (kabaca: ilk realize > 2000ms veya tick maliyeti > 16ms), A13.2'nin üçlü katman mimarisine (EdgeLayer tek `OnRender` + NodeLayer `DrawingVisual` + FlowOverlay **UIElement Path**) geçilir. **Bu büyük bir iştir ve `GraphRenderTests`'in ~20 iddiasını kırar** — bu noktada **DURULUR ve kullanıcıya bildirilir** (plan kuralı: "takılma/perf sorunu çözümü büyükse durup bildir"). Aksi halde Shapes yolu + cull yeterli sayılır ve `ShapesPathMaxNodes` ölü sabiti **ya canlandırılır ya da gerekçesiyle silinir**.

- [ ] **GÖZLE KONTROL:** OSYS'i Sync'le (177 düğüm) → graf katman katman belirir, seçim/dim ve akan dash **birebir eskisi gibi**; kamera hareketinde düğüm pop-in'i **görülmez**.

---

# FAZ L — Liste perf (It-4 acceptance #5'in AÇIK kalemi)

---

### Task L1: Kart sadeleştirme — lazy hover ikonları + VS chooser + tooltip

**Bugünkü durum (ölçülü):** 191 satır realize **~660–730ms medyan** (bütçe 400ms), 500 satır ~1200ms. Satır başına **37 XAML nesnesi** (`ProjectRow.xaml:1-158`); bunların **16'sı** hover ikonları + VS-chooser Popup alt-ağacıdır ve **hiç hover edilmese bile** her satırda kuruluyor (`:74-122`; `ApplyRightBlock` yalnız `Visibility` set eder — `ProjectRow.xaml.cs:290-297`). Ayrıca satır başına 4 `ToolTip` nesnesi ve ~5 unfrozen brush (`DsTransition` lokal kopyaları). Satırda **VM binding'i sıfır** — maliyet element sayısı + ~45 dynamic-resource lookup'tır (ve `ApplyAll` üretimde satır başına **iki kez** koşar: `:141` + `:123`). ⚠️ Mevcut ölçüm `Loaded` yolunu ve animasyon clock'larını **kapsamıyor** → gerçek üretim maliyeti ölçülenden **yüksek**.

**Files:**
- Modify: `src/BuildOrchestrator.App/Views/ProjectRow.xaml`, `Views/ProjectRow.xaml.cs`
- Test: `tests/BuildOrchestrator.Tests/App/ProjectRowTests.cs`, `App/ListRealizationPerfTests.cs`, **yeni** element-sayısı regresyon testi

- [ ] **Step 1: RED — deterministik element-sayısı metriği**

Duvar saati makineye göre oynak; sadeleştirmenin etkisini kanıtlayacak **deterministik** bir metrik gerekir: `ProjectRow` realize edildikten sonra görsel ağaçtaki descendant sayısı bir tavanın altında olmalı (hover **edilmemiş** satır için). Test önce mevcut sayıyı yakalar (RED bütçeyle), sadeleştirme sonrası GREEN olur.

- [ ] **Step 2: RED — hover ikonları ve VS-chooser İLK HOVER'da kurulur**

`ProjectRowTests.cs:98-121` (sha ↔ hover ikon değişimi) **korunmalı**: test hover'ı tetiklediğinde ikonlar var olmalı. Yeni test: hover **öncesi** `PART_HoverIcons`/`PART_VsChooser` ağaçta **yok**; hover sonrası var ve tüm davranış (tooltip metinleri, tıklama, chooser satırları) aynı.
> Uygulama yolu: XAML'de bu alt-ağaçlar `<ContentControl>`/`x:Name`'li bir yer tutucu altında **kod-tarafı lazy inşa** ile kurulur (WPF'te `DeferredContent` yok). İnşa `SetHover(true)` (`ProjectRow.xaml.cs:310-316`) ilk çağrısında bir kez yapılır; ctor'daki 3 event kablajı (`:77-79, :87`) inşa anına taşınır.

- [ ] **Step 3: RED — `ApplyAll` satır başına BİR kez**

`OnDataContextChanged` (`:141`) + `OnLoaded` (`:123`) ikilisi tek bir "hazır olduğunda bir kez" yoluna indirilir (idempotent guard). Test: bir satırda `ApplyAll` çağrı sayacı == 1.

- [ ] **Step 4: Ölçüm + KARAR KAPISI**

`ListRealizationPerfTests` yeniden koşulur (191 ve 500). **Bütçe (400ms/191) tutuyorsa → virtualization'a GİRİLMEZ** ve It-4 acceptance #5 kart-sadeleştirme ile kapanır; sonuç kayda geçer. **Tutmuyorsa** → Task L2 açılır (aşağıda), ancak önce kullanıcıya bildirilir.
> Ek olarak 500 satır için **bağlayıcı bir bütçe** eklenir (bugün yalnız kayıt amaçlı ölçülüyor) — acceptance #5'in doğrulama kancası budur.

- [ ] **GÖZLE KONTROL:** OSYS Sync sonrası liste **hızlı** açılır; bir satırın üzerine gel → klasör/VS ikonları **anında** belirir (ilk hover'da gecikme hissedilmez); ikonlara tıkla → Explorer/VS eylemleri çalışır; sha çifti hover dışında görünür.

---

### Task L2 (KOŞULLU — yalnız L1 bütçeyi tutturamazsa): Liste virtualization + drift kalibrasyonu

> ⚠️ **Bu task varsayılan olarak YAPILMAZ.** L1 Step 4 kapısı açarsa ve kullanıcı onay verirse yürütülür.

**Bugünkü durum:** virtualization **üç bağımsız noktada** kapalı: `StickyLayerList.xaml:40` (attached), `:41-45` (`ItemsPanelTemplate` = düz `StackPanel`), `:34` (dış `ScrollViewer` içeriği sonsuz yükseklikle measure eder). Üçü **birlikte** değişmeli. Açılırsa **dört kırılma noktası** doğar: (1) sticky başlık kümesi (`LayoutMetrics.cs:134-144`), (2) follow/selection scroll hedefi (`LayoutMetrics.cs:153-154` → `FollowScrollController.cs:62,81`), (3) near-bottom resume eşiği → `ScrollArbiter` biti (`StickyLayerList.xaml.cs:158-164`), (4) reveal stagger indeksi (`:240-245, :259-270`).

- [ ] **Step 1:** RED — drift kalibrasyonu seam'i (realized container'dan `TransformToAncestor` ile gerçek Y ölç, tabloyla farkı düzelt).
- [ ] **Step 2:** RED — iki aşamalı follow (`BringIntoView` → kesin offset'e animasyon).
- [ ] **Step 3:** RED — `CollectRows` GLOBAL indeks taşır (reveal stagger kaymaz).
- [ ] **Step 4:** RED — container recycling'de `ProjectRow` durum sıfırlama (`Opacity`/`ShakeTranslate.Y`).
- [ ] **Step 5:** **ZORUNLU regresyon paketi:** `StickyOverlayTests` · `LayoutMetricsTests` · `FollowScrollControllerTests` · `FollowScrollTests` · `FrontierFollowArbitrationTests` · `ScrollArbiterTests` · `StickyRevealTests` · `AccessibilityTests:106-112` (ok tuşu gezinme) — hepsi yeşil kalmalı; `ListRealizationPerfTests:89-90` (RevealRows == N) anlamı yeniden tanımlanır.

---

# FAZ W — It-4b'den devreden kayıtlı kalemler

---

### Task W1: CurrentSha tam BuiltCommit wire

**Bugünkü durum:** `BuildState.BuiltCommit` **var** (`ProjectModels.cs:81-88`) ve başarılı derlemede yazılıyor (`RunCoordinator.cs:755-766`, değer = plan anındaki yerel HEAD) ama **`src` genelinde hiç okunmuyor**. Per-proje commit taşıyan **hiçbir IPC alanı yok**. App'te `CurrentSha`'yı **SET EDEN kod yok** → üretimde hep `null` → interim guard dalı (`ProjectRow.xaml.cs:286`) her zaman koşuyor. **İki üretim defekti daha:** (a) `TargetSha` 40-hex geliyor (`GitService.cs:229`) ve **ham basılıyor** — kart 118px slotta 40 karakter gösteriyor (`Short7` mevcut ama kullanılmıyor: `RunViewModel.ActionBar.cs:62`); (b) `BuildPreviewEvent` `syncCompleted`'dan **önce** geldiği için ilk Sync'ten sonra sha slotu **boş kalıyor** (tazeleme yolu yok).

**Files:**
- Modify: `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs` (`BuildPreviewItem += BuiltCommit`)
- Modify: `src/BuildOrchestrator.Core/Workspace/SyncWorkspaceService.cs:109-110,181` · `src/BuildOrchestrator.Supervisor/RunCoordinator.cs:456-457`
- Modify: `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs:694-704` · `Views/ProjectRow.xaml.cs:278-297`
- Test: `Ipc/IpcMessagesTests.cs`, `Workspace/SyncWorkspaceServiceTests.cs`, `Supervisor/RunCoordinatorTests.cs`, `App/ProjectRowTests.cs`, `App/RunViewModelTests.cs`

- [ ] **Step 1: RED — `BuildPreviewItem.BuiltCommit` round-trip**

Taşıyıcı olarak `BuildPreviewItem` seçildi: **tek** per-proje ön-bilgi taşıyıcısıdır ve HEM Sync HEM run başı yolundan yayınlanır; App'te satır-başı reconcile handler'ı zaten var. `ProjectNode`'a **eklenmez** — `TopologySignature`'a girip her commit değişiminde tam graf yeniden inşası + reveal + kamera re-home tetiklerdi (`RunViewModel.Workspace.cs:222-241`).

- [ ] **Step 2: RED — Sync yolu state'ten BuiltCommit taşır**

`SyncWorkspaceService`: `stateStore.Load()` (`:181`) zaten elde; `BuildPreviewItem` projeksiyonuna (`:109-110`) `BuiltCommit` eklenir. ⚠️ **Sync SALT-OKUR kalır** — hiçbir şey persist edilmez (K1 + `:97-99` sözleşmesi).

- [ ] **Step 3: RED — run yolu da doldurur**

`RunCoordinator.cs:456-457` preview'ı `RunContext.StateStore`'dan (`:867`) okuyup aynı alanı doldurur; aksi halde run başlayınca sha slotu **sıfırlanır**. Continue/RetryFailed her segmentte preview yayınladığı için `Load()` **segment başına bir kez** çağrılır (tekrarlı deserialize riskine karşı segment-lokal cache).

- [ ] **Step 4: RED — App: CurrentSha set + Short7 + terminal-satır sırası**

`OnBuildPreview` (`:694-704`): `CurrentSha` ataması, `WillBuild`'i koruyan `continue` guard'ından (`:700`) **BAĞIMSIZ** yapılır — aksi halde Continue/RetryFailed 2. segmentte succeeded satırların sha'sı hiç güncellenmez. `ApplySha` **her iki tarafı da** 7 haneye kısaltır (`Short7`); test verisi 7 haneli olduğu için mevcut pinler (`ProjectRowTests.cs:298-308`) korunur, **yeni test 40-hex girdiyle** kısaltmayı kanıtlar.

- [ ] **Step 5: RED — TargetSha değişince satır tazelenir**

`syncCompleted` → `TargetSha` set edildiğinde görünür satırlar `ApplySha`'yı yeniden koşar (VM tarafında tek bir tazeleme sinyali; satır başına yeni `PropertyChanged` abonesi **eklenmez** — L1'in perf bütçesiyle çakışmaması için).

- [ ] **Step 6: KARAR — never-built gösterimi**

Bugün never-built ile "wire yok" **görsel olarak aynı** (ikisi de yalnız target basar). Tam wire sonrası ayrışırlar. **Karar:** never-built satırda sha çifti yerine yalnız `→ {target7}` gösterilir (mevcut interim davranışın korunması) ve tooltip/statü metni değişmez; design-v1'de karşılığı yok (prototip her projeye sentetik curSha üretiyor — `build-data.js:229`), bu yüzden **en az sürprizli** seçenek alınır ve kayda geçer.

- [ ] **GÖZLE KONTROL:** Repo Sync'le → dirty satırlarda `a3f81c2 → b7e91d4` çifti (her iki taraf da **7 hane**); daha önce hiç derlenmemiş projede yalnız hedef; bir Build koş → derlenen satırın sol tarafı yeni commit'e döner.

---

### Task W2: Motion seam-helper fold (`MotionGate` + `RevealStagger` + `PopoverBase`)

**Bugünkü durum:** `AnimationsEnabledProvider` default lambda'sı **6 tipte birebir kopya** (`GraphView:109-110`, `ProjectRow:57-58`, `StickyRibbon:67`, `StickyLayerList:69`, `EventStreamView:45`, `EventStreamRow:312`). subscribe-once idiomu + `_subscribedMotion` alanı + `MotionSettings` seam'i **5 sahipte** kopya. `PlayRevealStagger` bloğu `GraphView.xaml.cs:647-742` ↔ `StickyLayerList.xaml.cs:225-305` — iki metot **gövde olarak bayt-aynı**. `PopoverBase`/`MotionGate` **diye bir tip yok** (yalnız plan notlarında geçer).

**Files:**
- Create: `src/BuildOrchestrator.App/Controls/MotionGate.cs`, `Controls/RevealStagger.cs`, `Views/PopoverBase.cs`
- Modify: yukarıdaki sahipler + `Views/{BranchPopover,WorktreePopover}.xaml(.cs)`
- Test: `App/MotionOwnerHygieneTests.cs`, `App/StickyRevealTests.cs`, `App/GraphRenderTests.cs`, `App/PopoverTests.cs`, `App/ReducedMotionCoverageTests.cs`

- [ ] **Step 1: RED — `MotionGate` davranış sözleşmesi**

Tek tip: provider + `MotionSettings` seam'i + subscribe-once + `Loaded/Unloaded` kablajı + `Changed` yeniden yayını.
⚠️ **Bilinçli sapmayı KORU:** `GraphView.xaml.cs:127-133` guard'ı diğerlerinden **farklıdır** (`if (_subscribedMotion is not null) return;` — ilk abonelikten sonra `MotionSettings` ataması yok sayılır) ve `MainWindow.xaml.cs:79-80` bu sözleşmeye açıkça dayanır. `MotionGate` **her iki modu** desteklemeli (`latchFirst: true/false`) ve GraphView `latchFirst: true` kullanmalı. Test her iki modu ayrı ayrı pinler.

- [ ] **Step 2: RED — `RevealStagger` fold**

`Begin(provider, coordinator, key)` → `(animate, generation)`; `ScheduleRelease(maxDelayMs + revealMs, generation)`. ⚠️ Release tetiği **`DispatcherTimer` olarak kalır** — `Completed`-after-`BeginAnimation` gerçek HWND'de hiç ateşlenmez (E3 fix'i, `GraphView.xaml.cs:698-718`); `Completed`'e dönülürse hero **sonsuza dek** takılır. Hero anahtarının derleme-zamanı özdeşliği (`StickyLayerList.xaml.cs:81 = GraphView.RevealHeroKey`) korunur (DD9: graf+liste AYNI hero).

- [ ] **Step 3: RED — `PopoverBase` fold**

Ortak: `IsOpen` DP + `OnIsOpenChanged` (Refresh + `PopIn.Play` + odağı içeri) + `Esc → CloseRequested` + VM-swap aboneliği. **Popover'a özel davranışlar korunur** (Worktree: hover'da çöp kutusu, forced/disabled switch, üçüncü kolon — `WorktreePopover.xaml.cs:98-114,184-202`). `BuildMenu` **kapsam dışı** (IsOpen DP'si ve Esc'i yok; pop-in'i ActionBar tetikler).

- [ ] **Step 4: GREEN + seam genişletme**

`BuildingSpinner`/`StatusGlyph` `MotionGate` üzerinden **test seam'i kazanır** (bugün doğrudan `App.Motion` okuyorlar; bu yüzden `MotionOwnerHygieneTests.cs:56-76` statik set/restore yapmak zorunda). `MotionTokens.cs:77/97/119`'daki üç `App.Motion?...` okuması da aynı kapıdan geçer — **ancak** `ReducedMotionCoverageTests.cs:224,237`'nin `Assert.Null(App.Motion)` vacuous-PASS koruması korunmalı.

- [ ] **Step 5: Realize testi**

`PopoverBase`'e taşınan iki popover için `DsResources.Realize` ile realize testi (`PopoverTests.cs:74-86` deseni; `IsOpen=true` + `UpdateLayout`) — sınıf tabanı değişimi XAML kökünün `x:Class` tabanını değiştirir, bu realize-fatal sınıfının tam hedefidir.

- [ ] **GÖZLE KONTROL:** branch ve worktree popover'ları açılırken 140ms pop-in yapar, Esc kapatır, odak içeri gider; Windows animasyon efektlerini kapat → pop-in **anında** olur; graf+liste reveal'i hâlâ **tek hero** (ikisi aynı anda kademelenir, biri diğerini bloklamaz).

---

# FAZ T — Token son geçişi + realize güvenlik ağı

---

### Task T1 (T49): Token drift denetimi + eksik realize testleri

**Bugünkü durum (denetim yapıldı):** Tokens.xaml = **103 anahtar**, 56 Brush'ın **tamamı** `colors.css` ile birebir (alfa dönüşümü tutarlı `round(a×255)`); Motion.xaml'in 4 Duration + 3 KeySpline'ı `effects.css` ile birebir. **Tanımsız `DynamicResource` anahtarı YOK** (runtime-fatal eksik yok). **TEK gerçek değer sapması:** `Spikes/FontAbWindow.xaml.cs:34` — `BorderStrong = "#2a2a30"` (doğrusu `#3a3a42`); aynı blokta **12 ham hex** var ve bu dosya **shipped** (`--font-ab` ile açılıyor, hiçbir testte instantiate edilmiyor). **Guard boşlukları:** `NoHardcodedColorTests` yalnız `*.xaml` tarıyor (`:40,:57`) → bu 12 hex hiç görülmüyor; **hardcoded ms taraması hiç yok** (kodda 31 `*Ms` sabiti). **Realize testsiz kökler:** ⚠️ **`MainWindow.xaml`** (hiç instantiate edilmiyor; en yoğun token tüketicisi ve `MainWindow.xaml.cs:64` `(double)FindResource("Size.TitleBarHeight")` ile **cast** ediyor — `c6e9a21` launch-fatal sınıfının kalan tek kökü), `Console/ConsoleView.xaml`, `Console/ConsoleHeader.xaml`, `Controls/LatestPill.xaml`, `Controls/PanelHeader.xaml`.

**Files:**
- Modify: `src/BuildOrchestrator.App/Spikes/FontAbWindow.xaml.cs`
- Test: `App/NoHardcodedColorTests.cs`, **yeni** `App/MainWindowRealizeTests.cs`, `App/ConsoleViewTests.cs` / `ConsoleModesTests.cs` / `LatestPillTests.cs` (sözlük ekleme), **yeni** `App/PanelHeaderTests.cs`

- [ ] **Step 1: RED — `*.cs` renk taraması**

`NoHardcodedColorTests` `*.cs`'i de tarar (`Color.FromRgb`, `#RRGGBB` string literalleri, `Colors.*`). Test **önce RED** olur (FontAbWindow'un 12 hex'i), sonra Step 2 ile GREEN.

- [ ] **Step 2: GREEN — FontAbWindow token'a bağlanır**

12 ham hex `Application.Current.TryFindResource("Brush.*")` üzerinden çözülür (pencere üretimde `Application` altında açılıyor); `BorderStrong` sapması böylece kendiliğinden düzelir. Fallback yolu (kaynak bulunamazsa) **yoktur** — pencere zaten yalnız uygulama içinde açılır.

- [ ] **Step 3: RED — `MainWindow` realize testi** ⚠️ **BU FAZIN EN ÖNEMLİ ADIMI**

`ShellRootTests.cs:19-21` deseninde: `DsResources.Realize` ile `MainWindow` **tam realize** edilir (`ApplyTemplate` + `UpdateLayout`). `c6e9a21`'in sınıfı (Double token → GridLength/Thickness, tanımsız `DynamicResource`, `FindResource` cast'i) burada yakalanır. Test STA + `[Collection("Console UI (serial)")]`.
> Pencere ctor'ı `EngineHost` gibi ağır bağımlılıklar kuruyorsa, realize testi **bunları enjekte edilebilir seam üzerinden** by-pass eder ya da yalnız XAML kökünü `XamlReader` ile yükleyip realize eder — **hangi yol seçilirse seçilsin `FindResource` cast'inin (`:64`) gerçekten koştuğu kanıtlanmalıdır.**

- [ ] **Step 4: RED — kalan 4 kök sözlükle realize edilir**

`ConsoleView`, `ConsoleHeader`, `LatestPill`, `PanelHeader` bugün **sözlüksüz** `new X()` ile test ediliyor (`LatestPill.xaml:24-26` yorumu bunu açıkça sömürüyor: "headless test host'ta DynamicResource sessizce çözümsüz kalır"). Bu testler `DsResources.NewHost()` + `Realize()` ile beslenir → `Radius.Overlay` / `Brush.BorderStrong` / `Effect.PopoverShadow` bağlantıları **gerçekten** doğrulanır.

- [ ] **Step 5: RED — hardcoded ms guard'ı (kapsamlı değil, hedefli)**

Yeni bir guard: `*.xaml` içinde `Duration="0:0:..."` veya `BeginTime` **literal** kullanımı YASAK (süreler `{DynamicResource Duration.X}` olmalı). `*.cs` tarafındaki 31 `*Ms` sabiti **token'a çevrilmez** (hepsi `BuildApp.jsx` satır referansıyla gerekçeli ve `MotionTokens.ResolveDuration` kapısından geçiyor) — **ancak** her birinin XML-doc'unda prototip referansı bulunması bir guard testiyle sabitlenir. Gerekçe kayda geçer.

- [ ] **Step 6: Drift raporu**

`.claude/outputs/<ts>-it5-records.md`'ye (V3): "56/56 brush birebir · 7/7 motion birebir · tek sapma FontAbWindow (düzeltildi) · CSS'te olup XAML'de olmayan 4 token (`--toolbar-height:44` bilinçli 42 · `--dot-outline-width` literal · `--leading-tight` · `--tracking-tight/wide` — hiçbiri kullanılmıyor) · 33 tanımlı-kullanılmayan token **KORUNUR** (spec yüzeyi, `DesignTokenScaleTests` pinliyor)".

- [ ] **GÖZLE KONTROL:** uygulama açılır (realize testleri bunu garantiler ama gözle de doğrula); `--font-ab` ile A/B penceresi açılır ve **çerçeve rengi** diğer yüzeylerle tutarlıdır.

---

# FAZ D — Dağıtım + docs

---

### Task D1: `dotnet publish` çalışır çıktı üretir

**Bugünkü durum — KIRIK:** `CopySupervisorOutput` target'ı `AfterTargets="Build"` ile **yalnız `$(OutDir)`'e** çıplak `<Copy>` yapar (`BuildOrchestrator.App.csproj:35-48`); `ResolvedFileToPublish` item'ı üretmez, `CopyToPublishDirectory` metadata'sı yoktur. Tüm csproj'larda ve `Directory.Build.props`'ta **"Publish" kelimesi hiç geçmiyor**. Sonuç: **`dotnet publish` çıktısında `supervisor\` klasörü OLMAYACAK** → `App.xaml.cs:92`'deki yol çözülmez → **engine hiç başlamaz** ve kullanıcıya **hiçbir hata görünmez** (bugün yalnız `Debug.WriteLine` — `MainWindow.xaml.cs:479-484`). Ayrıca: `RemoveDir $(OutDir)supervisor` her Build'de koşar (uygulama açıkken MSB3061 riski); `GEIST-LICENSE.txt`'in publish'te bulunacağı garanti değil; hiçbir yerde sürüm bilgisi yok (`SupervisorHost.cs:43` hep `1.0.0` döner).

**Files:**
- Modify: `src/BuildOrchestrator.App/BuildOrchestrator.App.csproj`
- Modify: `src/BuildOrchestrator.App/Services/EngineHost.cs` veya `MainWindow.xaml.cs:479-484` (pre-flight + kullanıcıya görünür hata)
- Create: `Directory.Build.props`'a sürüm property'leri
- Test: **yeni** `tests/BuildOrchestrator.Tests/App/PublishLayoutTests.cs`

- [ ] **Step 1: RED — supervisor klasör adı TEK kaynak**

Bugün `"supervisor"` iki yerde **bağımsız string**: `csproj:46-47` ↔ `App.xaml.cs:92`. Biri değişirse derleme hatası **olmaz**, runtime'da sessizce kırılır. Test: csproj'daki hedef klasör adı ile C# sabiti aynı (kaynak dosyaları okuyan guard testi).

- [ ] **Step 2: RED — publish çıktısında supervisor var**

`PublishLayoutTests`: `dotnet publish` **çalıştırılmaz** (test suite'inde ağır); bunun yerine csproj **statik olarak** denetlenir — `ResolvedFileToPublish`/`CopyToPublishDirectory` üreten bir target'ın varlığı ve `AfterTargets`'ın publish akışını kapsaması. **Gerçek publish doğrulaması V3'te elle koşulur ve kanıtı kayda geçer** (bu ayrım kayda yazılır: statik guard + tek seferlik gerçek publish).

- [ ] **Step 3: GREEN — publish-aware kopyalama**

Supervisor çıktısı `ResolvedFileToPublish` item'ları olarak (`RelativePath` = `supervisor\...`) publish'e eklenir; `RemoveDir` yalnız Build akışında ve **Condition ile** (uygulama kilitliyse kırılmasın diye `ContinueOnError="WarnAndContinue"`). Kaynak glob'u `**\publish\**` **hariç** tutulur (self-contained bir Supervisor publish'i yüzlerce MB taşımasın).

- [ ] **Step 4: RED — supervisor bulunamazsa kullanıcı GÖRÜR**

Pre-flight `File.Exists` → yoksa sticky şeridin **kalıcı hata modu** (engine-died yolunun aynısı: `RibbonText.cs:46`) + `Restart engine` yerine açıklayıcı metin. ⚠️ `EngineExited` yolunun **duplicate sinyal üretmemesi** gerekir (`MainWindow.xaml.cs:139-143`). Test: fake yol → tek hata sinyali.

- [ ] **Step 5: Sürüm + lisans**

`Directory.Build.props`'a `Version`/`InformationalVersion` (tek yer); `GEIST-LICENSE.txt` için `CopyToPublishDirectory` açıkça set edilir (OFL "included in all copies" şartı dağıtılan artifact için geçerli) ve `FontAssetTests.cs:48-59`'un publish karşılığı statik olarak denetlenir.
> **Karar:** `PublishSingleFile` **KULLANILMAZ** — `AppContext.BaseDirectory` extraction dizinini gösterir ve `supervisor\` alt klasörü bundle'a giremez; ayrıca pack:// Resource çözümü ve `app.manifest` (PerMonitorV2) davranışı değişir. Framework-dependent, klasör tabanlı publish (`win-x64`) v1 kararıdır ve README'ye yazılır.

- [ ] **GÖZLE KONTROL:** `dotnet publish -c Release -r win-x64 --self-contained false` → çıktı klasöründen `BuildOrchestrator.App.exe` çift tıkla → **açılır**, tray'e küçülür, bir repo seç → Sync + Build **çalışır** (engine gerçekten başlar).

---

### Task D2 (T17): Trust-boundary dokümanı

**Bugünkü durum:** `grep "trust boundary" src/` → **0 eşleşme**; planlarda yalnız tek cümlelik varsayım olarak geçiyor. Keşifte **13 güven yüzeyi** dosya:satır ile çıkarıldı (repo kökü · csproj ham-XML · .sln regex · dosya sistemi tarama · git çıktı parse · kullanıcı regex'leri · IPC NDJSON · kalıcı JSON'lar · process quoting · **keyfi MSBuild yürütme** · path sanitization · Supervisor argümanları · HKCU Run).

**Files:** Create `docs/TRUST-BOUNDARY.md`

- [ ] **Step 1:** Yüzeyleri tabloya dök: girdi · nereden geliyor · nerede parse ediliyor (dosya:satır) · mevcut savunma · **kabul edilen risk**.
- [ ] **Step 2:** Çekirdek ifade: **"orchestrator, kullanıcının VS'de açacağı bir repoyu derler; güven sınırı reponun kendisidir."** Bunun sonucu olarak keyfi MSBuild target/pre-post-build event'leri **kullanıcı yetkisiyle koşar** (`MsBuildInvoker.cs:24`) — bu bir zafiyet değil, **ürünün tanımıdır**; ancak açıkça yazılmalıdır.
- [ ] **Step 3:** Kodun **gerçekten doğrulamadığı** şeyler dürüstçe listelenir: csproj `Include`'unun repo kökü dışına çıkabilmesi (`CsprojEvaluator.cs:70-74`), symlink/junction takibi (`WorkspaceScanner.cs:42-46`), Supervisor `--logs/--worktrees` doğrulamasızlığı (`Program.cs:62,73`), `explorer`/`devenv` elle quoting (`OsActions.cs:105-128`).
- [ ] **Step 4:** Sertleştirilmiş yüzeyler kanıtla anılır: kullanıcı regex'i ReDoS timeout'u (`LayerEngine.cs:49`), NDJSON 1 MiB satır sınırı (`NdjsonFraming.cs:9`), branch slug sanitization (`PathSanitizer.cs:28-59`), atomik state yazımı, bozuk-JSON toleransı.
- [ ] **Step 5:** **K1 salt-okur git** sözleşmesi ve **§4 OutDir dokunulmazlığı** güvenlik açısından yeniden ifade edilir.

---

### Task D3: README

**Bugünkü durum:** repo kökünde **README yok** (yalnız `CLAUDE.md`); `.claude` dışında hiç doküman yok.

**Files:** Create `README.md`

- [ ] **Step 1:** Ne olduğu · mimari (4 proje + nested Job + shell-out) · ekran görüntüsü yerine tek paragraf davranış özeti.
- [ ] **Step 2:** Gereksinimler (.NET 10 SDK, VS Build Tools / MSBuild.exe, git PATH'te), build/test/run komutları, `dotnet publish` yönergesi (D1'in kararlarıyla).
- [ ] **Step 3:** Kullanım: repo seç → Sync → branch/worktree → Build/Rebuild → Stop/Continue/Retry; kısayollar (K6 tablosu); perf modları (**6/4/2 + cpu cap ∞/%70/%40**, canlı değişen kısmın ne olduğu).
- [ ] **Step 4:** Tasarım/mimari kararlara işaretçiler: `docs/TRUST-BOUNDARY.md`, v7 planı, design-v1 paketi. Font lisansı (OFL) anılır.
- [ ] **Step 5:** Bilinen sınırlar: tek repo (v1) · worktree çıktı izolasyonu yok · `UseSharedCompilation=false` kararı (T33, aşağıda) · liste/graf ölçek notları.

---

# FAZ V — Doğrulama, kararlar, kapanış

---

### Task V1: T44 doğrulama + anti-drift testi

**Bugünkü durum:** T44 **teslim edilmiş** — glow yalnız `EventStreamView.xaml.cs:455-470` (GlowMs=1100, `StatusSuccessSoft`→Transparent, per-instance brush, `FillBehavior.Stop`); uygunluk VM'de tek yerde (`StreamEventViewModel.cs:63`: `Done && !anyFailed`); recycle guard **VM'de** (`GlowPlayed`, `:30`) — view-lokal olsaydı `RebuildRows` her reset'te yeniden oynatırdı. **Negatif kanıt:** `grep Glow` → kod tarafında yalnız bu üç dosya; `ProjectRow`/`GraphView`'de success'e bağlı **hiçbir animasyon yok**. Eksik olan tek şey: **iddiayı koruyan test**.

- [ ] **Step 1: RED — anti-drift kaynak taraması**

`AntiSlopTests` desenindeki bir guard: `StatusSuccessSoft` + `BeginAnimation` **aynı dosyada** yalnız `EventStreamView.xaml.cs`'te geçebilir; `ProjectRow`/`GraphView`/`StickyRibbon`'da success brush'ı **animasyon hedefi** olamaz. Test önce mevcut durumu GREEN doğrular, sonra kasıtlı bir ihlal eklenerek RED olduğu kanıtlanır (meta-test).

- [ ] **Step 2:** `EventStreamTests.cs:112-132` (glow-once + recycle) korunur; ek olarak "hatalı koşuda done satırı **parlamaz**" testi eklenir (bugün yalnız VM kuralı var).

- [ ] **GÖZLE KONTROL:** hatasız bir koşu bitir → **yalnız** stream'in son `Completed …` satırı bir kez yeşil parlar; liste ve grafta **hiçbir dalga yok**. Hatalı koşuda parıltı yok.

---

### Task V2: T33 kararı — KAPALI (gerekçe kayda geçer)

**Girdi (SPIKE D9/S5, plan `:341`):** v1 flag'leri kapalıyken 47–50s ↔ açıkken 16–21s (**≈2.9×**); kazanç **tamamen shared compilation'dan** (nodeReuse tek başına ≈0). ANCAK shared compilation açıkken emit **job-DIŞI kalıcı `VBCSCompiler`'da** gerçekleşir → kill anında in-flight emit job garantisinin dışında kalır = **torn-DLL riski** (§3 cascade-kill garantisini kırar).

- [ ] **Step 1:** Torn-DLL'i kapatan bir mekanizma **aranır ve değerlendirilir** (VBCSCompiler'ı inner job'a alma girişimi: `dotnet build-server shutdown` / `MSBUILDDISABLENODEREUSE` / compiler server'ın parent job'ı miras alıp almadığı). Bulgular yazılır.
- [ ] **Step 2:** Kanıtlanamıyorsa **KAPALI bırakılır** (varsayılan). `MsBuildArguments.cs:6-17`'deki flag seti **değişmez**; karar ve gerekçe `it5-records.md` + README "bilinen sınırlar" bölümüne yazılır.
> Ön değerlendirme: `VBCSCompiler` `MSBuild.exe`'nin **child'ı olarak doğar** ve inner job üyeliğini miras alır — ancak **kalıcıdır** (build bitince yaşamaya devam eder) ve bir sonraki run'da **farklı bir job'a** ait olamaz; `TerminateJobObject` onu da öldürür ama **in-flight emit** tam da öldürülmemesi gereken şeydir. Bu, spike'ın tespitini doğrular. Nihai karar Step 1'in bulgusuna göre yazılır.

---

### Task V3: It-5 acceptance sweep + GÖZLE KONTROL listesi üretimi

**Files:** Create `.claude/outputs/<ts>-it5-records.md`

- [ ] **Step 1:** v7 Part C It-5 acceptance'ının **her maddesi** kanıtla eşleştirilir: CPU cap tavanı tutar (P1 Step 3 testi + gözle) · copy fazı starve olmaz (P3) · graf 500–1000 akıcı + cull (G1/G2 ölçümleri) · publish çalışır exe (D1 + elle koşum) · flourish yalnız stream glow (V1) · README + trust-boundary (D2/D3). **It-4 acceptance #5'in kapandığı** ayrıca gösterilir (L1 ölçümleri).
- [ ] **Step 2:** `dotnet build` 0/0 + `dotnet test --filter "Category!=Acceptance"` + **canlı OSYS** `Category=Acceptance` (3/3) koşulur; sayılar kayda geçer.
- [ ] **Step 3: ERTELENEN GÖZLE KONTROL PASI — tek yürünebilir liste**

`it4-records.md §2`'deki 18 task / ~81 madde, **panelden panele sıralı** (task bazlı değil) tek oturumda yürünebilir bir kontrol listesine dönüştürülür: `0) Açılış & publish` → `1) Title bar` → `2) Sticky şerit` → `3) Graf` → `4) Liste` → `5) Konsol` → `6) Event stream` → `7) Action bar + popover'lar` → `8) Settings` → `9) OS eylemleri` → `10) Klavye/a11y` → `11) Motion/reduced-motion` → `12) Durumlar (empty/engine-died/autostart)`. **D4 (konsol gerçek akışı) ZORUNLU** olarak işaretlenir.
It-5'in kendi görsel kalemleri eklenir: perf chip + `cpu cap` konsol notu · graf 500+ akıcılık · publish edilmiş exe'nin ilk açılışı · kart hover ikonlarının ilk-hover davranışı (L1) · sha çiftinin 7-hane gösterimi (W1).

- [ ] **Step 4: E6 Step 3 — tasarımla yan yana**

`prototype/Build Orchestrator (standalone).html` tarayıcıda ↔ uygulama yan yana; README §2.1–§2.9 tek tek. Sapmalar ya düzeltilir ya **A13.1 "algısal eşdeğer"** sınıfına gerekçesiyle yazılır.

- [ ] **Step 5:** Liste kullanıcıya sunulur; kullanıcı yürür, bulguları bildirir → çıkan sapmalar için **fix wave** açılır.

---

## Sıra bağımlılıkları

```
P1 → P2 → P3
G1 → G2
L1 → (L2 yalnız koşullu)
W1 (bağımsız)   W2 (bağımsız)
T1  ← L1, W2'den SONRA (ikisi de XAML kökü/şablon değiştiriyor; realize testleri son halin üstünde yazılsın)
D1 → D2, D3
V1, V2 (bağımsız)  →  V3 (HEPSİNDEN sonra)
```

Önerilen yürütme sırası: **P1 · P2 · P3 · L1 · G1 · G2 · W1 · W2 · T1 · D1 · D2 · D3 · V1 · V2 · V3**
(Perf motoru önce — acceptance'ın ilk iki maddesi ona bağlı; L1 erken çünkü It-4'ün açık kalemi ve T1'in girdisi.)

## Yürütme notları

- **Branch:** `it5-perf-dist` (main'den, BASE `1b63720`). Task başına tek commit. İş bitince main'e merge + push; merge doğrulandıktan sonra branch local+remote silinir; oturum main'de biter.
- **Ledger:** her task sonunda `.superpowers/sdd/progress.md`'ye satır (task, commit SHA, review sonucu, hayatta kalan Minor'lar).
- **Her task sonunda kullanıcıya:** yukarıdaki **GÖZLE KONTROL** adımı + build/test sayıları.
- **Durma kuralı:** G2 Step 5 (DrawingVisual geçişi) veya L2 (virtualization) kapısı açılırsa **durulur ve kullanıcıya bildirilir** — ikisi de büyük, testleri geniş çapta kıran işlerdir.
