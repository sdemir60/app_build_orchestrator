# It-4b Implementation Plan — It-4'ün kalan UI işi + MUST-DO-FIRST motor kalemleri

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development` (önerilen) veya `superpowers:executing-plans` ile task-by-task uygula. Adımlar checkbox (`- [ ]`) ile izlenir.

**Goal:** It-4a'nın kurduğu UI altyapısını gerçek pencereye bağlayarak design-v1'in tam 2×2 uygulamasını üretmek; öncesinde It-3'ten devredilen 5 sessiz-under-build/eksik-wiring motor kalemini kapatmak.

**Architecture:** It-4a primitifleri (Console/, Controls/, Graph/, Shell/, Resources/) **tüketilir, yeniden yazılmaz**. Katman regex'i yalnız Core'da yaşar; App gruplamayı `ProjectNode.LayerName`'den okur. Yeni UI durumu tek `RunViewModel` + saf yardımcılara (RibbonText, RunCounters, ProjectFilter) bölünür. Sync/branch/worktree/topoloji tümüyle IPC üzerinden gelir.

**Tech Stack:** .NET 10 · WPF · CommunityToolkit.Mvvm · AvalonEdit · xUnit + Xunit.StaFact + Xunit.SkippableFact.

**Baseline (bu plan yazılırken doğrulandı):** `main` @ `903e503`, build **0 uyarı / 0 hata**, suite **763 passed / 1 skipped / 0 failed** (`--filter "Category!=Acceptance"`).

---

## Global Constraints

Her task'ın örtük gereksinimi. İhlal = plan ihlali.

- **Görsel otorite = design-v1.** Renk/ölçü/tipografi/animasyon/**kopya metinleri** `.claude/outputs/2026-07-15-19-00-design-v1/README.md` + `prototype/app/BuildApp.jsx` + `prototype/_ds/.../tokens/*.css` + `prototype/_ds/.../_ds_bundle.js`'ten **BİREBİR**. Davranış semantiğinde çelişki çıkarsa v7 planı (`.claude/outputs/2026-07-16-08-39-build-orchestrator-plan-v7-implementation.md`) kazanır.
- **Sim-metin istisnası:** prototipteki `36`, `net8.0`, `Osys.sln`, `D:\src\osys`, `a3f81c2` gibi değerler PLACEHOLDER'dır — **format birebir, sayılar/yollar/SHA gerçek veriden**.
- **MOTION SÖZLEŞMESİ (bağlayıcı, It-4a'dan):** kod-tarafı animasyonlar `App.Motion?.AnimationsEnabled` ve süre/eğriyi **animasyon BAŞLARKEN TAZE** okur (`Controls/MotionTokens.ResolveDuration/ResolveKeySpline` tek kapı; üçüncü bir yol açma). Saf-XAML Storyboard süreleri `{DynamicResource Duration.X}` kullanır, **`{StaticResource}` ASLA**. Hardcoded hex/ms **YASAK**.
- **A13.2 HARFİYEN:** tooltip `InitialShowDelay=0` + `CustomPopupPlacementCallback` (Top/Bottom ORTALAMAZ) · Settings sürükle-sırala `Mouse.Capture` (**`DragDrop.DoDragDrop` YASAK**) · Clipboard retry · 120ms geçişler **template-lokal brush** (frozen/paylaşılan brush anime edilemez) · **koleksiyon reset YASAK** · dash birimi = `StrokeThickness` çarpanı · `ContainerVisual.Opacity` animate edilemez · liste **virtualization KAPALI** kalır (500+ hedefi T51/It-5).
- **Kısayol şeması v7 K6'ya birebir:** `F5` = Build (stopped'ta Continue, koşarken Stop) · `Ctrl+F5`/`Shift+F5` = Rebuild · `Ctrl+F` = proje filtre inputu · `Esc` zinciri (dialog → popover/menü → seçim) · `Alt+B` global hotkey. **Çift-Shift ve Ctrl+P YOK.**
- **Tüm UI metni İNGİLİZCE** (kod yorumları Türkçe kalır). DS bundle'daki Türkçe sabitler (`aria-label="Kaldır"`, `title="Kapat"`, `DependencyGraphNode` başlığındaki `Keşfedildi/Sırada/...`) **taşınmaz** — İngilizce karşılıkları yazılır.
- **Sayı/tarih formatlaması `CultureInfo.InvariantCulture`** (VM'de). Türkçe Windows'ta `4,2s` tuzağı.
- **§4 dokunulmazlığı:** ortak OutDir okunmaz/yazılmaz; "değişti mi" yalnız kaynak sinyalinden. **§3/D8:** nested Job cascade ve ≤2sn kabulü bozulmaz. **D4:** stdout yalnız NDJSON.
- **Test kuralları (mevcut desen — `tests/BuildOrchestrator.Tests/`):** metot adı `Snake_case_tam_cümle`; sınıf başında `[Tnn]` etiketli `<summary>`; WPF'e dokunan test `[StaFact]`; kaynak yükleme `XamlReader.Load(TestAssets/Resources/*.xaml)` (pack:// headless'ta çözülmez); gerçek animasyon clock'u gerekiyorsa `App/AnimationHost.ShowOffscreen`; bekleme `App/DispatcherPump.PumpUntil` (**sabit `Thread.Sleep` YASAK**); WPF kaynak çekişmesi olan yeni test sınıfları `[Collection("Console UI (serial)")]`.
- **Suite yeşil kalır:** her task sonunda `dotnet build BuildOrchestrator.slnx` → 0/0 **ve** `dotnet test --filter "Category!=Acceptance"` → 0 failed. Kırmızıyla bir sonraki task'a geçilmez.
- **Uygulama çalışırken build kilitlenir.** Build/test öncesi `BuildOrchestrator.App` process'i kapalı olmalı.
- **Commit'ler:** her task sonunda tek commit; **push/merge YOK** (kullanıcı onayı gerekir).

---

## Dosya Haritası (bu planın yarattığı/değiştirdiği ana yüzeyler)

| Yol | Sorumluluk | Task |
|---|---|---|
| `src/BuildOrchestrator.Core/Planning/BuildPlanBuilder.cs` | layerPatterns + warnings taşıma | A1 |
| `src/BuildOrchestrator.Core/Incremental/IncrementalPlanner.cs` | SCC-aware kompozit imza | A3 |
| `src/BuildOrchestrator.Core/Scheduling/RetryPlanning.cs` | SCC-aware requeue | A3 |
| `src/BuildOrchestrator.Core/Formatting/DurationFormat.cs` | **YENİ** — `fmtDur`/`fmtElapsed` portu | C2 |
| `src/BuildOrchestrator.Core/Workspace/SyncWorkspaceService.cs` | **YENİ** — fetch + scan + plan + event akışı | A5 |
| `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs` | yeni komut/event'ler + LayerPatterns alanı | A1/A5 |
| `src/BuildOrchestrator.Supervisor/Program.cs` | worktree wiring, layer config, sync servisi | A1/A4/A5 |
| `src/BuildOrchestrator.Supervisor/SupervisorHost.cs` | yeni komut dispatch case'leri | A5 |
| `src/BuildOrchestrator.App/Resources/Tokens.xaml` | tam T49 token seti | B1 |
| `src/BuildOrchestrator.App/Resources/Icons.xaml` | **YENİ** — tüm ikon `Geometry`'leri | B2 |
| `src/BuildOrchestrator.App/Resources/Controls.xaml` | **YENİ** — DS ControlTemplate seti | B3 |
| `src/BuildOrchestrator.App/Controls/AppFonts.cs` | Sans/Mono/MonoConsole tek tanım yeri | B2 |
| `src/BuildOrchestrator.App/Controls/AppTooltip.cs` | **YENİ** — delay=0 + placement callback | B4 |
| `src/BuildOrchestrator.App/MainWindow.xaml(.cs)` | 2×2 kabuk, title bar, splitter'lar | C1 |
| `src/BuildOrchestrator.App/Shell/UiStateStore.cs` | genişletilmiş kalıcı durum | C1 |
| `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs` | faz/sayaç/seçim/filtre/branch/worktree/config/perf | C2 |
| `src/BuildOrchestrator.App/ViewModels/{RibbonText,RunCounters,ProjectFilter}.cs` | **YENİ** — saf yardımcılar | C2/D2 |
| `src/BuildOrchestrator.App/Views/ProjectRow.xaml` | **YENİ** — 7-slot kart | D1 |
| `src/BuildOrchestrator.App/Views/StickyRibbon.xaml` | **YENİ** — faz + chip + hata kümesi + progress | D2 |
| `src/BuildOrchestrator.App/Views/EventStreamView.xaml` | **YENİ** — olay akışı paneli | D3 |
| `src/BuildOrchestrator.App/Views/ActionBar.xaml` | **YENİ** — alt bar + popover'lar + split-button | D6 |
| `src/BuildOrchestrator.App/Views/SettingsDialog.xaml` | **YENİ** — LAYERS + REPOSITORY | D7 |
| `src/BuildOrchestrator.App/Services/{OsActions,MotionCoordinator,AutostartService}.cs` | **YENİ** | E1/E3/E2 |

---

# FAZ A — MUST-DO-FIRST (motor). UI'dan ÖNCE.

> Gerekçe: A1–A3 **sessiz under-build** üretiyor (yanlış "skipped — up to date"); A4 worktree'de **yanlış imza persist** ediyor; A5 olmadan Sync/branch/graf UI'ının besleneceği veri yok. Hiçbiri UI'a bağlı değil, hepsi deterministik test edilebilir.

---

### Task A1: LayerEngine'i canlandır — sıra-bağımsız propagation + warning taşıma

Bugün `BuildPlanBuilder.Build` 3. parametreyi (`layerPatterns`) **hiç almıyor** (`Supervisor/Program.cs:61`), `LayerEngine.AssignLayers` her koşuda `patterns.Count == 0` erken dönüşü yapıyor (`Core/Planning/LayerEngine.cs:48-49`) ve ters-katman uyarıları `BuildPlanBuilder.cs:52-56`'da bilinçli yutuluyor. Katman UI'ı (D7) bağlanmadan önce kapatılmalı: LayerEngine reorder'ı bir projeyi **kendi bağımlılığından önce** koyabilir (`LayerEngine.cs:31-36`, `:77-81`) ve iki tek-geçiş algoritması `plan.Nodes`'un topolojik olduğunu varsayar → sessiz under-build.

**Files:**
- Modify: `src/BuildOrchestrator.Core/Incremental/IncrementalPlanner.cs:114-138`
- Modify: `src/BuildOrchestrator.Core/Scheduling/RetryPlanning.cs:60-66`
- Modify: `src/BuildOrchestrator.Contracts/Model/ProjectModels.cs:62-65` (`BuildPlan` += `LayerWarnings`)
- Modify: `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs:50-51` (`StartRunCommand` += `LayerPatterns`)
- Modify: `src/BuildOrchestrator.Core/Planning/BuildPlanBuilder.cs:52-56`
- Modify: `src/BuildOrchestrator.Supervisor/Program.cs:51-66`
- Modify: `src/BuildOrchestrator.Supervisor/RunCoordinator.cs` (run başında warn satırları)
- Test: `tests/BuildOrchestrator.Tests/Incremental/IncrementalPlannerTests.cs`, `tests/BuildOrchestrator.Tests/Scheduling/RetryPlanningTests.cs`, `tests/BuildOrchestrator.Tests/Planning/BuildPlanBuilderTests.cs`, `tests/BuildOrchestrator.Tests/Supervisor/RunCoordinatorTests.cs`

**Interfaces:**
- Produces: `BuildPlan(IReadOnlyList<ProjectNode> Nodes, IReadOnlyList<IReadOnlyList<string>> Cycles, string Configuration, IReadOnlyList<string>? LayerWarnings = null)` — A5 ve D7 bunu okur.
- Produces: `StartRunCommand(..., DependentMode DependentMode = DependentMode.Safe, IReadOnlyList<LayerPattern>? LayerPatterns = null)` — D7 doldurur.

- [ ] **Step 1: Sıra-bağımsızlık için failing test yaz**

`tests/BuildOrchestrator.Tests/Incremental/IncrementalPlannerTests.cs` sonuna:

```csharp
[Fact]
public void Upstream_change_propagates_even_when_plan_order_is_not_topological()
{
    // LayerEngine reorder'ı bir projeyi kendi bağımlılığından ÖNCE koyabilir (LayerEngine.cs:31-36).
    // Dependent (Down) dizide Upstream'den ÖNCE geliyor; propagation buna rağmen çalışmalı.
    var up = Node("C:\\r\\Up.csproj", "Up", buildOrder: 1, deps: []);
    var down = Node("C:\\r\\Down.csproj", "Down", buildOrder: 0, deps: ["C:\\r\\Up.csproj"]);
    var plan = new BuildPlan([down, up], [], "Debug");

    string sigDownBefore = SignatureOf(plan, "C:\\r\\Down.csproj", upstreamContent: "v1");
    string sigDownAfter = SignatureOf(plan, "C:\\r\\Down.csproj", upstreamContent: "v2");

    Assert.NotEqual(sigDownBefore, sigDownAfter);
}
```

`Node(...)` ve `SignatureOf(...)` yardımcılarını dosyadaki mevcut helper deseniyle yaz (mevcut testler `IncrementalPlannerTests.cs:46` civarında aynı kalıbı kullanıyor); `SignatureOf`, `IncrementalPlanner.ComputeWillBuildWithSignatures`'ı çağırıp dönen `SignatureById[id]`'yi verir, `readFileContent` stub'ı `Up.csproj`'un kaynağı için `upstreamContent`'i döner.

- [ ] **Step 2: Testi çalıştır — kırmızı olduğunu gör**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~Upstream_change_propagates_even_when_plan_order"`
Expected: **FAIL** — `Assert.NotEqual() Failure: Values are equal` (memo miss → `FreshUpstream` `null` → sabit `NullMarker`, `BuildSignature.cs:72,144`).

- [ ] **Step 3: DFS-memo ile sıra bağımsızlığını uygula**

`IncrementalPlanner.cs` içinde `foreach (var node in plan.Nodes)` düz döngüsünü (`:125-136`) **on-stack cycle guard'lı DFS memo** ile değiştir:

```csharp
var byId = plan.Nodes.ToDictionary(n => n.Id, StringComparer.OrdinalIgnoreCase);
var computedMemo = new Dictionary<string, string?>(StringComparer.OrdinalIgnoreCase);
var onStack = new HashSet<string>(StringComparer.OrdinalIgnoreCase);

string Compute(ProjectNode node)
{
    if (computedMemo.TryGetValue(node.Id, out var done) && done is not null) return done;
    // Cycle: üye kendi kendine bağımlı — NullMarker'a düşer (A3 kompozit SCC imzasıyla kapatır).
    if (!onStack.Add(node.Id)) return BuildSignature.NullMarker;

    string? Upstream(string depId) =>
        byId.TryGetValue(depId, out var dep) ? Compute(dep) : null;

    var upstreamSignature = mode == DependentMode.Fast ? FrozenUpstream : (Func<string, string?>)Upstream;
    string signature = BuildSignature.Compute(
        node, plan.Configuration, committedFingerprintForNode(node), dirtyFilesForNode(node),
        readFileContent, upstreamSignature, inPlace);

    onStack.Remove(node.Id);
    computedMemo[node.Id] = signature;
    return signature;
}

foreach (var node in plan.Nodes) Compute(node);
```

`BuildSignature.NullMarker` şu an `private`/internal ise `internal const` yap ve `Core`→`Tests` görünürlüğü için mevcut `InternalsVisibleTo` kullan; **değerini değiştirme**. `Fast` modu davranışı (`FrozenUpstream`, `:121`) aynen korunur.

- [ ] **Step 4: Testi çalıştır — yeşil, sonra tüm Incremental suite'i koştur**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~Incremental"`
Expected: **PASS**, 0 failed. `cycle_members_never_build_regardless_of_signature_or_state` (`IncrementalPlannerTests.cs:246`) ve `unknown_upstream_signature_null_is_tolerated_and_differs_from_a_real_signature` (`BuildSignatureTests.cs:140`) **hâlâ yeşil olmalı**.

- [ ] **Step 5: RetryPlanning için failing test yaz**

`tests/BuildOrchestrator.Tests/Scheduling/RetryPlanningTests.cs`:

```csharp
[Fact]
public void Retry_requeues_a_dependent_that_appears_before_its_failed_dependency_in_plan_order()
{
    var failed = Node("C:\\r\\Up.csproj", "Up", buildOrder: 1, deps: []);
    var dependent = Node("C:\\r\\Down.csproj", "Down", buildOrder: 0, deps: ["C:\\r\\Up.csproj"]);
    var plan = new BuildPlan([dependent, failed], [], "Debug");
    var snapshot = new RunSnapshot(
        Completed: new Dictionary<string, BuildResult>(StringComparer.OrdinalIgnoreCase)
        {
            ["C:\\r\\Up.csproj"] = BuildResult.Failed,
            ["C:\\r\\Down.csproj"] = BuildResult.Succeeded,
        },
        Queued: [], ElapsedMs: 0);

    var requeued = RetryPlanning.RequeueFailedAndDependents(plan, snapshot);

    Assert.Contains("C:\\r\\Down.csproj", requeued);
}
```

`RunSnapshot` ctor imzasını `src/BuildOrchestrator.Supervisor/RunSnapshot.cs`'ten birebir al; dönüş tipi `RetryPlanning.RequeueFailedAndDependents`'ın gerçek dönüşüne göre uyarlanır (id kümesi).

- [ ] **Step 6: Kırmızıyı gör, fixed-point ile düzelt**

Run: `dotnet test ... --filter "FullyQualifiedName~Retry_requeues_a_dependent_that_appears_before"` → **FAIL** (`Assert.Contains` — tek ileri geçiş, `RetryPlanning.cs:60-66`).

Düzeltme: tek geçişi **değişim durana kadar tekrarlayan** sabit-nokta döngüsüne çevir:

```csharp
bool changed = true;
while (changed)
{
    changed = false;
    foreach (var node in plan.Nodes)
    {
        if (affected.Contains(node.Id)) continue;
        bool isAffected = (snapshot.Completed.TryGetValue(node.Id, out var r) && r == BuildResult.Failed)
                          || node.Dependencies.Any(affected.Contains);
        if (isAffected) { affected.Add(node.Id); changed = true; }
    }
}
```

Run: aynı filtre → **PASS**; ardından `--filter "FullyQualifiedName~RetryPlanning"` → 0 failed.

- [ ] **Step 7: layerPatterns + warnings'i uçtan uca taşı**

1. `ProjectModels.cs:62-65` → `public sealed record BuildPlan(IReadOnlyList<ProjectNode> Nodes, IReadOnlyList<IReadOnlyList<string>> Cycles, string Configuration, IReadOnlyList<string>? LayerWarnings = null);`
2. `BuildPlanBuilder.cs:52-56` → yutmayı kaldır: `return new BuildPlan(assignment.Nodes, topo.Cycles, configuration, assignment.Warnings);`
3. `IpcMessages.cs:50-51` → `StartRunCommand` sonuna `IReadOnlyList<LayerPattern>? LayerPatterns = null` ekle.
4. `Supervisor/Program.cs:61` → `.Build(scan, cmd.Configuration, cmd.LayerPatterns)`.
5. `RunCoordinator` run başlangıcında (`RunStartedEvent` emit'inden hemen sonra, `RunCoordinator.cs:429` civarı) her uyarıyı konsola bas: `run.Console($"warning: {w}")` — metin `LayerEngine.cs:62-75`'in ürettiği string, **değiştirilmeden**.

Yeni test — `tests/BuildOrchestrator.Tests/Planning/BuildPlanBuilderTests.cs`:

```csharp
[Fact]
public void Reverse_layer_dependency_warning_reaches_the_build_plan_instead_of_being_swallowed()
{
    var plan = BuildPlanWithLayers(); // mevcut :52 testindeki pattern kurulumunu yeniden kullan,
                                      // düşük katmandaki bir projeyi üst katmandakine bağımlı yap
    Assert.NotNull(plan.LayerWarnings);
    Assert.Contains(plan.LayerWarnings!, w => w.StartsWith("reverse layer dependency:", StringComparison.Ordinal));
}
```

Ve `RunCoordinatorTests.cs`'e: plan'ı `LayerWarnings: ["reverse layer dependency: X ..."]` ile veren stub planner → koşu konsol çıktısında `warning: reverse layer dependency:` satırının göründüğünü assert et (Harness `console:` delegesi zaten var, `RunCoordinatorTests.cs:100-118`).

- [ ] **Step 8: Tam suite + build**

Run: `dotnet build BuildOrchestrator.slnx` → **0 uyarı / 0 hata**
Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "Category!=Acceptance"` → **0 failed**

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "fix(A1/T15): LayerEngine wiring + order-independent propagation

Katman pattern'leri StartRunCommand ile Core'a ulasiyor, ters-katman
uyarilari BuildPlan.LayerWarnings uzerinden konsola cikiyor.
IncrementalPlanner artik dizi sirasi yerine DFS-memo ile, RetryPlanning
sabit-nokta ile calisiyor - katman reorder'i artik sessiz under-build
uretemez."
```

---

### Task A2: depIssue-persist stale-skip penceresini kapat + Build pre-skip'i deterministik test et

`RunCoordinator.cs:622` her başarıda `PersistBuildStateOnSuccess` çağırıyor; `depIssues` o noktada zaten hesaplı (`:581-587`). depIssue taşıyan bir success, **bayat upstream çıktısına** link'li olduğu halde taze imza persist ediyor → upstream kaynak değişmeden düzelirse (obj temizliği sınıfı) bağımlı bir daha asla derlenmiyor. Ayrıca `RunMode.Build` pre-skip yolunun (`:378-390`) **tek kanıtı** 30 dakikalık `Category=Acceptance` OSYS koşusu.

**Files:**
- Modify: `src/BuildOrchestrator.Supervisor/RunCoordinator.cs:622`
- Test: `tests/BuildOrchestrator.Tests/Supervisor/RunCoordinatorTests.cs`

**Interfaces:** Consumes: A1'in `BuildPlan.LayerWarnings` opsiyonel parametresi (imza değişimi bu dosyadaki plan kurulumlarını etkiler).

- [ ] **Step 1: Persist penceresi için failing test yaz**

```csharp
[Fact]
public async Task A_success_carrying_a_dep_issue_does_not_persist_build_state()
{
    using var temp = new TempDir();
    var store = new BuildStateStore(temp.Path);
    // Up fail eder, Down (Up'a bagimli) basarili olur -> Down depIssue tasir.
    await using var h = Harness.Create(plan: PlanOf(("Up", []), ("Down", ["Up"])), stateStore: store,
                                       invoker: FailFor("Up"));
    await h.Start(RunMode.Rebuild);
    await h.WaitForRunEnd();

    Assert.DoesNotContain(store.Load(), s => s.ProjectId.EndsWith("Down.csproj", StringComparison.Ordinal));
}
```

Harness/`FailFor`/`PlanOf` yardımcılarını dosyadaki mevcut `Harness` (`:88-118`) ve `FakeInvoker` (`:65-84`) üzerinden kur; `stateStore:` parametresi zaten var (`:117`) ve bugüne kadar **hiçbir test doldurmuyor**.

- [ ] **Step 2: Kırmızıyı gör**

Run: `dotnet test ... --filter "FullyQualifiedName~A_success_carrying_a_dep_issue_does_not_persist"`
Expected: **FAIL** — `Down` için `BuildState` yazılmış.

- [ ] **Step 3: Tek satırlık güvenli kapatma**

`RunCoordinator.cs:622`:

```csharp
// [A2] depIssue tasiyan success BAYAT upstream ciktisina link'lidir; taze imza persist edilirse
// upstream kaynak degismeden duzeldiginde bu proje bir daha asla derlenmez (stale-skip).
if (depIssues.All.Count == 0)
    PersistBuildStateOnSuccess(run, projectId, invoke.DurationMs);
```

- [ ] **Step 4: Yeşili gör**

Run: aynı filtre → **PASS**.

- [ ] **Step 5: Build pre-skip için deterministik (non-acceptance) test yaz**

```csharp
[Fact]
public async Task Build_mode_pre_skips_up_to_date_nodes_without_invoking_msbuild_and_persists_the_built_ones()
{
    using var temp = new TempDir();
    var store = new BuildStateStore(temp.Path);
    var clean = Node("C:\\r\\Clean.csproj", "Clean", willBuild: false);
    var dirty = Node("C:\\r\\Dirty.csproj", "Dirty", willBuild: true);
    var runPlan = new RunPlan(
        new BuildPlan([clean, dirty], [], "Debug"),
        Solutions: new Dictionary<string, IReadOnlyList<SolutionRef>>(StringComparer.OrdinalIgnoreCase),
        Incremental: new IncrementalPlan(
            new Dictionary<string, string> { ["C:\\r\\Dirty.csproj"] = "sig-dirty" }, "headsha", "main"));

    await using var h = Harness.Create(planner: _ => runPlan, stateStore: store);
    await h.Start(RunMode.Build);
    await h.WaitForRunEnd();

    Assert.Contains(h.Events.OfType<ProjectSkippedEvent>(),
        e => e.ProjectId == "C:\\r\\Clean.csproj" && e.Reason == "skipped — up to date");
    Assert.DoesNotContain(h.Invoker.Requests, r => r.ProjectId == "C:\\r\\Clean.csproj");
    Assert.Contains(store.Load(), s => s.ProjectId == "C:\\r\\Dirty.csproj");
}
```

`Node(...)` helper'ı (`RunCoordinatorTests.cs:30-32`) bugün `WillBuild: null` sabitliyor — **`bool? willBuild = null` parametresi ekleyerek genişlet** (mevcut çağrılar bozulmaz).

- [ ] **Step 6: Yeşili gör + tam suite**

Run: `dotnet test ... --filter "FullyQualifiedName~Build_mode_pre_skips"` → **PASS**
Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "fix(A2): depIssue tasiyan success BuildState persist etmesin + Build pre-skip deterministik test

Stale-skip penceresi kapandi (bayat upstream'e link'li binary artik
guncel sayilmiyor). RunMode.Build pre-skip yolu ilk kez acceptance
disinda, in-process ve deterministik olarak kanitlaniyor."
```

---

### Task A3: SCC-aware propagation — cycle-tangled transitive under-build

A1'den sonra tek geçiş sorunu kalktı ama SCC üyeleri hâlâ `NullMarker`'a düşüyor: SCC'nin **dışındaki** bir downstream, SCC üzerinden gelen gerçek kaynak değişimini asla görmüyor (`BuildSignature.cs:137-145` sabit `NullMarker`). Bu sınır `OsysIncrementalAcceptanceTests.cs:126-133`'te itiraf edilmiş ve iddia "çoğunluğa" zayıflatılmış (`:205`).

**Files:**
- Modify: `src/BuildOrchestrator.Core/Incremental/IncrementalPlanner.cs`
- Test: `tests/BuildOrchestrator.Tests/Incremental/IncrementalPlannerTests.cs`
- Modify (assertion güçlendirme): `tests/BuildOrchestrator.Tests/Integration/OsysIncrementalAcceptanceTests.cs:205`

**Interfaces:** Consumes: A1'in `Compute(node)` DFS'i. Produces: davranış değişikliği; `BuildSignature.Compute`'un `upstreamSignature` sözleşmesi **değişmez** (planner kompoziti besler).

- [ ] **Step 1: Failing test yaz**

```csharp
[Fact]
public void A_source_change_inside_a_cycle_propagates_to_a_downstream_outside_the_cycle()
{
    // SCC = {A,B} (A->B, B->A), D cycle DISINDA ve A'ya bagimli.
    var a = Node("C:\\r\\A.csproj", "A", deps: ["C:\\r\\B.csproj"], inCycle: true);
    var b = Node("C:\\r\\B.csproj", "B", deps: ["C:\\r\\A.csproj"], inCycle: true);
    var d = Node("C:\\r\\D.csproj", "D", deps: ["C:\\r\\A.csproj"]);
    var plan = new BuildPlan([a, b, d], [["C:\\r\\A.csproj", "C:\\r\\B.csproj"]], "Debug");

    string before = SignatureOf(plan, "C:\\r\\D.csproj", contentOf: "C:\\r\\B.csproj", value: "v1");
    string after  = SignatureOf(plan, "C:\\r\\D.csproj", contentOf: "C:\\r\\B.csproj", value: "v2");

    Assert.NotEqual(before, after);
}
```

- [ ] **Step 2: Kırmızıyı gör**

Run: `dotnet test ... --filter "FullyQualifiedName~A_source_change_inside_a_cycle_propagates"` → **FAIL** (eşit imzalar).

- [ ] **Step 3: SCC kompozit imzasını uygula**

`IncrementalPlanner`'da, `Compute` DFS'inden önce `plan.Cycles`'tan üye→component eşlemesi kur; bir SCC üyesi için imza **component başına bir kez** hesaplanır:

```csharp
// component id -> uyelerin kendi terimleri + SCC-DISI upstream imzalari uzerinden TEK hash
string ComputeComponent(IReadOnlyList<string> members)
{
    if (_componentMemo.TryGetValue(members[0], out var cached)) return cached;
    var ordered = members.OrderBy(m => m, StringComparer.OrdinalIgnoreCase).ToList();
    var sb = new StringBuilder();
    foreach (var id in ordered)
    {
        var n = byId[id];
        // uye kendi terimleri: SCC-ICI upstream'ler DISLANIR (donguyu kirar)
        sb.Append(BuildSignature.Compute(
            n, plan.Configuration, committedFingerprintForNode(n), dirtyFilesForNode(n), readFileContent,
            depId => membersSet.Contains(depId) ? BuildSignature.NullMarker : Compute(byId[depId]),
            inPlace));
        sb.Append(BuildSignature.ItemSeparator);
    }
    string composite = BuildSignature.HashText(sb.ToString());
    foreach (var id in ordered) _componentMemo[id] = composite;
    return composite;
}
```

`Compute(node)`: düğüm bir SCC üyesiyse `ComputeComponent(componentOf[node.Id])` döner; değilse A1'deki gövde çalışır. `HashText`/`ItemSeparator` `BuildSignature`'da zaten var (`:72`, `:137-145`) — gerekiyorsa `internal` yap, **algoritmayı değiştirme**.

Not: cycle üyeleri hâlâ **derlenmez** (`WillBuildEvaluator.cs:14`) — bu düzeltme yalnız downstream'in doğru cascade almasını sağlar.

- [ ] **Step 4: Yeşili gör; cycle-member davranışını koruduğunu kanıtla**

Run: `dotnet test ... --filter "FullyQualifiedName~Incremental"` → **PASS**, `cycle_members_never_build_regardless_of_signature_or_state` dahil 0 failed.

- [ ] **Step 5: RetryPlanning'i aynı körlükten kurtar**

A1'in sabit-nokta döngüsü SCC'yi zaten kapsıyor; bunu **pinleyen** bir test ekle:

```csharp
[Fact]
public void Retry_requeues_the_whole_scc_and_its_downstream_when_any_member_is_affected()
```
(SCC={A,B}, D→A; A failed → beklenen requeue kümesi {A, B, D}.)

- [ ] **Step 6: Acceptance iddiasını sıkılaştır**

`OsysIncrementalAcceptanceTests.cs:205`'teki `transFlipped >= transNonCycle.Count / 2` gevşetmesini `transFlipped == transNonCycle.Count` yap ve `:126-133`'teki "bilinen sınır" yorumunu **A3 ile kapatıldı** notuyla güncelle. (Acceptance normal koşuda çalışmaz; kullanıcı isterse ayrıca koşulur.)

- [ ] **Step 7: Tam suite + commit**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

```bash
git add -A
git commit -m "fix(A3): SCC-aware propagation - cycle uzerinden gecen degisim artik downstream'e ulasiyor

SCC uyeleri icin component basina TEK kompozit imza uretiliyor; sabit
NullMarker yerine gercek icerik hash'i akiyor. It-3'te 'bilinen sinir'
diye kayda gecen cycle-tangled transitive under-build kapandi."
```

---

### Task A4: Worktree'yi uçtan uca BUILD'e bağla

`WorktreeManager` src'de **hiç new'lenmiyor**; `RunCoordinator`'ın `worktreeObjRootResolver` seam'i (`:92`, `:407`, `:599-601`) hazır ama `Program.cs:36-44` onu **vermiyor** → obj izolasyonu ölü kod. Daha kötüsü `Program.cs:101` `inPlace = !cmd.UseWorktree` **tahmin** ediyor: `UseWorktree=true` geldiğinde imza local-diff terimini atlıyor (`BuildSignature.cs:110-129`) ama derleme gerçekte dirty working tree üzerinde koşuyor → **yanlış imza persist**. Continue ise `UseWorktree` default `false` ile gidiyor (`RunViewModel.cs:241`) → segment-2 sessizce default obj'ye düşüyor.

**Files:**
- Modify: `src/BuildOrchestrator.Supervisor/Program.cs:36-44, 51-66, 101`
- Modify: `src/BuildOrchestrator.Supervisor/RunCoordinator.cs:331-356` (Continue worktree mirası), `:363-373`
- Test: `tests/BuildOrchestrator.Tests/Supervisor/RunCoordinatorTests.cs`, `tests/BuildOrchestrator.Tests/Git/WorktreeManagerTests.cs`

**Interfaces:**
- Produces: `RunCoordinator` artık aktif run'ın çözülmüş worktree kökünü saklar; `Continue`/`RetryFailed` segmentleri **aynı kökü** kullanır.
- Produces: `Program.cs` içinde `Func<StartRunCommand, PreparedWorkspace>` — `PreparedWorkspace(string ScanRoot, string? WorktreeObjRoot, bool InPlace)`.

- [ ] **Step 1: Continue'nun worktree'yi miras aldığını iddia eden failing test yaz**

```csharp
[Fact]
public async Task Continue_inherits_the_original_runs_worktree_obj_root()
{
    var resolverCalls = new List<StartRunCommand>();
    await using var h = Harness.Create(worktreeObjRootResolver: cmd => { resolverCalls.Add(cmd); return "C:\\wt\\main-2"; });
    await h.Start(RunMode.Rebuild, useWorktree: true);
    await h.StopAndWait();
    await h.Start(RunMode.Continue, useWorktree: false);   // App bugun UseWorktree=false yolluyor
    await h.WaitForRunEnd();

    Assert.All(h.Invoker.Requests, r => Assert.StartsWith("C:\\wt\\main-2", r.BaseIntermediateOutputPath!, StringComparison.Ordinal));
}
```

- [ ] **Step 2: Kırmızıyı gör**

Run: `dotnet test ... --filter "FullyQualifiedName~Continue_inherits_the_original_runs_worktree"`
Expected: **FAIL** — ikinci segmentte `BaseIntermediateOutputPath` `null` (`:407` her segmentte yeni `cmd`'den hesaplıyor).

- [ ] **Step 3: Worktree kökünü run state'ine taşı**

`RunCoordinator`'da `_plan`/`_root`/`_logs`'un yanına `_worktreeObjRoot` alanı ekle; `:407`'yi şu şekilde değiştir: **yeni** run başlarken hesapla ve sakla, `Continue`/`RetryFailed` resume yolunda (`:331-356`) saklanandan oku.

- [ ] **Step 4: Yeşili gör**

Run: aynı filtre → **PASS**; ardından `--filter "FullyQualifiedName~worktree"` → mevcut 4 worktree testi (`:823`, `:846`, `:860`, `:1046`) hâlâ yeşil.

- [ ] **Step 5: Program.cs'te gerçek worktree hazırlığını bağla**

`Program.cs`:

```csharp
// [A4] Worktree Build ANINDA hazirlanir (K3). inPlace TAHMIN degil, COZULMUS koke gore turetilir.
static async Task<PreparedWorkspace> PrepareAsync(StartRunCommand cmd, ProcessRunner runner, CancellationToken ct)
{
    if (!cmd.UseWorktree) return new PreparedWorkspace(cmd.RootPath, null, InPlace: true);

    var mgr = new WorktreeManager(runner, cmd.RootPath, WorktreeManager.DefaultPoolRoot);
    var plan = mgr.PlanWorktree(cmd.Branch, activeBranch, useWorktreeToggle: true, cmd.WorktreeName);
    var prepared = await mgr.PrepareWorktreeAsync(plan, ct);
    if (!prepared.Ok)
    {
        Console.Error.WriteLine($"warning: worktree preparation failed ({prepared.Error}) — falling back to in-place build");
        return new PreparedWorkspace(cmd.RootPath, null, InPlace: true);   // basarisizsa GUVENLI taraf
    }
    return new PreparedWorkspace(prepared.Value, prepared.Value, InPlace: false);
}
```

Sonra: `BuildRunPlan` **`prepared.ScanRoot`'u tarar** (`Program.cs:60`, artık `cmd.RootPath` değil); `RunCoordinator` ctor'una `worktreeObjRootResolver: _ => prepared.WorktreeObjRoot` verilir; `Program.cs:101` → `bool inPlace = prepared.InPlace;`. Havuz cap'i için run sonunda `mgr.PruneToCapAsync(...)` çağrılır (`WorktreeManager.cs:237` bugün hiç çağrılmıyor).

- [ ] **Step 6: Gerçek worktree üzerinde build seam testi**

`WorktreeManagerTests.cs`'e (mevcut `GitTestRepo` fixture'ıyla):

```csharp
[Fact]
public async Task PrepareAsync_falls_back_to_in_place_and_reports_in_place_when_the_worktree_cannot_be_created()
[Fact]
public async Task PrepareAsync_returns_the_worktree_path_as_both_scan_root_and_obj_root_on_success()
```

- [ ] **Step 7: Tam suite + commit**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

```bash
git add -A
git commit -m "feat(A4): worktree ucdan uca BUILD wiring

Program.cs artik Build aninda gercek worktree hazirliyor, taramayi
worktree kokunden yapiyor ve obj izolasyonunu RunCoordinator'a besliyor.
inPlace bayragi tahmin yerine COZULMUS koke gore turetiliyor (yanlis
imza persist penceresi kapandi); Continue orijinal run'in worktree'sini
miras aliyor."
```

---

### Task A5: Sync / branch / worktree / topoloji IPC'sini uçtan uca aç

Bugün `SyncWorkspaceCommand` `SupervisorHost.cs:49-50` default dalına düşüp `unknownCommand` hatası veriyor; `SyncStartedEvent`/`SyncProgressEvent`/`SyncCompletedEvent`/`BranchListEvent` src'de **hiç new'lenmiyor**; `RunViewModel.OnEvent` (`:270-286`) bunlar için case taşımıyor. Ayrıca **hiçbir event dependency/layer/solution taşımıyor** → graf paneli (D5), katman gruplaması (D1) ve Open-in-VS (E1) beslenemez.

**Files:**
- Modify: `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs`
- Create: `src/BuildOrchestrator.Core/Workspace/SyncWorkspaceService.cs`
- Modify: `src/BuildOrchestrator.Supervisor/SupervisorHost.cs:33-52`
- Modify: `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs:270-286`
- Test: `tests/BuildOrchestrator.Tests/Ipc/IpcMessagesTests.cs`, `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs`, `tests/BuildOrchestrator.Tests/Workspace/SyncWorkspaceServiceTests.cs` (**yeni klasör**), `tests/BuildOrchestrator.Tests/App/RunViewModelTests.cs`

**Interfaces (sonraki tüm UI task'ları bunları tüketir):**

```csharp
// Contracts/Ipc/IpcMessages.cs — YENİ komutlar
[JsonDerivedType(typeof(ListBranchesCommand), "listBranches")]
[JsonDerivedType(typeof(ListWorktreesCommand), "listWorktrees")]
[JsonDerivedType(typeof(DeleteWorktreeCommand), "deleteWorktree")]
public sealed record ListBranchesCommand(string RootPath) : IpcCommand;
public sealed record ListWorktreesCommand(string RootPath) : IpcCommand;
public sealed record DeleteWorktreeCommand(string RootPath, string Name) : IpcCommand;
// SyncWorkspaceCommand genişler:
public sealed record SyncWorkspaceCommand(string RootPath, string Branch,
    IReadOnlyList<LayerPattern>? LayerPatterns = null) : IpcCommand;

// YENİ event'ler
[JsonDerivedType(typeof(WorkspaceTopologyEvent), "workspaceTopology")]
[JsonDerivedType(typeof(WorktreeListEvent), "worktreeList")]
public sealed record WorkspaceTopologyEvent(
    IReadOnlyList<ProjectNode> Nodes,
    IReadOnlyList<IReadOnlyList<string>> Cycles,
    IReadOnlyList<SolutionRef> Solutions,
    IReadOnlyList<string> LayerWarnings) : IpcEvent;
public sealed record WorktreeListEvent(IReadOnlyList<Worktree> Worktrees) : IpcEvent;
```

- [ ] **Step 1: Contract round-trip testlerini yaz (RED)**

`IpcMessagesTests.cs`'e mevcut `StartRun_roundtrips_with_discriminator` (`:43`) desenini kopyalayarak:

```csharp
[Fact] public void ListBranches_listWorktrees_deleteWorktree_roundtrip_with_discriminators()
[Fact] public void WorkspaceTopology_roundtrips_with_nodes_cycles_solutions_and_layer_warnings()
[Fact] public void WorktreeList_roundtrips_with_discriminator()
[Fact] public void SyncWorkspace_carries_layer_patterns()
```

Run: `dotnet test ... --filter "FullyQualifiedName~Ipc"` → **FAIL** (tipler yok).

- [ ] **Step 2: Contracts'ı ekle, yeşili gör**

Yeni record'ları ve `[JsonDerivedType]` satırlarını ekle. Run: aynı filtre → **PASS**.

- [ ] **Step 3: SyncWorkspaceService için failing test yaz**

`tests/BuildOrchestrator.Tests/Workspace/SyncWorkspaceServiceTests.cs`:

```csharp
[Fact]
public async Task Sync_emits_started_then_granular_progress_then_completed_with_topology()
{
    using var repo = GitTestRepo.CreateWithCommit();     // Git/GitTestRepo.cs
    var events = new List<IpcEvent>();
    var svc = new SyncWorkspaceService(scanner, evaluator, cache, gitService);
    await svc.RunAsync(new SyncWorkspaceCommand(repo.Path, "main"), events.Add, CancellationToken.None);

    Assert.IsType<SyncStartedEvent>(events[0]);
    Assert.Contains(events.OfType<SyncProgressEvent>(), e => e.Line.StartsWith("▸ git fetch origin ", StringComparison.Ordinal));
    Assert.Contains(events, e => e is WorkspaceTopologyEvent);
    var done = Assert.IsType<SyncCompletedEvent>(events[^1]);
    Assert.False(done.FetchDegraded);
}

[Fact]
public async Task Sync_degrades_with_a_warn_line_and_local_head_when_the_remote_is_unreachable()
{
    // ... Assert: SyncProgressEvent { Level = "warn" } var; SyncCompletedEvent.FetchDegraded == true
}
```

Granular satır metinleri **design-v1 §3.1'den birebir** (`README.md:225`):
`▸ git fetch origin main` (cmd) → `HEAD b7e91d4 — computing osys-state diff` (info) → `Sync complete — 7 changed projects, 14 to build` (info) → `22 projects up to date (will skip)` (dim). Sayılar/SHA **gerçek veriden**. Ek N1 tarama satırları v7 A5'e göre fetch satırından **sonra** dim/info tonunda basılır.

- [ ] **Step 4: Kırmızıyı gör, servisi yaz**

`SyncWorkspaceService`: `GitService.FetchRefOnlyAsync` (`Core/Git/GitService.cs:185`) → `GetRemoteTrackingShaAsync` (`:211`) → `scanner.Scan` → `BuildPlanBuilder.Build(scan, configuration, cmd.LayerPatterns)` → `WorkspaceTopologyEvent` + `SyncCompletedEvent`. **Checkout/pull/reset ASLA** (K1). Ağ yoksa: `SyncProgressEvent(Level:"warn")` + yerel HEAD ile devam.

Run: `dotnet test ... --filter "FullyQualifiedName~SyncWorkspaceService"` → **PASS**.

- [ ] **Step 5: SupervisorHost dispatch + IPC testi**

`SupervisorHost.DispatchAsync` switch'ine `SyncWorkspaceCommand`, `ListBranchesCommand`, `ListWorktreesCommand`, `DeleteWorktreeCommand` case'lerini ekle (`GitService.ListBranchesAsync` `:143`, `WorktreeManager.ListWorktreesAsync` `:200` / `DeleteAsync` `:262`). `SupervisorIpcTests.cs`'e: `syncWorkspace` gönder → stdout'ta **yalnız NDJSON** ve sırasıyla `syncStarted … workspaceTopology … syncCompleted` diskriminatörleri.

- [ ] **Step 6: App tarafında handler'lar**

`RunViewModel.OnEvent` switch'ine 5 case ekle; yeni state:

```csharp
[ObservableProperty] private AppPhase phase = AppPhase.Empty;             // C2'de enum tanımlanır; burada Syncing/Idle geçişleri
[ObservableProperty] private string? targetSha;
[ObservableProperty] private bool fetchDegraded;
public ObservableCollection<BranchRef> Branches { get; } = [];
public ObservableCollection<Worktree> Worktrees { get; } = [];
public IReadOnlyList<ProjectNode> Topology { get; private set; } = [];
public IReadOnlyList<SolutionRef> Solutions { get; private set; } = [];
public event EventHandler? TopologyChanged;                                // D1/D5 bunu dinler
```

`WorkspaceTopologyEvent` geldiğinde `Projects` koleksiyonu topolojiden **yeniden kurulur** (build-order sırasında) ve `TopologyChanged` tetiklenir. `SyncProgressEvent` konsola `ConsoleBatcher.Post` ile düşer.

`RunViewModelTests.cs`'e: `Sync_events_update_phase_target_sha_and_topology`, `Branch_list_event_fills_branches`, `Sync_progress_lines_reach_the_console_batcher`.

- [ ] **Step 7: Tam suite + commit**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

```bash
git add -A
git commit -m "feat(A5/T69): Sync + branch + worktree + topoloji IPC'si ucdan uca

syncWorkspace artik unknownCommand degil: fetch(ref-only) -> tarama ->
plan -> workspaceTopology + syncCompleted akisi calisiyor; offline'da
warn + yerel HEAD ile degrade ediyor. listBranches/listWorktrees/
deleteWorktree eklendi. App tarafi bu event'leri tuketiyor - graf,
katman gruplamasi ve Open-in-VS icin gereken veri artik UI'a ulasiyor."
```

---

# FAZ B — UI temelleri (token → asset → kontrol → tooltip)

---

### Task B1 (T49): Tam token ResourceDictionary + hardcoded hex temizliği

Bugün `Resources/Tokens.xaml` **bilinçli bir alt küme** (`:4-7`): 45 brush + `Radius.Overlay` + `Effect.PopoverShadow`. Tipografi/spacing/radius/layout ölçekleri **hiç yok**; `MainWindow.xaml` kökü 3 hardcoded hex taşıyor (`:6` `#0F1115`, `:6` `#D7DBE0`, `:44` `#14171C`) ve bunların hiçbiri token değeriyle **eşleşmiyor** (`#0e0e10`/`#ededee`/`#141417` olmalı). `ConsoleView.xaml:12` font boyutunun yerel kopyasını tutuyor.

**Files:**
- Modify: `src/BuildOrchestrator.App/Resources/Tokens.xaml`
- Modify: `src/BuildOrchestrator.App/MainWindow.xaml:6,44`, `MainWindow.xaml.cs:205`
- Modify: `src/BuildOrchestrator.App/Console/ConsoleView.xaml:12`
- Test: `tests/BuildOrchestrator.Tests/App/TokenBrushesTests.cs`, **yeni** `tests/BuildOrchestrator.Tests/App/DesignTokenScaleTests.cs`, **yeni** `tests/BuildOrchestrator.Tests/App/NoHardcodedColorTests.cs`

**Interfaces (sonraki her UI task'ı bu anahtarları kullanır — yeni ad uydurma):**

```
Brush.Black #08080a · Brush.White #ffffff · Brush.Neutral200 #cdcdd2
Brush.SurfaceHover #1a1a1e · Brush.SurfaceActive #202024 · Brush.Scrim #99040406
Brush.StatusBuilding (=Brush.Amber) · Brush.StatusBuildingText · Brush.StatusBuildingSoft · Brush.StatusBuildingBorder
Brush.StatusQueuedBorder #3D7C7C84                     (CSS'te yok — README §1.1 "4'lü ton" kuralından türetildi, %24 alfa)
FontSize.2xs 11 · FontSize.Xs 12 · FontSize.Sm 13 · FontSize.Md 14 · FontSize.Lg 16 · FontSize.Xl 20 · FontSize.2xl 26 · FontSize.3xl 34
FontWeight.Body Normal · FontWeight.Emphasis Medium · FontWeight.Heading SemiBold      (kaynak: README §1.2)
LineHeight.Snug13 17.55 · LineHeight.Normal13 19.5 · LineHeight.Mono12 18.6            (WPF LineHeight mutlak DIP ister)
Tracking.Caps 0.07                                                                     (TrackedTextBlock.TrackingEm)
Space.1 4 · Space.2 8 · Space.3 12 · Space.4 16 · Space.5 20 · Space.6 24 · Space.8 32 · Space.10 40 · Space.12 48 · Space.16 64
Radius.None 0 · Radius.Xs 3 · Radius.Sm 4 · Radius.Md 6 · Radius.Lg 8 · Radius.Full 999   (Radius.Overlay = Radius.Lg alias'ı KALIR)
Size.TitleBarHeight 40 · Size.RibbonHeight 32 · Size.ProgressHeight 2 · Size.ActionBarHeight 42
Size.PanelHeaderHeight 28        ← v7 T49: statusbar-28 token'ı BU ADI alır; ayrı statusbar bölgesi YOK
Size.RowHeight 36 · Size.RowHeightCompact 30 · Size.LayerHeaderHeight 24 · Size.DotSize 8 · Size.FocusRingWidth 2 · Size.FocusRingOffset 1
Size.WindowMinWidth 1240 · Size.WindowMinHeight 620
Effect.OverlayShadow   (DropShadowEffect: Direction=270 ShadowDepth=10 BlurRadius=28 Opacity=0.66 — spread yok, A13.1-2)
```

`Size.RowHeight`/`Size.LayerHeaderHeight` **ikinci otorite yaratmaz**: değerleri `Controls/LayoutMetrics.DefaultRowHeight/DefaultHeaderHeight`'tan gelir ve XAML tarafı **eskisi gibi `{x:Static controls:LayoutMetrics.DefaultRowHeight}` kullanmaya devam eder** (`StickyLayerList.xaml:13,27`). Token yalnız diğer (liste dışı) tüketiciler içindir ve bir test iki kaynağın eşitliğini pinler.

- [ ] **Step 1: Ölçek token'ları için failing test yaz**

`tests/BuildOrchestrator.Tests/App/DesignTokenScaleTests.cs`:

```csharp
/// <summary>[T49] design-v1 tokens/*.css ölçeklerinin WPF karşılıkları — değerler BİREBİR.</summary>
[Collection("Console UI (serial)")]
public sealed class DesignTokenScaleTests
{
    private static ResourceDictionary Tokens()
    {
        string path = Path.Combine(AppContext.BaseDirectory, "TestAssets", "Resources", "Tokens.xaml");
        using var stream = File.OpenRead(path);
        return (ResourceDictionary)XamlReader.Load(stream);
    }

    [StaFact]
    public void Font_size_scale_matches_typography_css()
    {
        var t = Tokens();
        Assert.Equal(11.0, (double)t["FontSize.2xs"]);
        Assert.Equal(12.0, (double)t["FontSize.Xs"]);
        Assert.Equal(13.0, (double)t["FontSize.Sm"]);
        Assert.Equal(14.0, (double)t["FontSize.Md"]);
        Assert.Equal(16.0, (double)t["FontSize.Lg"]);
        Assert.Equal(20.0, (double)t["FontSize.Xl"]);
        Assert.Equal(26.0, (double)t["FontSize.2xl"]);
        Assert.Equal(34.0, (double)t["FontSize.3xl"]);
    }

    [StaFact]
    public void Radius_and_layout_sizes_match_spacing_css()
    {
        var t = Tokens();
        Assert.Equal(new CornerRadius(0), (CornerRadius)t["Radius.None"]);
        Assert.Equal(new CornerRadius(3), (CornerRadius)t["Radius.Xs"]);
        Assert.Equal(new CornerRadius(4), (CornerRadius)t["Radius.Sm"]);
        Assert.Equal(new CornerRadius(6), (CornerRadius)t["Radius.Md"]);
        Assert.Equal(new CornerRadius(8), (CornerRadius)t["Radius.Lg"]);
        Assert.Equal(40.0, (double)t["Size.TitleBarHeight"]);
        Assert.Equal(42.0, (double)t["Size.ActionBarHeight"]);
        Assert.Equal(32.0, (double)t["Size.RibbonHeight"]);
        Assert.Equal(28.0, (double)t["Size.PanelHeaderHeight"]);   // statusbar-28 → PanelHeaderHeight (v7 T49)
        Assert.Equal(8.0,  (double)t["Size.DotSize"]);
    }

    [StaFact]
    public void Row_height_token_agrees_with_LayoutMetrics_so_there_is_exactly_one_authority()
    {
        Assert.Equal(LayoutMetrics.DefaultRowHeight, (double)Tokens()["Size.RowHeight"]);
        Assert.Equal(LayoutMetrics.DefaultHeaderHeight, (double)Tokens()["Size.LayerHeaderHeight"]);
    }

    [StaFact]
    public void Missing_colour_families_are_now_present_with_exact_css_values()
    {
        var t = Tokens();
        Assert.Equal(Color.FromRgb(0x1a, 0x1a, 0x1e), ((SolidColorBrush)t["Brush.SurfaceHover"]).Color);
        Assert.Equal(Color.FromRgb(0x20, 0x20, 0x24), ((SolidColorBrush)t["Brush.SurfaceActive"]).Color);
        Assert.Equal(Color.FromArgb(0x99, 0x04, 0x04, 0x06), ((SolidColorBrush)t["Brush.Scrim"]).Color);  // rgba(4,4,6,.60)
        Assert.Equal(((SolidColorBrush)t["Brush.Amber"]).Color, ((SolidColorBrush)t["Brush.StatusBuilding"]).Color);
    }
}
```

- [ ] **Step 2: Kırmızıyı gör**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~DesignTokenScaleTests"`
Expected: **FAIL** — `KeyNotFoundException` / `ResourceReferenceKeyNotFoundException` (anahtarlar yok).

- [ ] **Step 3: Tokens.xaml'i tamamla**

`xmlns:sys="clr-namespace:System;assembly=System.Runtime"` ekle; yukarıdaki **Interfaces** bloğundaki her anahtarı `<sys:Double>`, `<CornerRadius>`, `<SolidColorBrush>`, `<FontWeight>`, `<DropShadowEffect>` olarak ekle. Renk değerleri `tokens/colors.css`'ten, ölçekler `typography.css` + `spacing.css`'ten, gölge `effects.css:5`'ten. **Uydurma değer yok**; CSS'te olmayan tek iki değer (`Brush.StatusQueuedBorder`, `FontWeight.*`) yukarıda gerekçesiyle işaretli ve XAML'de yorumla belgelenir.

- [ ] **Step 4: Yeşili gör**

Run: aynı filtre → **PASS**; `--filter "FullyQualifiedName~TokenBrushesTests"` → hâlâ 0 failed.

- [ ] **Step 5: Hardcoded hex'leri sök (failing test önce)**

`tests/BuildOrchestrator.Tests/App/NoHardcodedColorTests.cs`:

```csharp
/// <summary>[T49] Uygulama XAML'lerinde ham renk literali kalmadığını pinler (token zorunluluğu).</summary>
public sealed class NoHardcodedColorTests
{
    private static readonly Regex HexLiteral = new(@"""#[0-9a-fA-F]{3,8}""", RegexOptions.Compiled);

    [Theory]
    [InlineData("MainWindow.xaml")]
    [InlineData("Console/ConsoleView.xaml")]
    [InlineData("Console/ConsoleHeader.xaml")]
    [InlineData("Controls/StickyLayerList.xaml")]
    [InlineData("Controls/LatestPill.xaml")]
    [InlineData("Graph/GraphView.xaml")]
    public void No_xaml_outside_the_token_dictionary_declares_a_raw_colour(string relative)
    {
        string path = Path.Combine(RepoRoot(), "src", "BuildOrchestrator.App", relative.Replace('/', Path.DirectorySeparatorChar));
        Assert.DoesNotMatch(HexLiteral, File.ReadAllText(path));
    }
}
```

`RepoRoot()` = `AppContext.BaseDirectory`'den yukarı doğru `BuildOrchestrator.slnx` aranarak bulunur (yeni helper, `tests/BuildOrchestrator.Tests/RepoPaths.cs`).

Run: → **FAIL** (`MainWindow.xaml` 3 literal).

- [ ] **Step 6: Değiştir, yeşili gör**

`MainWindow.xaml:6` → `Background="{DynamicResource Brush.SurfaceBase}" Foreground="{DynamicResource Brush.TextPrimary}"`; `:44` → `Background="{DynamicResource Brush.Surface}"`; `MainWindow.xaml.cs:205` DWM `0x002A2A2A` → `Brush.Border`'dan COLORREF türeten yeni yardımcı `Shell/Dwm.ColorRefFrom(Color)` (yeni birim testi: `#2a2a30` → `0x0030_2A2A`). `ConsoleView.xaml:12` yerel `Console.FontSize` → `{DynamicResource FontSize.Xs}`.

Run: `--filter "FullyQualifiedName~NoHardcodedColorTests"` → **PASS**.

- [ ] **Step 7: Tam suite + gözle kontrol**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

> **GÖZLE KONTROL:** `dotnet run --project src/BuildOrchestrator.App/BuildOrchestrator.App.csproj`
> Pencere zemini artık `#0e0e10`, title bar `#141417`, metin `#ededee` olmalı — bir önceki sürüme göre **hafifçe daha koyu ve daha nötr**. Pencere dış çerçevesi (1px) hâlâ görünür. Konsol paneli değişmemiş olmalı.

---

### Task B2 (T64 + triyaj-f): AppFonts tek tanım yeri + tek ikon stratejisi + ICO hattı

`AppFonts.cs:10-14` "pack URI'yi her tüketicide tekrar yazma" kuralını koyuyor ama **3 yerde ihlal ediliyor**: `TrackedTextBlock.cs:19-21` (Geist sans — sans yüzü **başka hiçbir yerden erişilemiyor**), `ConsoleView.xaml.cs:40-42` (CompositeFont), `Spikes/FontAbWindow.xaml.cs:17-20`. İkon tarafında dört uyumsuz kaynak var: `CaptionGlyphs.cs:14-19` bilinçli Unicode karakter, `ConsoleHeader.xaml:21` **Segoe MDL2 Assets** (aynı politikayı doğrudan çiğniyor, üstelik glyph hem XAML literali hem C# sabiti), düz Unicode semboller, ve inline `Geometry.Parse` path'leri. `LatestPill.xaml:23` ⌄'yi CompositeFont yerine düz `Geist Mono` ile çiziyor → CompositeFont'un `Segoe UI Symbol` eşlemesi (`Fonts/GeistMonoConsole.CompositeFont:13`) baypas ediliyor.

**Files:**
- Modify: `src/BuildOrchestrator.App/Controls/AppFonts.cs`, `Controls/TrackedTextBlock.cs:19-21`, `Console/ConsoleView.xaml.cs:40-42`, `Controls/LatestPill.xaml:23`
- Create: `src/BuildOrchestrator.App/Resources/Icons.xaml`
- Modify: `src/BuildOrchestrator.App/Console/ConsoleHeader.xaml:16-21`, `ConsoleHeader.xaml.cs:22-23`, `Shell/CaptionGlyphs.cs`, `MainWindow.xaml:46,49`, `App.xaml:5-13`
- Create: `src/BuildOrchestrator.App/Assets/app-icon.ico` (16/24/32/48/256), `Assets/GEIST-LICENSE.txt`
- Test: `tests/BuildOrchestrator.Tests/App/FontAssetTests.cs`, **yeni** `tests/BuildOrchestrator.Tests/App/IconGeometryTests.cs`, `tests/BuildOrchestrator.Tests/App/AppResourcesMergeTests.cs`

**Interfaces:**
```csharp
public static class AppFonts {
    public static FontFamily Ui { get; }          // ./#Geist            (sans — YENİ, tek erişim noktası)
    public static FontFamily Mono { get; }        // ./#Geist Mono
    public static FontFamily MonoConsole { get; } // ./#Geist Mono Console (CompositeFont)
}
```
`Resources/Icons.xaml` anahtarları (hepsi `Geometry`, path data `BuildApp.jsx:40-75`'ten **birebir**):
`Icon.Play Icon.Stop Icon.Sync Icon.Rot Icon.Redo Icon.Folder Icon.FolderOpen Icon.Vs Icon.Branch Icon.Tree Icon.Search Icon.Check Icon.Copy Icon.Down Icon.Trash Icon.Plus Icon.Back Icon.Grip Icon.Gear Icon.Sigma Icon.ChevUp Icon.AlertTri Icon.DepWarn Icon.LayQuad Icon.LayList Icon.LayFocus Icon.Package`
Caption (çizilmiş, karakter değil): `Icon.CaptionMinimize` (`M0.5 5h9`), `Icon.CaptionMaximize` (`M1 1h8v8h-8Z`), `Icon.CaptionRestore` (iki iç içe kare), `Icon.CaptionClose` (`M1 1l8 8M9 1l-8 8`) — geometri `_ds_bundle.js:1164-1172` WinBtn SVG'lerinden, restore K8'den.

- [ ] **Step 1: Failing testleri yaz**

`IconGeometryTests.cs`:
```csharp
[StaFact]
public void Every_declared_icon_parses_to_a_non_empty_geometry()
{
    var icons = LoadDictionary("Icons.xaml");
    Assert.NotEmpty(icons.Keys);
    foreach (var key in icons.Keys.Cast<object>().Select(k => k.ToString()!))
    {
        var g = Assert.IsAssignableFrom<Geometry>(icons[key]);
        Assert.False(g.Bounds.IsEmpty, $"{key} boş geometri");
    }
}

[StaFact]
public void No_control_depends_on_the_Segoe_MDL2_icon_font()
{
    foreach (string file in Directory.EnumerateFiles(AppSrcRoot(), "*.xaml*", SearchOption.AllDirectories))
        Assert.DoesNotContain("Segoe MDL2", File.ReadAllText(file), StringComparison.Ordinal);
}
```
`FontAssetTests.cs`'e:
```csharp
[StaFact]
public void Font_pack_uri_is_declared_in_exactly_one_place()
{
    int sites = Directory.EnumerateFiles(AppSrcRoot(), "*.cs", SearchOption.AllDirectories)
        .Where(f => !f.Contains("Spikes"))                        // FontAbWindow referans kabuğu haric
        .Count(f => File.ReadAllText(f).Contains("component/Fonts/", StringComparison.Ordinal));
    Assert.Equal(1, sites);                                        // yalnız AppFonts.cs
}
```

Run: `dotnet test ... --filter "FullyQualifiedName~IconGeometryTests|FullyQualifiedName~FontAssetTests"` → **FAIL**.

- [ ] **Step 2: AppFonts'u tekleştir**

`AppFonts.cs`'e `Ui` ve `MonoConsole` ekle; `TrackedTextBlock.cs:19-21`'deki `GeistFamily` → `AppFonts.Ui`; `ConsoleView.xaml.cs:40-42` → `AppFonts.MonoConsole`; `LatestPill.xaml:23` `AppFonts.Mono` → `AppFonts.MonoConsole` (⌄ için Segoe UI Symbol eşlemesi devreye girsin).

- [ ] **Step 3: Icons.xaml'i yaz ve MDL2'yi sök**

Prototipteki her `I.*` path'ini `Icons.xaml`'e `Geometry` olarak taşı. `ConsoleHeader.xaml:21`'deki MDL2 `TextBlock`'u `Path Data="{StaticResource Icon.Copy}"`/`Icon.Check` ile değiştir; `ConsoleHeader.xaml.cs:22-23` glyph sabitlerini sil, `ResetCopyVisual` artık `Path.Data`'yı değiştirsin. `CaptionGlyphs.cs` Unicode sabitlerini çizilmiş geometriye çevir (`MaxButtonGlyph(WindowState)` → `Geometry` döner); `MainWindow.xaml:46,49` `Content` literalleri → `Path`.

`App.xaml`'e `Resources/Icons.xaml` merge'ünü ekle ve `AppResourcesMergeTests.cs:53`'teki "exactly two" iddiasını **üçe** güncelle (gerekçeyi test doc'una yaz).

- [ ] **Step 4: Yeşili gör**

Run: `--filter "FullyQualifiedName~IconGeometryTests|FullyQualifiedName~FontAssetTests|FullyQualifiedName~AppResourcesMerge|FullyQualifiedName~RestoreGlyph"` → **PASS** (`RestoreGlyphTests.cs:27,38` artık geometri karşılaştırır — testi de güncelle).

- [ ] **Step 5: ICO hattı + lisans**

`Assets/generate-tray-icon.ps1`'i çok boyutlu üretime genişlet (16/24/32/48/256; 16 ve 24 elle netleştirilir — kaynak `prototype/assets/delta-app-icon.svg`), `app-icon.ico` üret, `csproj`'da `<ApplicationIcon>` ve `Window.Icon` olarak bağla; `AppTrayIcon.cs:19` 16px varyantı kullanmaya devam eder. Geist SIL OFL lisansını `Assets/GEIST-LICENSE.txt` olarak taşı ve `Content`/`CopyToOutputDirectory` ile çıktıya kopyala. Test: dosyaların varlığı + ICO'nun ≥5 kare taşıdığı (`new IconBitmapDecoder(...).Frames.Count`).

- [ ] **Step 6: Tam suite + gözle kontrol**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

> **GÖZLE KONTROL:** Uygulamayı çalıştır. (a) Title bar'daki min/max/close ikonları artık **çizilmiş 10×10 stroke** ikonlar (karakter değil) — maximize edince restore glyph'i iki iç içe kareye dönüşmeli. (b) Taskbar ve tray ikonu net (bulanık ölçekleme yok). (c) Bir projeye tıkla → konsol başlığındaki **Copy log** ikonu çizilmiş kopya ikonu; tıklayınca 1400ms tik'e dönüp geri gelmeli. (d) Konsolda yukarı kaydır → `⌄ latest` pill'indeki chevron düzgün render edilmeli (kutu/tofu yok).

---

### Task B3 (T60): DS kontrol kütüphanesi + 120ms geçiş altyapısı + focus ring

Bugün uygulamada **tek bir implicit style yok** — `Button`/`TextBox`/`ListBox` hâlâ WPF'in açık-tema chrome'uyla çiziliyor. DS'nin 9 kontrolünün 8'i custom `ControlTemplate` işi; en pahalı ortak kalem 120ms renk geçişleri (paylaşılan/frozen token brush **anime edilemez** → template-lokal brush zorunlu, A13.2).

**Files:**
- Create: `src/BuildOrchestrator.App/Resources/Controls.xaml`
- Create: `src/BuildOrchestrator.App/Controls/SplitButton.cs`, `Controls/StatusGlyph.cs`, `Controls/WillBuildDot.cs`, `Controls/BuildingSpinner.cs`
- Modify: `src/BuildOrchestrator.App/App.xaml` (4. merge)
- Test: **yeni** `tests/BuildOrchestrator.Tests/App/DsControlTemplateTests.cs`, `tests/BuildOrchestrator.Tests/App/MotionResourcesTests.cs`, `tests/BuildOrchestrator.Tests/App/AppResourcesMergeTests.cs`

**Interfaces (D-fazı bunları tüketir):** `Style` anahtarları
`Ds.Button.Primary|Secondary|Ghost|Danger` × `Ds.Size.Sm|Md|Lg` (attached `DsSize` yerine üç ayrı anahtar: `Ds.Button.Primary.Md` vb.) · `Ds.SplitButton` · `Ds.Chip` · `Ds.Chip.Counter` · `Ds.IconButton` · `Ds.Switch` · `Ds.Segment` · `Ds.Input` · `Ds.Kbd` · `Ds.ProgressBar` · `Ds.Popover` · `Ds.Dialog` · `Ds.FocusVisual`
Kontroller: `StatusGlyph { Status, Size }`, `WillBuildDot { State }` (`bool?`), `BuildingSpinner { Size }`, `SplitButton { PrimaryContent, MenuContent, PrimaryCommand }`.

**Kesin değerler (`_ds_bundle.js`'ten):** boy `sm=24 / md=28 / lg=32` (`:14-17`); Button padding `sm '0 10px'`, `md '0 12px'`, gap 6, radius `Radius.Sm`; primary `Brush.Amber` zemin + `Brush.TextOnAccent` metin, hover `Brush.AmberBright`, press `Brush.AmberDim` (`:20-31`); secondary `Brush.SurfaceRaised` + 1px `Brush.BorderStrong` (`:33-45`); danger `Brush.StatusFailSoft` + `Brush.StatusFailText` + 1px `Brush.StatusFailBorder` (`:60-72`); Chip `height 24, padding '0 8px', gap 6, Radius.Xs, FontSize.Xs, Medium`, aktif `Brush.AmberSoft` + 1px `Brush.AmberBorder` + `Brush.AmberText` (`:145-221`); IconButton `sm 22×22` (`:238-263`); Kbd `minWidth 16, height 18, padding '0 5px', alt kenar 2px` (`:271-296`); Segment dış `24`, `padding 1, gap 1`, `Brush.SurfaceSunken` + 1px `Brush.Border`, seçili `Brush.SurfaceOverlay` (`:302-356`); Input prefix'liyken `PaddingLeft 26`, büyüteç `left 8`, focus'ta `Brush.AmberBorder` + 2px `Brush.FocusRing` halka (`:748-774`); Popover `Brush.SurfaceOverlay` + 1px `Brush.BorderStrong` + `Radius.Lg` + `Effect.OverlayShadow` + `padding 8` (`BuildApp.jsx:820-823`); Dialog zemini **`Brush.SurfaceRaised`** (`_ds_bundle.js:965` — README §2.9 "surface-overlay" der, **kod kazanır**, sapma burada kayıtlıdır).

- [ ] **Step 1: MOTION SPIKE — `{DynamicResource Duration.X}` bir Storyboard'da canlı mı? (failing test)**

Bu, 120ms geçiş altyapısının hangi mekanizmayla kurulacağını belirler ve bugüne kadar **hiç sınanmadı** (uygulamada sıfır saf-XAML Storyboard var).

`MotionResourcesTests.cs`'e:
```csharp
[StaFact]
public void A_xaml_storyboard_duration_bound_with_DynamicResource_follows_a_live_dictionary_change()
{
    var host = new Border();
    host.Resources.MergedDictionaries.Add(LoadDictionary("Motion.xaml"));
    var sb = (Storyboard)XamlReader.Parse("""
        <Storyboard xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
          <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0"
                           Duration="{DynamicResource Duration.Fast}" />
        </Storyboard>
        """);
    host.Resources["Duration.Fast"] = new Duration(TimeSpan.Zero);
    var anim = (DoubleAnimation)sb.Children[0];
    Assert.Equal(TimeSpan.Zero, anim.Duration.TimeSpan);      // reduced-motion canlı yansımalı
}
```

Run: `dotnet test ... --filter "FullyQualifiedName~A_xaml_storyboard_duration_bound_with_DynamicResource"`

**Karar kapısı:**
- **PASS** → 120ms geçişler saf-XAML `ControlTemplate.Triggers` + `Storyboard` ile yazılır (`{DynamicResource Duration.Fast}` zorunlu).
- **FAIL** (Freezable/DynamicResource kısıtı) → testi `Skip` etme; **testi beklentiyi belgeleyecek şekilde tersine çevir** (`Assert.NotEqual` + neden yorumu) ve tüm 120ms geçişler `Controls/MotionTokens.SplineTo` + `BeginAnimation(HandoffBehavior.SnapshotAndReplace)` ile **kod-tarafı** yazılır (It-4a'nın kanıtlı yolu; `LatestPill.xaml.cs:66-71` referans implementasyon). Kararı `Controls.xaml` başına yorumla yaz.

- [ ] **Step 2: Kontrol şablonları için failing testler yaz**

`DsControlTemplateTests.cs` (hepsi `[StaFact]`, `[Collection("Console UI (serial)")]`):
```csharp
[StaFact] public void Primary_button_uses_amber_surface_and_on_accent_text_at_md_height_28()
[StaFact] public void Split_button_halves_share_one_body_with_flat_inner_corners()
[StaFact] public void Chip_active_state_uses_amber_soft_fill_amber_border_and_amber_text()
[StaFact] public void Switch_is_a_checkbox_template_because_wpf_has_no_toggle_switch()
[StaFact] public void Input_renders_a_watermark_and_a_prefix_slot_and_turns_red_when_invalid()
[StaFact] public void Focus_visual_is_a_two_pixel_amber_ring_with_one_pixel_offset()
[StaFact] public void A_hover_transition_animates_a_template_local_brush_not_the_shared_token_brush()
```
Sonuncusu A13.2'yi pinler: şablonun animasyon hedefi olan `SolidColorBrush`'ın `IsFrozen == false` **ve** `ReferenceEquals(brush, tokens["Brush.Surface"]) == false` olmalı.

- [ ] **Step 3: Controls.xaml'i yaz, App.xaml'e merge et, yeşili gör**

`AppResourcesMergeTests` beklentisini **dörde** çıkar (Motion, Tokens, Icons, Controls).
Run: `--filter "FullyQualifiedName~DsControlTemplateTests"` → **PASS**.

- [ ] **Step 4: Tam suite + gözle kontrol**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

> **GÖZLE KONTROL:** Uygulamayı çalıştır. Mevcut geçici butonlar (Rebuild/Stop/Continue/Restart engine) artık **DS görünümünde** olmalı: amber primary, koyu secondary, 24/28px yükseklikler, 4px radius. Üzerlerine gel → **yumuşak ~120ms** renk geçişi olmalı (ani sıçrama değil). Tab'a bas → 2px amber focus halkası, 1px boşlukla.

---

### Task B4 (T61): Tooltip altyapısı

WPF varsayılanı ~400-700ms gecikme (web'in anlık hissini öldürür) ve `PlacementMode.Top/Bottom` tooltip'i hedefe **ortalamaz**, sola hizalar → `CustomPopupPlacementCallback` şart (A13.2). Ayrı HWND olduğu için `ClearTypeHint` unutulmamalı.

**Files:**
- Create: `src/BuildOrchestrator.App/Controls/AppTooltip.cs`
- Modify: `src/BuildOrchestrator.App/Resources/Controls.xaml` (implicit `ToolTip` style)
- Test: **yeni** `tests/BuildOrchestrator.Tests/App/TooltipTests.cs`

**Interfaces:**
```csharp
public static class AppTooltip {
    public static readonly DependencyProperty SideProperty;       // AppTooltip.Side="Top|Bottom|Left|Right"
    public static CustomPopupPlacement[] Placement(Size popupSize, Size targetSize, Point offset); // ortalama + 6px
}
```

- [ ] **Step 1: Failing test yaz**

```csharp
[StaFact]
public void Tooltip_opens_with_zero_delay_and_stays_open()
{
    var style = (Style)LoadDictionary("Controls.xaml")[typeof(ToolTip)];
    Assert.Contains(style.Setters.OfType<Setter>(),
        s => s.Property == ToolTipService.InitialShowDelayProperty && (int)s.Value! == 0);
}

[Theory]
[InlineData(100.0, 40.0, 22.0, -28.0)]   // popup 100 genis, hedef 40 genis → x = (40-100)/2 = -30 ... y = -(22+6)
public void Placement_centres_horizontally_and_offsets_six_pixels(double pw, double tw, double ph, double expectedY)
{
    var p = AppTooltip.Placement(new Size(pw, ph), new Size(tw, 20), default)[0];
    Assert.Equal((tw - pw) / 2, p.Point.X, 3);
    Assert.Equal(expectedY, p.Point.Y, 3);
}
```

- [ ] **Step 2: Kırmızıyı gör → uygula → yeşili gör**

Implicit `ToolTip` style: `Brush.SurfaceOverlay` zemin, 1px `Brush.BorderStrong`, `Radius.Sm`, `FontSize.Xs`, `Brush.TextSecondary`, `Effect.PopoverShadow`, `TextOptions.TextRenderingMode=ClearType`, `ToolTipService.InitialShowDelay=0`, `ShowDuration` sonsuz, `Placement=Custom` + `CustomPopupPlacementCallback`. **Canlı içerik** (`Building — 24s`, `Copy → Copied`) binding ile çözülür.

Run: `--filter "FullyQualifiedName~TooltipTests"` → **PASS**; tam suite → 0 failed.

> **GÖZLE KONTROL:** Uygulamayı çalıştır, herhangi bir buton/ikonun üzerine gel → tooltip **anında** (gecikmesiz) ve hedefin **tam ortasında**, 6px boşlukla çıkmalı; fare üzerindeyken kaybolmamalı.

---

# FAZ C — Kabuk (gerçek 2×2 + VM omurgası)

---

### Task C1 (T35): 2×2 layout + görünüm modları + splitter'lar + persist

`MainWindow.xaml` hâlâ It-2 iskeleti: 40px title bar + `320px` sol `StackPanel` + ham `ListBox` + sağda konsol. `StickyLayerList`, `GraphView` ve bağımsız `LatestPill` **yalnız** `Spikes/It4aLabWindow.xaml`'de. Bu task primitifleri gerçek pencereye taşır.

**Files:**
- Modify: `src/BuildOrchestrator.App/MainWindow.xaml` (**tam gövde yeniden yazımı**), `MainWindow.xaml.cs`
- Create: `src/BuildOrchestrator.App/Controls/PanelHeader.xaml(.cs)`, `Controls/DsSplitter.cs`, `Shell/LayoutState.cs`
- Modify: `src/BuildOrchestrator.App/Shell/UiStateStore.cs:11-18`
- Delete: `src/BuildOrchestrator.App/Spikes/It4aLabWindow.xaml(.cs)`, `Spikes/SampleGraphData.cs`, `App.xaml.cs:35-40` (`--it4a-lab` dalı) — **`FontAbWindow` ve `--font-ab` KALIR**
- Test: **yeni** `tests/BuildOrchestrator.Tests/App/ShellLayoutTests.cs`, `tests/BuildOrchestrator.Tests/App/UiStateStoreTests.cs`

**Interfaces:**
```csharp
public enum LayoutMode { Quad, List, Focus }
public sealed record LayoutState(LayoutMode Mode, double ColPct, double LeftPct, double RightPct) {
    public static LayoutState Default => new(LayoutMode.Quad, 50, 50, 50);
    public LayoutState WithMode(LayoutMode m);      // quad→50/50/50 · list→right 50 · focus→right 76
    public LayoutState WithCol(double pct);         // clamp 28..72
    public LayoutState WithLeft(double pct);        // clamp 18..82
    public LayoutState WithRight(double pct);       // clamp 18..82
}
// UiState genişler: LayoutMode, ColPct, LeftPct, RightPct, RepositoryRoot, Configuration, PerfMode,
//                   Branch, UseWorktree, WorktreeName, LayerPatterns, Autostart
```
Pencere iskeleti: kök `Grid RowDefinitions="40,Auto,*,42"` (title bar · sticky şerit+2px progress · gövde · action bar); gövde `Grid` iki kolon (`sol = ColPct*`, `dikey splitter`, `sağ = *`); sol kolon `graf / yatay splitter / liste`, sağ kolon `konsol / yatay splitter / stream`.

- [ ] **Step 1: LayoutState için failing test yaz (saf, STA gerekmez)**

```csharp
[Fact] public void Quad_preset_resets_all_three_splits_to_fifty()
    => Assert.Equal(new LayoutState(LayoutMode.Quad, 50, 50, 50),
                    new LayoutState(LayoutMode.Focus, 60, 74, 76).WithMode(LayoutMode.Quad));

[Fact] public void List_preset_only_sets_right_to_fifty_and_keeps_col_and_left()
{
    var s = new LayoutState(LayoutMode.Quad, 60, 74, 76).WithMode(LayoutMode.List);
    Assert.Equal(LayoutMode.List, s.Mode);
    Assert.Equal(60, s.ColPct); Assert.Equal(74, s.LeftPct); Assert.Equal(50, s.RightPct);
}

[Fact] public void Focus_preset_only_sets_right_to_seventysix()
    => Assert.Equal(76, new LayoutState(LayoutMode.Quad, 60, 74, 50).WithMode(LayoutMode.Focus).RightPct);

[Theory]
[InlineData(5, 28)] [InlineData(95, 72)] [InlineData(50, 50)]
public void Column_split_is_clamped_between_twentyeight_and_seventytwo(double input, double expected)
    => Assert.Equal(expected, LayoutState.Default.WithCol(input).ColPct);

[Theory]
[InlineData(5, 18)] [InlineData(95, 82)]
public void Row_splits_are_clamped_between_eighteen_and_eightytwo(double input, double expected)
{
    Assert.Equal(expected, LayoutState.Default.WithLeft(input).LeftPct);
    Assert.Equal(expected, LayoutState.Default.WithRight(input).RightPct);
}
```
Kaynak: `BuildApp.jsx:1143` (varsayılan), `:1410-1417` (preset'ler), `:1394/:1399/:1404` (clamp'ler).

- [ ] **Step 2: Kırmızıyı gör → LayoutState'i yaz → yeşili gör**

Run: `dotnet test ... --filter "FullyQualifiedName~LayoutState"` → önce **FAIL**, sonra **PASS**.

- [ ] **Step 3: Persist için failing test yaz**

```csharp
[Fact]
public void Layout_survives_a_store_round_trip()
{
    using var temp = new TempDir();
    var store = new JsonUiStateStore(Path.Combine(temp.Path, "ui-state.json"));
    var state = store.Load();
    state.LayoutMode = LayoutMode.Focus; state.ColPct = 61; state.LeftPct = 33; state.RightPct = 76;
    store.Save(state);
    var reloaded = new JsonUiStateStore(Path.Combine(temp.Path, "ui-state.json")).Load();
    Assert.Equal(LayoutMode.Focus, reloaded.LayoutMode);
    Assert.Equal(61, reloaded.ColPct);
    Assert.True(reloaded.TrayBalloonShown == false && reloaded.Hotkey == "Alt+B");  // mevcut alanlar bozulmadi
}
```

- [ ] **Step 4: UiState'i genişlet, yeşili gör**

- [ ] **Step 5: Pencere iskeleti için failing test yaz**

```csharp
[StaFact]
public void Shell_has_four_panels_in_quad_mode_and_hides_the_graph_in_list_and_focus()
{
    var shell = new ShellRoot();                 // MainWindow'un test edilebilir icerik kontrolu
    shell.ApplyLayout(new LayoutState(LayoutMode.Quad, 50, 50, 50));
    Assert.Equal(Visibility.Visible, shell.GraphHost.Visibility);
    shell.ApplyLayout(LayoutState.Default.WithMode(LayoutMode.List));
    Assert.Equal(Visibility.Collapsed, shell.GraphHost.Visibility);
    Assert.Equal(Visibility.Collapsed, shell.LeftSplitter.Visibility);   // graf gizliyken yatay splitter da gider
    shell.ApplyLayout(LayoutState.Default.WithMode(LayoutMode.Focus));
    Assert.Equal(Visibility.Collapsed, shell.GraphHost.Visibility);
}

[StaFact]
public void Splitter_grab_band_is_seven_pixels_with_a_one_pixel_visible_line()
```

`MainWindow`'un gövdesi `ShellRoot : UserControl` olarak ayrılır — pencere kabuğu (WindowChrome/DWM/tray/hotkey) `MainWindow`'da kalır, layout test edilebilir kontrole taşınır.

- [ ] **Step 6: ShellRoot'u yaz; primitifleri yerleştir**

- Sol-üst: `graph:GraphView` (kendi 28px başlığını getirir).
- Sol-alt: `Controls/PanelHeader` (`TrackedTextBlock Text="PROJECTS"` + `build-order` mono etiketi + filtre chip'i + `Filter…` inputu) + `controls:StickyLayerList` + `controls:LatestPill` (aynı `Grid`'de üst üste).
- Sağ-üst: mevcut `appConsole:ConsoleHeader` + `appConsole:ConsoleView` (kendi pill'i var — **ikinci pill ekleme**).
- Sağ-alt: `PanelHeader Text="EVENT STREAM"` + geçici boş `ItemsControl` (D3 dolduracak).
- Title bar: logo (`Assets/delta-logo-dark.svg` → XAML `DrawingImage`, 15px) + `Build Orchestrator` (`FontSize.Xs`, `Brush.TextDim`) + mono 11px bağlam metni + sağda 3 layout `IconButton` (`Icon.LayQuad/LayList/LayFocus`, tooltip'leri `BuildApp.jsx:1448-1450`'den birebir) + 1px ayraç + gear (`Settings — layer definitions`) + min/max/close.
- `DsSplitter`: `GridSplitter` türevi, 7px hit alanı + ortada 1px `Brush.Border` çizgi, `IsDragging`'de `Brush.AmberBorder`; `DragCompleted`'da `LayoutState` + `UiStateStore.Save`.
- `App.Motion`'ı `GraphView.MotionSettings`'e **`Loaded`'dan önce** ata (`GraphView.xaml.cs:111-119` sözleşmesi).

- [ ] **Step 7: Lab harness'ı kaldır**

`It4aLabWindow` + `SampleGraphData` sil, `App.xaml.cs`'ten `--it4a-lab` dalını çıkar. `FontAbWindow`/`--font-ab` **dokunulmaz**. Lab'e atıf yapan test/doc yorumlarını güncelle.

- [ ] **Step 8: Tam suite + gözle kontrol**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

> **GÖZLE KONTROL:** Uygulamayı çalıştır. (a) Pencere artık **4 panelli**: sol-üst graf (boş durum `Graph appears after Sync`), sol-alt PROJECTS, sağ-üst konsol, sağ-alt EVENT STREAM. (b) Title bar sağındaki 3 ikon: **quad/list/focus** — list ve focus'ta graf kaybolmalı, focus'ta konsol belirgin büyümeli, quad'a dönünce split'ler 50/50/50'ye sıfırlanmalı. (c) Splitter'ları sürükle: kavrama alanı rahat (7px), çizgi sürüklerken **amber** olmalı; kolonu uca dayamaya çalış → %28/%72'de durmalı. (d) Uygulamayı kapat-aç → **split konumları ve mod korunmalı**. (e) `--it4a-lab` artık açılmamalı; `--font-ab` hâlâ açılmalı.

---

### Task C2: VM omurgası — faz, sayaçlar, seçim, filtre, branch/worktree/config/perf, süre formatı

`RunViewModel` bugün faz modeli, sayaç, filtre, seçim, branch/worktree/perf state'i **taşımıyor**; `DurationMsText` ham milisaniye basıyor (`:28`) — tasarımın `fmtDur`'u (`build-data.js:16-23`) kodda **hiç yok**. `RunMode.Build` ve `RunMode.RetryFailed` App'ten hiç gönderilmiyor.

**Files:**
- Create: `src/BuildOrchestrator.Core/Formatting/DurationFormat.cs`
- Create: `src/BuildOrchestrator.App/ViewModels/AppPhase.cs`, `ViewModels/RunCounters.cs`, `ViewModels/ProjectFilter.cs`
- Modify: `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs`
- Test: **yeni** `tests/BuildOrchestrator.Tests/Formatting/DurationFormatTests.cs`, `tests/BuildOrchestrator.Tests/App/RunCountersTests.cs`, `tests/BuildOrchestrator.Tests/App/ProjectFilterTests.cs`, `tests/BuildOrchestrator.Tests/App/RunViewModelTests.cs`

**Interfaces:**
```csharp
// Core/Formatting/DurationFormat.cs — build-data.js:16-23 + BuildApp.jsx:76-80 portu, InvariantCulture
public static class DurationFormat {
    public static string Duration(long? ms);    // null→"—" · <9950→"4.2s" · <60s→"12s" · →"1m 12s"
    public static string Elapsed(long ms);      // <60s→"24s" · →"1m 12s" (saniye 2 hane pad)
}

public enum AppPhase { Empty, Boot, Syncing, Idle, Running, Done, Stopped }

public readonly record struct RunCounters(int Total, int Building, int Queued, int Succeeded,
                                          int Failed, int Skipped, int DepAffected) {
    public static RunCounters From(IEnumerable<ProjectRowViewModel> rows);   // DepAffected = succeeded && depIssue
}

public static class ProjectFilter {
    public const string Building = "building", Succeeded = "succeeded", Failed = "failed",
                        Skipped = "skipped", Dep = "dep";
    public static bool Matches(ProjectRowViewModel row, string? query, string? filter);
    public static string Label(string filter);   // Building/Succeeded/Failed/Skipped/"Dependency issues"
}

// RunViewModel'e eklenenler
[ObservableProperty] AppPhase phase; [ObservableProperty] string? selectedProjectId;
[ObservableProperty] string? activeFilter; [ObservableProperty] string projectQuery = "";
[ObservableProperty] string branch = ""; [ObservableProperty] bool useWorktree;
[ObservableProperty] string? worktreeName; [ObservableProperty] string perfMode = "Balanced";
[ObservableProperty] RunCounters counters; [ObservableProperty] bool isMidRunLocked;   // T12
public IReadOnlyList<ProjectRowViewModel> VisibleProjects { get; }
public ICommand SyncCommand, BuildCommand, RebuildCommand, ContinueCommand, RetryFailedCommand, StopCommand;
```

- [ ] **Step 1: DurationFormat için failing testler yaz**

```csharp
[Theory]
[InlineData(null, "—")]
[InlineData(4200L, "4.2s")]
[InlineData(9949L, "9.9s")]     // 9950 esigi ALTI → ondalik
[InlineData(9950L, "10s")]      // esik ve USTU → tam saniye
[InlineData(59_000L, "59s")]
[InlineData(72_000L, "1m 12s")]
public void Duration_matches_the_prototype_formatter(long? ms, string expected)
    => Assert.Equal(expected, DurationFormat.Duration(ms));

[Theory]
[InlineData(24_000L, "24s")]
[InlineData(72_000L, "1m 12s")]
[InlineData(65_000L, "1m 05s")]  // saniye 2 hane pad
public void Elapsed_matches_the_prototype_formatter(long ms, string expected)
    => Assert.Equal(expected, DurationFormat.Elapsed(ms));

[Fact]
public void Formatting_is_invariant_under_a_comma_decimal_culture()
{
    var prev = CultureInfo.CurrentCulture;
    try { CultureInfo.CurrentCulture = new CultureInfo("tr-TR"); Assert.Equal("4.2s", DurationFormat.Duration(4200)); }
    finally { CultureInfo.CurrentCulture = prev; }
}
```

- [ ] **Step 2: Kırmızıyı gör → DurationFormat'ı yaz → yeşili gör; `ProjectRowViewModel.DurationMsText`'i buna bağla**

- [ ] **Step 3: Sayaç ve filtre için failing testler yaz**

```csharp
[Fact] public void Dep_counter_counts_only_succeeded_rows_that_carry_a_dep_issue()   // build-data.js:524-528
[Fact] public void Building_filter_includes_queued_rows()                            // BuildApp.jsx:465-470
[Fact] public void Dep_filter_matches_any_row_with_a_dep_issue_regardless_of_status() // chip sayacindan FARKLI
[Fact] public void Query_matches_the_project_name_only_and_is_ANDed_with_the_status_filter()
[Fact] public void Query_is_a_case_insensitive_substring_not_a_fuzzy_match()
```

- [ ] **Step 4: Kırmızıyı gör → `RunCounters`/`ProjectFilter`'ı yaz → yeşili gör**

- [ ] **Step 5: VM davranışları için failing testler yaz**

```csharp
[Fact] public void Phase_walks_empty_boot_syncing_idle_running_done_from_engine_events()
[Fact] public void Selecting_the_same_project_twice_clears_the_selection()            // canonical deselect
[Fact] public void Sync_clears_the_selection_but_keeps_the_filter()                   // asimetri, BuildApp.jsx:1186-1193
[Fact] public void Build_and_retry_clear_both_selection_and_filter()                  // :1194-1225
[Fact] public void Continue_clears_neither()                                          // :1212-1217
[Fact] public void Build_command_sends_RunMode_Build_with_branch_worktree_and_layer_patterns()
[Fact] public void Retry_failed_command_sends_RunMode_RetryFailed_and_is_enabled_only_when_a_failure_exists()
[Fact] public void Branch_worktree_and_configuration_are_locked_while_running_but_perf_stays_live()  // T12
[Fact] public void Switching_configuration_marks_everything_dirty_and_writes_the_warn_line()         // T43
```
T43 konsol satırı **birebir**: `Configuration → Release — all projects will rebuild` (`BuildApp.jsx:1362`). Perf notu **birebir**: `parallelism: 4 (Balanced)` (`:1372`), yalnız koşarken.

- [ ] **Step 6: Kırmızıyı gör → VM'i genişlet → yeşili gör**

`PerfMode` döngüsü `Full → Balanced → Light → Full`, paralellik `6/4/2` **tek sabitte** (prototipteki 3 kopya taşınmaz).

- [ ] **Step 7: Tam suite + gözle kontrol**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0; `dotnet test ... --filter "Category!=Acceptance"` → 0 failed

> **GÖZLE KONTROL:** Uygulamayı çalıştır, repo kökünü ver ve Rebuild başlat. Proje satırlarındaki süre kolonu artık **ham milisaniye değil** `4.2s` / `1m 12s` biçiminde olmalı. Koşu sırasında Debug/Release ve branch/worktree kontrolleri **sönük (disabled)**, perf chip'i **canlı** olmalı.

---

# FAZ D — Paneller

---

### Task D1 (T53-UI + T54-UI): Proje kartı (7 slot) + katman gruplaması + dep rozeti

`StickyLayerList.xaml:24-28` `RowTemplate`'i **kasıtlı stub** (`{Binding Name}`) — tam kart It-4b'nin işi. `ProjectRowViewModel` kart verisini zaten taşıyor (`Id/Name/State/DurationMs/WillBuild/DepIssues/HasDepIssue`), eksik olan ikisi: **sln adı** ve **`curSha → targetSha`**.

**Files:**
- Create: `src/BuildOrchestrator.App/Views/ProjectRow.xaml(.cs)`
- Modify: `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml:24-28` (RowTemplate → `ProjectRow`)
- Modify: `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs` (`ProjectRowViewModel` += `SolutionName`, `CurrentSha`; gruplama)
- Test: **yeni** `tests/BuildOrchestrator.Tests/App/ProjectRowTests.cs`, `tests/BuildOrchestrator.Tests/App/LayerGroupingTests.cs`

**Kesin geometri (`BuildApp.jsx:355-416` — birebir):** satır `Size.RowHeight` (36), `gap 8`, `padding '0 10px 0 0'` (**sol padding YOK**, şerit kenara yapışık), alt çizgi 1px `Brush.BorderSubtle`, zemin seçili→`Brush.SurfaceRaised` · hover→`Brush.SurfaceHover` · yoksa `Transparent`, geçiş 120ms `EaseStandard`.
1. **Statü şeridi** `Width = seçili ? 3 : 2`, dikey stretch, renk: discovered→`Transparent`, queued→`Brush.StatusQueued`, building→`Brush.Amber`, succeeded→`Brush.StatusSuccess`, failed→`Brush.StatusFail`, skipped→`Brush.StatusSkipped`, cycle→`Brush.StatusCycle`; seçili+discovered→`Brush.Amber`; genişlik geçişi 80ms `EaseStandard`.
2. **WillBuildDot** 8px, `MarginLeft 4`: dirty→dolu `Brush.DotDirty`, clean→dolu `Brush.DotClean`, unknown(null)→şeffaf + 1px `Brush.TextFaint` halka.
3. **Ad + sln**: baseline hizalı, `gap 8`; ad `FontSize.Sm`/`FontWeight.Emphasis`, skipped|discovered ise `Brush.TextDim` yoksa `Brush.TextPrimary`; sln `FontSize.Xs`/`Brush.TextFaint`; ikisi de ellipsis.
4. **Sağ blok** `MinWidth 118`, sağa yaslı, `gap 2`.
5. **Statü glyph'i** 14px + tooltip: `Building — 24s` / `Succeeded` / `Failed` / `Skipped` / `Queued` / `Discovered` / `Cycle`; depIssue varsa ` — dependency issue` eki.
6. **depIssue slotu sabit 14px** — boşken bile var (hiza asla bozulmaz); doluysa 12px `Icon.AlertTri`, `Brush.StatusFailText`, tooltip **birebir**: `Failed dependency: Sales.Core — last successful output referenced` (adlar kısa ad, `OSYS.` atılmış, virgülle).
7. **Süre** mono `FontSize.Xs`, tabular, sağa yaslı `MinWidth 46`; building→canlı `DurationFormat.Elapsed`, bitmiş→`DurationFormat.Duration`, yoksa `—`; failed'da `Brush.StatusFailText`, aksi `Brush.TextDim`.
- **İç sarmalayıcı** seçiliyken `TranslateX 4` (120ms `EaseOut`).
- **Hover ikonları** (sha'nın **yerine** aynı 118px blokta): `Icon.FolderOpen` (tooltip `Reveal in Explorer`) + `Icon.Vs` (tooltip `Open in Visual Studio`), 13px.
- **SHA** yalnız `WillBuild == true` ve hover yokken: mono **10.5px** (literal — token ölçeğinde yok, prototip `:399`), `Brush.TextFaint`, `{CurrentSha} → {TargetSha}`.
- **Nefes** yalnız building: `Brush.AmberSoft` katmanı `Opacity 0→0.32→0`, **3.8s** `EaseInOut`, sonsuz, `DesiredFrameRate=30`.
- **Shake** yalnız hata anından itibaren **700ms** penceresinde, bir kez: 360ms `EaseStandard`, keyframe'ler `10%,90% → -2px · 25%,75% → +3px · 50% → -3px`.

- [ ] **Step 1: Failing testler yaz**

```csharp
[StaFact] public void Row_is_thirtysix_pixels_with_a_two_pixel_status_stripe_that_becomes_three_when_selected()
[StaFact] public void Dep_issue_slot_is_fourteen_pixels_even_when_empty_so_columns_never_shift()
[StaFact] public void Will_build_dot_is_amber_when_dirty_grey_when_clean_and_a_hollow_ring_when_unknown()
[StaFact] public void Sha_pair_is_shown_only_for_dirty_rows_and_is_replaced_by_the_two_hover_icons()
[StaFact] public void Duration_column_uses_the_shared_formatter_and_turns_red_on_failure()
[StaFact] public void Breathing_layer_only_exists_while_building_and_is_capped_at_thirty_fps()
[Fact]    public void Rows_are_grouped_by_layer_name_from_the_topology_not_by_a_regex_in_the_app()
```
Sonuncusu mimari kuralı pinler: App'te **regex yok**; gruplama `ProjectNode.LayerName`/`LayerIndex`'ten (`LayerName == null` → tek isimsiz grup = düz build-order).

- [ ] **Step 2: Kırmızıyı gör → `ProjectRow` + gruplamayı yaz → yeşili gör**

`SetGroups` **yalnız topoloji/gruplama değişiminde** çağrılır (tam reset — `StickyLayerList.xaml.cs:56-70`); statü tikleri satır VM'lerinin `INotifyPropertyChanged`'inden gider. `{x:Static controls:LayoutMetrics.DefaultRowHeight}` bağlaması **korunur**.

- [ ] **Step 3: Tam suite + gözle kontrol**

> **GÖZLE KONTROL:** Sync + Build koş. Satırlar tasarımdaki kart olmalı: soldaki ince statü şeridi, 8px nokta, ad + soluk sln adı, sağda süre. (a) Bir satırın üzerine gel → sha çifti yerini **klasör + VS ikonlarına** bırakmalı. (b) Derlenen satırda çok hafif amber "nefes" olmalı (süpürme/parlama YOK). (c) Bir proje hata alırsa satır **bir kez** yatay sarsılmalı. (d) Bağımlılığı hatalı olan bir proje başarıyla derlenirse satırda **▲** görünmeli ve tooltip'i `Failed dependency: … — last successful output referenced` demeli.

---

### Task D2 (T38 + T39 + T70): Sticky şerit — faz metni, chip'ler, hata kümesi, progress, ETA

**Files:**
- Create: `src/BuildOrchestrator.App/Views/StickyRibbon.xaml(.cs)`, `ViewModels/RibbonText.cs`
- Modify: `src/BuildOrchestrator.App/MainWindow.xaml` (Row 1)
- Test: **yeni** `tests/BuildOrchestrator.Tests/App/RibbonTextTests.cs`, `tests/BuildOrchestrator.Tests/App/StickyRibbonTests.cs`

**Interfaces:**
```csharp
public readonly record struct RibbonLine(string Text, string BrushKey, string? Glyph);
public static class RibbonText {
    public static RibbonLine Compose(AppPhase phase, bool hasWorkspace, bool allClean, RunCounters c,
                                     int willBuild, int finishedOfWillBuild, int totalProjects,
                                     long elapsedMs, long? etaMs, long? checkDurMs, int warnings);
    public static string? EtaSuffix(long? etaMs, RunCounters c);   // " · almost done" | " · ~35s left" | null
    public static double Progress(AppPhase phase, bool allClean, RunCounters c, int willBuild,
                                  int finishedOfWillBuild, int totalProjects);
}
```

**Faz metinleri — BİREBİR (`BuildApp.jsx:752-770`; `36` yerine gerçek proje sayısı):**

| Koşul | Metin | Renk | Glyph |
|---|---|---|---|
| repo yok | `Not ready — no repository selected` | `Brush.TextFaint` | — |
| boot | `▸ Waiting for Sync — project states appear after Sync` | `Brush.TextDim` | — |
| syncing | `▸ Sync — git fetch origin…` | `Brush.TextSecondary` | — |
| idle + allClean | `▸ Ready — everything looks up to date` | secondary | — |
| idle | `▸ Ready — {wb} to build · {total-wb} up to date` | secondary | — |
| running + allClean | `▸ Checking — scanning for changes…` | secondary | — |
| running | `▸ Building {fin}/{wb} · {elapsed}{eta}` | secondary | — |
| stopped | `▸ Stopped — {fin}/{wb} · rest queued` | `Brush.TextDim` | — |
| done + allClean | `Everything up to date — {total} projects checked in {checkDur}, nothing to build` | `Brush.StatusSuccessText` | succeeded |
| done + failed | `Completed — {failed} failed · {succeeded} succeeded{ (N dependency-affected)} · {skipped} skipped{ · N warnings} · {elapsed}` | `Brush.StatusFailText` | failed |
| done temiz | `Completed — {succeeded} succeeded · {skipped} skipped{ · N warnings} · {elapsed}` | `Brush.StatusSuccessText` | succeeded |

**ETA (`build-data.js:466-476` + `BuildApp.jsx:760-763`):** `remain = Σ(queued tahmin) + Σ(building kalan)`; `est = remain / max(1, parallelism) + (building varsa 400ms)`; EMA `0.75*eski + 0.25*yeni`; gösterim kapısı `eta != null && building + queued > 0`; `< 4000ms` → ` · almost done`, aksi ` · ~{max(5, round(eta/5000)*5)}s left`. Tahmin girdisi `BuildState.LastDurationMs` (T70 — Contracts'ta zaten var).

**Progress (`:773-776`):** allClean → (done ? 100 : skipped/total*100); aksi wb>0 ? fin/wb*100 : 0. Renk: **herhangi bir hata varsa anında kırmızı**, done+hatasız yeşil, aksi amber. `indeterminate` **yalnız** `phase == Syncing`. `Radius.None`, yükseklik `Size.ProgressHeight` (2), track `Brush.Surface`.

**Building chip'leri (`:783-789`):** ilk **4**, `Height 20, Padding '0 6px', FontSize.2xs`, ikon = 10px dönen halka, etiket kısa ad; tıkla → seç. Taşan **`+N` düz metin, tıklanamaz**.

**Hata kümesi (`:791-807`, yalnız failed>0):** sağa yaslı — 13px failed glyph + `{n} failed` (`FontSize.Xs`, `Brush.StatusFailText`, Medium) + varsa `· {di} dependency-affected` (`FontSize.2xs`, `Brush.TextDim`) + ilk **3** hatalı chip (tıkla→seç) + `+{n-3} more` chip (tıkla→**Failed filtresi**). **Kapatılabilir banner YOK.**

- [ ] **Step 1: `RibbonText` için failing testler yaz (saf, hızlı)**

Yukarıdaki 11 satırın her biri için birer `[Theory]`/`[Fact]`; ayrıca:
```csharp
[Theory] [InlineData(3999, " · almost done")] [InlineData(4000, " · ~5s left")]
[InlineData(34_000, " · ~35s left")] [InlineData(1_000, " · almost done")]
public void Eta_suffix_matches_the_prototype_thresholds(long eta, string expected)

[Fact] public void Eta_is_hidden_once_nothing_is_building_or_queued()
[Fact] public void Progress_turns_red_as_soon_as_one_project_fails_even_mid_run()
[Fact] public void Progress_uses_the_skipped_ratio_during_an_all_clean_check_and_snaps_to_hundred_when_done()
```

- [ ] **Step 2: Kırmızıyı gör → `RibbonText`'i yaz → yeşili gör**

- [ ] **Step 3: Şerit görünümü için failing testler yaz → uygula → yeşili gör**

```csharp
[StaFact] public void Ribbon_is_thirtytwo_pixels_over_a_two_pixel_progress_bar_with_zero_radius()
[StaFact] public void At_most_four_building_chips_are_shown_and_the_overflow_is_plain_text()
[StaFact] public void Failure_cluster_shows_three_chips_and_a_more_chip_that_applies_the_failed_filter()
[StaFact] public void Sync_phase_puts_the_progress_bar_into_indeterminate_mode()
```

- [ ] **Step 4: Tam suite + gözle kontrol**

> **GÖZLE KONTROL:** (a) Uygulamayı aç → şerit `▸ Waiting for Sync — project states appear after Sync`. (b) Sync'e bas → `▸ Sync — git fetch origin…` + progress **belirsiz (kayan) mod**. (c) Build başlat → `▸ Building 7/14 · 24s · ~35s left` biçimi ve derlenen projelerin chip'leri; chip'e tıkla → o proje seçilmeli. (d) Bir hata oluşsun → progress **anında kırmızıya** dönmeli, sağda `✗ 5 failed · 4 dependency-affected` + 3 chip + `+2 more`; `+2 more`'a tıkla → liste **Failed** filtresine geçmeli. (e) Bitince `Completed — …` satırı.

---

### Task D3: Event stream paneli

**Files:** Create `src/BuildOrchestrator.App/Views/EventStreamView.xaml(.cs)`, `ViewModels/StreamEventViewModel.cs`; Test: **yeni** `tests/BuildOrchestrator.Tests/App/EventStreamTests.cs`

**Kesin değerler (`BuildApp.jsx:639-737`):** satır `MinHeight 24`, `padding '0 10px'`, `gap 8`, mono `FontSize.Xs`, tabular, `LineHeight.Mono12`; saat `Brush.TextFaint`; glyph 12px (ok→succeeded, fail→failed, skip→skipped, done→failed?failed:succeeded, sync|info→amber `▸` 12px kolonda); renkler fail→`Brush.StatusFailText`, skip→`Brush.TextFaint`, done→duruma göre yeşil/kırmızı, sync|info→`Brush.TextDim`, ok→`Brush.TextSecondary`.
**Metinler birebir:** `{name} built ({dur})` · `{name} built — dependency issue ({dur})` · `{name} failed — {n} errors ({dur})` · `{name} skipped — up to date` · `Sync — {n} to build, {m} up to date` · `Build started — {n} projects, parallelism {p}` · `Stopped — {n} remaining projects queued` · `Continue — {n} remaining, parallelism {p}` · `Completed — …`.
**Aktif satır:** listenin altında ayrı satır, `Brush.AmberText`, saat + imleç + daktilo `{name} building…`; izlenen proje bitince **en son başlayan** building projeye atlar (yeni id → daktilo yeniden koşar).
**Daktilo/instant:** `burst = (şimdi - sonEmit) < 340ms`; `instant = burst || kind == fail`; adım `max(1, ceil(len/22))` karakter / **11ms**, bitişte 420ms sonra imleç söner.
**Glow-once:** yalnız `done` + hatasız → `Brush.StatusSuccessSoft` → şeffaf, **1.1s** `EaseOut`, **bir kez** (VM `GlowPlayed` bayrağı — recycle tekrar oynatmasın).
**Seçim:** projeli satırlar tıklanabilir, tekrar tık → deselect; seçilide sol 2px amber şerit + `Brush.SurfaceRaised`.
**Sayaç:** `{n} events` = **tam tampon** (render dilimi 150, tampon 260).
**Alta yapışma:** `BottomAnchorBehavior` (48px) + `LatestPill` — **yeniden yazma, tüket**.

- [ ] **Step 1–3: Failing testler → uygula → yeşil**
```csharp
[Fact]    public void Burst_events_under_three_hundred_forty_milliseconds_are_printed_instantly()
[Fact]    public void Failure_events_skip_the_typewriter_entirely()
[Fact]    public void The_active_line_jumps_to_the_most_recently_started_building_project()
[StaFact] public void Done_row_glows_once_and_never_replays_after_container_recycling()
[StaFact] public void Event_counter_reports_the_full_buffer_not_the_rendered_slice()
[StaFact] public void Selected_row_gets_a_two_pixel_amber_stripe_and_a_raised_surface()
```

- [ ] **Step 4: Tam suite + gözle kontrol**

> **GÖZLE KONTROL:** Build koş. Sağ-alt panelde olaylar akmalı; **en yeni satır daktiloyla** yazılmalı, ama olaylar hızlandığında ve **hatalarda anında** basılmalı. Bir olay satırına tıkla → o proje her yerde seçilmeli. Hatasız biten koşuda son `Completed …` satırı **bir kez** yeşil parlamalı. Yukarı kaydır → `⌄ latest` pill'i çıkmalı.

---

### Task D4 (T34 + T56-UI): Konsol gerçek akışı, anlatı satırları, reseed flicker

Bugün gerçek pencerede `ConsoleView.TypeActiveLine` **hiç çağrılmıyor**; anlatı satırlarının saat/ikon/renk dili yok; mod değişiminde doküman **~50ms + Dispatcher hop** gecikiyor (başlık anında değişip gövde eski içeriği göstermeye devam ediyor).

**Files:** Modify `src/BuildOrchestrator.App/MainWindow.xaml.cs:120-155`, `Console/ConsoleBatcher.cs`, `ViewModels/RunViewModel.cs`; Test: `tests/BuildOrchestrator.Tests/App/ConsoleModesTests.cs`, `App/ConsoleViewTests.cs`, **yeni** `App/TypingDegradationTests.cs`

- [ ] **Step 1: Reseed flicker için failing test yaz**

```csharp
[StaFact]
public void Switching_to_a_project_log_swaps_the_document_in_the_same_ui_turn_as_the_header()
{
    // Baslik ProjectLog moduna gectiginde konsol dokumani ARTIK onceki icerigi gostermemeli.
    ...
    Assert.Equal(ConsoleHeader.HeaderMode.ProjectLog, header.Mode);
    Assert.DoesNotContain(previousRunText, view.Document.Text, StringComparison.Ordinal);
}
```

- [ ] **Step 2: Kırmızıyı gör → düzelt**

Seçilen çözüm (**B**, `agent6` §4.3'teki üç yönden): doküman **tıklama anında UI thread'inde senkron** kurulur; `ConsoleBatcher` sentinel'i yalnız **uçuştaki bayat satırları atmak** için kalır. Böylece başlık ve gövde aynı karede değişir; batching argümanı (log satırı throughput'u) bozulmaz.

- [ ] **Step 3: Anlatı satırlarını bağla**

`SyncProgressEvent` + run olayları → `HH:MM:SS` + 10px ikon kolonu (`▸` cmd'de amber) + metin; renkler `ConsoleColorizer`/`ConsolePalette` ile (**mevcut, yeniden yazma**). Boşta tek satır `ready` (dim). En yeni satır **hibrit typewriter** ile (`TypeActiveLine`) — artık gerçekten çağrılır.

- [ ] **Step 4: T34 typing degradation testleri**

```csharp
[Fact] public void Live_line_drops_to_latest_when_lines_arrive_faster_than_the_typewriter()
[Fact] public void Typing_is_suspended_entirely_above_the_throughput_threshold()
[Fact] public void Errors_are_always_printed_instantly()
[Fact] public void Raw_msbuild_output_is_never_typed_character_by_character()
```
Tempo **Stopwatch-bazlı** (`TypewriterScheduler` `StepMs=11`, `MaxSteps=22` — mevcut sınıfı tüket).

- [ ] **Step 5: Tam suite + gözle kontrol**

> **GÖZLE KONTROL:** (a) Sync'e bas → konsolda `▸ git fetch origin main`, ardından granular satırlar; **en yeni satır harf harf** yazılmalı, imleç yanıp sönmeli. (b) MSBuild çıktısı akarken konsol **kilitlenmemeli** ve ham çıktı harf harf yazılmamalı. (c) Bir karta tıkla → başlık `← Back · {proje} · statü` olurken gövde **aynı anda** proje loguna geçmeli (**eski içerik bir an bile kalmamalı**) ve satırlar kaskatla açılmalı. (d) `Back` → anlatıya dönmeli.

---

### Task D5 (T50): Graf panelini gerçek veriye bağla

`GraphView` hazır ve testli; eksik olan **besleme**: `GraphNode(Name, Layer, Status, HasDepIssue)` + `GraphEdge(From=bağımlılık, To=bağımlı)`. `Layer` **çağıranın** işi; `GraphStatus` ≠ `ProjectRowState` — eşleme çağıranın işi (`GraphModels.cs:5-7`).

**Files:** Create `src/BuildOrchestrator.App/ViewModels/GraphBinder.cs`; Modify `MainWindow.xaml(.cs)`, `Graph/GraphModels.cs:23` (prefix); Test: **yeni** `tests/BuildOrchestrator.Tests/App/GraphBinderTests.cs`

**Interfaces:**
```csharp
public static class GraphBinder {
    public static IReadOnlyList<GraphNode> Nodes(IReadOnlyList<ProjectNode> topology,
                                                 IReadOnlyDictionary<string, ProjectRowViewModel> rows);
    public static IReadOnlyList<GraphEdge> Edges(IReadOnlyList<ProjectNode> topology);
    public static int LayerOf(ProjectNode n);           // LayerIndex ?? topolojik derinlik
    public static GraphStatus StatusOf(ProjectRowViewModel? row, bool inCycle, bool synced);
}
```
Eşleme: `Pending→Queued · Started→Building · Succeeded→Succeeded · Failed→Failed · Skipped→Skipped`; Sync öncesi → `Discovered`; `InCycle` → `Cycle`.
`Layer`: `ProjectNode.LayerIndex` varsa o; yoksa **topolojik derinlik** (`depth(n) = 0` bağımsızsa, aksi `1 + max(depth(deps))`; cycle üyeleri komponent derinliğini paylaşır).
`GraphNode.ShortLabel`'ın hardcoded `"OSYS."` prefix'i (`GraphModels.cs:23`) **ortak önek tespitine** çevrilir (tüm proje adlarının en uzun ortak nokta-sınırlı öneki; yoksa kırpma yok).

- [ ] **Step 1–2: Failing testler → uygula**
```csharp
[Fact] public void Edges_point_from_dependency_to_dependent()
[Fact] public void Layer_falls_back_to_topological_depth_when_no_layer_patterns_are_configured()
[Fact] public void Cycle_members_are_reported_as_cycle_status()
[Fact] public void Node_order_within_a_layer_follows_build_order()
[Fact] public void Short_label_strips_the_common_prefix_derived_from_the_data_not_a_hardcoded_one()
```

- [ ] **Step 3: Pencereye bağla**

`SetGraph` **yalnız** topoloji değişince (`TopologyChanged`); `UpdateStatuses` her statü değişiminde; run bitince `IsSettled = true`; `SelectionChanged` → VM seçimi (iki yönlü senkron, döngü koruması). `MotionSettings` **`Loaded`'dan önce** atanmış olmalı (C1'de yapıldı).

- [ ] **Step 4: Tam suite + gözle kontrol**

> **GÖZLE KONTROL:** Sync sonrası graf **katman katman belirmeli** (fade + 5px yukarıdan, katman başına 55ms). Build sırasında derlenen düğüme giden kenarlar **amber kesikli akmalı**, kamera frontier'i yumuşak takip etmeli. Bir düğüme tıkla → komşuları normal, gerisi sönmeli (%25/%16); liste ve konsol **aynı anda** o projeye geçmeli. Boşluğa tıkla → seçim kalkmalı. depIssue taşıyan düğümde sağ üstte **kırmızı üçgen rozeti** olmalı.

---

### Task D6 (T40 + T12 + T43-UI): Action bar, sayaç chip'leri, popover'lar, Build split-button

**Files:** Create `src/BuildOrchestrator.App/Views/ActionBar.xaml(.cs)`, `Views/BranchPopover.xaml`, `Views/WorktreePopover.xaml`, `Views/BuildMenu.xaml`; Test: **yeni** `tests/BuildOrchestrator.Tests/App/ActionBarTests.cs`, `App/PopoverTests.cs`

**Ölçüler (`BuildApp.jsx:1543-1614`):** bar `Height 42`, `padding '0 10px'`, `gap 8`, `Brush.Surface`, üstte 1px `Brush.Border`. Sıra: `Sync` (sm secondary) · 1px ayraç · 6 sayaç chip'i · esnek boşluk · `branch: {ad} ▾` · `worktree: {ad|off} ▾` · `Debug | Release` segment · `perf: {mod}` · 1px ayraç · Build split-button (koşarken **Stop**, danger).

**Sayaç chip'leri:** `Σ {total}` (tooltip `All projects — clear filter`, **her zaman temizler**) · building (boşken 8px gri nokta; tooltip `Building now — filter`) · `✓ {n}` (`Succeeded — filter`) · `✗ {n}` (`Failed — filter`) · `— {n}` (`Skipped — filter`) · `▲ {n}` (`Dependency-affected — filter`; sayı>0 ise üçgen `Brush.StatusFailText`, yoksa `Brush.TextFaint`). Toggle: aynı chip'e ikinci tık temizler. **Not (prototip hatası düzeltilir):** repo yokken sayaç chip'leri de **disabled** olur (README §3.1 "Sync/Build/chip'ler disabled" — prototipteki canlılık gözden kaçmadır).

**Build split-button:** sol yarı `Build` (stopped'ta **`Continue`**), sağ yarı `▾`; menü `width 276`, chip'in **üstünde 8px boşlukla**, `Radius.Lg`, `Effect.OverlayShadow`, 140ms pop-in (4px yukarı + scale .985→1, `EaseOut`), kapanış animasyonu **yok**. Maddeler ve **birebir kopya**:
1. *(yalnız stopped)* `Continue` — `{n} queued projects resume` — `F5`
2. `Build` — `Only changed projects` (stopped'ta `Start over — only changed projects`, **Kbd rozeti kaldırılır**) — `F5`
3. `Rebuild` — `All {total} projects — cache ignored` — `Ctrl+F5`
4. *(yalnız failed>0)* `Retry failed` — `{n} failed + dependents` — Kbd yok

**Branch popover (272px):** caps `SWITCH BRANCH` · `Search branches…` inputu (büyüteç) · 28px satırlar (seçilide amber ✓, değilse branch ikonu; ad mono; aktif branch'te `active` rozeti, diğerlerinde mono 7-hane SHA) · boş sonuç `No branches match “{q}”.` · alt not **birebir**: `Picking a non-active branch requires a worktree; the active branch stays untouched.` · kapanınca arama sıfırlanır.
**Branch seçimi (K3 — prototipten SAPMA, plan kazanır):** `git switch --detach …` satırı **YAZILMAZ**; yerine konsola niyet satırı: `branch target: {branch} ({sha}) — worktree will be used at Build` + `Branch changed: {branch} — Sync required`. Farklı branch → worktree **zorunlu ON**; proje durumları sıfırlanır, faz `Boot`.

**Worktree popover (300px):** caps `WORKTREE` · `Build in worktree` switch (zorunluysa disabled) · üç açıklama **birebir**:
- zorunlu: `Different branch selected — worktree required. The committed HEAD is built; active branch and local changes stay untouched.`
- açık: `The committed HEAD builds in a separate worktree; local changes excluded.`
- kapalı: `Off: in-place build — local changes included.`
· `TARGET WORKTREE` listesi: `{auto} (new)` + mevcutlar (ad + not), hover'da çöp kutusu (silince konsola dim `worktree removed: {ad}`) · `source` satırı: `working directory — local changes included` **veya** `committed HEAD ({branch}) → {worktree}`.
Auto-ad: slug = branch'te `/`→`-`; ek sayı = aynı prefix'li mevcut sayısı + 1.

**T12 mid-run kilit:** koşarken `branch`, `worktree`, `Debug|Release` **görünür şekilde disabled**; `perf` canlı. **T43:** config değişiminde tümü dirty + konsola warn `Configuration → Release — all projects will rebuild`.

- [ ] **Step 1–3: Failing testler → uygula → yeşil**
```csharp
[StaFact] public void Counter_chips_toggle_the_filter_and_sigma_always_clears_it()
[StaFact] public void Build_menu_shows_continue_only_when_stopped_and_retry_only_when_something_failed()
[StaFact] public void Stopped_state_moves_the_F5_badge_from_build_to_continue()
[StaFact] public void Branch_popover_filters_case_insensitively_and_resets_its_query_on_close()
[Fact]    public void Selecting_a_non_active_branch_forces_worktree_on_and_writes_the_intent_line_not_git_switch()
[Fact]    public void Auto_worktree_name_slugs_slashes_and_increments_the_suffix()
[StaFact] public void Branch_worktree_and_configuration_are_disabled_while_running_but_perf_is_not()
```

- [ ] **Step 4: Tam suite + gözle kontrol**

> **GÖZLE KONTROL:** (a) Alt bardaki sayaç chip'lerine tıkla → liste filtrelenmeli, aynı chip'e tekrar tıkla veya `Σ`'ya bas → temizlenmeli; Projects başlığında kaldırılabilir filtre chip'i görünmeli. (b) `branch` chip'ine tıkla → 272px popover, arama çalışmalı; **aktif olmayan** bir branch seç → worktree otomatik ON + kilitli, konsolda **`git switch` DEĞİL** niyet satırı olmalı. (c) `worktree` popover'ında üç açıklama metni duruma göre değişmeli; `source` satırı doğru olmalı. (d) Build'in `▾` okuna bas → menü **yukarı** açılmalı; Stop'tan sonra sol yarı `Continue` olmalı ve `F5` rozeti oraya geçmeli. (e) Koşarken branch/worktree/Debug-Release sönük, perf canlı olmalı.

---

### Task D7 (T66): Settings dialog — LAYERS + REPOSITORY

**Files:** Create `src/BuildOrchestrator.App/Views/SettingsDialog.xaml(.cs)`, `ViewModels/LayerEditorViewModel.cs`, `Controls/DragReorderBehavior.cs`; Test: **yeni** `tests/BuildOrchestrator.Tests/App/SettingsDialogTests.cs`, `App/DragReorderTests.cs`

**Dialog:** 620px, `Brush.SurfaceRaised` (DS kodu; README §2.9'daki `surface-overlay` **sapması burada kayıtlı**), `Radius.Lg`, `Effect.OverlayShadow`, scrim `Brush.Scrim`, başlık `Settings`, footer 1px `Brush.BorderSubtle`.
**LAYERS:** caps başlık + açıklama **birebir**: `Projects are grouped by the first matching pattern (regex on the project name), top to bottom; card order is the layer order in the list. Non-matching projects fall under Other.` (`Other` mono). Boş durum kutusu (kesikli): `No layers yet — projects show as a single list in build order.` Kolon başlıkları `Layer name` (170px) / `Pattern`. Kart 36px + 6px boşluk (adım **42px**): grip · ad inputu (170px) · regex inputu (mono, geçersizse invalid) · çöp. `Add layer` ghost butonu (yeni öğe `Layer {n+1}`, boş regex). Footer: `Load sample layers` (6 örnek katman — `BuildApp.jsx:965-972`'den birebir) · `Cancel` · `Save` (**yalnız** boş ad veya derlenemeyen regex varken disabled; **boş regex geçerlidir**).
**Sürükle-sırala:** `Mouse.Capture` + **±21px** eşikte komşuyla **swap**, `startY` 42px kaydırılır (çoklu adım); sürüklenen kart `Brush.SurfaceRaised` + `Brush.BorderStrong` + üstte; komşular **animasyonsuz** snap. **`DragDrop.DoDragDrop` YASAK.**
**Kaydet:** konsola dim **birebir** `Layer definitions updated — {n} layers` veya `Layers removed — single project list`; `UiState.LayerPatterns`'a yazılır ve **sonraki Sync/Build komutlarıyla motora gider** (A1/A5'in `LayerPatterns` alanları) → gruplama Core'dan `LayerName` olarak geri döner.
**REPOSITORY (K10):** caps başlık + mevcut kök yolu (mono, ellipsis) + `Change…` butonu → `OpenFolderDialog`; seçimde kök değişir, durumlar sıfırlanır, **otomatik Sync** başlar. Kök yoksa `no repository`.

- [ ] **Step 1–3: Failing testler → uygula → yeşil**
```csharp
[Fact]    public void Save_is_blocked_only_by_an_empty_name_or_an_uncompilable_regex_never_by_an_empty_pattern()
[Fact]    public void Drag_swaps_with_the_neighbour_at_twentyone_pixels_and_supports_multi_step_drags()
[Fact]    public void Cancel_discards_the_draft()
[Fact]    public void Saving_layers_writes_the_exact_console_note_and_persists_the_patterns()
[StaFact] public void Reorder_uses_mouse_capture_and_never_calls_the_ole_drag_drop_api()
```

- [ ] **Step 4: Tam suite + gözle kontrol**

> **GÖZLE KONTROL:** Dişliye tıkla. (a) Boşken kesikli kutu metni görünmeli. (b) `Load sample layers` → 6 katman; grip'ten tut ve sürükle → kart **yerinde kayarak** komşusuyla yer değiştirmeli (OS sürükle-bırak hayaleti **olmamalı**). (c) Regex'i boz → input kırmızı, `Save` disabled. (d) `Save` → liste **katman başlıklarıyla** gruplanmalı, başlıklar kaydırırken **birikerek yapışmalı**, konsolda `Layer definitions updated — 6 layers`. (e) `Change…` → klasör seçici; yeni kök seçince otomatik Sync koşmalı.

---

# FAZ E — Çapraz kesen

---

### Task E1 (T67): OS eylemleri

`grep Process.Start src/BuildOrchestrator.App` bugün **0 sonuç**.

**Files:** Create `src/BuildOrchestrator.App/Services/OsActions.cs`; Test: **yeni** `tests/BuildOrchestrator.Tests/App/OsActionsTests.cs`

```csharp
public interface IOsActions {
    void RevealInExplorer(string path);                     // explorer.exe /select,"<path>"  (tırnak ŞART)
    OpenInVsResult OpenInVisualStudio(IReadOnlyList<SolutionRef> candidates);  // 0 → hata, 1 → aç, >1 → seçtir (T32)
    string? PickFolder(string? initial);                    // Microsoft.Win32.OpenFolderDialog
}
```
Konsol notları **birebir**: `{name}.csproj revealed in Explorer` · `{name} opened in Visual Studio`. VS çözümü Core'un `vswhere` yolunu **yeniden kullanır** (`MsBuildResolver` deseni), `devenv.exe` ile bağlı `.sln` açılır; >1 aday varsa küçük seçim popover'ı. Clipboard **zaten** `ClipboardRetry` ile korunuyor (mevcut).

- [ ] **Step 1–3:** Failing testler (argüman kaçışı, 0/1/N sln dalları, vswhere yoksa `SkippableFact`) → uygula → yeşil.
- [ ] **Step 4: Gözle kontrol**
> Bir satırın üzerine gel → klasör ikonuna tıkla: Explorer **o dosya seçili** açılmalı, konsolda dim not. VS ikonuna tıkla: bağlı solution VS'de açılmalı (birden çok sln varsa seçim çıkmalı).

---

### Task E2 (T10 + T37 + T16): Etkileşim/hata durumları, engine-died, autostart

Prototipte **modellenmemiş** olanlar (git hatası, engine öldü, 0 proje, geçersiz klasör) plan kazanır — tasarım diline çevrilir.

**Files:** Modify `Views/*`, `ViewModels/RunViewModel.cs`, `App.xaml.cs:44-51`; Create `Services/AutostartService.cs`; Test: `App/RunViewModelTests.cs`, **yeni** `App/AutostartServiceTests.cs`, `App/InteractionStateTests.cs`

- **empty:** liste daveti **birebir** `Pick a repository to get started` + `Point to the OSYS solution root — projects and the dependency graph are discovered automatically.` + `Choose Folder`; şerit `Not ready — no repository selected`; graf `Graph appears after Sync`; stream `No events yet.`; Sync/Build/chip'ler disabled.
- **0 proje:** liste `No projects found under this folder.`; şerit `Ready — nothing to build`.
- **git/sync hatası:** konsola `error` satırı + şeritte kırmızı faz metni `Sync failed — {reason}`; Sync ile retry.
- **engine-died (T37):** şeridin **kalıcı hata modu** + `Restart engine` aksiyonu (banner/toast **YOK**); `EngineExited` → `IsRunning/IsStarting` sıfırlanır (It-3 backlog kalemi de burada kapanır).
- **all-skipped DELIGHT:** `Everything up to date — {n} projects checked in {dur}, nothing to build`.
- **ikinci instance sessizliği (triyaj-f):** `ActivateExistingInstance` `false` dönerse artık **sessiz çıkma yok** — kullanıcıya tek satırlık bir bildirim (tray balloon) gösterilir ve exit kodu ayrışır.
- **T16 autostart:** `UiState.Autostart` + `HKCU\...\Run` kaydı; açılışta **temiz Idle** (tray'e küçültülmüş, otomatik Sync yok).

- [ ] **Step 1–3:** Failing testler → uygula → yeşil (registry testi enjekte edilebilir arayüz üzerinden, gerçek registry'ye yazmadan).
- [ ] **Step 4: Gözle kontrol**
> (a) Ayarlardan kökü temizle → boş durum daveti. (b) Supervisor'ı Task Manager'dan öldür → şerit kalıcı hata moduna geçmeli ve `Restart engine` görünmeli; ona bas → motor geri gelmeli. (c) Uygulama açıkken ikinci kez başlat → mevcut pencere öne gelmeli (gelemezse **sessiz kalmamalı**). (d) Autostart'ı aç, oturumu kapat-aç → uygulama tray'de temiz Idle ile açılmalı.

---

### Task E3 (T41 + T42 + T36): MotionCoordinator, sync reveal stagger, reduced-motion tam kapsam

**Files:** Create `src/BuildOrchestrator.App/Services/MotionCoordinator.cs`; Modify tüm animasyon çağrı yerleri; Test: **yeni** `App/MotionCoordinatorTests.cs`, `App/ReducedMotionCoverageTests.cs`

```csharp
public sealed class MotionCoordinator {           // DD9: aynı anda EN FAZLA 1 hero
    public bool TryBeginHero(string key);          // graf+liste frontier AYNI hero sayılır
    public void EndHero(string key);
    public IDisposable? Hero(string key);          // null → reddedildi (dekoratif yol)
}
```
**T42 stagger:** liste satırı **10ms** gecikme/satır, tavan **380ms**; graf katmanı **55ms**, tavan **330ms**; başlangıç `Opacity=0` **set edilir** (gecikme sırasında flash olmaz).
**T36 kapsam:** reduced-motion açıkken **hiçbir** süre >0 olmamalı — bunu **kapsam testi** kanıtlar: tüm `Storyboard`/`BeginAnimation` çağrı yerlerini dolaşan bir test, `FakeMotionSignal.AnimationsEnabled=false` iken efektif sürelerin sıfır olduğunu doğrular; ayrıca dekoratif sonsuz animasyonlar (nefes, spinner, dash-flow, imleç blink) **durur**. `GraphView` dışındaki uzun ömürlü animasyon sahipleri de `AnimationsEnabledChanged`'e abone olur.

- [ ] **Step 1–3:** Failing testler → uygula → yeşil.
- [ ] **Step 4: Gözle kontrol**
> Windows → Ayarlar → Erişilebilirlik → Görsel efektler → **Animasyon efektleri**'ni KAPAT (uygulama açıkken). Anında: nefes/spinner/dash akışı/daktilo/kamera geçişi/pop-in durmalı, her şey anlık olmalı. Geri aç → animasyonlar dönmeli. Ayrıca Sync sonrası kartların ve graf katmanlarının **kademeli** belirdiğini gör.

---

### Task E4 (T48): Auto-scroll arbitration

**Files:** Create `src/BuildOrchestrator.App/Services/ScrollArbiter.cs`; Test: **yeni** `App/ScrollArbiterTests.cs`

Kural: kullanıcı scroll'u **bölgesel** duraklatır (o panel); öncelik **frontier > console > stream**; **yo-yo yasak** (animasyon devri + suppress bayrağı). `FollowScrollController`/`BottomAnchorBehavior`/`ScrollAnimator` **tüketilir**.

- [ ] **Step 1–3:** Failing testler (`User_scroll_suppresses_only_its_own_panel`, `Follow_and_bottom_anchor_never_fight_within_one_frame`, `Selection_scroll_wins_over_follow`) → uygula → yeşil.
- [ ] **Step 4: Gözle kontrol**
> Koşarken listede yukarı kaydır → takip durmalı, **konsol** akmaya devam etmeli; dibe dön → takip sürmeli. Karta tıkla → takip durmalı; seçimi kaldır → sürmeli. Hiçbir panelde ileri-geri zıplama olmamalı.

---

### Task E5 (T46 + T47 + T68 + T45): Klavye, focus, ekran okuyucu, kontrast, anti-slop

**Files:** Modify tüm view'lar; Create `App/AccessibilityNames.cs`; Test: **yeni** `App/KeyboardShortcutTests.cs`, `App/AccessibilityTests.cs`, `App/AntiSlopTests.cs`

- **Kısayollar (K6, birebir):** `F5` → koşarken **Stop**, stopped'ta **Continue**, aksi **Build**; `Ctrl+F5`/`Shift+F5` → **Rebuild**; `Ctrl+F` → filtre inputuna odak; `Esc` zinciri **dialog → popover/menü → seçim**; filtre inputundaki `Esc` **yalnız temizler + blur** (global zincire sızmaz); `Alt+B` global hotkey. **Çift-Shift / Ctrl+P YOK** (bir test bunların bağlı olmadığını pinler).
- **Klavye nav:** liste satırları focusable + `Enter` = seçim toggle; ok tuşları; dialog'da focus-trap; popover açılınca focus içeri, kapanınca tetikleyiciye döner.
- **SR:** `AutomationProperties.Name` tüm etkileşimli öğelerde (İngilizce); şerit faz metni **live region**.
- **Kontrast:** her metin/zemin çiftinin ≥4.5:1 olduğunu hesaplayan test (`Brush.TextFaint` üzerinde `Brush.SurfaceBase` dahil).
- **T45 anti-slop:** emoji yok · gradient yok · amber dışı dekoratif renk yok · panel gölgesi yok (yalnız floating overlay) · konsol radius 0 · dolu renkli rozet yok (amaçlı halka glyph serbest) · dönen globe yok. Testler statik XAML taraması + token denetimi.

- [ ] **Step 1–3:** Failing testler → uygula → yeşil.
- [ ] **Step 4: Gözle kontrol**
> Fareye dokunmadan: `Tab` ile dolaş (amber halka her yerde görünür olmalı), listede ok tuşları + `Enter` ile seçim yap, `Ctrl+F` ile filtreye geç, `Esc` ile sırasıyla dialog → popover → seçim kapansın. `F5`/`Ctrl+F5` build/rebuild tetiklesin. Pencere gizliyken `Alt+B` ile geri getir.

---

### Task E6: It-4 acceptance sweep + perf ölçümü + tasarımla yan yana karşılaştırma

**Files:** Create `.claude/outputs/<ts>-it4-records.md`; Test: **yeni** `App/ListRealizationPerfTests.cs`

- [ ] **Step 1: Perf ölçümü (virtualization KAPALI riski)**

`StickyLayerList` her satırı **eager** realize ediyor. 191 ve 500 satırlık `ProjectRow` ile `Measure/Arrange` süresini ölç; 191 için bütçe **< 400ms**. Aşılırsa kartı sadeleştir (T51/It-5'e devretme kararını kayda geç).

- [ ] **Step 2: v7 Part C It-4 acceptance'ını madde madde kanıtla**

Aşağıdaki matristeki her satır için: kanıt = test adı **veya** gözle doğrulama adımı. Sonuç `it4-records.md`'ye yazılır.

- [ ] **Step 3: Tasarımla yan yana karşılaştırma**

`prototype/Build Orchestrator (standalone).html` tarayıcıda açılır, uygulama yanına alınır; §2.1–§2.9'un her bölümü tek tek karşılaştırılır. Sapmalar ya düzeltilir ya A13.1 "algısal eşdeğer" sınıfına gerekçesiyle yazılır.

- [ ] **Step 4: Tam suite + acceptance suite**

Run: `dotnet build BuildOrchestrator.slnx` → 0/0
Run: `dotnet test ... --filter "Category!=Acceptance"` → 0 failed
Run (kullanıcı onaylarsa, ~30dk): `dotnet test ... --filter "Category=Acceptance"`

---

## It-4 Acceptance matrisi (v7 Part C It-4 → kanıt)

| # | v7 It-4 acceptance maddesi | Kanıt |
|---|---|---|
| 1 | T65 kararı kayıtlı | ✅ It-4a'da kapandı (`2026-07-19-03-29-t65-font-decision.md`) — saf WPF |
| 2 | design-v1 birebirliği gözle doğrulandı (token/animasyon/kopya) | B1 + E6 Step 3 |
| 3 | 2×2 layout + görünüm modları | C1 |
| 4 | graf paneli canlı, frontier senkron (tek hero) | D5 + E3 |
| 5 | 500–1000 kart+node akıcı | E6 Step 1 (191 bütçesi; 500+ → T51/It-5 kaydı) |
| 6 | seçim/deselect her yerde çalışır | C2 + D1/D3/D5 |
| 7 | all-skipped DELIGHT | D2 + E2 |
| 8 | reduce-motion anlık | E3 |
| 9 | keyboard-first | E5 |
| 10 | failure sticky kümesi + per-row log | D2 + D4 |
| 11 | ▲ dep filtresi | D1 + D6 |
| 12 | Continue/Retry menüde | D6 |
| 13 | Ctrl+F filtresi | D6 + E5 |
| 14 | Copy log | ✅ It-4a'da bağlı; B2'de ikon hattına taşındı |
| 15 | restore glyph | ✅ It-4a; B2'de çizilmiş geometriye taşındı |
| 16 | Snap Layouts | ✅ It-4a (kullanıcı doğruladı) |
| 17 | tray balloon | ✅ It-4a (kullanıcı doğruladı) |
| 18 | typing degradation | D4 |
| 19 | global progress + ETA | D2 |
| 20 | Settings (LAYERS + REPOSITORY) | D7 |
| 21 | interaction states + engine-died | E2 |
| 22 | autostart | E2 |
| 23 | mid-run kilit | C2 + D6 |
| 24 | OS eylemleri | E1 |
| 25 | SR + kontrast | E5 |
| 26 | anti-slop | E5 |
| 27 | auto-scroll arbitration | E4 |
| 28 | sync reveal stagger | E3 |
| 29 | config-değişti uyarısı | C2 |
| 30 | tam token dictionary | B1 |
| 31 | DS kontrol kütüphanesi | B3 |
| 32 | tooltip altyapısı | B4 |
| 33 | ikon/ICO hattı | B2 |

---

## Yürütme notları

- **Branch:** `it4b-ui` (main'den). `main` ve `origin`'e **dokunulmaz**; merge/push kullanıcı onayına tabidir (It-2/It-3/It-4a deseni).
- **Ledger:** her task sonunda `.superpowers/sdd/progress.md`'ye satır eklenir (task, commit SHA, review sonucu, kalan Minor'lar).
- **Yöntem:** `superpowers:subagent-driven-development` — task başına taze implementer → review → fix → re-review.
- **Sıra bağımlılıkları:** A1 → (A2, A3, D7) · A5 → (C2, D1, D5, D6) · B1 → (B2, B3, B4, tüm D) · B3 → (D2, D3, D6, D7) · C1 → (tüm D) · C2 → (D1, D2, D6).
- **Her task sonunda kullanıcıya:** yukarıdaki **GÖZLE KONTROL** adımı + build/test sayıları.



