# It-1 (Sync/Graph/Planning + It-0 Hardening) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v7 Part C It-1'i teslim et — gerçek OSYS'i tarayıp HintPath→producer grafını, cycle/topo build-order'ı, `BuildPlan`'i ve pre-run willBuild kümesini saf Core'da üret; It-0 final review'ın devrettiği 5 sertleştirmeyi kapat.

**Architecture:** Yeni domain DTO'ları `Contracts/Model`'de sabitlenir; Core'da `Discovery → Graph → Planning` pipeline'ı kurulur. Graf primer = **HintPath-basename→producer** (D11), ProjectReference ikincil. `file→project = evaluated Compile items` (path-prefix DEĞİL). Evaluation **raw-XML** ile yapılır (aşağıdaki "Evaluation kararı"); Core saf ve MSBuild'siz test edilebilir kalır.

**Tech Stack:** .NET 10 · xUnit · `System.Xml.Linq` (evaluation) · `System.Text.Json` (cache). Yeni paket bağımlılığı YOK.

## Evaluation kararı (gerekçeli sapma — kayıt)

D5 "batch MSBuild evaluation" / D11 "MSBuild-evaluated Compile items" **raw-XML evaluation** ile karşılanır (spike S3 yöntemiyle aynı). Gerekçe: hedef repoda 173/177 legacy non-SDK proje → `<Compile Include>`/`<Reference><HintPath>`/`<ProjectReference>` **explicit** listelenir, yani XML değeri = evaluated item (glob yok). Bu, Core'u `Microsoft.Build` bağımlılığından ve MSBuild.exe spawn'ından (177× cold) kurtarır, TDD'yi saf tutar, spike'ın 0-belirsizlik/0-kaçırılan-edge sonucunu tekrarlar. **SDK-style 4 proje** için default `**/*.cs` glob (obj/bin hariç) replike edilir; `<Compile Include>` içindeki wildcard'lar diskle genişletilir; linked file'lar (`..\..`) Include yoluyla çözülür. D11'in ruhu (path-prefix heuristic YOK) tam korunur. Kalan sınıflandırılamayan artık → T71 warn. `Directory.Build.props` koşul/property evaluation kapsam DIŞI (It-3 scope-dirty kuralı ayrı ele alır) — bu bilinen boşluk raporlanır.

---

## Global Constraints

Her task'ın örtük gereksinimi (v7 planından, ihlal = plan ihlali):

- **Graf primer = HintPath-basename→producer (D11); ProjectReference İKİNCİL.** `file→project = evaluated Compile items, path-prefix DEĞİL.`
- **Determinizm (D8): sleep-poll YASAK.** Tüm sıralama/iterasyon deterministik — bağ kırma daima `projectId` (tam yol) `StringComparer.OrdinalIgnoreCase` ordinal sırasıyla. Aynı graf+derece → aynı çıktı.
- **Core saf:** UI/process/MSBuild bağımsız. In-process MSBuild (BuildManager) YOK. OutDir okunmaz/dokunulmaz — "değişti mi" yalnız kaynak sinyalinden; DLL/bin timestamp asla okunmaz.
- **It-0 yasakları korunur:** stdout yalnız NDJSON; AllowsTransparency yok; ortak OutDir'e dokunulmaz.
- **Dil/üslup:** kod yorumları Türkçe + İngilizce teknik terim; plan-iz etiketleri (`[D11]`, `[T24]` vb.) korunur. Dosya adları/kod İngilizce (2026-07-16 kuralı).
- **Cache:** atomik temp+rename (single-writer); `dependency-graph.json` mtime+hash invalidation.
- **Path kanonik:** `Id = Path.GetFullPath(path)` (orijinal case saklanır); anahtar/dedup/karşılaştırma `StringComparer.OrdinalIgnoreCase`.
- **Scan ignore:** `.git bin obj node_modules .vs`. **Build-etkileyen uzantılar:** `.cs .xaml .resx .csproj .props .targets`.
- **Hedef repo (girdi, varlık değil):** `D:\Projects\Delta\OSYS` — 177 csproj · 1854 HintPath · 44 sln · 173 legacy + 4 SDK. Spike S3: Edge≈1060 (%57.2), 0 ambiguity, 0 kaçırılan edge; unmatched'ler kanıtlanabilir repo-DIŞI.
- **Commit'ler kullanıcı istemeden yapılmaz** (bu proje kuralı). Aşağıdaki `git commit` adımları subagent iş-akışı içindir; nihai commit onayı kullanıcıdadır — subagent-driven review sonrası kullanıcıya sorulur.

---

# FAZ 0 — It-0 Sertleştirme Devri (final review çıktısı)

> Kaynak: it0-records "It-1 giriş listesi". Bu 5 task grafiği inşa etmeden ÖNCE tabanı stabilize eder; birbirinden bağımsızdır.

### Task 1: NdjsonWriter base-type polimorfizm footgun'ı

**Files:**
- Modify: `src/BuildOrchestrator.Contracts/Ipc/NdjsonFraming.cs`
- Test: `tests/BuildOrchestrator.Tests/Ipc/NdjsonFramingTests.cs`

**Sorun (it0-records):** `WriteAsync<T>` concrete tiple (`WriteAsync(new PongEvent(...))`) çağrılırsa `T=PongEvent` → `System.Text.Json` polimorfik discriminator (`"type"`) YAZMAZ → reader `IpcEvent` olarak deserialize edemez. Bugün her çağrı elle `WriteAsync<IpcEvent>(...)` yazıyor; kısıt yok = footgun.

**Interfaces:**
- Produces: `NdjsonWriter.WriteAsync(IpcCommand, CancellationToken)` ve `WriteAsync(IpcEvent, CancellationToken)` (iki base overload; generic private).

- [ ] **Step 1: Failing test** — concrete-tip çağrının discriminator yazdığını doğrula.

```csharp
// NdjsonFramingTests.cs içine ekle
[Fact]
public async Task WriteAsync_concrete_type_still_emits_polymorphic_discriminator()
{
    using var ms = new MemoryStream();
    var writer = new NdjsonWriter(ms);
    await writer.WriteAsync(new PongEvent(7)); // concrete tip — base overload'a bağlanmalı
    string line = System.Text.Encoding.UTF8.GetString(ms.ToArray());
    Assert.Contains("\"type\":\"pong\"", line); // discriminator yazıldı
    ms.Position = 0;
    var ev = await new NdjsonReader(ms).ReadAsync<IpcEvent>();
    Assert.IsType<PongEvent>(ev); // round-trip base tipe deserialize oldu
}
```

- [ ] **Step 2: Run — FAIL**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~WriteAsync_concrete_type"`
Expected: FAIL — bugün ya derlenmez (overload yok) ya da discriminator eksik.

- [ ] **Step 3: Implement** — generic'i private yap, iki base overload aç.

```csharp
public sealed class NdjsonWriter(Stream stream)
{
    public const int MaxLineBytes = 1_048_576;
    private static readonly byte[] NewLine = [(byte)'\n'];
    private readonly SemaphoreSlim _gate = new(1, 1);

    // Base-type kısıtı: discriminator daima yazılır (polimorfizm footgun kapandı). [it0-devir]
    public Task WriteAsync(IpcCommand message, CancellationToken ct = default) => WriteCoreAsync(message, ct);
    public Task WriteAsync(IpcEvent message, CancellationToken ct = default) => WriteCoreAsync(message, ct);

    private async Task WriteCoreAsync<T>(T message, CancellationToken ct)
    {
        byte[] payload = JsonSerializer.SerializeToUtf8Bytes(message, IpcJson.Options);
        if (payload.Length + 1 > MaxLineBytes)
            throw new IpcFramingException($"IPC mesajı {payload.Length} bayt — MaxLineBytes ({MaxLineBytes}) aşıldı.");
        await _gate.WaitAsync(ct);
        try
        {
            await stream.WriteAsync(payload, ct);
            await stream.WriteAsync(NewLine, ct);
            await stream.FlushAsync(ct);
        }
        finally { _gate.Release(); }
    }
}
```

- [ ] **Step 4: Çağrı yerlerini sadeleştir (opsiyonel ama tutarlılık için).** `WriteAsync<IpcEvent>(...)` / `WriteAsync<IpcCommand>(...)` çağrılarındaki explicit type-arg kaldırılabilir (overload çözer). Bırakılırsa da derlenir; DAVRANIŞ değişmez. Zorunlu değil — mevcut testleri kırma.

- [ ] **Step 5: Run — PASS**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~NdjsonFraming"`
Expected: PASS (yeni + mevcut framing testleri).

- [ ] **Step 6: Commit**

```bash
git add src/BuildOrchestrator.Contracts/Ipc/NdjsonFraming.cs tests/BuildOrchestrator.Tests/Ipc/NdjsonFramingTests.cs
git commit -m "harden(ipc): NdjsonWriter base-type overloads to guarantee polymorphic discriminator"
```

---

### Task 2: ProcessRunner kill-path sertleştirme

**Files:**
- Modify: `src/BuildOrchestrator.Core/Processes/ProcessRunner.cs:44-52`
- Test: `tests/BuildOrchestrator.Tests/Processes/ProcessRunnerTests.cs`

**Sorun (it0-records):** timeout dalında `process.Kill()` sonrası `WaitForExitAsync(CancellationToken.None)` **unbounded** (kill takılırsa sonsuz bekler); `catch (InvalidOperationException)` dar — `Win32Exception` (ör. erişim reddi) caller'a sızar.

**Interfaces:**
- Produces: aynı `RunAsync` imzası; davranış değişmez ama post-kill wait bounded + geniş catch.

- [ ] **Step 1: Failing test** — timeout'ta bounded dönüş (deterministik: hızlı timeout + kısa uyuyan child).

```csharp
[Fact]
public async Task RunAsync_timeout_kills_and_returns_bounded_TimedOut()
{
    var runner = new ProcessRunner();
    // 30sn uyuyan child'ı 500ms timeout ile öldür; test 30sn beklememeli
    var spec = new ProcessSpec("cmd.exe",
        ["/c", "powershell -NoProfile -Command Start-Sleep -Seconds 30"],
        Timeout: TimeSpan.FromMilliseconds(500));
    var sw = System.Diagnostics.Stopwatch.StartNew();
    var result = await runner.RunAsync(spec).WaitAsync(TimeSpan.FromSeconds(10)); // aşım = FAIL
    sw.Stop();
    Assert.True(result.TimedOut);
    Assert.Equal(-1, result.ExitCode);
    Assert.True(sw.ElapsedMilliseconds < 10_000, $"kill-path {sw.ElapsedMilliseconds}ms — bounded olmalı");
}
```

- [ ] **Step 2: Run — verify current pass/observe.** Bu test bugünkü kodda muhtemelen GEÇER (kill hızlı). Amaç regression guard + sonraki adımda bounded-wait + geniş catch garantisi. Run: `dotnet test ... --filter "FullyQualifiedName~RunAsync_timeout_kills"` → gözlemle.

- [ ] **Step 3: Implement** — post-kill wait bounded (5sn tavan), catch genişletildi.

```csharp
catch (OperationCanceledException)
{
    try { process.Kill(entireProcessTree: true); }
    catch (Exception ex) when (ex is InvalidOperationException or System.ComponentModel.Win32Exception) { /* zaten öldü / erişim */ }
    // post-kill wait BOUNDED — kill takılsa da sonsuz beklemeyiz [it0-devir]
    try { await process.WaitForExitAsync(new CancellationTokenSource(TimeSpan.FromSeconds(5)).Token); }
    catch (OperationCanceledException) { /* çıkış onayı gelmedi; yine de sonucu döneriz */ }
    if (ct.IsCancellationRequested) throw;
    return new ProcessResult(-1, await stdoutTask, await stderrTask, sw.Elapsed, TimedOut: true);
}
```

- [ ] **Step 4: Run — PASS**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~ProcessRunner"`
Expected: PASS (yeni + mevcut).

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Processes/ProcessRunner.cs tests/BuildOrchestrator.Tests/Processes/ProcessRunnerTests.cs
git commit -m "harden(process): bound post-kill wait and widen kill-path catch in ProcessRunner"
```

---

### Task 3: EngineHost sertleştirme kümesi

**Files:**
- Modify: `src/BuildOrchestrator.App/Services/EngineHost.cs`
- Test: `tests/BuildOrchestrator.Tests/App/EngineHostTests.cs`

**Sorunlar (it0-records):** (a) `_generation` cross-thread `int` okuması → `volatile`/`Interlocked`. (b) `ReadLoopAsync` swallow-all catch `while` DIŞINDA → tek bozuk frame = kalıcı sağırlık (Supervisor yaşarken UI "ready" kalır) → catch loop İÇİNE + **framing hatasında engine'i öldür + `EngineExited`** (Supervisor `exit 2` ile simetri). (c) startup-crash çift sinyal (hem `_ready` timeout hem exit). (d) `StartAsync` timeout'ta child-referans sızıntısı (temizlenmeli). (e) graceful `ShutdownCommand` + generation (şu an dispose/restart doğrudan kill).

**Interfaces:**
- Consumes: `NdjsonWriter.WriteAsync(IpcCommand,...)` (Task 1), `ShutdownCommand` (mevcut).
- Produces: `EngineHost.StartAsync/RestartAsync/SendAsync/DisposeAsync` (imza korunur) + `EngineExited` framing-hatasında da tetiklenir.

- [ ] **Step 1: Failing test — framing hatası kalıcı sağırlık YARATMAZ, EngineExited tetikler.** Deterministik: Supervisor'a çöp (garbage) satır enjekte edip stream'i bozamayız kolayca; bunun yerine bozuk-frame'i doğrudan reader seviyesinde test etmek zor. Bu davranışı **birim** olarak izole et: `ReadLoopAsync`'in framing-exception'da `EngineExited` tetiklediğini, StartAsync timeout'unda child'ın temizlendiğini test et.

```csharp
[Fact]
public async Task StartAsync_timeout_disposes_child_and_no_leak()
{
    // Var olmayan exe → child hızla ölür; StartAsync 5sn timeout'a düşmeden EngineExited ya da hata dönmeli.
    await using var host = new EngineHost(Path.Combine(AppContext.BaseDirectory, "does-not-exist.exe"));
    await Assert.ThrowsAnyAsync<Exception>(async () =>
        await host.StartAsync(new CancellationTokenSource(TimeSpan.FromSeconds(6)).Token));
    Assert.Null(host.EnginePid); // child referansı sızmadı/temizlendi
}
```

- [ ] **Step 2: Run — FAIL/observe** (bugün EnginePid null olmayabilir ya da 5sn takılabilir).

Run: `dotnet test ... --filter "FullyQualifiedName~StartAsync_timeout_disposes_child"`

- [ ] **Step 3: Implement** — tüm küme.

```csharp
public sealed class EngineHost(string supervisorExePath) : IAsyncDisposable
{
    private readonly JobObject _outerJob = JobObject.CreateKillOnClose();
    private JobChildProcess? _child;
    private NdjsonWriter? _writer;
    private TaskCompletionSource<EngineReadyEvent>? _ready;
    private volatile int _generation; // cross-thread okuma → volatile [it0-devir]

    public event Action<IpcEvent>? EventReceived;
    public event Action<int?>? EngineExited;
    public int? EnginePid => _child?.Pid;

    public async Task<EngineReadyEvent> StartAsync(CancellationToken ct = default)
    {
        int gen = Interlocked.Increment(ref _generation);
        _ready = new TaskCompletionSource<EngineReadyEvent>(TaskCreationOptions.RunContinuationsAsynchronously);
        string cmdLine = WindowsCommandLine.Build(supervisorExePath);
        _child = JobProcessLauncher.Launch(_outerJob, cmdLine, new LaunchOptions(RedirectStdio: true));
        _writer = new NdjsonWriter(_child.StandardInput!);
        var reader = new NdjsonReader(_child.StandardOutput!);
        _ = Task.Run(() => ReadLoopAsync(reader, gen), CancellationToken.None);
        var watched = _child;
        _ = Task.Run(async () =>
        {
            int code = await watched.WaitForExitAsync(CancellationToken.None);
            if (Volatile.Read(ref _generation) == gen)
            {
                _ready?.TrySetException(new InvalidOperationException($"Engine startup'ta öldü (exit {code}).")); // startup-crash tek sinyal
                EngineExited?.Invoke(code);
            }
        }, CancellationToken.None);
        try
        {
            return await _ready.Task.WaitAsync(TimeSpan.FromSeconds(5), ct);
        }
        catch
        {
            if (Volatile.Read(ref _generation) == gen) KillCurrent(); // timeout/iptal → child sızmasın [it0-devir]
            throw;
        }
    }

    public Task SendAsync(IpcCommand command, CancellationToken ct = default) =>
        _writer?.WriteAsync(command, ct) ?? throw new InvalidOperationException("Engine başlatılmadı.");

    public async Task<EngineReadyEvent> RestartAsync(CancellationToken ct = default)
    {
        await ShutdownGracefullyAsync();
        return await StartAsync(ct);
    }

    private async Task ReadLoopAsync(NdjsonReader reader, int gen)
    {
        try
        {
            while (true)
            {
                IpcEvent? ev;
                try { ev = await reader.ReadAsync<IpcEvent>(CancellationToken.None); }
                catch (IpcFramingException)
                {
                    // Bozuk frame = kalıcı sağırlık YARATMAZ; Supervisor exit-2 ile simetri: engine'i öldür + bildir. [it0-devir]
                    if (Volatile.Read(ref _generation) == gen) { KillCurrent(); EngineExited?.Invoke(null); }
                    return;
                }
                if (ev is null) return; // EOF — exit watcher bildirir
                if (ev is EngineReadyEvent ready) _ready?.TrySetResult(ready);
                EventReceived?.Invoke(ev);
            }
        }
        catch { /* stream koptu — exit watcher bildirir */ }
    }

    private async Task ShutdownGracefullyAsync()
    {
        Interlocked.Increment(ref _generation); // eski watcher/reader sustur
        try
        {
            if (_writer is not null)
                await _writer.WriteAsync(new ShutdownCommand()).WaitAsync(TimeSpan.FromMilliseconds(500)); // graceful [it0-devir]
        }
        catch { /* zaten ölmüş olabilir */ }
        KillCurrent();
    }

    private void KillCurrent()
    {
        if (_child is null) return;
        try { System.Diagnostics.Process.GetProcessById(_child.Pid).Kill(entireProcessTree: true); }
        catch (ArgumentException) { /* zaten öldü */ }
        _child.Dispose(); _child = null; _writer = null;
    }

    public async ValueTask DisposeAsync()
    {
        await ShutdownGracefullyAsync();
        _outerJob.Dispose(); // KILL_ON_JOB_CLOSE: her koşulda süpürge
    }
}
```

- [ ] **Step 4: Run — PASS** (yeni + mevcut EngineHost testleri: ping/pong ve kill→restart hâlâ geçmeli).

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~EngineHost"`
Expected: PASS. Not: `Supervisor_kill_raises_EngineExited_and_restart_recovers` graceful-shutdown ile hâlâ geçmeli (restart yeni pid verir).

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.App/Services/EngineHost.cs tests/BuildOrchestrator.Tests/App/EngineHostTests.cs
git commit -m "harden(engine): volatile generation, in-loop framing-death signal, graceful shutdown, start-timeout cleanup"
```

---

### Task 4: App copy-target hardcoded TFM + stale-file birikimi

**Files:**
- Modify: `src/BuildOrchestrator.App/BuildOrchestrator.App.csproj:24-29`

**Sorun (it0-records):** `CopySupervisorOutput` target'ında kaynak yolu `...\bin\$(Configuration)\net10.0-windows\**` **hardcoded TFM**; Supervisor TFM değişirse kopya boşalır. Ayrıca `SkipUnchangedFiles` silinen dosyaları temizlemez → **stale birikim**.

- [ ] **Step 1: Implement** — TFM wildcard + kopya öncesi hedefi temizle.

```xml
  <Target Name="CopySupervisorOutput" AfterTargets="Build">
    <!-- TFM'i wildcard'la; Supervisor TFM değişse de bozulmaz [it0-devir] -->
    <ItemGroup>
      <_SupervisorFiles Include="..\BuildOrchestrator.Supervisor\bin\$(Configuration)\**\*.*" />
    </ItemGroup>
    <!-- stale birikimi önle: hedef supervisor klasörünü kopya öncesi sil [it0-devir] -->
    <RemoveDir Directories="$(OutDir)supervisor" />
    <Copy SourceFiles="@(_SupervisorFiles)" DestinationFolder="$(OutDir)supervisor\%(RecursiveDir)" SkipUnchangedFiles="true" />
  </Target>
```

- [ ] **Step 2: Verify by build** — temiz build + kopya yolu doğru.

Run:
```powershell
dotnet build BuildOrchestrator.slnx -c Debug
```
Expected: 0 hata; `src/BuildOrchestrator.App/bin/Debug/net10.0-windows/supervisor/BuildOrchestrator.Supervisor.dll` mevcut (Glob ile doğrula).

- [ ] **Step 3: Regression — tam suite yeşil.**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj`
Expected: mevcut testler PASS (EngineHost/Cascade Supervisor exe'sini kopyadan çözer).

- [ ] **Step 4: Commit**

```bash
git add src/BuildOrchestrator.App/BuildOrchestrator.App.csproj
git commit -m "harden(build): TFM-agnostic supervisor copy + clean stale files before copy"
```

---

### Task 5: CascadeKillTests handles.Count>=5 assert

**Files:**
- Modify: `tests/BuildOrchestrator.Tests/Supervisor/CascadeKillTests.cs:39-41`

**Sorun (it0-records):** handle'lar `livePids`'ten açılırken, teorik "vakum-geçiş" (tüm process kill'den önce ölürse) `handles` boşalır → assert'ler vakumsuz geçer. Tek satır guard: kill ÖNCESİ en az 5 açık handle olduğunu doğrula.

- [ ] **Step 1: Implement** — handle açma sonrası guard.

```csharp
            handles = livePids.Select(pid => { try { return Process.GetProcessById(pid); } catch (ArgumentException) { return null; } })
                              .Where(p => p is not null).Cast<Process>().ToList();
            Assert.True(handles.Count >= 5, $"kill öncesi {handles.Count} açık handle — beklenen ≥5 (vakum-geçiş guard)"); // [it0-devir]
```

- [ ] **Step 2: Run — PASS** (davranış aynı, sadece guard eklendi).

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~CascadeKill"`
Expected: PASS (2 test).

- [ ] **Step 3: Commit**

```bash
git add tests/BuildOrchestrator.Tests/Supervisor/CascadeKillTests.cs
git commit -m "test(cascade): assert >=5 live handles before kill to close vacuum-pass gap"
```

---

# FAZ 1 — Contracts Domain DTO'ları (pin)

### Task 6: It-1 domain DTO'ları (Contracts/Model)

**Files:**
- Create: `src/BuildOrchestrator.Contracts/Model/ProjectModels.cs`
- Test: `tests/BuildOrchestrator.Tests/Contracts/ProjectModelsTests.cs`

**Neden Contracts:** A9 `ProjectNode`/`BuildPlan`/`BuildState`'i IPC sözleşmesinde konumlandırır; Core → Contracts referansı ZATEN var, Core bu tipleri ÜRETİR (planlama Core'da, D3). It-1 yalnız It-1'in ürettiği/tükettiği tipleri sabitler (YAGNI — `ProjectResult.depIssues`/`RunRequest.mode` It-2/3'e bırakılır).

**Interfaces:**
- Produces (sonraki tüm task'lar tüketir):

```csharp
namespace BuildOrchestrator.Contracts.Model;

public enum HintPathClass { Edge, ExternalThirdParty, ExternalOsysPlatform, Unclassified }
public enum BuildResult { Succeeded, Failed, Skipped }

public sealed record SolutionRef(string Name, string Path);

// Ham HintPath + basename + sınıf + (varsa) üretici projectId
public sealed record HintPathRef(string Raw, string BaseName, HintPathClass Class, string? ProducerProjectId);

public sealed record ProjectNode(
    string Id,                              // kanonik tam csproj yolu
    string Name,                            // AssemblyName türevi kısa ad
    string ProjectPath,                     // == Id (A9 şekli)
    IReadOnlyList<string> SolutionNames,    // T32
    IReadOnlyList<string> Dependencies,     // üretici projectId'ler (graf kenarları, deduped, sıralı)
    int BuildOrder,
    int? LayerIndex,
    string? LayerName,
    bool InCycle,
    bool? WillBuild);                       // T53: dirty=true, güncel=false, imza-yok/pre-Sync=null

public sealed record BuildPlan(
    IReadOnlyList<ProjectNode> Nodes,                       // build-order'da
    IReadOnlyList<IReadOnlyList<string>> Cycles,            // her biri bir SCC (>1 üye), üyeler sıralı
    string Configuration);

public sealed record BuildState(
    string ProjectId,
    string? BuiltSignature,
    string? BuiltCommit = null,
    BuildResult? LastResult = null,
    DateTimeOffset? LastRunAt = null,
    string? LastBranch = null,
    long? LastDurationMs = null);           // T70 (It-3) burada alan olarak hazır
```

- [ ] **Step 1: Failing test** — DTO'lar mevcut + JSON round-trip (IpcJson.Options ile) enum'ları camelCase yazar.

```csharp
using BuildOrchestrator.Contracts.Ipc;
using BuildOrchestrator.Contracts.Model;
using System.Text.Json;

public class ProjectModelsTests
{
    [Fact]
    public void ProjectNode_round_trips_with_ipc_json_options()
    {
        var node = new ProjectNode("C:\\r\\A.csproj", "A", "C:\\r\\A.csproj",
            ["Sln1"], ["C:\\r\\B.csproj"], BuildOrder: 3, LayerIndex: null, LayerName: null,
            InCycle: false, WillBuild: true);
        string json = JsonSerializer.Serialize(node, IpcJson.Options);
        Assert.Contains("\"willBuild\":true", json); // camelCase
        var back = JsonSerializer.Deserialize<ProjectNode>(json, IpcJson.Options)!;
        Assert.Equal(node, back); // record value-equality
    }

    [Fact]
    public void HintPathClass_serializes_camelCase()
    {
        string json = JsonSerializer.Serialize(HintPathClass.ExternalOsysPlatform, IpcJson.Options);
        Assert.Equal("\"externalOsysPlatform\"", json);
    }
}
```

- [ ] **Step 2: Run — FAIL** (tipler yok).

Run: `dotnet test ... --filter "FullyQualifiedName~ProjectModels"`
Expected: derleme hatası (tip yok).

- [ ] **Step 3: Implement** — yukarıdaki `ProjectModels.cs`'i oluştur.

- [ ] **Step 4: Run — PASS**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~ProjectModels"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Contracts/Model/ProjectModels.cs tests/BuildOrchestrator.Tests/Contracts/ProjectModelsTests.cs
git commit -m "feat(contracts): pin It-1 domain DTOs (ProjectNode, BuildPlan, BuildState, HintPathRef)"
```

---

# FAZ 2 — Core: Discovery & Evaluation

### Task 7: Project/Solution discovery (recursive scan + ignore)

**Files:**
- Create: `src/BuildOrchestrator.Core/Discovery/WorkspaceScanner.cs`
- Test: `tests/BuildOrchestrator.Tests/Discovery/WorkspaceScannerTests.cs`

**Interfaces:**
- Produces: `WorkspaceScanner.Scan(string root) → ScanResult(IReadOnlyList<string> CsprojPaths, IReadOnlyList<string> SlnPaths)` — yollar kanonik (`Path.GetFullPath`), `OrdinalIgnoreCase` sıralı, `.git bin obj node_modules .vs` klasörleri atlanır.

- [ ] **Step 1: Failing test** — temp ağacı: ignore klasörleri elenmeli, sonuç sıralı.

```csharp
using BuildOrchestrator.Core.Discovery;

public class WorkspaceScannerTests
{
    [Fact]
    public void Scan_finds_csproj_and_sln_and_skips_ignored_dirs()
    {
        string root = Path.Combine(Path.GetTempPath(), "wsscan-" + Guid.NewGuid().ToString("N"));
        try
        {
            Directory.CreateDirectory(Path.Combine(root, "A"));
            Directory.CreateDirectory(Path.Combine(root, "obj"));      // ignore
            Directory.CreateDirectory(Path.Combine(root, ".git"));     // ignore
            File.WriteAllText(Path.Combine(root, "A", "A.csproj"), "<Project/>");
            File.WriteAllText(Path.Combine(root, "Root.sln"), "");
            File.WriteAllText(Path.Combine(root, "obj", "Ghost.csproj"), "<Project/>"); // elenmiş
            var result = new WorkspaceScanner().Scan(root);
            Assert.Single(result.CsprojPaths);
            Assert.EndsWith("A.csproj", result.CsprojPaths[0]);
            Assert.Single(result.SlnPaths);
        }
        finally { Directory.Delete(root, recursive: true); }
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~WorkspaceScanner"`

- [ ] **Step 3: Implement**

```csharp
namespace BuildOrchestrator.Core.Discovery;

public sealed record ScanResult(IReadOnlyList<string> CsprojPaths, IReadOnlyList<string> SlnPaths);

public sealed class WorkspaceScanner
{
    private static readonly HashSet<string> Ignored = new(StringComparer.OrdinalIgnoreCase)
        { ".git", "bin", "obj", "node_modules", ".vs" }; // [Global Constraints scan ignore]

    public ScanResult Scan(string root)
    {
        var csproj = new List<string>();
        var sln = new List<string>();
        Walk(root, csproj, sln);
        csproj.Sort(StringComparer.OrdinalIgnoreCase); // determinizm [D8]
        sln.Sort(StringComparer.OrdinalIgnoreCase);
        return new ScanResult(csproj, sln);
    }

    private static void Walk(string dir, List<string> csproj, List<string> sln)
    {
        foreach (var file in Directory.EnumerateFiles(dir))
        {
            if (file.EndsWith(".csproj", StringComparison.OrdinalIgnoreCase)) csproj.Add(Path.GetFullPath(file));
            else if (file.EndsWith(".sln", StringComparison.OrdinalIgnoreCase)) sln.Add(Path.GetFullPath(file));
        }
        foreach (var sub in Directory.EnumerateDirectories(dir))
        {
            if (Ignored.Contains(Path.GetFileName(sub))) continue;
            Walk(sub, csproj, sln);
        }
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~WorkspaceScanner"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Discovery/WorkspaceScanner.cs tests/BuildOrchestrator.Tests/Discovery/WorkspaceScannerTests.cs
git commit -m "feat(core): workspace scanner with ignore-list and deterministic ordering"
```

---

### Task 8: Csproj XML evaluator (AssemblyName, Compile, HintPath, ProjectReference)

**Files:**
- Create: `src/BuildOrchestrator.Core/Discovery/CsprojEvaluator.cs`
- Test: `tests/BuildOrchestrator.Tests/Discovery/CsprojEvaluatorTests.cs`

**Interfaces:**
- Consumes: `HintPathRef` yok (ham çıkarır); `System.Xml.Linq`.
- Produces:

```csharp
public sealed record EvaluatedProject(
    string Path,                            // kanonik
    string AssemblyName,                    // <AssemblyName> ya da dosya adı (ext'siz)
    IReadOnlyList<string> CompileFiles,     // kanonik, glob+link çözülmüş
    IReadOnlyList<RawHintPath> HintPaths,   // ham + basename (lower)
    IReadOnlyList<string> ProjectReferences,// kanonik csproj yolları
    bool IsSdkStyle);

public sealed record RawHintPath(string Raw, string BaseName);

public sealed class CsprojEvaluator { public EvaluatedProject Evaluate(string csprojPath); }
```

**Kurallar:** legacy = `Sdk` attribute YOK → Compile explicit `<Compile Include>` (wildcard varsa diskle genişlet, `..\` link'ler Include yoluyla çözülür); SDK-style = `Sdk` attribute VAR → default glob `**/*.cs` (obj/bin hariç) + explicit `<Compile Include>` birleşimi (Remove kapsam dışı — YAGNI, warn'lanmaz). AssemblyName yoksa `Path.GetFileNameWithoutExtension`. HintPath yalnız `<Reference><HintPath>` olanlar (GAC ref'leri kenar değil). MSBuild namespace'i toleranslı (legacy `xmlns` var, SDK yok) — `LocalName` ile eşle.

- [ ] **Step 1: Failing test** — legacy proje: explicit Compile + HintPath basename + ProjectReference + default AssemblyName.

```csharp
using BuildOrchestrator.Core.Discovery;

public class CsprojEvaluatorTests
{
    private static string WriteProj(string dir, string name, string body)
    {
        Directory.CreateDirectory(dir);
        string p = Path.Combine(dir, name);
        File.WriteAllText(p, body);
        return p;
    }

    [Fact]
    public void Evaluate_legacy_project_extracts_items()
    {
        string root = Path.Combine(Path.GetTempPath(), "eval-" + Guid.NewGuid().ToString("N"));
        try
        {
            string dir = Path.Combine(root, "A");
            File.WriteAllText(Path.Combine(root, "dummy.txt"), ""); // ensure root
            string proj = WriteProj(dir, "A.csproj", """
                <Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
                  <PropertyGroup><AssemblyName>OSYS.A</AssemblyName></PropertyGroup>
                  <ItemGroup>
                    <Compile Include="Foo.cs" />
                    <Reference Include="OSYS.B"><HintPath>..\B\bin\OSYS.B.dll</HintPath></Reference>
                    <Reference Include="System.Xml" />
                    <ProjectReference Include="..\C\C.csproj" />
                  </ItemGroup>
                </Project>
                """);
            var ev = new CsprojEvaluator().Evaluate(proj);
            Assert.Equal("OSYS.A", ev.AssemblyName);
            Assert.False(ev.IsSdkStyle);
            Assert.Contains(ev.CompileFiles, f => f.EndsWith("Foo.cs", StringComparison.OrdinalIgnoreCase));
            Assert.Single(ev.HintPaths);                       // System.Xml (HintPath yok) elendi
            Assert.Equal("osys.b.dll", ev.HintPaths[0].BaseName);
            Assert.Single(ev.ProjectReferences);
            Assert.EndsWith("C.csproj", ev.ProjectReferences[0]);
        }
        finally { Directory.Delete(root, recursive: true); }
    }

    [Fact]
    public void Evaluate_sdk_style_defaults_assemblyname_and_globs_cs()
    {
        string root = Path.Combine(Path.GetTempPath(), "eval-" + Guid.NewGuid().ToString("N"));
        try
        {
            string dir = Path.Combine(root, "S");
            File.WriteAllText(Path.Combine(dir + "_x"), ""); // no-op guard
            Directory.CreateDirectory(dir);
            File.WriteAllText(Path.Combine(dir, "S.csproj"),
                "<Project Sdk=\"Microsoft.NET.Sdk\"><PropertyGroup><TargetFramework>net10.0</TargetFramework></PropertyGroup></Project>");
            File.WriteAllText(Path.Combine(dir, "Bar.cs"), "class Bar{}");
            Directory.CreateDirectory(Path.Combine(dir, "obj"));
            File.WriteAllText(Path.Combine(dir, "obj", "Skip.cs"), "class Skip{}"); // obj → glob dışı
            var ev = new CsprojEvaluator().Evaluate(Path.Combine(dir, "S.csproj"));
            Assert.True(ev.IsSdkStyle);
            Assert.Equal("S", ev.AssemblyName);                // default = dosya adı
            Assert.Contains(ev.CompileFiles, f => f.EndsWith("Bar.cs", StringComparison.OrdinalIgnoreCase));
            Assert.DoesNotContain(ev.CompileFiles, f => f.EndsWith("Skip.cs", StringComparison.OrdinalIgnoreCase));
        }
        finally { Directory.Delete(root, recursive: true); }
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~CsprojEvaluator"`

- [ ] **Step 3: Implement**

```csharp
using System.Xml.Linq;

namespace BuildOrchestrator.Core.Discovery;

public sealed record RawHintPath(string Raw, string BaseName);

public sealed record EvaluatedProject(
    string Path, string AssemblyName, IReadOnlyList<string> CompileFiles,
    IReadOnlyList<RawHintPath> HintPaths, IReadOnlyList<string> ProjectReferences, bool IsSdkStyle);

public sealed class CsprojEvaluator
{
    private static readonly EnumerationOptions Recurse = new() { RecurseSubdirectories = true };
    private static readonly HashSet<string> SkipDirs = new(StringComparer.OrdinalIgnoreCase) { "obj", "bin" };

    public EvaluatedProject Evaluate(string csprojPath)
    {
        csprojPath = System.IO.Path.GetFullPath(csprojPath);
        string dir = System.IO.Path.GetDirectoryName(csprojPath)!;
        var doc = XDocument.Load(csprojPath);
        var root = doc.Root!;
        bool sdk = root.Attribute("Sdk") is not null;

        string asmName = Elements(root, "PropertyGroup").SelectMany(pg => Elements(pg, "AssemblyName"))
            .Select(e => e.Value.Trim()).FirstOrDefault(v => v.Length > 0)
            ?? System.IO.Path.GetFileNameWithoutExtension(csprojPath);

        var compile = new SortedSet<string>(StringComparer.OrdinalIgnoreCase);
        foreach (var inc in Items(root, "Compile").Select(i => i.Attribute("Include")?.Value).Where(v => !string.IsNullOrWhiteSpace(v)))
            foreach (var f in ResolveInclude(dir, inc!)) compile.Add(f);
        if (sdk)
            foreach (var f in Directory.EnumerateFiles(dir, "*.cs", Recurse))
                if (!IsUnderSkipped(dir, f)) compile.Add(System.IO.Path.GetFullPath(f));

        var hints = Items(root, "Reference")
            .Select(r => Elements(r, "HintPath").Select(h => h.Value.Trim()).FirstOrDefault())
            .Where(h => !string.IsNullOrWhiteSpace(h))
            .Select(h => new RawHintPath(h!, System.IO.Path.GetFileName(h!).ToLowerInvariant()))
            .OrderBy(h => h.BaseName, StringComparer.OrdinalIgnoreCase).ToList();

        var projRefs = Items(root, "ProjectReference")
            .Select(p => p.Attribute("Include")?.Value).Where(v => !string.IsNullOrWhiteSpace(v))
            .Select(v => System.IO.Path.GetFullPath(System.IO.Path.Combine(dir, v!)))
            .Distinct(StringComparer.OrdinalIgnoreCase).OrderBy(v => v, StringComparer.OrdinalIgnoreCase).ToList();

        return new EvaluatedProject(csprojPath, asmName, compile.ToList(), hints, projRefs, sdk);
    }

    // MSBuild namespace toleransı: legacy'de xmlns var, SDK'da yok → LocalName ile eşle.
    private static IEnumerable<XElement> Elements(XElement parent, string local) =>
        parent.Elements().Where(e => e.Name.LocalName == local);
    private static IEnumerable<XElement> Items(XElement root, string local) =>
        Elements(root, "ItemGroup").SelectMany(ig => Elements(ig, local));

    private static IEnumerable<string> ResolveInclude(string dir, string include)
    {
        // MSBuild ';' ile çoklu Include ayırır
        foreach (var part in include.Split(';', StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries))
        {
            if (part.Contains('*') || part.Contains('?'))
            {
                string pat = System.IO.Path.GetFileName(part);
                string sub = System.IO.Path.GetDirectoryName(part) ?? "";
                string baseDir = System.IO.Path.GetFullPath(System.IO.Path.Combine(dir, sub));
                if (Directory.Exists(baseDir))
                    foreach (var f in Directory.EnumerateFiles(baseDir, pat,
                        part.Contains("**") ? Recurse : new EnumerationOptions()))
                        yield return System.IO.Path.GetFullPath(f);
            }
            else yield return System.IO.Path.GetFullPath(System.IO.Path.Combine(dir, part));
        }
    }

    private static bool IsUnderSkipped(string projDir, string file)
    {
        string rel = System.IO.Path.GetRelativePath(projDir, file);
        return rel.Split(System.IO.Path.DirectorySeparatorChar, System.IO.Path.AltDirectorySeparatorChar)
                  .Any(seg => SkipDirs.Contains(seg));
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~CsprojEvaluator"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Discovery/CsprojEvaluator.cs tests/BuildOrchestrator.Tests/Discovery/CsprojEvaluatorTests.cs
git commit -m "feat(core): raw-XML csproj evaluator (AssemblyName, Compile, HintPath, ProjectReference)"
```

---

### Task 9: mtime+hash evaluation cache

**Files:**
- Create: `src/BuildOrchestrator.Core/Discovery/EvaluationCache.cs`
- Test: `tests/BuildOrchestrator.Tests/Discovery/EvaluationCacheTests.cs`

**Interfaces:**
- Consumes: `CsprojEvaluator.Evaluate`, `EvaluatedProject`.
- Produces: `EvaluationCache.GetOrEvaluate(string csprojPath, Func<string,EvaluatedProject> evaluate) → EvaluatedProject` — mtime eşit ise cache-hit; değilse hash doğrula, gerekiyorsa yeniden değerlendir. `Flush()` diske atomik yazar (temp+rename). Cache dosyası JSON.

- [ ] **Step 1: Failing test** — dokunulmayan proje cache-hit (evaluate SAYACI artmaz); mtime değişince yeniden değerlendirilir.

```csharp
using BuildOrchestrator.Core.Discovery;

public class EvaluationCacheTests
{
    [Fact]
    public void GetOrEvaluate_returns_cached_when_file_unchanged()
    {
        string root = Path.Combine(Path.GetTempPath(), "evcache-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(root);
        try
        {
            string proj = Path.Combine(root, "A.csproj");
            File.WriteAllText(proj, "<Project/>");
            var cache = new EvaluationCache(Path.Combine(root, "cache.json"));
            int calls = 0;
            EvaluatedProject Fake(string p) { calls++; return new EvaluatedProject(p, "A", [], [], [], false); }
            cache.GetOrEvaluate(proj, Fake);
            cache.GetOrEvaluate(proj, Fake); // aynı mtime → cache-hit, çağırma
            Assert.Equal(1, calls);
        }
        finally { Directory.Delete(root, recursive: true); }
    }

    [Fact]
    public void GetOrEvaluate_reevaluates_when_content_changes()
    {
        string root = Path.Combine(Path.GetTempPath(), "evcache-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(root);
        try
        {
            string proj = Path.Combine(root, "A.csproj");
            File.WriteAllText(proj, "<Project/>");
            var cache = new EvaluationCache(Path.Combine(root, "cache.json"));
            int calls = 0;
            EvaluatedProject Fake(string p) { calls++; return new EvaluatedProject(p, "A", [], [], [], false); }
            cache.GetOrEvaluate(proj, Fake);
            File.SetLastWriteTimeUtc(proj, DateTime.UtcNow.AddSeconds(5)); // mtime değişti
            File.WriteAllText(proj, "<Project><!-- changed --></Project>"); // içerik değişti
            cache.GetOrEvaluate(proj, Fake);
            Assert.Equal(2, calls);
        }
        finally { Directory.Delete(root, recursive: true); }
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~EvaluationCache"`

- [ ] **Step 3: Implement**

```csharp
using System.Security.Cryptography;
using System.Text.Json;

namespace BuildOrchestrator.Core.Discovery;

public sealed class EvaluationCache(string cachePath)
{
    private sealed record Entry(long MtimeTicks, string Hash, EvaluatedProject Project);
    private readonly Dictionary<string, Entry> _entries = Load(cachePath);
    private static readonly JsonSerializerOptions Json = new() { WriteIndented = false };

    public EvaluatedProject GetOrEvaluate(string csprojPath, Func<string, EvaluatedProject> evaluate)
    {
        csprojPath = Path.GetFullPath(csprojPath);
        long mtime = new FileInfo(csprojPath).LastWriteTimeUtc.Ticks;
        if (_entries.TryGetValue(csprojPath, out var e))
        {
            if (e.MtimeTicks == mtime) return e.Project;                 // hızlı yol: mtime eşit
            if (Hash(csprojPath) is var h && h == e.Hash)                // mtime kaydı ama içerik aynı
            { _entries[csprojPath] = e with { MtimeTicks = mtime }; return e.Project; }
        }
        var proj = evaluate(csprojPath);
        _entries[csprojPath] = new Entry(mtime, Hash(csprojPath), proj);
        return proj;
    }

    public void Flush() // atomik temp+rename [D2/D8]
    {
        string tmp = cachePath + ".tmp";
        Directory.CreateDirectory(Path.GetDirectoryName(cachePath)!);
        File.WriteAllText(tmp, JsonSerializer.Serialize(_entries, Json));
        File.Move(tmp, cachePath, overwrite: true);
    }

    private static string Hash(string path) => Convert.ToHexString(SHA256.HashData(File.ReadAllBytes(path)));

    private static Dictionary<string, Entry> Load(string path)
    {
        if (!File.Exists(path)) return new(StringComparer.OrdinalIgnoreCase);
        try
        {
            var d = JsonSerializer.Deserialize<Dictionary<string, Entry>>(File.ReadAllText(path), Json);
            return d is null ? new(StringComparer.OrdinalIgnoreCase) : new(d, StringComparer.OrdinalIgnoreCase);
        }
        catch { return new(StringComparer.OrdinalIgnoreCase); } // bozuk cache → yeniden kur
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~EvaluationCache"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Discovery/EvaluationCache.cs tests/BuildOrchestrator.Tests/Discovery/EvaluationCacheTests.cs
git commit -m "feat(core): mtime+hash evaluation cache with atomic flush"
```

---

# FAZ 3 — Core: Graph

### Task 10: Producer map + graf kenarları (HintPath primer, ProjectReference ikincil)

**Files:**
- Create: `src/BuildOrchestrator.Core/Graph/ProducerMap.cs`
- Create: `src/BuildOrchestrator.Core/Graph/GraphBuilder.cs`
- Test: `tests/BuildOrchestrator.Tests/Graph/GraphBuilderTests.cs`

**Interfaces:**
- Consumes: `EvaluatedProject`, `RawHintPath`.
- Produces:

```csharp
public sealed record ProducerMap(
    IReadOnlyDictionary<string, string> DllToProducer,   // "osys.b.dll" (lower) → projectId
    IReadOnlyList<string> AmbiguousDlls);                // ≥2 üretici olan DLL adları (deterministik: hariç bırakılır)

public static class ProducerMapBuilder { public static ProducerMap Build(IReadOnlyList<EvaluatedProject> projects); }

public sealed record ProjectEdges(string ProjectId, IReadOnlyList<string> Dependencies); // deduped, sıralı

public static class GraphBuilder
{
    // HintPath primer (D11): HintPath basename → producer edge. ProjectReference İKİNCİL: eklenir ama HintPath edge'iyle dedup.
    public static IReadOnlyList<ProjectEdges> BuildEdges(IReadOnlyList<EvaluatedProject> projects, ProducerMap producers);
}
```

**Producer DLL adı:** `AssemblyName + ".dll"` (lower). Ambiguity: aynı DLL adını 2 proje üretiyorsa → `AmbiguousDlls`'e, `DllToProducer`'dan çıkar (deterministik; kenar üretmez). Spike: OSYS'de 0 ambiguity beklenir.

- [ ] **Step 1: Failing test** — HintPath primer edge; ProjectReference ikincil + dedup; self-edge yok.

```csharp
using BuildOrchestrator.Core.Discovery;
using BuildOrchestrator.Core.Graph;

public class GraphBuilderTests
{
    private static EvaluatedProject P(string id, string asm, string[] hints, string[] projRefs) =>
        new(id, asm, [], hints.Select(h => new RawHintPath(h, Path.GetFileName(h).ToLowerInvariant())).ToList(), projRefs, false);

    [Fact]
    public void HintPath_basename_maps_to_producer_edge()
    {
        var a = P("C:\\r\\A.csproj", "OSYS.A", ["..\\B\\OSYS.B.dll"], []);
        var b = P("C:\\r\\B.csproj", "OSYS.B", [], []);
        var producers = ProducerMapBuilder.Build([a, b]);
        var edges = GraphBuilder.BuildEdges([a, b], producers).Single(e => e.ProjectId == a.Path);
        Assert.Equal(["C:\\r\\B.csproj"], edges.Dependencies); // HintPath→producer
    }

    [Fact]
    public void ProjectReference_is_secondary_and_deduped_with_hintpath()
    {
        // A hem HintPath hem ProjectReference ile B'ye bağlı → tek edge
        var a = P("C:\\r\\A.csproj", "OSYS.A", ["..\\B\\OSYS.B.dll"], ["C:\\r\\B.csproj"]);
        var b = P("C:\\r\\B.csproj", "OSYS.B", [], []);
        var producers = ProducerMapBuilder.Build([a, b]);
        var edges = GraphBuilder.BuildEdges([a, b], producers).Single(e => e.ProjectId == a.Path);
        Assert.Single(edges.Dependencies);
    }

    [Fact]
    public void ambiguous_producer_is_excluded_deterministically()
    {
        var a = P("C:\\r\\A.csproj", "DUP", [], []);
        var b = P("C:\\r\\B.csproj", "DUP", [], []); // aynı AssemblyName
        var producers = ProducerMapBuilder.Build([a, b]);
        Assert.Contains("dup.dll", producers.AmbiguousDlls);
        Assert.False(producers.DllToProducer.ContainsKey("dup.dll"));
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~GraphBuilder"`

- [ ] **Step 3: Implement**

```csharp
namespace BuildOrchestrator.Core.Graph;

using BuildOrchestrator.Core.Discovery;

public sealed record ProducerMap(IReadOnlyDictionary<string, string> DllToProducer, IReadOnlyList<string> AmbiguousDlls);

public static class ProducerMapBuilder
{
    public static ProducerMap Build(IReadOnlyList<EvaluatedProject> projects)
    {
        var multi = new Dictionary<string, List<string>>(StringComparer.OrdinalIgnoreCase);
        foreach (var p in projects.OrderBy(p => p.Path, StringComparer.OrdinalIgnoreCase))
        {
            string dll = p.AssemblyName.ToLowerInvariant() + ".dll";
            (multi.TryGetValue(dll, out var l) ? l : multi[dll] = new()).Add(p.Path);
        }
        var map = new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase);
        var ambiguous = new List<string>();
        foreach (var (dll, producers) in multi.OrderBy(kv => kv.Key, StringComparer.OrdinalIgnoreCase))
            if (producers.Count == 1) map[dll] = producers[0];
            else ambiguous.Add(dll); // determinizm: belirsizi kenar yapma [D8/D11]
        return new ProducerMap(map, ambiguous);
    }
}

public sealed record ProjectEdges(string ProjectId, IReadOnlyList<string> Dependencies);

public static class GraphBuilder
{
    public static IReadOnlyList<ProjectEdges> BuildEdges(IReadOnlyList<EvaluatedProject> projects, ProducerMap producers)
    {
        var byId = projects.ToDictionary(p => p.Path, StringComparer.OrdinalIgnoreCase);
        var result = new List<ProjectEdges>();
        foreach (var p in projects.OrderBy(p => p.Path, StringComparer.OrdinalIgnoreCase))
        {
            var deps = new SortedSet<string>(StringComparer.OrdinalIgnoreCase);
            foreach (var h in p.HintPaths)                                // PRİMER: HintPath→producer [D11]
                if (producers.DllToProducer.TryGetValue(h.BaseName, out var prod) &&
                    !prod.Equals(p.Path, StringComparison.OrdinalIgnoreCase))
                    deps.Add(prod);
            foreach (var pr in p.ProjectReferences)                       // İKİNCİL: ProjectReference (dedup)
                if (byId.ContainsKey(pr) && !pr.Equals(p.Path, StringComparison.OrdinalIgnoreCase))
                    deps.Add(pr);
            result.Add(new ProjectEdges(p.Path, deps.ToList()));
        }
        return result;
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~GraphBuilder"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Graph/ProducerMap.cs src/BuildOrchestrator.Core/Graph/GraphBuilder.cs tests/BuildOrchestrator.Tests/Graph/GraphBuilderTests.cs
git commit -m "feat(core): producer map + HintPath-primary/ProjectReference-secondary graph edges"
```

---

### Task 11: T71 HintPath 3-sınıf sınıflandırıcı + metrik

**Files:**
- Create: `src/BuildOrchestrator.Core/Graph/HintPathClassifier.cs`
- Test: `tests/BuildOrchestrator.Tests/Graph/HintPathClassifierTests.cs`

**Interfaces:**
- Consumes: `EvaluatedProject`, `ProducerMap`, `HintPathClass`, `HintPathRef`.
- Produces:

```csharp
public sealed record ClassificationReport(
    IReadOnlyList<HintPathRef> Classified,       // her (proje, hintpath) için sınıf
    int EdgeCount, int ThirdPartyCount, int OsysPlatformCount, int UnclassifiedCount,
    double RepoResolveRatio,                      // matched / (matched + unclassified)  [SPIKE S3 metriği]
    IReadOnlyList<string> Warnings);              // sınıflandırılamayan artıklar

public static class HintPathClassifier
{
    public static ClassificationReport Classify(IReadOnlyList<EvaluatedProject> projects, ProducerMap producers);
}
```

**Sınıf kuralları (spike S3):** producer map hit → `Edge`; ham yol `\packages\` içerir VEYA `Program Files` altında → `ExternalThirdParty`; ham yol `\Bin\`/`\bin\` segmenti içerir ve producer YOK (repo-dışı OSYS platform) → `ExternalOsysPlatform`; hiçbiri → `Unclassified` (+warn). `RepoResolveRatio = Edge / (Edge + Unclassified)` (external sınıflar payda-dışı, meşru dış girdi).

- [ ] **Step 1: Failing test** — 4 sınıf + oran.

```csharp
using BuildOrchestrator.Core.Discovery;
using BuildOrchestrator.Core.Graph;
using BuildOrchestrator.Contracts.Model;

public class HintPathClassifierTests
{
    private static EvaluatedProject P(string id, string asm, params string[] hints) =>
        new(id, asm, [], hints.Select(h => new RawHintPath(h, Path.GetFileName(h).ToLowerInvariant())).ToList(), [], false);

    [Fact]
    public void classifies_edge_thirdparty_osysplatform_and_unclassified()
    {
        var a = P("C:\\r\\A.csproj", "OSYS.A",
            "..\\B\\OSYS.B.dll",                                  // Edge (producer var)
            "..\\packages\\Newtonsoft.Json.12\\lib\\Newtonsoft.Json.dll", // ExternalThirdParty
            "C:\\OSYS\\Server\\Bin\\OSYS.Kernel.dll",            // ExternalOsysPlatform (\Bin\, producer yok)
            "..\\weird\\Mystery.dll");                            // Unclassified
        var b = P("C:\\r\\B.csproj", "OSYS.B");
        var producers = ProducerMapBuilder.Build([a, b]);
        var r = HintPathClassifier.Classify([a, b], producers);
        Assert.Equal(1, r.EdgeCount);
        Assert.Equal(1, r.ThirdPartyCount);
        Assert.Equal(1, r.OsysPlatformCount);
        Assert.Equal(1, r.UnclassifiedCount);
        Assert.Equal(0.5, r.RepoResolveRatio, 3); // 1/(1+1)
        Assert.Single(r.Warnings);
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~HintPathClassifier"`

- [ ] **Step 3: Implement**

```csharp
namespace BuildOrchestrator.Core.Graph;

using BuildOrchestrator.Core.Discovery;
using BuildOrchestrator.Contracts.Model;

public sealed record ClassificationReport(
    IReadOnlyList<HintPathRef> Classified, int EdgeCount, int ThirdPartyCount,
    int OsysPlatformCount, int UnclassifiedCount, double RepoResolveRatio, IReadOnlyList<string> Warnings);

public static class HintPathClassifier
{
    public static ClassificationReport Classify(IReadOnlyList<EvaluatedProject> projects, ProducerMap producers)
    {
        var classified = new List<HintPathRef>();
        var warnings = new List<string>();
        int edge = 0, third = 0, plat = 0, unc = 0;
        foreach (var p in projects.OrderBy(p => p.Path, StringComparer.OrdinalIgnoreCase))
            foreach (var h in p.HintPaths)
            {
                HintPathClass cls; string? prod = null;
                if (producers.DllToProducer.TryGetValue(h.BaseName, out prod)) { cls = HintPathClass.Edge; edge++; }
                else if (IsThirdParty(h.Raw)) { cls = HintPathClass.ExternalThirdParty; third++; }
                else if (IsUnderBin(h.Raw)) { cls = HintPathClass.ExternalOsysPlatform; plat++; }
                else { cls = HintPathClass.Unclassified; unc++; warnings.Add($"{p.Path}: sınıflandırılamayan HintPath → {h.Raw}"); }
                classified.Add(new HintPathRef(h.Raw, h.BaseName, cls, prod));
            }
        double ratio = (edge + unc) == 0 ? 1.0 : (double)edge / (edge + unc);
        return new ClassificationReport(classified, edge, third, plat, unc, ratio, warnings);
    }

    private static bool IsThirdParty(string raw)
    {
        string n = raw.Replace('/', '\\');
        return n.Contains("\\packages\\", StringComparison.OrdinalIgnoreCase)
            || n.Contains("Program Files", StringComparison.OrdinalIgnoreCase);
    }

    private static bool IsUnderBin(string raw) =>
        raw.Replace('/', '\\').Contains("\\bin\\", StringComparison.OrdinalIgnoreCase);
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~HintPathClassifier"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Graph/HintPathClassifier.cs tests/BuildOrchestrator.Tests/Graph/HintPathClassifierTests.cs
git commit -m "feat(core): T71 3-class HintPath classifier + repo-resolve metric (S3 fallback)"
```

---

### Task 12: Tarjan SCC (cycle) + Kahn topo (build-order)

**Files:**
- Create: `src/BuildOrchestrator.Core/Graph/TopoSort.cs`
- Test: `tests/BuildOrchestrator.Tests/Graph/TopoSortTests.cs`

**Interfaces:**
- Consumes: `ProjectEdges`.
- Produces:

```csharp
public sealed record TopoResult(
    IReadOnlyList<string> BuildOrder,                    // bağımlılıklar önce; deterministik
    IReadOnlyList<IReadOnlyList<string>> Cycles);        // her biri >1 üyeli SCC, üyeler sıralı

public static class TopoSort { public static TopoResult Compute(IReadOnlyList<ProjectEdges> edges); }
```

**Algoritma:** Tarjan SCC → SCC'leri düğüm kabul eden condensation DAG → Kahn topo (bağ kırma: SCC'nin min projectId'si ordinal). SCC içi üyeler `projectId` ordinal sıralı. `Cycles` = üyesi >1 olan SCC'ler. Dependency edge A→B (A, B'ye bağımlı) → build-order'da B önce.

- [ ] **Step 1: Failing test** — düz DAG sırası + cycle tespiti.

```csharp
using BuildOrchestrator.Core.Graph;

public class TopoSortTests
{
    private static ProjectEdges E(string id, params string[] deps) => new(id, deps);

    [Fact]
    public void linear_chain_orders_dependencies_first()
    {
        // A→B→C (A depends B, B depends C) ⇒ build-order C,B,A
        var r = TopoSort.Compute([E("A", "B"), E("B", "C"), E("C")]);
        Assert.Equal(["C", "B", "A"], r.BuildOrder);
        Assert.Empty(r.Cycles);
    }

    [Fact]
    public void independent_nodes_break_ties_by_id_ordinal()
    {
        var r = TopoSort.Compute([E("B"), E("A"), E("C")]);
        Assert.Equal(["A", "B", "C"], r.BuildOrder); // deterministik
    }

    [Fact]
    public void detects_cycle_as_scc()
    {
        // A→B, B→A ⇒ SCC {A,B}
        var r = TopoSort.Compute([E("A", "B"), E("B", "A")]);
        Assert.Single(r.Cycles);
        Assert.Equal(["A", "B"], r.Cycles[0]);
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~TopoSort"`

- [ ] **Step 3: Implement**

```csharp
namespace BuildOrchestrator.Core.Graph;

public sealed record TopoResult(IReadOnlyList<string> BuildOrder, IReadOnlyList<IReadOnlyList<string>> Cycles);

public static class TopoSort
{
    public static TopoResult Compute(IReadOnlyList<ProjectEdges> edges)
    {
        var adj = edges.ToDictionary(e => e.ProjectId, e => e.Dependencies, StringComparer.OrdinalIgnoreCase);
        var nodes = adj.Keys.OrderBy(k => k, StringComparer.OrdinalIgnoreCase).ToList();

        // --- Tarjan SCC ---
        var index = new Dictionary<string, int>(StringComparer.OrdinalIgnoreCase);
        var low = new Dictionary<string, int>(StringComparer.OrdinalIgnoreCase);
        var onStack = new HashSet<string>(StringComparer.OrdinalIgnoreCase);
        var stack = new Stack<string>();
        int idx = 0;
        var sccs = new List<List<string>>();
        var sccOf = new Dictionary<string, int>(StringComparer.OrdinalIgnoreCase);

        void StrongConnect(string v)
        {
            index[v] = low[v] = idx++; stack.Push(v); onStack.Add(v);
            foreach (var w in Neighbors(v))
            {
                if (!index.ContainsKey(w)) { StrongConnect(w); low[v] = Math.Min(low[v], low[w]); }
                else if (onStack.Contains(w)) low[v] = Math.Min(low[v], index[w]);
            }
            if (low[v] == index[v])
            {
                var comp = new List<string>();
                string w;
                do { w = stack.Pop(); onStack.Remove(w); comp.Add(w); } while (!w.Equals(v, StringComparison.OrdinalIgnoreCase));
                comp.Sort(StringComparer.OrdinalIgnoreCase);
                int id = sccs.Count; sccs.Add(comp);
                foreach (var m in comp) sccOf[m] = id;
            }
        }
        IEnumerable<string> Neighbors(string v) =>
            (adj.TryGetValue(v, out var d) ? d : []).Where(adj.ContainsKey)
            .OrderBy(x => x, StringComparer.OrdinalIgnoreCase);

        foreach (var n in nodes) if (!index.ContainsKey(n)) StrongConnect(n);

        // --- condensation DAG + Kahn (bağ kırma: SCC min id) ---
        int k = sccs.Count;
        var sccDeps = new HashSet<int>[k];
        for (int i = 0; i < k; i++) sccDeps[i] = new();
        foreach (var (v, deps) in adj)
            foreach (var w in deps)
                if (adj.ContainsKey(w) && sccOf[v] != sccOf[w]) sccDeps[sccOf[v]].Add(sccOf[w]);

        var indeg = new int[k];
        for (int i = 0; i < k; i++) foreach (var _ in sccDeps[i]) indeg[i]++; // i, bağımlılıklarına derece verir
        // build-order: bağımlılık önce ⇒ derecesi (bağımlı olduğu SCC sayısı) 0 olanlar önce
        var ready = new SortedSet<int>(Comparer<int>.Create((x, y) =>
            string.Compare(sccs[x][0], sccs[y][0], StringComparison.OrdinalIgnoreCase)));
        var remaining = Enumerable.Range(0, k).ToArray();
        for (int i = 0; i < k; i++) if (indeg[i] == 0) ready.Add(i);
        var dependents = new List<int>[k]; // ters kenar: kim bana bağımlı
        for (int i = 0; i < k; i++) dependents[i] = new();
        for (int i = 0; i < k; i++) foreach (var d in sccDeps[i]) dependents[d].Add(i);

        var order = new List<string>();
        while (ready.Count > 0)
        {
            int cur = ready.Min; ready.Remove(cur);
            order.AddRange(sccs[cur]); // SCC üyeleri (sıralı) ardışık
            foreach (var dep in dependents[cur].OrderBy(x => x))
                if (--indeg[dep] == 0) ready.Add(dep);
        }

        var cycles = sccs.Where(s => s.Count > 1).Select(s => (IReadOnlyList<string>)s).ToList();
        return new TopoResult(order, cycles);
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~TopoSort"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Graph/TopoSort.cs tests/BuildOrchestrator.Tests/Graph/TopoSortTests.cs
git commit -m "feat(core): Tarjan SCC + Kahn topo build-order (deterministic)"
```

---

# FAZ 4 — Core: Solution Mapping, Planning, willBuild, Stale-obj

### Task 13: T32 Solution mapping (csproj → solutionNames[])

**Files:**
- Create: `src/BuildOrchestrator.Core/Discovery/SolutionMapper.cs`
- Test: `tests/BuildOrchestrator.Tests/Discovery/SolutionMapperTests.cs`

**Interfaces:**
- Consumes: `ScanResult.SlnPaths`, csproj kanonik yolları.
- Produces:

```csharp
public static class SolutionMapper
{
    // csprojId → o csproj'u içeren sln adları (dosya adı, ext'siz), sıralı. 0 → boş liste. >1 → çok değerli.
    public static IReadOnlyDictionary<string, IReadOnlyList<string>> Map(
        IReadOnlyList<string> slnPaths, IReadOnlyList<string> csprojPaths);
}
```

**sln parse:** satır formatı `Project("{...}") = "Name", "relative\path.csproj", "{...}"` → 2. tırnaklı alan (yol), sln dizinine göre kanonikleştir, csproj kümesinde eşleşenleri ekle.

- [ ] **Step 1: Failing test** — 0/1/>1 sln eşlemesi.

```csharp
using BuildOrchestrator.Core.Discovery;

public class SolutionMapperTests
{
    [Fact]
    public void maps_csproj_to_containing_solutions()
    {
        string root = Path.Combine(Path.GetTempPath(), "slnmap-" + Guid.NewGuid().ToString("N"));
        try
        {
            Directory.CreateDirectory(Path.Combine(root, "A"));
            Directory.CreateDirectory(Path.Combine(root, "B"));
            string aProj = Path.GetFullPath(Path.Combine(root, "A", "A.csproj"));
            string bProj = Path.GetFullPath(Path.Combine(root, "B", "B.csproj"));
            File.WriteAllText(aProj, "<Project/>"); File.WriteAllText(bProj, "<Project/>");
            string sln1 = Path.Combine(root, "One.sln");
            File.WriteAllText(sln1,
                "Project(\"{X}\") = \"A\", \"A\\A.csproj\", \"{1}\"\n" +
                "Project(\"{X}\") = \"B\", \"B\\B.csproj\", \"{2}\"\n");
            string sln2 = Path.Combine(root, "Two.sln");
            File.WriteAllText(sln2, "Project(\"{X}\") = \"A\", \"A\\A.csproj\", \"{1}\"\n");
            var map = SolutionMapper.Map([sln1, sln2], [aProj, bProj]);
            Assert.Equal(["One", "Two"], map[aProj]); // >1, sıralı
            Assert.Equal(["One"], map[bProj]);          // 1
        }
        finally { Directory.Delete(root, recursive: true); }
    }

    [Fact]
    public void csproj_in_no_solution_maps_to_empty()
    {
        string root = Path.Combine(Path.GetTempPath(), "slnmap-" + Guid.NewGuid().ToString("N"));
        try
        {
            Directory.CreateDirectory(root);
            string orphan = Path.GetFullPath(Path.Combine(root, "Orphan.csproj"));
            File.WriteAllText(orphan, "<Project/>");
            var map = SolutionMapper.Map([], [orphan]);
            Assert.Empty(map[orphan]);
        }
        finally { Directory.Delete(root, recursive: true); }
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~SolutionMapper"`

- [ ] **Step 3: Implement**

```csharp
using System.Text.RegularExpressions;

namespace BuildOrchestrator.Core.Discovery;

public static partial class SolutionMapper
{
    [GeneratedRegex("""Project\("\{[^}]*\}"\)\s*=\s*"[^"]*",\s*"([^"]+\.csproj)"""", RegexOptions.IgnoreCase)]
    private static partial Regex ProjectLine();

    public static IReadOnlyDictionary<string, IReadOnlyList<string>> Map(
        IReadOnlyList<string> slnPaths, IReadOnlyList<string> csprojPaths)
    {
        var csprojSet = new HashSet<string>(csprojPaths.Select(Path.GetFullPath), StringComparer.OrdinalIgnoreCase);
        var acc = csprojSet.ToDictionary(c => c, _ => new SortedSet<string>(StringComparer.OrdinalIgnoreCase),
                                         StringComparer.OrdinalIgnoreCase);
        foreach (var sln in slnPaths.OrderBy(s => s, StringComparer.OrdinalIgnoreCase))
        {
            string slnDir = Path.GetDirectoryName(Path.GetFullPath(sln))!;
            string name = Path.GetFileNameWithoutExtension(sln);
            foreach (Match m in ProjectLine().Matches(File.ReadAllText(sln)))
            {
                string rel = m.Groups[1].Value;
                string full = Path.GetFullPath(Path.Combine(slnDir, rel));
                if (acc.TryGetValue(full, out var set)) set.Add(name);
            }
        }
        return acc.ToDictionary(kv => kv.Key, kv => (IReadOnlyList<string>)kv.Value.ToList(), StringComparer.OrdinalIgnoreCase);
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~SolutionMapper"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Discovery/SolutionMapper.cs tests/BuildOrchestrator.Tests/Discovery/SolutionMapperTests.cs
git commit -m "feat(core): T32 solution mapping (csproj -> solutionNames, 0/>1 handling)"
```

---

### Task 14: T26 BuildPlanBuilder (pipeline → BuildPlan)

**Files:**
- Create: `src/BuildOrchestrator.Core/Planning/BuildPlanBuilder.cs`
- Test: `tests/BuildOrchestrator.Tests/Planning/BuildPlanBuilderTests.cs`

**Interfaces:**
- Consumes: `WorkspaceScanner`, `CsprojEvaluator`, `EvaluationCache`, `ProducerMapBuilder`, `GraphBuilder`, `SolutionMapper`, `TopoSort`, `BuildPlan`, `ProjectNode`.
- Produces:

```csharp
public sealed class BuildPlanBuilder(WorkspaceScanner scanner, CsprojEvaluator evaluator, EvaluationCache cache)
{
    // Tam pipeline: scan → (cache'li) evaluate → producer map → edges → solution map → topo → BuildPlan (build-order'da).
    public BuildPlan Build(string root, string configuration);
}
```

**Node üretimi:** her proje için `ProjectNode` — `Id=Name=path`, `Name=AssemblyName`, `SolutionNames` (T32), `Dependencies` (graf), `BuildOrder` (topo index), `LayerIndex/LayerName=null` (layer pattern editörü It-4; It-1'de pattern yok → tek liste), `InCycle` (SCC üyeliği), `WillBuild=null` (Task 15 doldurur). `Nodes` build-order sırasında.

- [ ] **Step 1: Failing test** — küçük gerçek temp repo: node'lar build-order'da, dependency + solution + cycle doğru.

```csharp
using BuildOrchestrator.Core.Discovery;
using BuildOrchestrator.Core.Planning;

public class BuildPlanBuilderTests
{
    [Fact]
    public void builds_plan_in_build_order_with_edges_and_solutions()
    {
        string root = Path.Combine(Path.GetTempPath(), "plan-" + Guid.NewGuid().ToString("N"));
        try
        {
            // B (yaprak) ← A (HintPath ile B'ye bağımlı). Build-order: B, A.
            Directory.CreateDirectory(Path.Combine(root, "A"));
            Directory.CreateDirectory(Path.Combine(root, "B"));
            File.WriteAllText(Path.Combine(root, "B", "B.csproj"),
                "<Project xmlns=\"http://schemas.microsoft.com/developer/msbuild/2003\"><PropertyGroup><AssemblyName>OSYS.B</AssemblyName></PropertyGroup></Project>");
            File.WriteAllText(Path.Combine(root, "A", "A.csproj"),
                "<Project xmlns=\"http://schemas.microsoft.com/developer/msbuild/2003\"><PropertyGroup><AssemblyName>OSYS.A</AssemblyName></PropertyGroup>" +
                "<ItemGroup><Reference Include=\"OSYS.B\"><HintPath>..\\B\\bin\\OSYS.B.dll</HintPath></Reference></ItemGroup></Project>");
            File.WriteAllText(Path.Combine(root, "Root.sln"),
                "Project(\"{X}\") = \"A\", \"A\\A.csproj\", \"{1}\"\nProject(\"{X}\") = \"B\", \"B\\B.csproj\", \"{2}\"\n");

            var builder = new BuildPlanBuilder(new WorkspaceScanner(), new CsprojEvaluator(),
                new EvaluationCache(Path.Combine(root, "cache.json")));
            var plan = builder.Build(root, "Debug");

            Assert.Equal("Debug", plan.Configuration);
            Assert.Equal(2, plan.Nodes.Count);
            Assert.Equal("OSYS.B", plan.Nodes[0].Name);   // build-order: bağımlılık önce
            Assert.Equal("OSYS.A", plan.Nodes[1].Name);
            var a = plan.Nodes[1];
            Assert.Single(a.Dependencies);
            Assert.EndsWith("B.csproj", a.Dependencies[0]);
            Assert.Equal(["Root"], a.SolutionNames);
            Assert.Empty(plan.Cycles);
            Assert.All(plan.Nodes, n => Assert.Null(n.WillBuild)); // Task 15 dolduracak
        }
        finally { Directory.Delete(root, recursive: true); }
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~BuildPlanBuilder"`

- [ ] **Step 3: Implement**

```csharp
namespace BuildOrchestrator.Core.Planning;

using BuildOrchestrator.Core.Discovery;
using BuildOrchestrator.Core.Graph;
using BuildOrchestrator.Contracts.Model;

public sealed class BuildPlanBuilder(WorkspaceScanner scanner, CsprojEvaluator evaluator, EvaluationCache cache)
{
    public BuildPlan Build(string root, string configuration)
    {
        var scan = scanner.Scan(root);
        var evaluated = scan.CsprojPaths.Select(p => cache.GetOrEvaluate(p, evaluator.Evaluate)).ToList();
        cache.Flush();

        var producers = ProducerMapBuilder.Build(evaluated);
        var edges = GraphBuilder.BuildEdges(evaluated, producers);
        var solutions = SolutionMapper.Map(scan.SlnPaths, scan.CsprojPaths);
        var topo = TopoSort.Compute(edges);

        var edgeById = edges.ToDictionary(e => e.ProjectId, e => e.Dependencies, StringComparer.OrdinalIgnoreCase);
        var nameById = evaluated.ToDictionary(e => e.Path, e => e.AssemblyName, StringComparer.OrdinalIgnoreCase);
        var inCycle = new HashSet<string>(topo.Cycles.SelectMany(c => c), StringComparer.OrdinalIgnoreCase);

        var nodes = new List<ProjectNode>();
        for (int i = 0; i < topo.BuildOrder.Count; i++)
        {
            string id = topo.BuildOrder[i];
            nodes.Add(new ProjectNode(
                Id: id, Name: nameById.GetValueOrDefault(id, Path.GetFileNameWithoutExtension(id)), ProjectPath: id,
                SolutionNames: solutions.GetValueOrDefault(id, []),
                Dependencies: edgeById.GetValueOrDefault(id, []),
                BuildOrder: i, LayerIndex: null, LayerName: null,
                InCycle: inCycle.Contains(id), WillBuild: null));
        }
        return new BuildPlan(nodes, topo.Cycles, configuration);
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~BuildPlanBuilder"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Planning/BuildPlanBuilder.cs tests/BuildOrchestrator.Tests/Planning/BuildPlanBuilderTests.cs
git commit -m "feat(core): T26 BuildPlanBuilder pipeline (scan->eval->graph->solution->topo->BuildPlan)"
```

---

### Task 15: T53 (Core) pre-run willBuild kümesi

**Files:**
- Create: `src/BuildOrchestrator.Core/Planning/WillBuildEvaluator.cs`
- Create: `src/BuildOrchestrator.Core/Planning/BuildPreview.cs`
- Test: `tests/BuildOrchestrator.Tests/Planning/WillBuildTests.cs`

**Interfaces:**
- Consumes: `BuildPlan`, `ProjectNode`, `BuildState`, `BuildResult`.
- Produces:

```csharp
public static class WillBuildEvaluator
{
    // dirty=true, güncel=false, imza-yok/pre-Sync=null; inCycle → false. [T53 · v7Δ-8]
    public static bool? Evaluate(bool inCycle, string? currentSignature, BuildState? state);
}

public static class BuildPreview
{
    // Her node için willBuild türet; WillBuild alanı dolu YENİ BuildPlan döner.
    public static BuildPlan ComputeWillBuild(BuildPlan plan,
        Func<ProjectNode, string?> currentSignature, Func<string, BuildState?> stateLookup);
}
```

**Karar tablosu (A6 + v7Δ-8):**
- `inCycle` → `false` (cycle projesi derlenmez, rozet taşır).
- `currentSignature == null` → `null` (hollow: imza hesaplanamadı / Sync öncesi).
- `state == null` VEYA `state.BuiltSignature == null` VEYA `state.LastResult != Succeeded` → `true` (hiç başarıyla derlenmemiş).
- `currentSignature != state.BuiltSignature` → `true` (dirty); eşit → `false` (güncel/clean).

> It-1'de imza motoru (BuildSignature, T25) yok; `currentSignature` bir provider ile enjekte edilir (It-3 gerçek imzayı bağlar). Bu task saf karar mantığını test eder.

- [ ] **Step 1: Failing test** — dört dal + cycle + succeeded→clean.

```csharp
using BuildOrchestrator.Core.Planning;
using BuildOrchestrator.Contracts.Model;

public class WillBuildTests
{
    [Fact]
    public void hollow_when_signature_null()
        => Assert.Null(WillBuildEvaluator.Evaluate(false, null, null));

    [Fact]
    public void true_when_never_built()
        => Assert.True(WillBuildEvaluator.Evaluate(false, "sig1", null));

    [Fact]
    public void true_when_dirty()
        => Assert.True(WillBuildEvaluator.Evaluate(false,
            "sig2", new BuildState("A", BuiltSignature: "sig1", LastResult: BuildResult.Succeeded)));

    [Fact]
    public void false_when_up_to_date_after_success() // succeeded → clean
        => Assert.False(WillBuildEvaluator.Evaluate(false,
            "sig1", new BuildState("A", BuiltSignature: "sig1", LastResult: BuildResult.Succeeded)));

    [Fact]
    public void false_when_in_cycle_regardless()
        => Assert.False(WillBuildEvaluator.Evaluate(true, "sig1", null));

    [Fact]
    public void true_when_last_result_failed_even_if_signature_matches()
        => Assert.True(WillBuildEvaluator.Evaluate(false,
            "sig1", new BuildState("A", BuiltSignature: "sig1", LastResult: BuildResult.Failed)));

    [Fact]
    public void ComputeWillBuild_populates_node_field()
    {
        var node = new ProjectNode("A", "A", "A", [], [], 0, null, null, InCycle: false, WillBuild: null);
        var plan = new BuildPlan([node], [], "Debug");
        var result = BuildPreview.ComputeWillBuild(plan, _ => "sig1", _ => null); // never built
        Assert.True(result.Nodes[0].WillBuild);
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~WillBuild"`

- [ ] **Step 3: Implement**

```csharp
namespace BuildOrchestrator.Core.Planning;

using BuildOrchestrator.Contracts.Model;

public static class WillBuildEvaluator
{
    public static bool? Evaluate(bool inCycle, string? currentSignature, BuildState? state)
    {
        if (inCycle) return false;                              // cycle → derlenmez + rozet [A6]
        if (currentSignature is null) return null;              // hollow: imza-yok / pre-Sync [v7Δ-8]
        if (state?.BuiltSignature is null) return true;         // hiç derlenmemiş
        if (state.LastResult != BuildResult.Succeeded) return true; // son koşu başarısız/skip
        return !string.Equals(currentSignature, state.BuiltSignature, StringComparison.Ordinal); // dirty=true, güncel=false
    }
}

public static class BuildPreview
{
    public static BuildPlan ComputeWillBuild(BuildPlan plan,
        Func<ProjectNode, string?> currentSignature, Func<string, BuildState?> stateLookup)
    {
        var nodes = plan.Nodes.Select(n => n with
        {
            WillBuild = WillBuildEvaluator.Evaluate(n.InCycle, currentSignature(n), stateLookup(n.Id))
        }).ToList();
        return plan with { Nodes = nodes };
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~WillBuild"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Planning/WillBuildEvaluator.cs src/BuildOrchestrator.Core/Planning/BuildPreview.cs tests/BuildOrchestrator.Tests/Planning/WillBuildTests.cs
git commit -m "feat(core): T53 pre-run willBuild evaluator (dirty/clean/hollow + cycle)"
```

---

### Task 16: T72 bayat (stale) obj tanı/warn

**Files:**
- Create: `src/BuildOrchestrator.Core/Discovery/StaleObjDetector.cs`
- Test: `tests/BuildOrchestrator.Tests/Discovery/StaleObjDetectorTests.cs`

**Sorun (spike S2, OSYS.Types.NewSales.Print):** in-place build öncesi default `obj`'de yabancı-TFM artığı (`project.assets.json` hedef framework uyuşmazlığı, `*.nuget.g.props/targets` kalıntısı) build'i kırar. **Dokunmadan** warn.

**Interfaces:**
- Produces:

```csharp
public sealed record StaleObjDiagnosis(string ProjectId, bool IsStale, string? Reason);

public static class StaleObjDetector
{
    // in-place default obj (proj\obj) incele; expectedTfm ile project.assets.json'daki tfm'i karşılaştır.
    // DOKUNMAZ — yalnız teşhis. obj yoksa IsStale=false.
    public static StaleObjDiagnosis Inspect(string csprojPath, string expectedTfm);
}
```

**Kural:** `obj/project.assets.json` varsa ve içinde `expectedTfm` GEÇMİYOR ama başka bir `netstandard`/`net`-TFM geçiyorsa → stale (Reason = bulunan yabancı TFM). Basit, dosya-tabanlı; JSON substring kontrolü yeterli (tam parse YAGNI).

- [ ] **Step 1: Failing test** — yabancı TFM → stale; eşleşen → temiz; obj yok → temiz.

```csharp
using BuildOrchestrator.Core.Discovery;

public class StaleObjDetectorTests
{
    [Fact]
    public void flags_stale_when_foreign_tfm_in_assets()
    {
        string root = Path.Combine(Path.GetTempPath(), "stale-" + Guid.NewGuid().ToString("N"));
        try
        {
            string dir = Path.Combine(root, "P"); Directory.CreateDirectory(Path.Combine(dir, "obj"));
            string proj = Path.Combine(dir, "P.csproj"); File.WriteAllText(proj, "<Project/>");
            File.WriteAllText(Path.Combine(dir, "obj", "project.assets.json"),
                "{ \"targets\": { \".NETStandard,Version=v2.0\": {} } }"); // yabancı
            var d = StaleObjDetector.Inspect(proj, "net46");
            Assert.True(d.IsStale);
            Assert.Contains("netstandard", d.Reason!, StringComparison.OrdinalIgnoreCase);
        }
        finally { Directory.Delete(root, recursive: true); }
    }

    [Fact]
    public void clean_when_no_obj()
    {
        string root = Path.Combine(Path.GetTempPath(), "stale-" + Guid.NewGuid().ToString("N"));
        try
        {
            string dir = Path.Combine(root, "P"); Directory.CreateDirectory(dir);
            string proj = Path.Combine(dir, "P.csproj"); File.WriteAllText(proj, "<Project/>");
            Assert.False(StaleObjDetector.Inspect(proj, "net46").IsStale);
        }
        finally { Directory.Delete(root, recursive: true); }
    }
}
```

- [ ] **Step 2: Run — FAIL.** Run: `dotnet test ... --filter "FullyQualifiedName~StaleObjDetector"`

- [ ] **Step 3: Implement**

```csharp
using System.Text.RegularExpressions;

namespace BuildOrchestrator.Core.Discovery;

public sealed record StaleObjDiagnosis(string ProjectId, bool IsStale, string? Reason);

public static partial class StaleObjDetector
{
    [GeneratedRegex(@"\.NETStandard,Version=v[\d.]+|netstandard[\d.]+", RegexOptions.IgnoreCase)]
    private static partial Regex ForeignTfm();

    public static StaleObjDiagnosis Inspect(string csprojPath, string expectedTfm)
    {
        csprojPath = Path.GetFullPath(csprojPath);
        string assets = Path.Combine(Path.GetDirectoryName(csprojPath)!, "obj", "project.assets.json");
        if (!File.Exists(assets)) return new StaleObjDiagnosis(csprojPath, false, null);
        string text = File.ReadAllText(assets);
        if (text.Contains(expectedTfm, StringComparison.OrdinalIgnoreCase))
            return new StaleObjDiagnosis(csprojPath, false, null);
        var m = ForeignTfm().Match(text);
        return m.Success
            ? new StaleObjDiagnosis(csprojPath, true, $"obj/project.assets.json yabancı TFM içeriyor: {m.Value} (beklenen {expectedTfm}) — dokunulmadı, build kırılabilir")
            : new StaleObjDiagnosis(csprojPath, false, null);
    }
}
```

- [ ] **Step 4: Run — PASS.** Run: `dotnet test ... --filter "FullyQualifiedName~StaleObjDetector"`

- [ ] **Step 5: Commit**

```bash
git add src/BuildOrchestrator.Core/Discovery/StaleObjDetector.cs tests/BuildOrchestrator.Tests/Discovery/StaleObjDetectorTests.cs
git commit -m "feat(core): T72 stale-obj detector (foreign-TFM warn, no touch)"
```

---

# FAZ 5 — Entegrasyon & Kabul

### Task 17: Gerçek OSYS entegrasyon kontrolü (kabul kanıtı)

**Files:**
- Create: `tests/BuildOrchestrator.Tests/Integration/OsysGraphIntegrationTests.cs`

**Amaç:** It-1 acceptance'ı gerçek repoda kanıtla — 177 csproj taranır; sınıflandırma spike'ın 3-sınıf modeline uyar (repo-içi kenar %100 çözülür, sınıflandırılamayan artığa warn); cache-hit ikinci koşuda yeniden değerlendirmez. OSYS yoksa `SkippableFact` ile atla.

**Interfaces:**
- Consumes: `BuildPlanBuilder`, `HintPathClassifier`, `WorkspaceScanner`, `CsprojEvaluator`, `EvaluationCache`, `ProducerMapBuilder`.

- [ ] **Step 1: Test yaz** (gerçek OSYS; skip-if-absent).

```csharp
using BuildOrchestrator.Core.Discovery;
using BuildOrchestrator.Core.Graph;
using BuildOrchestrator.Core.Planning;
using Xunit;

namespace BuildOrchestrator.Tests.Integration;

[Trait("Category", "Integration")]
public class OsysGraphIntegrationTests
{
    private const string OsysRoot = @"D:\Projects\Delta\OSYS";

    [SkippableFact]
    public void osys_scan_classifies_per_spike_3class_model_and_cache_hits()
    {
        Skip.IfNot(Directory.Exists(OsysRoot), "OSYS yok — entegrasyon atlandı.");
        string cachePath = Path.Combine(Path.GetTempPath(), "osys-it1-cache-" + Guid.NewGuid().ToString("N") + ".json");
        try
        {
            var scanner = new WorkspaceScanner();
            var scan = scanner.Scan(OsysRoot);
            Assert.InRange(scan.CsprojPaths.Count, 150, 260); // spike yer-gerçeği ~177

            var evaluator = new CsprojEvaluator();
            var evaluated = scan.CsprojPaths.Select(evaluator.Evaluate).ToList();
            var producers = ProducerMapBuilder.Build(evaluated);
            Assert.Empty(producers.AmbiguousDlls);              // spike: 0 ambiguity

            var report = HintPathClassifier.Classify(evaluated, producers);
            // repo-içi çözülme = Edge/(Edge+Unclassified) — spike 3-sınıf modeli: sınıflandırılamayan artık küçük olmalı
            Assert.True(report.RepoResolveRatio >= 0.95,
                $"repo-resolve {report.RepoResolveRatio:P1} < %95 — sınıflandırılamayan: {report.UnclassifiedCount}");

            // cache-hit: ikinci koşu yeniden değerlendirmemeli
            var cache = new EvaluationCache(cachePath);
            int calls = 0;
            EvaluatedProject Counting(string p) { calls++; return evaluator.Evaluate(p); }
            foreach (var p in scan.CsprojPaths) cache.GetOrEvaluate(p, Counting);
            int afterCold = calls;
            foreach (var p in scan.CsprojPaths) cache.GetOrEvaluate(p, Counting);
            Assert.Equal(afterCold, calls);                     // warm koşu: 0 yeni eval [cache-hit hızlı]

            // build-order + cycle rozeti verisi mevcut
            var plan = new BuildPlanBuilder(scanner, evaluator, new EvaluationCache(cachePath + ".2"))
                .Build(OsysRoot, "Debug");
            Assert.Equal(evaluated.Count, plan.Nodes.Count);
            Assert.True(plan.Nodes.Select(n => n.BuildOrder).SequenceEqual(Enumerable.Range(0, plan.Nodes.Count)));
        }
        finally { if (File.Exists(cachePath)) File.Delete(cachePath); }
    }
}
```

- [ ] **Step 2: Run — gözlemle + raporla.**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "Category=Integration" --logger "console;verbosity=detailed"`

**Beklenen:** OSYS varsa PASS. Eğer `RepoResolveRatio < %95` çıkarsa: bu bir spike-sapması SİNYALİDİR — `report.Warnings`'i incele, sınıf kurallarını (Task 11 `IsThirdParty`/`IsUnderBin`) gerçek unmatched yollarına göre daralt/genişlet (ör. `\lib\`, DevExpress/Infragistics kök yolları). Kuralı değiştirdiysen Task 11 unit testine yeni kenar-vaka ekle, sonra bu testi tekrar koştur. **Metriği (Edge/ThirdParty/OsysPlatform/Unclassified sayıları + oran) summary'ye kaydet.**

- [ ] **Step 3: Commit**

```bash
git add tests/BuildOrchestrator.Tests/Integration/OsysGraphIntegrationTests.cs
git commit -m "test(integration): OSYS scan/classify per spike 3-class model + cache-hit proof"
```

---

## İterasyon Kabul Kanıtı (It-1 acceptance — her madde bir task'a bağlı)

| Acceptance maddesi (Part C It-1) | Kanıt |
|---|---|
| Kartlar build-order'da | Task 12 (topo) + Task 14 (`plan.Nodes` build-order'da, `BuildOrder` = index) |
| Cycle rozeti verisi | Task 12 (`Cycles` SCC) + Task 14 (`ProjectNode.InCycle`) |
| will-build kümesi testli | Task 15 (`WillBuildEvaluator` 4 dal + cycle + `ComputeWillBuild`) |
| Gerçek OSYS Sync cache-hit'te hızlı | Task 9 (cache) + Task 17 (warm koşu 0 yeni eval) |
| Sınıflandırma metriği (matched/(matched+sınıflandırılamayan)) raporlanır | Task 11 (`RepoResolveRatio`) + Task 17 (assert + summary) |
| sınıflandırılamayan → warn | Task 11 (`Warnings`) |
| bayat obj → dokunmadan warn | Task 16 (`StaleObjDetector`, no touch) |
| It-0 devir sertleştirmeleri | Task 1–5 |

## Devir sertleştirme izleme

| it0-records giriş kalemi | Task |
|---|---|
| EngineHost: `_generation` volatile/Interlocked | Task 3 |
| EngineHost: ReadLoop catch loop içine + framing→kill+EngineExited (exit-2 simetri) | Task 3 |
| EngineHost: startup çift-sinyal | Task 3 |
| EngineHost: StartAsync-timeout child temizliği | Task 3 |
| EngineHost: graceful ShutdownCommand + generation | Task 3 |
| App copy-target hardcoded TFM + stale | Task 4 |
| NdjsonWriter base-type kısıtı | Task 1 |
| ProcessRunner kill-path sertleştirme | Task 2 |
| CascadeKillTests handles.Count>=5 | Task 5 |
| T71 3-sınıf sınıflandırıcı | Task 11 |
| T72 bayat obj tanı/warn | Task 16 |

## Global doğrulama (her task sonrası + iterasyon sonu)

```powershell
dotnet build BuildOrchestrator.slnx           # 0 hata, 0 uyarı hedefi
dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj   # tüm yeşil (+ Integration OSYS varsa)
```

## Self-Review (yazım sonrası — writing-plans)

- **Spec coverage:** It-1 tasklarının (T24/T32/T26/T53-Core/T71/T72) + 5 devir sertleştirmesi + A8 willBuild kalemi hepsi bir task'a bağlı (yukarıdaki tablolar). T24 = Task 7+8+9+10+12 (scan/eval/cache/graph/topo). ✓
- **Placeholder taraması:** her code adımı gerçek kod içerir; "TODO/TBD" yok. ✓
- **Type tutarlılığı:** `EvaluatedProject`/`RawHintPath` (Task 8) → Task 9/10/11/14'te aynı imza; `ProducerMap`/`ProjectEdges` (Task 10) → Task 11/12/14; `BuildPlan`/`ProjectNode`/`BuildState`/`HintPathClass` (Task 6) → Task 14/15. `WillBuildEvaluator.Evaluate(bool,string?,BuildState?)` (Task 15) tutarlı. ✓
- **Determinizm:** her sıralama `OrdinalIgnoreCase`; sleep yok. ✓
```
