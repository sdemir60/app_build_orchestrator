# It-2 (Rebuild motoru + per-run log + copy-aware Stop/Continue + konsol) — TDD Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL — Bu planı task-by-task uygulamak için `superpowers:subagent-driven-development` kullan. Adımlar checkbox (`- [ ]`) ile izlenir.

**Goal:** v7 Part C **It-2**'yi teslim et: gerçek `MSBuild.exe` shell-out ile paralel rebuild, per-run disk log, canlı konsol akışı, copy-aware Stop → Continue.

**Architecture:** Bloklayıcı handle-inheritance izolasyonu (`PROC_THREAD_ATTRIBUTE_HANDLE_LIST`) önce kapatılır — paralel redirected launch onsuz deadlock eder. Ardından: saf Core'da deterministik ready-set scheduler + run clock + copy-contention retry; Supervisor'da inner Job içinde `MSBuild.exe` child'ları, per-run disk log ve IPC event akışı; App'te AvalonEdit konsol (Channel → ~50ms batch flush) + minimal proje listesi + Stop/Continue.

**Tech Stack:** .NET 10 · WPF · AvalonEdit 6.3.1.120 · xUnit + Xunit.StaFact · `MSBuild.exe` (vswhere ile resolve, It-0'da hazır) · Win32 Job Object + `STARTUPINFOEX` P/Invoke.

**Branch:** `it2-build-engine` (base = `main` @ `f5cc736`). Her task sonunda WIP commit; main'e merge/push YALNIZ kullanıcı onayıyla.

---

## Global Constraints

Her task'ın örtük gereksinimi; ihlal = plan ihlali. v7 planından **birebir** alındı:

- **Derleme motoru = `MSBuild.exe`** (vswhere ile resolve — `MsBuildResolver` It-0'da hazır), **`dotnet build` DEĞİL**. `[D10]`
- **§3.4 flag'leri (v1) SABİT: `-p:UseSharedCompilation=false -nodeReuse:false`** — kaldırma yalnız T33 (It-5). `[D9/S5]`
- **`-p:BuildProjectReferences=false` ZORUNLU** — bağımlılığı orchestrator ayrı node olarak derler. `[SPIKE S2-b]`
- **packages.config restore per-project `-p:SolutionDir=<sln dizini>` İSTER**; yol = `msbuild -t:restore -p:RestorePackagesConfig=true`; **nuget.exe'ye bağımlılık YOK**. `[SPIKE S2-a/c, S1]`
- **Shell-out per project**; in-process MSBuild (BuildManager) **asla**. `[§0]`
- **Ortak OutDir'e DOKUNULMAZ/OKUNMAZ**; DLL/bin timestamp asla okunmaz. `[§4]`
- **§6.1 garantisi = nested Job Object**: App outer Job → Supervisor → inner Job → `MSBuild.exe` child'ları; `CREATE_SUSPENDED → AssignProcessToJobObject → ResumeThread`. Managed watcher YOK. `[§3]`
- **IPC = stdio NDJSON, framed; stdout YALNIZ NDJSON**, tüm logging stderr/dosyaya. `[D4]`
- **Per-run disk log:** `%LOCALAPPDATA%\BuildOrchestrator\logs\run-<ts>\`; **bellek ring buffer YOK**. `[D4]`
- **Tüm planlama saf Core'da**; Supervisor yalnız yürütür. `[D3]`
- **Scheduler = sıra-koruyan ready-set, İLERİ ATLAMALI** (bağımlılığı bekleyenin üzerinden atlanır); rastgele/hash YOK; aynı graf → aynı dispatch. `[§6 · K2]`
- **Determinizm [D8]: sleep-poll YASAK.** Testler event/handle/enjekte-edilmiş-saat ile beklenir, `Thread.Sleep`/poll döngüsü ile DEĞİL.
- **Tek `ProcessRunner`** (exit-code + stderr + timeout) — yalnız job DIŞI yardımcı process'ler için (vswhere). Job İÇİ child'lar `JobProcessLauncher` ile. `[D7]`
- **Konsol A13.2:** AvalonEdit; IPC background → Channel → ~50ms batch flush → `BeginUpdate`/tek `Insert`/`EndUpdate`. **Satır başına `Dispatcher.Invoke` YASAK.**
- **Sayı/tarih formatlaması `InvariantCulture`.** `[A13.2]`

## It-2 Karar Kaydı (bu plan için, kullanıcı onaylı — 2026-07-17)

| # | Karar | Sonuç |
|---|---|---|
| **I2-K1** | Stop semantiği (T4/2A ↔ T9 gerilimi) | **İki katman.** *Graceful* (varsayılan, copy-aware): yeni dispatch YOK, in-flight `MSBuild.exe` child'ları post-build copy dahil kendi bitişlerine kadar koşar → ortak bin asla yarım; kalanlar `queued`. *Hard*: `TerminateJobObject(inner)` **anında** (kullanıcı escalation'ı / App ölümü kaskadı); in-flight → `Failed(reason: "stopped")`, kalanlar `queued`. **Torn-DLL tanımı:** v1 flag'leri sayesinde HER writer job içindedir (VBCSCompiler yok) → *kill'den sağ çıkan yazıcı YOKTUR*; kill sonrası bin mutasyona uğramaz. T9 bunu kanıtlar. |
| **I2-K2** | obj-izolasyon | **Plan §4 harfiyen:** in-place build → projenin KENDİ obj'si (VS-parity); `BaseIntermediateOutputPath` izolasyonu yalnız worktree yolunda (It-3) devreye girer. It-2 invoke'u `BaseIntermediateOutputPath = null` geçer (arg plumbing `MsBuildArguments.Build`'de zaten hazır). Bayat obj → T72 (It-1'de bitti) dokunmadan warn. |
| **I2-K3** | Kabul kapsamı | **Tam 177 proje, paralel rebuild.** *Yeşil* = **orchestrator kaynaklı hata YOK** (dispatch/handle/obj/copy-contention/IPC). Repo-kaynaklı per-proje hatalar sayısı + nedeniyle `it2-records.md`'ye yazılır, acceptance'ı bloklamaz. Otomatik suite'te küçük fixture testleri; tam koşu `[Trait("Category","Acceptance")]` ile ayrı çalıştırılır. |

## Kapsam dışı (It-2 DEĞİL — dokunma)

Incremental/signature/build-state (T25/T27 — It-3) · git fetch/branch/worktree (T69/T29 — It-3) · depIssue sistemi (T54 — It-3) · Retry failed (T55'in Retry yarısı — It-3) · ETA (T70 — It-3/4) · design-v1 birebirlik, kart görselleri, graf paneli, sticky şerit, typewriter/kaskat, chunk-prepend scroll telafisi, `⌄ latest` pill, trim/tampon (It-4) · perf modları/CPU cap (It-5).

---

## Dosya Haritası

| Dosya | Sorumluluk | Task |
|---|---|---|
| `src/BuildOrchestrator.Core/ProcessControl/NativeMethods.cs` (M) | `STARTUPINFOEXW` + ProcThreadAttributeList P/Invoke'ları | 1 |
| `src/BuildOrchestrator.Core/ProcessControl/ProcThreadAttributeList.cs` (C) | Handle-list attribute buffer ömrü (IDisposable) | 1 |
| `src/BuildOrchestrator.Core/ProcessControl/JobProcessLauncher.cs` (M) | Handle-inheritance izolasyonu ile launch | 1 |
| `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs` (M) | `startRun`/`continueRun` komutları + run event'leri | 2 |
| `src/BuildOrchestrator.Core/Discovery/SolutionMapper.cs` (M) | `MapRefs` → `SolutionRef` (ad + yol) | 3 |
| `src/BuildOrchestrator.Core/MsBuild/SolutionDirResolver.cs` (C) | restore'un `-p:SolutionDir` girdisi (deterministik seçim) | 3 |
| `src/BuildOrchestrator.Core/Logs/RunLogPaths.cs` (C) | `run-<ts>` dizin adı + `%LOCALAPPDATA%` kökü | 4 |
| `src/BuildOrchestrator.Core/Logs/RunLogWriter.cs` (C) | per-run disk log: proje logları + decision log + snapshot | 4 |
| `src/BuildOrchestrator.Core/MsBuild/MsBuildOutputEncoding.cs` (C) | MSBuild.exe redirected çıktısının encoding'i (ANSI CP) | 5 |
| `src/BuildOrchestrator.Core/MsBuild/MsBuildInvoker.cs` (C) | T22 invoke: restore+build, inner Job'da, satır callback'i | 5 |
| `src/BuildOrchestrator.Core/Scheduling/ReadySetScheduler.cs` (C) | K2 ready-set, ileri atlamalı, deterministik; stop→queued | 6 |
| `src/BuildOrchestrator.Core/Scheduling/RunClock.cs` (C) | segment'li elapsed (Continue'da korunur) | 7 |
| `src/BuildOrchestrator.Core/Scheduling/RunSnapshot.cs` (C) | Continue girdisi: completed + queued + elapsed | 7 |
| `src/BuildOrchestrator.Core/MsBuild/CopyContention.cs` (C) | MSB3021/26/27 tespiti | 8 |
| `src/BuildOrchestrator.Core/MsBuild/RetryingMsBuildInvoker.cs` (C) | T8: contention'da backoff'lu retry decorator | 8 |
| `src/BuildOrchestrator.Supervisor/RunCoordinator.cs` (C) | Worker'lar + event akışı + graceful/hard stop + continue | 9 |
| `src/BuildOrchestrator.Supervisor/SupervisorHost.cs` (M) | `startRun`/`continueRun`/`stopRun`/`getProjectLog` dispatch | 9, 10 |
| `src/BuildOrchestrator.App/Console/ConsoleBatcher.cs` (C) | Channel → ~50ms batch (UI-bağımsız, testli) | 11 |
| `src/BuildOrchestrator.App/Console/ConsoleView.xaml(.cs)` (C) | AvalonEdit host: BeginUpdate/tek Insert/EndUpdate | 11 |
| `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs` (C) | Event → satır/proje durumu; elapsed; log yükleme dikişi | 12 |
| `src/BuildOrchestrator.App/MainWindow.xaml(.cs)` (M) | Root TextBox + Rebuild/Stop/Continue + liste + konsol | 12 |

(C)=Create, (M)=Modify.

---

## Task 1 [BLOKLAYICI]: Handle-inheritance izolasyonu (`PROC_THREAD_ATTRIBUTE_HANDLE_LIST`)

> **Neden İLK:** `bInheritHandles=true` **tüm** inheritable handle'ları miras verir. Paralel redirected launch'ta A'nın pipe uçları B'ye sızar → A'nın stdout'u EOF görmez → okuma sonsuza kadar asılı (it0-records "It-2 GİRİŞ KRİTERİ"). Tek child'lı It-0 akışı bunu tetiklemedi; It-2'nin paralelliği tetikler. **Ek kazanç:** Supervisor'ın App'ten miras aldığı stdio handle'ları (inherit bayrağı ÜZERİNDE kalır) `MSBuild.exe` child'larına sızmaz → Supervisor ölünce App'in okuması EOF alır.

**Files:**
- Modify: `src/BuildOrchestrator.Core/ProcessControl/NativeMethods.cs`
- Create: `src/BuildOrchestrator.Core/ProcessControl/ProcThreadAttributeList.cs`
- Modify: `src/BuildOrchestrator.Core/ProcessControl/JobProcessLauncher.cs:16-85`
- Test: `tests/BuildOrchestrator.Tests/ProcessControl/HandleInheritanceTests.cs`

**Interfaces:**
- Consumes: `JobObject.CreateKillOnClose()`, `JobProcessLauncher.Launch(JobObject, string, LaunchOptions)`, `WindowsCommandLine.Build(string exe, params string[] args)` (mevcut).
- Produces: `JobProcessLauncher.Launch` imzası **DEĞİŞMEZ** — davranış sertleşir. Task 5 bunu `MSBuild.exe` için paralel çağırır.

- [ ] **Step 1: Failing test — sentinel handle mirası**

`tests/BuildOrchestrator.Tests/ProcessControl/HandleInheritanceTests.cs`:

```csharp
using System.IO.Pipes;
using BuildOrchestrator.Core.ProcessControl;
using BuildOrchestrator.Core.Processes;
using Xunit;

namespace BuildOrchestrator.Tests.ProcessControl;

[Trait("Category", "ProcessControl")]
public class HandleInheritanceTests
{
    private static string CmdExe => Path.Combine(Environment.SystemDirectory, "cmd.exe");

    [Fact] // It-2 giriş kriteri: redirected child YALNIZ kendi 3 pipe ucunu miras almalı
    public async Task Redirected_child_does_not_inherit_unrelated_inheritable_handles()
    {
        using var job = JobObject.CreateKillOnClose();
        // Kardeş bir launch'ın client-handle'ını taklit eder: parent READ ucunu tutar,
        // WRITE (client) ucu inheritable — paralel launch penceresinde parent'ta AÇIK olan tam da budur.
        using var sentinel = new AnonymousPipeServerStream(PipeDirection.In, HandleInheritability.Inheritable);

        using var child = JobProcessLauncher.Launch(job,
            WindowsCommandLine.Build(CmdExe, "/c", "pause"), // stdin redirected → EOF gelene dek yaşar
            new LaunchOptions(RedirectStdio: true));
        sentinel.DisposeLocalCopyOfClientHandle(); // parent artık write ucunu tutmuyor

        var buf = new byte[1];
        int n = await sentinel.ReadAsync(buf, 0, 1).WaitAsync(TimeSpan.FromSeconds(5));
        // EOF (0) ⇒ hiçbir process write ucunu tutmuyor ⇒ child sentinel'ı miras ALMADI.
        // Sızsaydı write ucu child'da yaşar, EOF gelmez, WaitAsync TimeoutException ile patlardı.
        Assert.Equal(0, n);
    }
}
```

- [ ] **Step 2: Testi çalıştır — FAIL doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~HandleInheritanceTests"`
Expected: **FAIL** — `TimeoutException` (5 sn içinde EOF yok; child sentinel'ın write ucunu miras aldı).

- [ ] **Step 3: NativeMethods'a `STARTUPINFOEXW` + attribute-list P/Invoke'ları ekle**

`NativeMethods.cs` — mevcut sabitlerin yanına:

```csharp
    public const uint EXTENDED_STARTUPINFO_PRESENT = 0x00080000;
    public const int ERROR_INSUFFICIENT_BUFFER = 122;
    public static readonly nint PROC_THREAD_ATTRIBUTE_HANDLE_LIST = new(0x00020002);
```

`PROCESS_INFORMATION`'ın altına struct:

```csharp
    [StructLayout(LayoutKind.Sequential)]
    public struct STARTUPINFOEXW
    {
        public STARTUPINFOW StartupInfo;
        public nint lpAttributeList;
    }
```

Mevcut `CreateProcessW` bildiriminin altına (EXTENDED overload + attribute-list API'leri):

```csharp
    [DllImport("kernel32.dll", EntryPoint = "CreateProcessW", CharSet = CharSet.Unicode, SetLastError = true)]
    public static extern bool CreateProcessW(
        string? lpApplicationName, StringBuilder lpCommandLine, nint lpProcessAttributes, nint lpThreadAttributes,
        bool bInheritHandles, uint dwCreationFlags, nint lpEnvironment, string? lpCurrentDirectory,
        ref STARTUPINFOEXW lpStartupInfo, out PROCESS_INFORMATION lpProcessInformation);

    [DllImport("kernel32.dll", SetLastError = true)]
    public static extern bool InitializeProcThreadAttributeList(nint lpAttributeList, int dwAttributeCount, int dwFlags, ref nint lpSize);

    [DllImport("kernel32.dll", SetLastError = true)]
    public static extern bool UpdateProcThreadAttribute(nint lpAttributeList, uint dwFlags, nint attribute,
        nint lpValue, nint cbSize, nint lpPreviousValue, nint lpReturnSize);

    [DllImport("kernel32.dll")]
    public static extern void DeleteProcThreadAttributeList(nint lpAttributeList);
```

- [ ] **Step 4: `ProcThreadAttributeList` sarmalayıcısını yaz**

`src/BuildOrchestrator.Core/ProcessControl/ProcThreadAttributeList.cs`:

```csharp
using System.ComponentModel;
using System.Runtime.InteropServices;

namespace BuildOrchestrator.Core.ProcessControl;

/// <summary>
/// PROC_THREAD_ATTRIBUTE_HANDLE_LIST taşıyan attribute list. Listedeki handle'lar DIŞINDA hiçbir
/// inheritable handle child'a geçmez — paralel redirected launch'ta kardeş pipe uçlarının çapraz
/// sızmasını (EOF/deadlock) kökten keser. Handle dizisi CreateProcess dönene kadar canlı kalmalıdır.
/// </summary>
internal sealed class ProcThreadAttributeList : IDisposable
{
    private nint _list;
    private nint _handles;
    private bool _initialized;

    public nint Handle => _list;

    /// <param name="handles">Miras verilecek handle'lar — hepsi inheritable ve BİRBİRİNDEN FARKLI olmalı.</param>
    public ProcThreadAttributeList(IReadOnlyList<nint> handles)
    {
        nint size = 0;
        NativeMethods.InitializeProcThreadAttributeList(nint.Zero, 1, 0, ref size); // boyut sorgusu: false + ERROR_INSUFFICIENT_BUFFER beklenir
        int err = Marshal.GetLastWin32Error();
        if (size == 0) throw new Win32Exception(err);

        _list = Marshal.AllocHGlobal(size);
        try
        {
            if (!NativeMethods.InitializeProcThreadAttributeList(_list, 1, 0, ref size))
                throw new Win32Exception(Marshal.GetLastWin32Error());
            _initialized = true;

            _handles = Marshal.AllocHGlobal(nint.Size * handles.Count);
            for (int i = 0; i < handles.Count; i++) Marshal.WriteIntPtr(_handles, i * nint.Size, handles[i]);

            if (!NativeMethods.UpdateProcThreadAttribute(_list, 0, NativeMethods.PROC_THREAD_ATTRIBUTE_HANDLE_LIST,
                    _handles, nint.Size * handles.Count, nint.Zero, nint.Zero))
                throw new Win32Exception(Marshal.GetLastWin32Error());
        }
        catch { Dispose(); throw; }
    }

    public void Dispose()
    {
        if (_initialized) { NativeMethods.DeleteProcThreadAttributeList(_list); _initialized = false; }
        if (_list != nint.Zero) { Marshal.FreeHGlobal(_list); _list = nint.Zero; }
        if (_handles != nint.Zero) { Marshal.FreeHGlobal(_handles); _handles = nint.Zero; }
    }
}
```

- [ ] **Step 5: `JobProcessLauncher.Launch`'u izolasyonlu yola geçir**

`JobProcessLauncher.cs`'i tamamen şununla değiştir:

```csharp
using System.ComponentModel;
using System.IO.Pipes;
using System.Runtime.InteropServices;
using System.Text;

namespace BuildOrchestrator.Core.ProcessControl;

public sealed record LaunchOptions(bool RedirectStdio = false, bool Breakaway = false, string? WorkingDirectory = null);

/// <summary>
/// §3 binding launch sırası: (1) pipes → (2) CreateProcessW SUSPENDED → (3) job.Assign while SUSPENDED
/// (kaçış penceresi yok) → (4) ResumeThread + CloseHandle(hThread) → (5) parent-copy client-handle temizliği.
/// [It-2 giriş kriteri] Redirected launch'ta miras YALNIZ PROC_THREAD_ATTRIBUTE_HANDLE_LIST'teki 3 pipe ucuyla
/// sınırlıdır: bInheritHandles=true aksi halde parent'taki TÜM inheritable handle'ları verirdi ve paralel
/// launch'ta kardeş pipe uçları çapraz sızıp EOF'u engellerdi (deadlock).
/// </summary>
public static class JobProcessLauncher
{
    public static JobChildProcess Launch(JobObject job, string commandLine, LaunchOptions options)
    {
        ArgumentNullException.ThrowIfNull(job);
        ArgumentNullException.ThrowIfNull(options);

        AnonymousPipeServerStream? stdinPipe = null;
        AnonymousPipeServerStream? stdoutPipe = null;
        AnonymousPipeServerStream? stderrPipe = null;
        ProcThreadAttributeList? attributes = null;

        var six = new NativeMethods.STARTUPINFOEXW();
        six.StartupInfo.cb = Marshal.SizeOf<NativeMethods.STARTUPINFOEXW>();
        bool inheritHandles = false;

        try
        {
            uint flags = NativeMethods.CREATE_SUSPENDED | NativeMethods.CREATE_NO_WINDOW | NativeMethods.CREATE_UNICODE_ENVIRONMENT;
            if (options.Breakaway) flags |= NativeMethods.CREATE_BREAKAWAY_FROM_JOB;

            if (options.RedirectStdio)
            {
                stdinPipe = new AnonymousPipeServerStream(PipeDirection.Out, HandleInheritability.Inheritable);
                stdoutPipe = new AnonymousPipeServerStream(PipeDirection.In, HandleInheritability.Inheritable);
                stderrPipe = new AnonymousPipeServerStream(PipeDirection.In, HandleInheritability.Inheritable);

                six.StartupInfo.dwFlags = NativeMethods.STARTF_USESTDHANDLES;
                six.StartupInfo.hStdInput = stdinPipe.ClientSafePipeHandle.DangerousGetHandle();
                six.StartupInfo.hStdOutput = stdoutPipe.ClientSafePipeHandle.DangerousGetHandle();
                six.StartupInfo.hStdError = stderrPipe.ClientSafePipeHandle.DangerousGetHandle();
                inheritHandles = true;

                // 3 uç birbirinden farklı (ayrı pipe'lar) — HANDLE_LIST tekrar kabul etmez.
                attributes = new ProcThreadAttributeList(
                    [six.StartupInfo.hStdInput, six.StartupInfo.hStdOutput, six.StartupInfo.hStdError]);
                six.lpAttributeList = attributes.Handle;
                flags |= NativeMethods.EXTENDED_STARTUPINFO_PRESENT;
            }

            // CreateProcessW lpCommandLine değiştirilebilir bir buffer OLMALI — readonly string asla geçilmez.
            var cmdLineBuffer = new StringBuilder(commandLine);
            bool created = NativeMethods.CreateProcessW(
                null, cmdLineBuffer, nint.Zero, nint.Zero, inheritHandles, flags,
                nint.Zero, options.WorkingDirectory, ref six, out var pi);

            if (!created)
                throw new Win32Exception(Marshal.GetLastWin32Error());

            try
            {
                job.Assign(pi.hProcess); // process hâlâ SUSPENDED — kaçış penceresi yok
            }
            catch
            {
                NativeMethods.TerminateProcess(pi.hProcess, 1);
                NativeMethods.CloseHandle(pi.hThread);
                NativeMethods.CloseHandle(pi.hProcess);
                throw;
            }

            NativeMethods.ResumeThread(pi.hThread);
            NativeMethods.CloseHandle(pi.hThread);

            // Parent tarafındaki client-handle kopyalarını kapat (child kendi kopyasını miras aldı).
            stdinPipe?.DisposeLocalCopyOfClientHandle();
            stdoutPipe?.DisposeLocalCopyOfClientHandle();
            stderrPipe?.DisposeLocalCopyOfClientHandle();

            return new JobChildProcess((int)pi.dwProcessId, pi.hProcess, stdinPipe, stdoutPipe, stderrPipe);
        }
        catch
        {
            stdinPipe?.Dispose();
            stdoutPipe?.Dispose();
            stderrPipe?.Dispose();
            throw;
        }
        finally
        {
            attributes?.Dispose(); // CreateProcess döndü — buffer'lar artık serbest
        }
    }
}
```

- [ ] **Step 6: Testi çalıştır — PASS doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~HandleInheritanceTests"`
Expected: **PASS** (1 test).

- [ ] **Step 7: Paralel redirected launch testini ekle (asıl senaryo)**

`HandleInheritanceTests.cs` sınıfına ekle:

```csharp
    [Fact] // Paralel launch: her child'ın stdout'u KENDİ EOF'una ulaşmalı (çapraz sızıntı = ReadToEnd askıda kalır)
    public async Task Parallel_redirected_launches_each_reach_eof_on_their_own_stdout()
    {
        using var job = JobObject.CreateKillOnClose();
        var children = new List<JobChildProcess>();
        await Task.WhenAll(Enumerable.Range(0, 6).Select(i => Task.Run(() =>
        {
            var c = JobProcessLauncher.Launch(job,
                WindowsCommandLine.Build(CmdExe, "/c", $"echo child{i}"), new LaunchOptions(RedirectStdio: true));
            lock (children) children.Add(c);
        })));
        try
        {
            foreach (var c in children)
            {
                using var reader = new StreamReader(c.StandardOutput!);
                string text = await reader.ReadToEndAsync().WaitAsync(TimeSpan.FromSeconds(10));
                Assert.Contains("child", text);
            }
        }
        finally { foreach (var c in children) c.Dispose(); }
    }
```

- [ ] **Step 8: Tüm process-control paketini çalıştır — regresyon yok**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "Category=ProcessControl"`
Expected: **PASS** — `HandleInheritanceTests` (2) + `CascadeKillTests` (2) + `JobObjectTests` yeşil. Cascade-kill hâlâ ≤2000ms, 0 orphan, breakaway err5.

- [ ] **Step 9: Commit**

```bash
git add src/BuildOrchestrator.Core/ProcessControl tests/BuildOrchestrator.Tests/ProcessControl/HandleInheritanceTests.cs
git commit -m "fix(process): isolate handle inheritance via PROC_THREAD_ATTRIBUTE_HANDLE_LIST (It-2 entry blocker)"
```

---

## Task 2: Contracts — run/build IPC yüzeyi

**Files:**
- Modify: `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs`
- Test: `tests/BuildOrchestrator.Tests/Ipc/IpcMessagesTests.cs`

**Interfaces:**
- Consumes: `BuildResult` (`Contracts.Model`, It-1: `Succeeded|Failed|Skipped`), mevcut `IpcCommand`/`IpcEvent` polimorfik hiyerarşileri, `NdjsonWriter.WriteAsync(IpcCommand|IpcEvent)` base-type overload'ları.
- Produces (Task 9/10/12 bunlara bağlanır):
  - `RunMode { Rebuild, Continue }` · `RunOutcome { Completed, Stopped }`
  - `StartRunCommand(string RunId, RunMode Mode, string RootPath, string Configuration, int Parallelism)`
  - `RunStartedEvent(string RunId, RunMode Mode, int TotalProjects, int Parallelism, string Configuration, long ElapsedMsAtStart)`
  - `ProjectStartedEvent(string RunId, string ProjectId, string Name)`
  - `ProjectLogEvent(string RunId, string ProjectId, int LineNumber, string Text)`
  - `ProjectSucceededEvent(string RunId, string ProjectId, long DurationMs)`
  - `ProjectFailedEvent(string RunId, string ProjectId, long DurationMs, string Reason)`
  - `ProjectSkippedEvent(string RunId, string ProjectId, string Reason)`
  - `RunCompletedEvent(string RunId, RunOutcome Outcome, int Succeeded, int Failed, int Skipped, int Queued, long DurationMs)`
  - `ProjectLogChunkEvent` **genişler**: `(string ProjectId, int Sequence, string Text, bool IsLast, int ThroughLineNumber)`

> **YAGNI notu (It-1 deseni):** `RunMode`'a `Build`/`RetryFailed`, `ProjectResult.depIssues`, `runCompleted.depIssueCount`, `runProgress`/ETA **eklenmez** — It-3'ün işi. Event adları v6 A9 setinden birebir (`runStarted`/`projectStarted`/`projectLog`/`projectSucceeded`/`projectFailed`/`projectSkipped`/`runCompleted`); `runStopped` It-0'dan korunur (v6'daki `runCancelled` karşılığı).

- [ ] **Step 1: Failing test — round-trip + discriminator**

`tests/BuildOrchestrator.Tests/Ipc/IpcMessagesTests.cs` sınıfına ekle:

```csharp
    [Fact]
    public void StartRun_roundtrips_with_discriminator()
    {
        var cmd = new StartRunCommand("r1", RunMode.Rebuild, @"D:\repo", "Debug", 6);
        string json = JsonSerializer.Serialize<IpcCommand>(cmd, IpcJson.Options);
        Assert.Contains("\"type\":\"startRun\"", json);
        Assert.Contains("\"mode\":\"rebuild\"", json); // camelCase enum
        var back = Assert.IsType<StartRunCommand>(JsonSerializer.Deserialize<IpcCommand>(json, IpcJson.Options));
        Assert.Equal(cmd, back);
    }

    [Fact]
    public void Run_events_roundtrip_with_discriminators()
    {
        IpcEvent[] events =
        [
            new RunStartedEvent("r1", RunMode.Continue, 177, 6, "Debug", 4200),
            new ProjectStartedEvent("r1", @"C:\p\a.csproj", "A"),
            new ProjectLogEvent("r1", @"C:\p\a.csproj", 1, "  A.cs(3,5): error CS0103: ..."),
            new ProjectSucceededEvent("r1", @"C:\p\a.csproj", 2400),
            new ProjectFailedEvent("r1", @"C:\p\b.csproj", 900, "exit 1"),
            new ProjectSkippedEvent("r1", @"C:\p\c.csproj", "in dependency cycle"),
            new RunCompletedEvent("r1", RunOutcome.Stopped, 3, 1, 2, 171, 65000),
            new ProjectLogChunkEvent(@"C:\p\a.csproj", 0, "line\n", true, 42),
        ];
        foreach (var ev in events)
        {
            string json = JsonSerializer.Serialize(ev, IpcJson.Options);
            Assert.Equal(ev, JsonSerializer.Deserialize<IpcEvent>(json, IpcJson.Options));
        }
    }
```

- [ ] **Step 2: Testi çalıştır — FAIL doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~IpcMessagesTests"`
Expected: **FAIL** — derleme hatası `CS0246: 'StartRunCommand' bulunamadı`.

- [ ] **Step 3: Contracts'ı genişlet**

`IpcMessages.cs` — `IpcCommand` attribute bloğuna ekle:

```csharp
[JsonDerivedType(typeof(StartRunCommand), "startRun")]
```

`DebugSpawnChildrenCommand` satırının altına:

```csharp
public enum RunMode { Rebuild, Continue }
/// <param name="Mode">Rebuild = tüm projeler; Continue = önceki run'ın queued'larından sürer (elapsed korunur). [v7Δ-4]</param>
public sealed record StartRunCommand(string RunId, RunMode Mode, string RootPath, string Configuration, int Parallelism) : IpcCommand;
```

`IpcEvent` attribute bloğuna ekle:

```csharp
[JsonDerivedType(typeof(RunStartedEvent), "runStarted")]
[JsonDerivedType(typeof(ProjectStartedEvent), "projectStarted")]
[JsonDerivedType(typeof(ProjectLogEvent), "projectLog")]
[JsonDerivedType(typeof(ProjectSucceededEvent), "projectSucceeded")]
[JsonDerivedType(typeof(ProjectFailedEvent), "projectFailed")]
[JsonDerivedType(typeof(ProjectSkippedEvent), "projectSkipped")]
[JsonDerivedType(typeof(RunCompletedEvent), "runCompleted")]
```

`ProjectLogChunkEvent` satırını **değiştir** ve altına yeni event'leri ekle:

```csharp
/// <param name="ThroughLineNumber">Snapshot anında diske yazılmış son satır no — App canlı `projectLog`
/// satırlarını bununla dikiş yapar (LineNumber &lt;= ThroughLineNumber olanlar zaten chunk'ta). [T28]</param>
public sealed record ProjectLogChunkEvent(string ProjectId, int Sequence, string Text, bool IsLast, int ThroughLineNumber) : IpcEvent;

public enum RunOutcome { Completed, Stopped }
public sealed record RunStartedEvent(string RunId, RunMode Mode, int TotalProjects, int Parallelism,
    string Configuration, long ElapsedMsAtStart) : IpcEvent;
public sealed record ProjectStartedEvent(string RunId, string ProjectId, string Name) : IpcEvent;
public sealed record ProjectLogEvent(string RunId, string ProjectId, int LineNumber, string Text) : IpcEvent;
public sealed record ProjectSucceededEvent(string RunId, string ProjectId, long DurationMs) : IpcEvent;
public sealed record ProjectFailedEvent(string RunId, string ProjectId, long DurationMs, string Reason) : IpcEvent;
public sealed record ProjectSkippedEvent(string RunId, string ProjectId, string Reason) : IpcEvent;
public sealed record RunCompletedEvent(string RunId, RunOutcome Outcome, int Succeeded, int Failed, int Skipped,
    int Queued, long DurationMs) : IpcEvent;
```

- [ ] **Step 4: `ProjectLogChunkEvent` çağrı yerini derlet**

`SupervisorHost.cs:65` yeni parametreyi ister. Geçici olarak `ThroughLineNumber: 0` geç (Task 10 doğru snapshot'ı bağlayacak):

```csharp
            await writer.WriteAsync(new ProjectLogChunkEvent(g.ProjectId, c.Sequence, c.Text, c.IsLast, 0), ct);
```

- [ ] **Step 5: Testi çalıştır — PASS doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~IpcMessagesTests"`
Expected: **PASS** (mevcut testler + 2 yeni).

- [ ] **Step 6: Commit**

```bash
git add src/BuildOrchestrator.Contracts src/BuildOrchestrator.Supervisor/SupervisorHost.cs tests/BuildOrchestrator.Tests/Ipc/IpcMessagesTests.cs
git commit -m "feat(contracts): add run/build IPC surface (startRun, run + project events)"
```

---

## Task 3: `SolutionMapper.MapRefs` + `SolutionDirResolver` (restore'un `-p:SolutionDir` girdisi)

**Files:**
- Modify: `src/BuildOrchestrator.Core/Discovery/SolutionMapper.cs`
- Create: `src/BuildOrchestrator.Core/MsBuild/SolutionDirResolver.cs`
- Test: `tests/BuildOrchestrator.Tests/Discovery/SolutionMapperTests.cs` (M), `tests/BuildOrchestrator.Tests/MsBuild/SolutionDirResolverTests.cs` (C)

**Interfaces:**
- Consumes: `SolutionRef(string Name, string Path)` (Contracts.Model, It-1'de tanımlı), `SolutionMapper.Map` (mevcut, davranışı DEĞİŞMEZ).
- Produces:
  - `SolutionMapper.MapRefs(IReadOnlyList<string> slnPaths, IReadOnlyList<string> csprojPaths) → IReadOnlyDictionary<string, IReadOnlyList<SolutionRef>>` (csprojId → sln'ler, Name'e göre OrdinalIgnoreCase sıralı)
  - `SolutionDirResolver.Resolve(string projectId, IReadOnlyList<SolutionRef> refs) → string` (trailing backslash YOK; `MsBuildArguments` ekler)

- [ ] **Step 1: Failing test — MapRefs yol taşır**

`SolutionMapperTests.cs` sınıfına ekle (mevcut test dosyasındaki temp-dizin kurulum desenini kullan; aşağıdaki test kendi fixture'ını kurar):

```csharp
    [Fact]
    public void MapRefs_carries_solution_path_not_just_name()
    {
        string root = Path.Combine(Path.GetTempPath(), "bo-slnrefs-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(Path.Combine(root, "sub"));
        try
        {
            string csproj = Path.Combine(root, "sub", "A.csproj");
            File.WriteAllText(csproj, "<Project />");
            string sln = Path.Combine(root, "Osys.sln");
            File.WriteAllText(sln,
                "Project(\"{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}\") = \"A\", \"sub\\A.csproj\", \"{1}\"\nEndProject\n");

            var refs = SolutionMapper.MapRefs([sln], [csproj]);

            var one = Assert.Single(refs[csproj]);
            Assert.Equal("Osys", one.Name);
            Assert.Equal(Path.GetFullPath(sln), one.Path);
            // Map(), MapRefs üzerinden aynı adları vermeye devam eder (davranış korunur)
            Assert.Equal(["Osys"], SolutionMapper.Map([sln], [csproj])[csproj]);
        }
        finally { Directory.Delete(root, recursive: true); }
    }
```

- [ ] **Step 2: Testi çalıştır — FAIL doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~SolutionMapperTests"`
Expected: **FAIL** — `CS0117: 'SolutionMapper' 'MapRefs' tanımını içermiyor`.

- [ ] **Step 3: `SolutionMapper`'ı MapRefs üzerine kur (DRY)**

`SolutionMapper.cs`'te `Map` metodunu şununla değiştir (regex ve determinizm kuralları aynen korunur):

```csharp
    /// <summary>csprojId → onu içeren solution'lar (ad + tam yol), Name'e göre OrdinalIgnoreCase sıralı. [T32]</summary>
    public static IReadOnlyDictionary<string, IReadOnlyList<SolutionRef>> MapRefs(
        IReadOnlyList<string> slnPaths, IReadOnlyList<string> csprojPaths)
    {
        var csprojSet = new HashSet<string>(csprojPaths.Select(Path.GetFullPath), StringComparer.OrdinalIgnoreCase);
        var acc = csprojSet.ToDictionary(c => c, _ => new List<SolutionRef>(), StringComparer.OrdinalIgnoreCase);
        // determinizm [D8]: sln'leri OrdinalIgnoreCase sırayla gez.
        foreach (var sln in slnPaths.OrderBy(s => s, StringComparer.OrdinalIgnoreCase))
        {
            string full = Path.GetFullPath(sln);
            string slnDir = Path.GetDirectoryName(full)!;
            var slnRef = new SolutionRef(Path.GetFileNameWithoutExtension(full), full);
            foreach (Match m in ProjectLine().Matches(File.ReadAllText(sln)))
            {
                string proj = Path.GetFullPath(Path.Combine(slnDir, m.Groups[1].Value));
                if (acc.TryGetValue(proj, out var list) && !list.Any(r => r.Path.Equals(slnRef.Path, StringComparison.OrdinalIgnoreCase)))
                    list.Add(slnRef);
            }
        }
        return acc.ToDictionary(
            kv => kv.Key,
            kv => (IReadOnlyList<SolutionRef>)kv.Value.OrderBy(r => r.Name, StringComparer.OrdinalIgnoreCase).ToList(),
            StringComparer.OrdinalIgnoreCase);
    }

    /// <summary>0 solution → boş liste, &gt;1 solution → çok değerli (sıralı) liste. [T32]</summary>
    public static IReadOnlyDictionary<string, IReadOnlyList<string>> Map(
        IReadOnlyList<string> slnPaths, IReadOnlyList<string> csprojPaths) =>
        MapRefs(slnPaths, csprojPaths).ToDictionary(
            kv => kv.Key,
            kv => (IReadOnlyList<string>)kv.Value.Select(r => r.Name).ToList(),
            StringComparer.OrdinalIgnoreCase);
```

Dosyanın başındaki using'lere ekle: `using BuildOrchestrator.Contracts.Model;`

- [ ] **Step 4: Testi çalıştır — PASS doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~SolutionMapperTests"`
Expected: **PASS** — yeni test + It-1'in tüm mevcut `SolutionMapperTests` testleri (Map davranışı korundu).

- [ ] **Step 5: Failing test — SolutionDirResolver**

`tests/BuildOrchestrator.Tests/MsBuild/SolutionDirResolverTests.cs`:

```csharp
using BuildOrchestrator.Contracts.Model;
using BuildOrchestrator.Core.MsBuild;
using Xunit;

namespace BuildOrchestrator.Tests.MsBuild;

public class SolutionDirResolverTests
{
    [Fact] // [SPIKE S2-a] restore sln bağlamı ister: tek sln → onun dizini
    public void Single_solution_resolves_to_its_directory()
    {
        var refs = new[] { new SolutionRef("Osys", @"C:\repo\Osys.sln") };
        Assert.Equal(@"C:\repo", SolutionDirResolver.Resolve(@"C:\repo\sub\A.csproj", refs));
    }

    [Fact] // >1 sln → deterministik: Name'e göre ordinal-ignore-case ilk (T32 çoklu-değer; UI seçtirmesi It-4)
    public void Multiple_solutions_pick_first_by_name_deterministically()
    {
        var refs = new[]
        {
            new SolutionRef("Zeta", @"C:\repo\z\Zeta.sln"),
            new SolutionRef("Alpha", @"C:\repo\a\Alpha.sln"),
        };
        Assert.Equal(@"C:\repo\a", SolutionDirResolver.Resolve(@"C:\repo\sub\A.csproj", refs));
        Assert.Equal(SolutionDirResolver.Resolve(@"C:\repo\sub\A.csproj", refs),
                     SolutionDirResolver.Resolve(@"C:\repo\sub\A.csproj", refs.Reverse().ToArray())); // giriş sırası etkilemez
    }

    [Fact] // 0 sln → projenin kendi dizini (restore yine de sln bağlamı görür; kayda değer sapma)
    public void No_solution_falls_back_to_project_directory()
    {
        Assert.Equal(@"C:\repo\sub", SolutionDirResolver.Resolve(@"C:\repo\sub\A.csproj", []));
    }
}
```

- [ ] **Step 6: Testi çalıştır — FAIL doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~SolutionDirResolverTests"`
Expected: **FAIL** — `CS0246: 'SolutionDirResolver' bulunamadı`.

- [ ] **Step 7: `SolutionDirResolver`'ı yaz**

`src/BuildOrchestrator.Core/MsBuild/SolutionDirResolver.cs`:

```csharp
using BuildOrchestrator.Contracts.Model;

namespace BuildOrchestrator.Core.MsBuild;

/// <summary>
/// packages.config restore'un İSTEDİĞİ `-p:SolutionDir` değerini üretir [SPIKE S2-a]: sln bağlamı
/// olmadan `-t:restore` "Çözüm bulunamadı" verir. Determinizm [D8]: &gt;1 sln'de Name'e göre ordinal
/// ilk sln kazanır (T32'nin çok-değerli listesi korunur; kullanıcıya seçtirme yalnız "Open in VS", It-4).
/// </summary>
public static class SolutionDirResolver
{
    public static string Resolve(string projectId, IReadOnlyList<SolutionRef> refs)
    {
        var chosen = refs.OrderBy(r => r.Name, StringComparer.OrdinalIgnoreCase).FirstOrDefault();
        return chosen is null
            ? Path.GetDirectoryName(Path.GetFullPath(projectId))!
            : Path.GetDirectoryName(Path.GetFullPath(chosen.Path))!;
    }
}
```

- [ ] **Step 8: Testi çalıştır — PASS doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~SolutionDirResolverTests"`
Expected: **PASS** (3 test).

- [ ] **Step 9: Commit**

```bash
git add src/BuildOrchestrator.Core/Discovery/SolutionMapper.cs src/BuildOrchestrator.Core/MsBuild/SolutionDirResolver.cs tests/BuildOrchestrator.Tests
git commit -m "feat(core): expose solution paths (MapRefs) and resolve restore SolutionDir"
```

---

## Task 4: `RunLogPaths` + `RunLogWriter` (T5 — per-run disk log)

**Files:**
- Create: `src/BuildOrchestrator.Core/Logs/RunLogPaths.cs`, `src/BuildOrchestrator.Core/Logs/RunLogWriter.cs`
- Test: `tests/BuildOrchestrator.Tests/Logs/RunLogWriterTests.cs`

**Interfaces:**
- Consumes: `ProjectLogNaming.FileNameFor(string projectId)` (It-0, SHA256'nın ilk 16 hex'i + `.log`).
- Produces (Task 9/10 kullanır):
  - `RunLogPaths.DefaultLogsRoot` → `%LOCALAPPDATA%\BuildOrchestrator\logs`
  - `RunLogPaths.RunDirName(DateTimeOffset ts)` → `run-yyyyMMdd-HHmmss-fff`
  - `RunLogWriter(string logsRoot, DateTimeOffset startedAt)` · `.RunDirectory` · `.OpenProjectLog(string projectId) → ProjectLogFile` · `.AppendDecision(string line)` · `.ProjectLogPath(string projectId)` · `.SnapshotProjectLog(string projectId) → (string Text, int ThroughLineNumber)?`
  - `ProjectLogFile.AppendLine(string text) → int` (yazılan satırın 1-tabanlı numarası) · `.Dispose()`

- [ ] **Step 1: Failing test**

`tests/BuildOrchestrator.Tests/Logs/RunLogWriterTests.cs`:

```csharp
using BuildOrchestrator.Core.Logs;
using Xunit;

namespace BuildOrchestrator.Tests.Logs;

public class RunLogWriterTests : IDisposable
{
    private readonly string _root = Path.Combine(Path.GetTempPath(), "bo-runlog-" + Guid.NewGuid().ToString("N"));
    public void Dispose() { if (Directory.Exists(_root)) Directory.Delete(_root, recursive: true); }

    private static readonly DateTimeOffset Ts = new(2026, 7, 17, 14, 5, 9, 250, TimeSpan.Zero);

    [Fact] // [D4] per-run dizin; InvariantCulture, deterministik ad
    public void Run_directory_name_is_timestamped_and_created()
    {
        Assert.Equal("run-20260717-140509-250", RunLogPaths.RunDirName(Ts));
        using var w = new RunLogWriter(_root, Ts);
        Assert.Equal(Path.Combine(_root, "run-20260717-140509-250"), w.RunDirectory);
        Assert.True(Directory.Exists(w.RunDirectory));
    }

    [Fact] // [D4] bellek ring buffer YOK: satırlar diske gider, numaralar 1'den monotonik
    public void Project_log_lines_go_to_disk_and_are_numbered()
    {
        using var w = new RunLogWriter(_root, Ts);
        string id = @"C:\repo\A\A.csproj";
        using (var log = w.OpenProjectLog(id))
        {
            Assert.Equal(1, log.AppendLine("first"));
            Assert.Equal(2, log.AppendLine("second"));
        }
        string path = w.ProjectLogPath(id);
        Assert.Equal(Path.Combine(w.RunDirectory, ProjectLogNaming.FileNameFor(id)), path);
        Assert.Equal(["first", "second"], File.ReadAllLines(path));
    }

    [Fact] // Task 10 dikişi: snapshot metin + o ana kadar yazılmış satır sayısını ATOMİK verir
    public void Snapshot_returns_text_and_through_line_number()
    {
        using var w = new RunLogWriter(_root, Ts);
        string id = @"C:\repo\A\A.csproj";
        using var log = w.OpenProjectLog(id);
        log.AppendLine("a");
        log.AppendLine("b");
        var snap = w.SnapshotProjectLog(id);
        Assert.NotNull(snap);
        Assert.Equal("a\r\nb\r\n".Replace("\r\n", Environment.NewLine), snap!.Value.Text);
        Assert.Equal(2, snap.Value.ThroughLineNumber);
        log.AppendLine("c"); // snapshot sonrası satır snapshot'ı etkilemez
        Assert.Equal(2, snap.Value.ThroughLineNumber);
    }

    [Fact]
    public void Snapshot_of_unknown_project_is_null()
    {
        using var w = new RunLogWriter(_root, Ts);
        Assert.Null(w.SnapshotProjectLog(@"C:\repo\Nope\Nope.csproj"));
    }

    [Fact] // decision log çok worker'dan yazılır: satır kaybı/karışması YOK
    public void Decision_log_is_thread_safe()
    {
        using var w = new RunLogWriter(_root, Ts);
        Parallel.For(0, 200, i => w.AppendDecision($"decision-{i:D3}"));
        w.Dispose();
        var lines = File.ReadAllLines(Path.Combine(w.RunDirectory, "decision.log"));
        Assert.Equal(200, lines.Length);
        Assert.All(lines, l => Assert.Matches(@"^\d{2}:\d{2}:\d{2}\.\d{3} decision-\d{3}$", l));
    }
}
```

- [ ] **Step 2: Testi çalıştır — FAIL doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~RunLogWriterTests"`
Expected: **FAIL** — `CS0246: 'RunLogPaths' / 'RunLogWriter' bulunamadı`.

- [ ] **Step 3: `RunLogPaths`'i yaz**

`src/BuildOrchestrator.Core/Logs/RunLogPaths.cs`:

```csharp
using System.Globalization;

namespace BuildOrchestrator.Core.Logs;

/// <summary>[D4] Per-run disk log konumları. Bellek ring buffer YOKTUR — tek kaynak disktir.</summary>
public static class RunLogPaths
{
    public static string DefaultLogsRoot => Path.Combine(
        Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), "BuildOrchestrator", "logs");

    public static string RunDirName(DateTimeOffset ts) =>
        "run-" + ts.ToString("yyyyMMdd-HHmmss-fff", CultureInfo.InvariantCulture);
}
```

- [ ] **Step 4: `RunLogWriter` + `ProjectLogFile`'ı yaz**

`src/BuildOrchestrator.Core/Logs/RunLogWriter.cs`:

```csharp
using System.Globalization;
using System.Text;

namespace BuildOrchestrator.Core.Logs;

/// <summary>
/// [T5/D4] Bir run'ın tüm disk logu: `<logsRoot>\run-<ts>\` altında proje-başına log + decision.log.
/// Proje logunu proje-başına TEK worker yazar (scheduler garantisi); decision.log çok worker'dan yazılır.
/// </summary>
public sealed class RunLogWriter : IDisposable
{
    private readonly Dictionary<string, ProjectLogFile> _projects = new(StringComparer.OrdinalIgnoreCase);
    private readonly StreamWriter _decision;
    private readonly Lock _decisionGate = new();
    private readonly Lock _projectsGate = new();
    private bool _disposed;

    public string RunDirectory { get; }

    public RunLogWriter(string logsRoot, DateTimeOffset startedAt)
    {
        RunDirectory = Path.Combine(logsRoot, RunLogPaths.RunDirName(startedAt));
        Directory.CreateDirectory(RunDirectory);
        _decision = new StreamWriter(Path.Combine(RunDirectory, "decision.log"), append: true) { AutoFlush = true };
    }

    public string ProjectLogPath(string projectId) => Path.Combine(RunDirectory, ProjectLogNaming.FileNameFor(projectId));

    /// <summary>Projenin log dosyasını sıfırdan açar (rebuild = taze log). Çağıran Dispose etmelidir.</summary>
    public ProjectLogFile OpenProjectLog(string projectId)
    {
        var file = new ProjectLogFile(ProjectLogPath(projectId));
        lock (_projectsGate) _projects[projectId] = file;
        return file;
    }

    /// <summary>Log metni + o ana kadar diske yazılmış satır sayısı — ATOMİK (yazıcı ile aynı kilit). [T28 dikişi]</summary>
    public (string Text, int ThroughLineNumber)? SnapshotProjectLog(string projectId)
    {
        ProjectLogFile? file;
        lock (_projectsGate) _projects.TryGetValue(projectId, out file);
        if (file is not null) return file.Snapshot();
        string path = ProjectLogPath(projectId);
        if (!File.Exists(path)) return null;
        string text = File.ReadAllText(path);
        return (text, CountLines(text));
    }

    public void AppendDecision(string line)
    {
        lock (_decisionGate)
            _decision.WriteLine(DateTimeOffset.Now.ToString("HH:mm:ss.fff", CultureInfo.InvariantCulture) + " " + line);
    }

    internal static int CountLines(string text)
    {
        int n = 0;
        foreach (char c in text) if (c == '\n') n++;
        return n;
    }

    public void Dispose()
    {
        if (_disposed) return;
        _disposed = true;
        lock (_projectsGate) { foreach (var f in _projects.Values) f.Dispose(); _projects.Clear(); }
        lock (_decisionGate) _decision.Dispose();
    }
}

/// <summary>Tek projenin disk logu. Tek yazıcı + snapshot okuyucuları aynı kilidi paylaşır.</summary>
public sealed class ProjectLogFile : IDisposable
{
    private readonly string _path;
    private readonly StreamWriter _writer;
    private readonly Lock _gate = new();
    private int _lineNumber;
    private bool _disposed;

    internal ProjectLogFile(string path)
    {
        _path = path;
        _writer = new StreamWriter(new FileStream(path, FileMode.Create, FileAccess.Write, FileShare.Read)) { AutoFlush = true };
    }

    /// <returns>Yazılan satırın 1-tabanlı numarası (IPC `projectLog.lineNumber`).</returns>
    public int AppendLine(string text)
    {
        lock (_gate)
        {
            if (_disposed) return _lineNumber;
            _writer.WriteLine(text);
            return ++_lineNumber;
        }
    }

    internal (string Text, int ThroughLineNumber) Snapshot()
    {
        lock (_gate)
        {
            if (!_disposed) _writer.Flush();
            using var fs = new FileStream(_path, FileMode.Open, FileAccess.Read, FileShare.ReadWrite);
            using var reader = new StreamReader(fs, Encoding.UTF8);
            return (reader.ReadToEnd(), _lineNumber);
        }
    }

    public void Dispose()
    {
        lock (_gate)
        {
            if (_disposed) return;
            _disposed = true;
            _writer.Dispose();
        }
    }
}
```

- [ ] **Step 5: Testi çalıştır — PASS doğrula**

Run: `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj --filter "FullyQualifiedName~RunLogWriterTests"`
Expected: **PASS** (5 test).

- [ ] **Step 6: Commit**

```bash
git add src/BuildOrchestrator.Core/Logs tests/BuildOrchestrator.Tests/Logs/RunLogWriterTests.cs
git commit -m "feat(logs): per-run disk log (project logs + decision log + atomic snapshot) [T5]"
```

---

> **Not — Task 5'ten itibaren:** Bu plan uzunluğu nedeniyle Task 5–15 aynı biçimde (Files / Interfaces / adım-adım TDD + tam kod) devam eder ve `subagent-driven-development` ile aynı ledger'da yürütülür. Task listesi ve her birinin sözleşmesi aşağıda sabittir; implementer subagent'a task metni verilirken **Interfaces bloğu birebir** aktarılır.

## Task 5: `MsBuildOutputEncoding` + `MsBuildInvoker` (T22 invoke) + legacy fixture green

**Files:** Create `src/BuildOrchestrator.Core/MsBuild/MsBuildOutputEncoding.cs`, `src/BuildOrchestrator.Core/MsBuild/MsBuildInvoker.cs`; Test `tests/BuildOrchestrator.Tests/MsBuild/MsBuildInvokerTests.cs`, `tests/BuildOrchestrator.Tests/MsBuild/LegacyFixture.cs`.

**Interfaces:**
- Consumes: `MsBuildArguments.Build(projectPath, configuration, baseIntermediateOutputPath = null)` · `MsBuildArguments.RestorePackagesConfig(projectPath, solutionDir)` · `JobProcessLauncher.Launch` (Task 1) · `JobObject` · `WindowsCommandLine.Build` · `MsBuildResolver.ResolveAsync` (It-0).
- Produces:
  - `MsBuildInvokeRequest(string ProjectId, string Configuration, string SolutionDir, bool NeedsRestore, string? BaseIntermediateOutputPath = null)`
  - `MsBuildInvokeResult(int ExitCode, long DurationMs, bool TimedOut, bool Killed)`
  - `interface IMsBuildInvoker { Task<MsBuildInvokeResult> InvokeAsync(MsBuildInvokeRequest req, Action<string> onLine, CancellationToken ct); }`
  - `MsBuildInvoker(JobObject innerJob, string msbuildExePath) : IMsBuildInvoker` — `PerProjectTimeout = 10 dk`
  - `MsBuildOutputEncoding.Value` (ANSI codepage; `CodePagesEncodingProvider` register edilmiş)
  - `LegacyFixture.CreateClassLib(string dir, string assemblyName) → string` (v4.6 classlib csproj yolu)

**Davranış sözleşmesi:**
- `NeedsRestore` (csproj yanında `packages.config` varsa çağıran set eder) → önce `msbuild -t:restore -p:RestorePackagesConfig=true -p:SolutionDir=…`; exit≠0 → build DENENMEZ, o sonuç döner.
- Build: `MsBuildArguments.Build(projectId, config, req.BaseIntermediateOutputPath)` — **It-2 her zaman `null` geçer (I2-K2: in-place = default obj)**.
- Her child: `JobProcessLauncher.Launch(innerJob, …, new LaunchOptions(RedirectStdio: true, WorkingDirectory: <csproj dizini>))` → **inner Job içinde** (§3).
- stdout **ve** stderr satır satır okunur; `onLine` çağrıları invoke-lokal bir kilitle serileştirilir (iki reader task).
- `ct` iptali veya `PerProjectTimeout` → child `Kill(entireProcessTree: true)` + `Killed`/`TimedOut` işaretlenir.
- Okuma encoding'i = `MsBuildOutputEncoding.Value` (MSBuild.exe = .NET Framework; redirected stdout ANSI codepage'te yazar — UTF-8 varsayımı Türkçe çıktıyı mojibake yapar).

**Test paketi:** (a) `[Trait("Category","MsBuild")]` — `LegacyFixture` v4.6 classlib → `InvokeAsync` exit 0 + `onLine` satırları geldi + fixture'ın `bin\Debug\<Name>.dll` üretildi; (b) derleme hatası olan fixture → exit≠0 + satırlarda `error CS`; (c) `ct` iptali → `Killed=true` + process yok (deterministik: `Process.GetProcessById` → `ArgumentException`); (d) `MsBuildOutputEncoding.Value.CodePage == Encoding ANSI CP` ve Türkçe metin round-trip. **MSBuild yoksa** `MsBuildResolver` `MsBuildResolveException` atar → testler `SkippableFact` ile skip (mevcut `MsBuildResolverTests` deseni).

**Commit:** `feat(msbuild): invoke MSBuild.exe per project inside inner job with line streaming [T22]`

## Task 6: `ReadySetScheduler` (K2 — deterministik, ileri atlamalı)

**Files:** Create `src/BuildOrchestrator.Core/Scheduling/ReadySetScheduler.cs`; Test `tests/BuildOrchestrator.Tests/Scheduling/ReadySetSchedulerTests.cs`.

**Interfaces:**
- Consumes: `BuildPlan(Nodes, Cycles, Configuration)` · `ProjectNode(Id, …, Dependencies, BuildOrder, InCycle, …)` · `BuildResult`.
- Produces:
  - `ReadySetScheduler(BuildPlan plan)` (rebuild: tüm node'lar hedef) · `ReadySetScheduler(BuildPlan plan, RunSnapshot snapshot)` (Task 7'de eklenir)
  - `bool TryDispatch(out string projectId)` · `void Complete(string projectId, BuildResult result)` · `void RequestStop()` · `IReadOnlyList<string> QueuedProjectIds` · `IReadOnlyDictionary<string, BuildResult> Completed` · `bool IsDone` · `int InFlight`
  - `IReadOnlyList<(string ProjectId, string Reason)> PreSkipped` — `InCycle` node'ları

**Davranış sözleşmesi:**
- **Ready** = tüm `Dependencies`'i *çözülmüş*; çözülmüş = `Succeeded | Failed | Skipped`. **Failed bağımlılık alt projeleri BLOKLAMAZ** — aksi halde run asla bitmez ("hata derlemeyi öldürmez", A3). depIssue *raporlaması* It-3 (T54).
- **Cycle:** `InCycle=true` node'lar plan anında `Skipped(reason: "in dependency cycle")` → `PreSkipped`; bağımlıları böylece çözülür (yoksa kilitlenir; `TopoSort` SCC üyelerini BuildOrder'a koyuyor).
- **Dispatch:** ready set'ten `BuildOrder`'da EN ÖNDE olan; bağımlılığı bekleyenin **ÜZERİNDEN ATLANIR** (K2). Rastgele/hash YOK.
- **RequestStop:** `TryDispatch` bundan sonra daima `false`; `QueuedProjectIds` = hiç dispatch edilmemişler (BuildOrder sıralı).
- **IsDone:** `InFlight == 0 && (stop istendi || dispatch edilecek bir şey kalmadı)`.

**Test paketi:** ileri atlama (A bloklu, B ready → B); determinizm (aynı plan + aynı complete sırası → aynı dispatch dizisi, 5×); failed bağımlılık dependent'i açar; cycle üyeleri PreSkipped + dependent'leri koşar; slot doluyken TryDispatch sırası; RequestStop → queued kümesi = kalanlar, in-flight bitince IsDone; diamond (A→B,A→C,B→D,C→D).

**Commit:** `feat(core): deterministic ready-set scheduler with forward skipping [K2]`

## Task 7: `RunClock` + `RunSnapshot` (T55 Continue çekirdeği)

**Files:** Create `src/BuildOrchestrator.Core/Scheduling/RunClock.cs`, `src/BuildOrchestrator.Core/Scheduling/RunSnapshot.cs`; Modify `ReadySetScheduler.cs` (snapshot ctor); Test `tests/BuildOrchestrator.Tests/Scheduling/RunClockTests.cs`, `.../ContinueRunTests.cs`.

**Interfaces:**
- Produces:
  - `RunClock(Func<long> nowMs, long accumulatedMs = 0)` · `.Start()` · `.Pause()` · `.ElapsedMs`
  - `RunSnapshot(IReadOnlyDictionary<string, BuildResult> Completed, IReadOnlyList<string> Queued, long ElapsedMs)`
  - `ReadySetScheduler(BuildPlan plan, RunSnapshot snapshot)` · `RunSnapshot ReadySetScheduler.TakeSnapshot(long elapsedMs)`

**Davranış sözleşmesi:** Continue = **mevcut plandan** kalan queued'lar; `Completed` korunur (yeniden derlenmez); elapsed `accumulatedMs`'ten devam eder (K/T55: "elapsed korunur"). Saat **enjekte edilir** (`Func<long>`) — testte `Thread.Sleep` YOK (D8).

**Test paketi:** segment toplama (0→1500 pause, 5000→6000 → 2500); snapshot round-trip; Continue'da completed yeniden dispatch edilmez; Continue'un dispatch sırası = orijinal BuildOrder'ın kalanı; boş queued → anında IsDone.

**Commit:** `feat(core): run clock + snapshot so Continue resumes queued work with elapsed preserved [T55]`

## Task 8: `CopyContention` + `RetryingMsBuildInvoker` (T8)

**Files:** Create `src/BuildOrchestrator.Core/MsBuild/CopyContention.cs`, `src/BuildOrchestrator.Core/MsBuild/RetryingMsBuildInvoker.cs`; Test `tests/BuildOrchestrator.Tests/MsBuild/RetryingMsBuildInvokerTests.cs`.

**Interfaces:**
- Consumes: `IMsBuildInvoker` (Task 5).
- Produces:
  - `CopyContention.IsContention(string line)` (MSB3021/MSB3026/MSB3027 — ortak bin'e paralel post-build copy'de sharing violation)
  - `RetryingMsBuildInvoker(IMsBuildInvoker inner, IReadOnlyList<TimeSpan> backoff, Func<TimeSpan, CancellationToken, Task> delay, Action<string>? onRetry = null) : IMsBuildInvoker`
  - `RetryingMsBuildInvoker.DefaultBackoff` = `[200ms, 600ms]` (toplam 3 deneme)

**Davranış sözleşmesi:** Yalnız **contention** işaretli başarısızlık retry edilir (compile error → tek deneme). Her retry'da `onRetry` (decision log + konsol warn) çağrılır. `delay` **enjekte** (test gerçek zaman beklemez — D8). Başarı/son deneme → sonuç aynen döner. Orchestrator **kopyalamaz** (§4) — retry, projenin KENDİ copy event'inin yarışını yeniden dener.

**Test paketi:** contention 2 kez → 3. denemede exit 0, `delay` çağrıları `[200ms, 600ms]`; `error CS0103` → 1 deneme; contention ama son deneme de başarısız → başarısız sonuç + 3 deneme; `onLine` satırları her denemede akar.

**Commit:** `feat(msbuild): retry copy-contention failures with injected backoff [T8]`

## Task 9: `RunCoordinator` + Supervisor wiring (T22 invoke akışı + I2-K1 Stop)

**Files:** Create `src/BuildOrchestrator.Supervisor/RunCoordinator.cs`; Modify `src/BuildOrchestrator.Supervisor/SupervisorHost.cs`, `src/BuildOrchestrator.Supervisor/Program.cs`; Test `tests/BuildOrchestrator.Tests/Supervisor/RunCoordinatorTests.cs`.

**Interfaces:**
- Consumes: `BuildPlanBuilder.Build(root, configuration)` · `ReadySetScheduler` · `RunClock`/`RunSnapshot` · `IMsBuildInvoker` · `RunLogWriter` · `SolutionMapper.MapRefs` + `SolutionDirResolver` · `JobObject innerJob` · `NdjsonWriter`.
- Produces: `RunCoordinator(IMsBuildInvoker invoker, Func<DateTimeOffset, RunLogWriter> logFactory, NdjsonWriter writer, JobObject innerJob, Func<long> nowMs)` · `Task StartAsync(StartRunCommand cmd, CancellationToken ct)` · `void RequestStop(StopKind kind)` · `bool HasResumableRun`.

**Davranış sözleşmesi:**
- `Mode.Rebuild`: plan kur → `runStarted(TotalProjects, Parallelism, Configuration, ElapsedMsAtStart: 0)` → `PreSkipped` için `projectSkipped` → N worker (`Parallelism`) ready-set'ten çeker.
- Worker: `projectStarted` → `logs.OpenProjectLog` → MSBuild komut satırını **proje logunun ilk satırı** olarak yaz (v7Δ-7: konsolda solution-level msbuild izlenimi verilmez; gerçek komutlar proje logunda) → `InvokeAsync(onLine: satır → proje logu + `projectLog` event)` → exit 0 → `projectSucceeded(durationMs)` ; değilse `projectFailed(durationMs, "exit N")` → `scheduler.Complete` + decision log satırı.
- `NeedsRestore` = csproj yanında `packages.config` var; `SolutionDir` = `SolutionDirResolver.Resolve(id, refs[id])`.
- **Stop/Graceful (I2-K1):** `scheduler.RequestStop()` — yeni dispatch yok, in-flight child'lar post-build copy dahil biter → `runStopped(wasHard:false)` + `runCompleted(Outcome.Stopped, Queued: N)`; `HasResumableRun = true`.
- **Stop/Hard (I2-K1):** `innerJob.Terminate()` ANINDA → in-flight `projectFailed(reason: "stopped")` → `runStopped(wasHard:true)` + `runCompleted(Outcome.Stopped, …)`. (Terminate edilmiş job yeni assign kabul eder → Continue çalışır.)
- `Mode.Continue`: `HasResumableRun` yoksa `error("noResumableRun")`; varsa snapshot'tan scheduler + `RunClock(accumulated)` → `runStarted(…, ElapsedMsAtStart: elapsed)`.
- Run başına **tek** `RunLogWriter`; Continue **aynı run dizinine** yazar (log dikişi bozulmaz).
- Tek seferde tek run (A6 "orchestrator tek seferde tek run") → koşarken `startRun` → `error("runInProgress")`.

**Test paketi (fake `IMsBuildInvoker` ile, gerçek MSBuild yok — deterministik):** paralellik tavanı aşılmaz (eşzamanlı in-flight ≤ N); event sırası (`runStarted` → `projectStarted`* → `projectSucceeded`/`projectFailed`* → `runCompleted`); graceful stop → in-flight tamamlanır + `Queued` doğru + `Outcome.Stopped`; hard stop → in-flight `projectFailed("stopped")`; Continue → yalnız queued'lar dispatch + `ElapsedMsAtStart` korunur; ikinci `startRun` → `error("runInProgress")`; `stdout` yalnız NDJSON (mevcut `SupervisorIpcTests` deseni, run event akışı altında).

**Commit:** `feat(supervisor): parallel run coordinator with copy-aware graceful stop and continue [T4/T55]`

## Task 10: T28 — canlı interleave + `getProjectLog` run dizininden

**Files:** Modify `src/BuildOrchestrator.Supervisor/SupervisorHost.cs`; Test `tests/BuildOrchestrator.Tests/Supervisor/ProjectLogStreamTests.cs`.

**Interfaces:**
- Consumes: `RunLogWriter.SnapshotProjectLog(projectId)` (Task 4) · `LogChunker.Chunk(text)` (It-0) · `ProjectLogChunkEvent(…, ThroughLineNumber)` (Task 2).
- Produces: `getProjectLog` artık **aktif run'ın dizininden** okur (Task 2'deki geçici `ThroughLineNumber: 0` yerine gerçek snapshot); log yoksa `error("logNotFound", projectId)` (mevcut davranış korunur).

**Davranış sözleşmesi:** `getProjectLog` → `SnapshotProjectLog` (atomik metin + `ThroughLineNumber`) → `LogChunker.Chunk` → her chunk `ProjectLogChunkEvent(…, ThroughLineNumber)`. Canlı `projectLog` event'leri kesintisiz akmaya devam eder; App `LineNumber <= ThroughLineNumber` olanları eler (dikiş — Task 12).

**Test paketi:** koşan projenin logu istenir → chunk'lar + `ThroughLineNumber` = o ana kadarki satır sayısı; sonraki canlı satırların `LineNumber > ThroughLineNumber`; 64K'yı aşan log → çok chunk, yalnız sonuncusu `IsLast`; bilinmeyen proje → `error("logNotFound")`; chunk akışı altında stdout yalnız NDJSON.

**Commit:** `feat(supervisor): serve project logs from the run directory with live-stream stitching [T28]`

## Task 11: T56 — `ConsoleBatcher` + AvalonEdit `ConsoleView`

**Files:** Create `src/BuildOrchestrator.App/Console/ConsoleBatcher.cs`, `src/BuildOrchestrator.App/Console/ConsoleView.xaml(.cs)`; Test `tests/BuildOrchestrator.Tests/App/ConsoleBatcherTests.cs`, `tests/BuildOrchestrator.Tests/App/ConsoleViewTests.cs`.

**Interfaces:**
- Produces:
  - `ConsoleBatcher(Func<CancellationToken, Task> tick)` · `void Post(string line)` · `Task PumpAsync(Action<string> flush, CancellationToken ct)` · `void Complete()`
  - `ConsoleView : UserControl` · `.AppendBatch(string text)` · `.Document` (run/proje doküman swap'ı için) · `.StickToBottom`

**Davranış sözleşmesi (A13.2):** IPC background thread `Post` eder (kilitsiz `Channel<string>`, `SingleReader`); pump `tick` (~50ms) bekler, kanalı boşaltır, **tek** `flush(joinedText)` çağırır. `flush` UI tarafında `Document.BeginUpdate()` → **tek** `Insert(TextLength, text)` → `EndUpdate()` (+ `StickToBottom` ise `ScrollToEnd`). **Satır başına `Dispatcher.Invoke` YASAK** — batch başına bir `Dispatcher.InvokeAsync`. Editor `IsReadOnly = true`, `FontFamily` = gömülü Geist Mono (It-0 asset'i), `WordWrap = false`. Trim/tampon, colorizer, typewriter/kaskat, `⌄ latest` pill → **It-4**.
- `tick` **enjekte** → testte gerçek 50ms beklenmez (D8).

**Test paketi:** 3 `Post` + tek tick → **tek** `flush("a\nb\nc\n")`; tick'ler arası post yoksa flush YOK; `Complete()` → pump biter; 10.000 satır → flush sayısı ≪ satır sayısı (batching kanıtı); `[StaFact]` `ConsoleView.AppendBatch` iki batch → `Document.LineCount` doğru + `Document.Text` sıralı.

**Commit:** `feat(app): AvalonEdit console with channel-backed ~50ms batch flush [T56]`

## Task 12: App — minimal run UI (liste + Stop/Continue + karta tıkla → tam log)

**Files:** Create `src/BuildOrchestrator.App/ViewModels/RunViewModel.cs`; Modify `src/BuildOrchestrator.App/MainWindow.xaml(.cs)`, `src/BuildOrchestrator.App/App.xaml.cs`; Test `tests/BuildOrchestrator.Tests/App/RunViewModelTests.cs`.

**Interfaces:**
- Consumes: `EngineHost.SendAsync/EventReceived` (It-0) · Task 2 event'leri · `ConsoleBatcher`/`ConsoleView` (Task 11).
- Produces: `RunViewModel(EngineHost engine, ConsoleBatcher console, Func<string> newRunId)` · `.Projects` (`ObservableCollection<ProjectRowViewModel>`) · `.RebuildCommand/.StopCommand/.ContinueCommand` · `.ElapsedMs` · `.OnEvent(IpcEvent)` · `.LoadProjectLogAsync(string projectId)` · `ProjectRowViewModel { Id, Name, State, DurationMs }`.

**Davranış sözleşmesi (It-2 iskelesi — design-v1 birebirliği It-4):**
- `MainWindow`: repo kökü **TextBox**'ı (It-4'te Settings/OpenFolderDialog — K10) + `Rebuild`/`Stop`/`Continue` butonları + proje `ListBox`'ı + `ConsoleView`.
- Satırlar `projectStarted` ile eklenir, `projectSucceeded/Failed/Skipped` ile güncellenir (tam liste + kart görselleri It-4).
- **Elapsed:** `runStarted.ElapsedMsAtStart` + yerel `DispatcherTimer`; `runCompleted`'ta durur. Continue → engine'in verdiği offset'ten devam ⇒ "elapsed korunur" gözle kanıtlanır.
- **Karta tıkla → tam log:** `getProjectLog` → chunk'lar **proje dokümanına** yazılır → `ThroughLineNumber`'dan büyük tamponlanmış canlı satırlar eklenir (dikiş) → konsol proje moduna geçer; `Back` → run dokümanı (canlı akış proje modunda da run dokümanına birikmeye devam eder).
- `projectLog` satırları: aktif proje modundaysa ilgili proje dokümanına, her hâlde run dokümanına `ConsoleBatcher.Post` ile.

**Test paketi (UI-siz VM):** `projectStarted` → satır eklenir; `projectSucceeded` → State/Duration; `runStarted(ElapsedMsAtStart: 4200)` → `ElapsedMs` 4200'den başlar; log yükleme dikişi — chunk(`Through=2`) + tamponlanmış `projectLog(1..4)` → yalnız 3-4 eklenir, tekrar YOK; `Stop` → `StopRunCommand(Graceful)`; `Continue` → `StartRunCommand(Mode.Continue)`.

**Commit:** `feat(app): minimal run UI — project rows, stop/continue, click-to-full-log`

## Task 13: T9 — kill mid-parallel-build (torn DLL yok + leftover process yok)

**Files:** Test `tests/BuildOrchestrator.Tests/Supervisor/KillMidBuildTests.cs`.

**Davranış sözleşmesi:** I2-K1'in torn-DLL tanımını kanıtlar: **kill'den sağ çıkan yazıcı YOKTUR** (v1 flag'leri → VBCSCompiler yok → her writer inner Job'da).
- Deterministik tetik: gerçek Supervisor process'i (App outer Job'unda) `startRun` ile koşar; test **`projectStarted` event sayısı ≥2 olana kadar IPC okur** (sleep-poll YOK), sonra outer Job'u `Dispose` eder (App'in en sert ölümü).
- Assert: outer IOCP ile toplanan tüm pid'ler ≤2000ms içinde exit (CascadeKillTests deseni: handle'lar kill ÖNCESİ açılır); `Process.GetProcessById` → `ArgumentException` (0 orphan); kill'den önce `projectSucceeded` raporlamış her projenin çıktı DLL'i `AssemblyName.GetAssemblyName` ile **geçerli PE** (yarım/torn değil).
- Build hedefi: `LegacyFixture` (Task 5) ile üretilmiş, ortak bir `bin`'e post-build copy yapan **≥4 projelik** sentetik ağaç → gerçek `MSBuild.exe`, gerçek paralellik. MSBuild yoksa `SkippableFact`.

**Commit:** `test(process): prove no surviving writers and zero orphans on kill mid-parallel-build [T9]`

## Task 14: It-1'den ertelenen It-2-ilgili minor'lar

**Files:** Modify `tests/BuildOrchestrator.Tests/Graph/TopoSortTests.cs`, `tests/BuildOrchestrator.Tests/Discovery/StaleObjDetectorTests.cs`; Modify `.superpowers/sdd/progress.md`.

**Kapsam (ledger "Minor findings roll-up"tan, yalnız It-2-ilgili):**
- **Task12 Minor (HIGH):** `TopoSort` diamond (`A→B, A→C, B→D, C→D` ⇒ `[D,B,C,A]`) + çok-üyeli SCC yerleşimi testleri — indeg/dependents yön mantığını sıkıştırır (scheduler'ın doğruluğu buna dayanır).
- **Task16 follow-up:** `StaleObjDetector` bozuk-JSON/eksik-`targets` never-throw dalı testi + `expectedTfm`'in **tam moniker** (`.NETFramework,Version=v4.6`) istediğinin XML doc'a yazılması.
- Kapatılanlar ledger'da `[It-2'de kapatıldı]` diye işaretlenir; kalanlar It-3'e devreder.

**Commit:** `test(core): close deferred It-1 minors relevant to It-2 (topo diamond/SCC, stale-obj robustness)`

## Task 15: It-2 kabul koşusu + `it2-records.md`

**Files:** Create `tests/BuildOrchestrator.Tests/Integration/OsysRebuildAcceptanceTests.cs`, `.claude/outputs/<ts>-it2-records.md`.

**Kabul (I2-K3 — tam 177, yeşil = orchestrator kaynaklı 0 hata):**
1. `[Trait("Category","Acceptance")]` test: gerçek Supervisor'ı `D:\Projects\Delta\OSYS` üzerinde `Mode.Rebuild`, `Parallelism: 6`, `Debug` ile koşturur; tüm event'leri toplar. Run: `dotnet test … --filter "Category=Acceptance" -- xUnit.MaxParallelThreads=1`.
2. **Kanıt tablosu → `it2-records.md`:** toplam proje · succeeded/failed/skipped · süre · eşzamanlı in-flight tavanı (≤6) · dispatch sırası ilk 20 (determinizm: 2 koşuda aynı) · copy-contention retry sayısı · **her failed projenin nedeni + orchestrator-kaynaklı mı repo-kaynaklı mı sınıflandırması** (orchestrator-kaynaklı > 0 → acceptance FAIL).
3. **Manuel (App ile) kanıtlar:** paralel rebuild canlı akışı; **Stop → kalanlar queued → Continue kalanlardan sürer + elapsed korunur**; **karta tıkla → tam log diskten** (proje logunda gerçek MSBuild komut satırı ilk satırda); kill mid-build (Task 13 otomatik + burada canlı gözlem); konsol MSBuild-verbose altında akıcı (UI donmaz); proje logunda Türkçe MSBuild çıktısı okunabilir (encoding doğrulaması — mojibake varsa `MsBuildOutputEncoding` bulgusu kayda geçer).
4. Clean build **0 uyarı / 0 hata** + tam suite yeşil (It-1 tabanı: 85 geçti / 1 skip).
5. `it2-records.md` şunları da taşır: I2-K1/K2/K3 kararları · It-3'e devreden backlog · varsa yeni bulgular.

**Commit:** `docs(it2): record acceptance run results and It-3 handoff backlog`

---

## Self-Review

**Spec coverage (v7 Part C It-2 satırı):** T22(invoke)→Task 5 · T28(stream)→Task 2/10 · T5→Task 4 · T4(copy-aware)→Task 9 (I2-K1) · T8→Task 8 · T9→Task 13 · T55(Continue)→Task 7/9/12 · T56(canlı akış + batch flush)→Task 11. It-2 giriş kriteri (handle-inheritance)→**Task 1**. Acceptance ("OSYS rebuild paralel green; dispatch deterministik ready-set; kill mid-build torn DLL yok; karta tıkla→tam log; Stop→Continue; konsol akıcı")→Task 15 + Task 6 (determinizm testleri) + Task 13.

**Yasaklar:** in-process MSBuild yok (Task 5 = shell-out) · OutDir okunmaz (Task 8 retry = projenin kendi copy'si; orchestrator kopyalamaz) · stdout yalnız NDJSON (Task 9/10 testleri) · sleep-poll yok (Task 6/7/8/11 enjekte saat/tick; Task 13 event-tetikli) · v1 flag'leri sabit (`MsBuildArguments`, It-1'de zaten sabit).

**Tip tutarlılığı:** `RunMode`/`RunOutcome`/`BuildResult` tek yerde (Task 2 + It-1 `Contracts.Model`); `IMsBuildInvoker` imzası Task 5'te tanımlı, Task 8 decorator'ı ve Task 9 aynısını tüketir; `RunSnapshot` Task 7'de tanımlı, Task 9 tüketir; `ProjectLogChunkEvent.ThroughLineNumber` Task 2'de eklenir, Task 10 doldurur, Task 12 tüketir; `ProjectLogFile.AppendLine → int` (Task 4) = `ProjectLogEvent.LineNumber` (Task 2).

**Bilinen risk (Task 15'te ölçülür):** MSBuild.exe redirected çıktı encoding'i (ANSI CP varsayımı) — tek noktada (`MsBuildOutputEncoding`), mojibake çıkarsa tek satırlık düzeltme + kayıt.
