# A13 — Park Edilen Bulguların Triyajı

**Branch:** `a13-visual-debt-automation` (BASE `8e6ebbe` = `main`) · **HEAD:** `dcf740f` · **Tarih:** 2026-08-03
**Kapsam:** A13'ün 11 task'ı (T1 · T2 · T3 · T4 · B0 · B1 · T5 · T6 · B2 · B3 · B4) boyunca **bilerek park edilen**
tüm Minor / dağınıklık-organizasyon / concern kalemleri + `it5-records.md` §2'nin A13 tarafından etkilenen satırları.

**Kaynaklar (hepsi okundu):**
`.superpowers/sdd/2026-07-30-14-59-visual-check-automation-tdd-plan/progress.md` (1243 satır, task-task park kayıtları) ·
`review-B3-lens{1,2,3}.md` · `review-B3-fix1-lens{1,3}.md` · `review-B4-lens{A,B}.md` · `review-B4-fix1-lens{A,B}.md` ·
`task-B3-report.md` (C1–C9) · `task-B4-report.md` (concerns 1–5) · `final-review-C-docs.md` ·
`.claude/outputs/2026-07-26-10-17-it5-records.md` §2.

**Bu bir KARAR TABLOSUDUR, rapor değildir.** Her satır: kova → ID → tek cümlelik bulgu → `Konum:` → karar → kaynak.

---

## 0. Özet

| Kova | Adet |
|---|---|
| **KAPANDI** (A13 sırasında ya da A13 öncesinde kapatıldı, commit'i doğrulandı) | **14** |
| **TAŞINIYOR** (gerçek borç, A13 kapsamı dışı) | **80** |
| **KAPATILDI-GEÇERSİZ** (ölçüldü, gerçek çıkmadı ya da başka bir değişiklik anlamsız kıldı) | **12** |
| **TOPLAM karar satırı** | **106** |

> Sayı, ham bulgu sayısından küçüktür: **aynı bulgunun birden çok lens tarafından raporlanmış kopyaları
> birleştirildi** (ör. `EngineHost.cs:48` yanlış test adı üç lens tarafından bağımsız işaretlenmişti → tek satır).
> Birleştirilen satırlarda kaynak sütunu tüm lens'leri anar.

**Ölçüm notları:**
- KAPANDI kovasındaki **14 commit'in 14'ü** `git log -1` / `git show --stat` / `git show <c> -- <path>` ile doğrulandı.
  A13 ÖNCESİ kapanan ikisi ayrıca `git merge-base --is-ancestor <c> 8e6ebbe` ile "BASE'in atası" diye teyit edildi.
- Satır numaraları **bugünkü `HEAD` (`dcf740f`)** üzerinden yeniden ölçüldü. Lens raporundaki koordinat hâlâ
  geçerliyse birebir taşındı; kaydıysa yeni değer yazıldı ve eskisi parantezle anıldı.
- `final-review-C-docs.md:103` `it5-records.md` §2 satır **#7** için `:427` veriyor; dosyayı bizzat okudum,
  **doğrusu `:428`**'dir (`:427` = satır #6). Aşağıda ölçülen değer kullanıldı.

---

### 0.1 Bir sonraki iş kalemine (A14) taşınanlar — ÖNE ÇIKANLAR

| Öncelik | Küme | ID'ler | Neden A14 |
|---|---|---|---|
| **1** | **Test izolasyonu — KURAL ihlali adayı** | **T10–T15** | Beş test kullanıcının GERÇEK `%LOCALAPPDATA%\BuildOrchestrator` logs/cache/worktree köklerini kullanıyor; düzeltme için gereken seam A13/B4'te KURULDU ama bağlanmadı. Tek turda kapanır, reçete hazır. |
| **2** | **Ribbon "· N warnings" — ÖZELLİK boşluğu** | **T72** | Test boşluğu değil: IPC sözleşmesinde derleyici-warning ALANI yok. Ledger (`progress.md:1237-1238`) A14'e taşınmasını **öneriyor**; kullanıcı kararı bekliyor. |
| **3** | **`docs/TRUST-BOUNDARY.md` TAM TAZELEME** | **T79** | 53 doğrulanmamış atıf; ölçülen isabet **%76** (34 örnekten 26 doğru), hata `RunCoordinator`/`RunViewModel`/`EngineHost` ekseninde yoğun. Kontrolcü zaten ayrı task'a aldı. |
| **4** | **a11y ikinci turu** | **T01–T04** | T5 adları kapattı; peer'i olmayan yüzeyler, klavye erişimi ve `ISelectionItemProvider` açık kaldı — her biri yeni tip/DP ister. |
| **5** | **DPI taşınabilirliği triyajı** | **T33–T35** | B0'ın açtığı sınıf (`UseLayoutRounding` altında dar toleranslı eşitlik) süitte üç dosyada daha duruyor; B0 sweep'i bilerek tek dosyayla sınırlandı. |
| **6** | **DS Scrollbar Restyle** | **T78** | `.claude/outputs/2026-07-30-19-37-scrollbar-restyle-plan.md` (33 KB) **TAKİPSİZ** ve A13 kapsamı dışı bir iş kalemi. **KULLANICI KARAR VERDİ (2026-08-03): olduğu gibi kalacak** — takipsiz bırakılıyor, A13 merge'ü onu etkilemez. |
| **7** | **Merge sonrası doküman borcu** | **T74–T77, T80** | Kapalı bir iterasyonun kaydına uyarı bloğu · residue belgesi · 3 bayat TRUST-BOUNDARY refi · eski ledger satırı. |

---

## 1. KOVA — **KAPANDI** (commit doğrulandı)

| ID | Bulgu | `Konum:` (bugünkü HEAD) | Karar / Commit | Kaynak |
|---|---|---|---|---|
| **K01** | `CursorHoldMs` üretimde ÜÇ kopyaydı; ikisi `private` + PİNSİZ, sürüklenirse yakalanmazdı. | Tek tanım `src/BuildOrchestrator.App/Console/TypewriterScheduler.cs:36`; alias'lar `Console/ConsoleView.xaml.cs:37` · `Views/EventStreamView.xaml.cs:32` · `:334`; pin `tests/BuildOrchestrator.Tests/App/MotionOwnerHygieneTests.cs:54-68` | **KAPANDI — `46d6921`** (B3 k3). `git show --stat`: 4 dosya, `TypewriterScheduler.cs` +16. Kaynak guard'ı geri-inline'ı yakalıyor (mutasyonla kırmızı gösterildi). | ledger `progress.md:178-180` (T4 parked D11) |
| **K02** | `EngineHost.cs` XML doc'u OLMAYAN bir test adına atıf yapıyordu (`..._is_five_seconds`; gerçeği `..._stays_five_seconds`). | `src/BuildOrchestrator.App/Services/EngineHost.cs:48` ↔ `tests/BuildOrchestrator.Tests/App/EngineHostTests.cs:67` | **KAPANDI — `dbe6273`** (B3 k1). Diff birebir okundu: tek kelime düzeltmesi. | B1 lens2 L2-1 + lens1 L1-3 + lens3 L3-3 (**üç lens bağımsız**) |
| **K03** | `MainWindow.xaml`'deki MaxWidth 320/200 için "ölçülüdür" deniyordu ama **kırpmayı pinleyen test yoktu**. | `src/BuildOrchestrator.App/MainWindow.xaml:175` (320) · `:186` (200); pin `tests/BuildOrchestrator.Tests/App/TitleBarContextTests.cs:165-236` | **KAPANDI — `cf9993e`** (T3b b12) + `f8db289` (T3 fix-1) + `c245c4c` (T3 fix-2). *Kalemin "sayılar OTORİTESİZ" yarısı kapanmadı → **T45**.* | ledger `progress.md:108-109` (T2 minor, T3 GİRDİSİ) |
| **K04** | `debugSpawnChildren` (cmd.exe üzerinden çocuk doğuran test komutu) **üretim derlemesinde de dinleniyordu**. | Kapı `src/BuildOrchestrator.Supervisor/SupervisorHost.cs:242-243`; sabit `:47` (`DebugHooksArg = "--debug-hooks"`); parse `src/BuildOrchestrator.Supervisor/Program.cs:79`; pinler `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:171` (negatif) · `:279` (pozitif) · `:199-274` (hiç process doğmuyor) | **KAPANDI — `1704778`** (kapı) · **`887bdf0`** (iki taraflı ayırt edicilik) · **`4715cda`** (TRUST-BOUNDARY senkronu) · **`a0ac251`** (no-spawn pini). | `it5-records.md:435` **§2 #14** (D2) · `final-review-C-docs.md:104` |
| **K05** | 17 Minor'ın içindeki **`AutomationProperties.Name` eksiği (a11y)** — graf düğümlerinde 0 atıf. | `src/BuildOrchestrator.App/AccessibilityNames.cs` (yeni dosya) · `src/BuildOrchestrator.App/Graph/GraphView.xaml.cs:747` (`AutomationProperties.SetName(...)`) · kapsam testi `tests/BuildOrchestrator.Tests/App/AccessibilityTests.cs:385` (`MinimumScannedSurfaces`), `:462` | **KAPANDI — `de7efb8`** · **`96ef951`** · **`1243487`** · **`bd60378`** (T5 + T5 fix-1). *Aynı §2 satırının diğer ~16 Minor'ı KAPANMADI — `it5-records.md §2` satır #7 olduğu gibi geçerli.* | `it5-records.md:428` **§2 #7** (G2) · `final-review-C-docs.md:103` |
| **K06** | E6 getter testinin ikinci iddiası (`…and_never_writes_to_it`) pinlenmiş sayılmıyordu; **ad kanıtlanandan fazlasını söylüyordu**. | Test adı bugün `The_getter_reads_the_os_setting_without_writing_to_it`; aynalama guard'ı `tests/BuildOrchestrator.Tests/App/SystemParametersMotionSignalTests.cs:117` (`Assert.Matches`) | **KAPANDI — `bbd4e8d`** (B3 fix-1 e6). Doğrulama: `review-B3-fix1-lens1.md:131` — *"round-0'daki Minor m2'm de böylece kapandı"*; `review-B3-fix1-lens3.md:148-152` mutasyonla (getter `=> true`) guard'ı KIRMIZI gösterdi. | B3 lens1 m2 (+ lens3 Ö1 aynı eksende) |
| **K07** | Assert mesajı, doc'un "bugün olmaz" dediği sebebi **tanı olarak** öne sürüyordu (*"CollectRows onu sessizce düşürdü"*). | `tests/BuildOrchestrator.Tests/App/StickyRevealTriggerTests.cs:158` | **KAPANDI — `1459a33`** (B3 fix-2, kontrolcü düzeltmesi). Diff birebir okundu: mesaj sebep iddia etmeyen biçime çevrildi. | `review-B3-fix1-lens1.md` m1 |
| **K08** | `BuildApp.jsx:1378` atfı **kardeş doc'ta** düzeltilmemişti; iki üretim doc'u aynı otorite satırını ZIT niteliyordu. | `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs:110-116` (bugün `doSync():1190` ↔ `pickFolder():1378` ayrımı açıkça yazılı) | **KAPANDI — `1459a33`**. Diff birebir okundu (+6/−2 satır doc). | `review-B3-fix1-lens3.md` Ö1 (NOT-ADDRESSED olarak işaretlenmişti) |
| **K09** | Karar kaydı, A13.2'nin **repoya ait DAR okumasını** otoritenin metniymiş gibi aktarıyordu. | `src/BuildOrchestrator.App/ViewModels/RunViewModel.Workspace.cs:76-80` + kopyası `tests/BuildOrchestrator.Tests/App/ProjectListFilterTests.cs:268-270` | **KAPANDI — `1459a33`**. Bugün: *"Otoritenin metni niteliksizdir … Bu okuma repoya aittir, otoritenin metni DEĞİLDİR."* | `review-B3-fix1-lens1.md` Ö1 |
| **K10** | `DebugHooksCommandLine`'ın xmldoc'u `KillMidBuildTests`'i **tüketicisiymiş gibi** anıyordu; değildi. | `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:51-56` (bugün `SupervisorCommandLine`'a atıf; `KillMidBuildTests` ibaresi yok) | **KAPANDI — `a0ac251`**. Doğrulama: `review-B4-fix1-lensA.md:266` — *"ÇÖZÜLDÜ — xmldoc yeniden yazıldı"*. | B4 lensA M2 + lensB D2 (**iki lens**) |
| **K11** | `docs/TRUST-BOUNDARY.md`'de iki bayat ref; biri (`SupervisorHost.cs:53`) **B4'ün kendi sabitine** işaret ediyordu. | `docs/TRUST-BOUNDARY.md:95` → `SupervisorHost.cs:209` (`innerJob.Terminate()`) · `:98` → `:75` (`if (cmd is null) return 0; // stdin EOF`) | **KAPANDI — `0b72803`**. Doğrulama: `review-B4-fix1-lensA.md:116`, `:111`, `:268` — ikisi de koda karşı tek tek okundu. | B4 lensA M4 |
| **K12** | §1 tablosu ile §3, **aynı framing makinesi** için farklı koordinat veriyordu (`:45-57` ↔ `:70-74`). | `docs/TRUST-BOUNDARY.md:58` ve `:158` — ikisi de bugün `SupervisorHost.cs:70-74` | **KAPANDI — `3243d4a`**. Doğrulama: `review-B4-fix1-lensA.md:79-93`. | B4 lensA Ö2 + lensB Mi1 (**iki lens**) |
| **K13** | `RunCoordinator`'ın `EffectivePriorityLocked`'ı, simetriği diye tanıtılmasına rağmen **drain durumunu kontrol etmiyordu**. | `src/BuildOrchestrator.Supervisor/RunCoordinator.cs:350-354` (`CapWritableLocked => _perfApplied && !_capDrained`) | **KAPANDI — `1546783`** (2026-07-26). ⚠️ **A13 ÖNCESİ** — `git merge-base --is-ancestor 1546783 8e6ebbe` → YES. A13'ün işi değil, ama §2 satırı yine de geçersiz. | `it5-records.md:423` **§2 #2** (P3) · `final-review-C-docs.md:102` |
| **K14** | `CLAUDE.md`'de üç bayat olgusal ifade (`dotnet build` ↔ `MSBuild.exe` · test target `net10.0` ↔ `net10.0-windows`+`UseWPF` · "kod henüz yoktur"). | `CLAUDE.md:25` · `:21` · `:9` (üçü de bugün doğru) | **KAPANDI — `4bb6158`** (2026-07-30). ⚠️ **A13 ÖNCESİ** — `merge-base --is-ancestor` → YES. | `it5-records.md:438` **§2 #17** (D3) · `final-review-C-docs.md:106` |

---

## 2. KOVA — **TAŞINIYOR** (gerçek borç, A13 kapsamı dışı)

### 2.A · a11y ikinci turu (T5 kalıntıları) → **A14**

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T01** | `SplitButton`'ın sol yarımı ("Build"/"Continue") UIA'da **ADSIZ**; sağ yarımı adlı. | `src/BuildOrchestrator.App/Resources/Controls.xaml:269` (`PART_Primary`, `AutomationProperties.Name` yok) ↔ `:275` (`PART_Menu`, `Name="Build options"`) | Düzeltme DS şablonuna **yeni bir DependencyProperty** ister → T5'in "ad ekle, görünümü DEĞİŞTİRME" kısıtının dışında. Kapsam testinde gerekçeli istisna olarak duruyor. | T5 concern C1 |
| **T02** | Peer'i olmayan tıklanabilir `Border` satırlar (branch · build-menu · worktree hedef satırı · event stream · VS chooser) UIA ağacında **HİÇ yok**. | Kapsam testinin doc notu `tests/BuildOrchestrator.Tests/App/AccessibilityTests.cs:352` ve `:399` | n1'in kök nedeninin (düz `Border`/`StackPanel`'in automation peer'i yoktur) **beş ayrı yüzeyde tekrarı**; her biri `GraphNodeBody` sınıfından yeni bir tip ister. Ayrı a11y kalemi. | T5 concern C2 |
| **T03** | Graf düğümleri **klavyeden erişilemiyor** (`Focusable` hiç `true` değil, `KeyDown` yok). | `src/BuildOrchestrator.App/Graph/GraphView.xaml.cs:672-680` — tek giriş `MouseLeftButtonDown` → yerel `Toggle()`; aynı delege'yi UIA `Invoke` de çağırıyor | T5 fix-1 brief'i klavye erişimini **açıkça kapsam dışı** bıraktı; öneri kayıtlı (roving-tabindex + Enter/Space → aynı `Toggle` yolu). | T5 fix-1 concern C6 |
| **T04** | Düğümün kalıcı "seçili" durumu `ISelectionItemProvider` olmadığı için AT'ye **hiç açılmıyor**. | `src/BuildOrchestrator.App/Graph/GraphView.xaml.cs` → `ApplyNodeSelection` (lens koordinatı `:838-850`) | T5 fix-1 yalnız `IInvokeProvider`'ı kapsadı (rol↔yetenek uyumsuzluğu); seçim pattern'i ayrı yüzey. | T5 lens2 F2 |
| **T05** | `AccessibilityNames` ↔ XAML **ham literal kopyaları** (eskiden kalan 5 satır); C3 kataloğu `"Delete layer"`ı atlamış. | `src/BuildOrchestrator.App/Views/SettingsDialog.xaml:126` ve `:128` ham `"Delete layer"` ↔ tek-kaynak deseni `src/BuildOrchestrator.App/AccessibilityNames.cs:68` (`AddLayer`, `{x:Static}`) | Davranış değişmiyor; envanter + tek-kaynak toplaması. | T5 concern C3 + lens1 M1 |
| **T06** | `ActionBar` adlarını `Loaded`'da kurar; diğer **altı** yüzey ctor/production-path'te adlanır. | `src/BuildOrchestrator.App/Views/ActionBar.xaml.cs` (T5 raporu C4) | Lens2 ölçtü: bugün zararsız (üretimde `Loaded` her zaman koşuyor). Tutarlılık kalemi. | T5 concern C4 |
| **T07** | `MinimumScannedSurfaces = 40` eşiğinin **gerçek ölçülen marjı** hiçbir yerde yazılı değil; test XML doc'undaki "kapsam dışı" listesi raporun C2'sinden **bir madde geri** (VS chooser eksik). | `tests/BuildOrchestrator.Tests/App/AccessibilityTests.cs:385` (sabit) · `:462` (assert) · `:399` (liste) | Eşik bugün geçiyor; belgeleme kalemi. | T5 lens3 minor |
| **T08** | Üç ayrı kayıt kusuru: `"0 → 1 kart"` sayısı **yanlış** (gerçeği 2) · `Invoke`'un `disabled` dalı test edilmiyor · VS-chooser scope-touch raporda bildirilmemiş. | `tests/BuildOrchestrator.Tests/App/AccessibilityTests.cs:415` (kod yorumu, hâlâ `0 → 1`) + `task-T5-report.md:314-316`; scope-touch `AccessibilityTests.cs:356` | Sonuç **lehte** (kapsam iddia edilenden GENİŞ), yalnız sayı yanlış. | T5 fix-1 scoped re-review minor |
| **T09** | Settings-dialog kurulumu (n4 ↔ kapsam testi) ve worktree+switch kurulumu (n5 ↔ kapsam testi) **birebir tekrarlanıyor**; kapsam testi bölümü n1–n5'ten ~130 satır ayrı duruyor. | `tests/BuildOrchestrator.Tests/App/AccessibilityTests.cs` — n1–n5 bölümü ↔ `:340-475` kapsam testi bölümü | Dağınıklık; ortak fixture'a çıkarma işi. | T5 lens3 dağınıklık |

### 2.B · Test izolasyonu ve Supervisor test seam'leri (B4) → **A14, ÖNCELİK 1**

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T10** | **BEŞ test** kullanıcının GERÇEK `%LOCALAPPDATA%\BuildOrchestrator` logs/cache/worktree köklerini kullanıyor (`--logs`/`--worktrees` geçmeden Supervisor başlatıyorlar). | `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:67` · `:93` · `:117` · `tests/BuildOrchestrator.Tests/Supervisor/CascadeKillTests.cs:23` · `:65`. Mekanizma (**mevcut kod, değişmez**): `src/BuildOrchestrator.Supervisor/Program.cs:62-64` (`logsRoot` + koşulsuz `Directory.CreateDirectory`), `:73` (`WorktreeManager.DefaultPoolRoot`) | B4 brief kural 6 gereği (kapsam dışı kusur → **düzeltme, raporla**). **BEŞİ DE A13 ÖNCESİNDEN VAR**, B4 getirmedi. Reçete hazır: her iki çağrı yerinde `TestPaths.SupervisorCommandLine("--logs", <temp>, "--worktrees", <temp>)`. | `task-B4-report.md` concern 1 + lensA Ö4 + lensB Mi2 (**iki lens**) |
| **T11** | `DebugHooksCommandLine(params string[] extraArgs)`'ın `extraArgs`'ını geçen **SIFIR** çağıran var (speculative generality); ayrıca rapor §F7 bunu yanlışlıkla "artık kullanılıyor" diyor. | Seam `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:55-56`; çağıranlar `CascadeKillTests.cs:23`, `:65` — **ikisi de argümansız**. Fiilen kullanılan başka bir metot: `SupervisorCommandLine` (`SupervisorIpcTests.cs:48`, çağıran `:263`) | T10'un düzeltmesi tam olarak bu parametreyi kullanır → ikisi **aynı turda** kapanmalı. Rapor cümlesi de o turda düzeltilmeli. | B4 lensA M1 + lensB D1 + fix1-lensB Mi1 (**üç kayıt**) |
| **T12** | İzolasyon reçetesi (`CreateTempSubdirectory("bo-ipc-")` → `logs` → `worktrees`) artık **İKİ yerde inline kopya**. | `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:259-266` (`LaunchIsolatedSupervisorIn`) ↔ `:302-306` (`IsolatedPsi`) | İki sandbox tanımı ileride ayrışırsa **sessiz izolasyon deliği** olur. Çare: `TestPaths` içinde tek bir `IsolatedRoots()`. | B4 fix1-lensB Mi2 |
| **T13** | `SupervisorCommandLine` seam'i kuruldu ama **en doğal çağıranı bağlanmadı** → "ham komut satırı kur" üçüncü kopya olarak yaşıyor. | Seam `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:48-49`; bağlanmayan çağıran `tests/BuildOrchestrator.Tests/Supervisor/KillMidBuildTests.cs:46` (`WindowsCommandLine.Build(TestPaths.SupervisorExe, "--logs", logsDir)`) | Tek satırlık migrasyon, ama B4 kapsamı dışı. T10–T12 ile aynı turda. | B4 lensA O2 kalanı + fix1-lensB D1 |
| **T14** | `Directory.CreateTempSubdirectory("bo-ipc-")` sonucu **hiç silinmiyor**; koşum başına 10 dizin birikiyor. | `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:261` ve `:304` | Process/handle sızıntısı YOK (ölçüldü), sızan yalnız disk; mevcut desen. T12'nin yardımcısında birlikte çözülür. | B4 lensB Mi3 + fix1-lensB D2 |
| **T15** | `TimeSpan.FromSeconds(30)` literali, B1'in merkezîleştirdiği `TestPaths.WideStartupTimeout`'un yanında yaşamaya devam ediyor. | **Bugün ölçüldü — 9 yer:** `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:99, :122, :149, :348, :392, :395, :421, :424, :428` ↔ merkezî sabit `:25` (lens'in gördüğü 6 yerden bugün 9'a çıkmış) | B1 sweep'i **gerekçeli olarak** yalnız cold-boot sitelerini yamaladı (G3/G5 kararı: ısınmış process'in çıkışı ≠ cold start riski). Bu kalanlar DRY kalemi, fonksiyonel değil. | B1 lens2 L2-3 + B4 lensB D3 |

### 2.C · Supervisor CLI / argüman katmanı (B4)

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T16** | `Program.Main`'de **iki farklı argüman ayrıştırma şekli** yan yana: `GetArg` isim+değer ↔ `args.Contains` çıplak bayrak. | `src/BuildOrchestrator.Supervisor/Program.cs:62`, `:73` (`GetArg`) ↔ `:79` (`args.Contains`); `GetArg` tanımı `:367-371` | Tek çağıranı olan bir `HasFlag` helper'ı bugün **erken** olurdu (YAGNI). İkinci bayrak eklendiğinde extract edilmeli. | `task-B4-report.md` concern 4 |
| **T17** | Argüman **adının sahipliği** iki tipe bölündü: `--logs`/`--worktrees` `Program.cs`'te, `--debug-hooks` `SupervisorHost`'ta `public const`. Supervisor'ın CLI sözleşmesi tek yerde değil. | `src/BuildOrchestrator.Supervisor/SupervisorHost.cs:47` ↔ `src/BuildOrchestrator.Supervisor/Program.cs:62`, `:73`, `:79` | İtiraz sahipliğe değil **katmana**: `SupervisorHost` bir IPC-loop tipidir, bir CLI bayrağının adı onun sorumluluğu değil. Concern 4 bu ayrımı ıskalıyor. | B4 lensA O1 |

### 2.D · B1 kalıntıları (process/timeout)

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T18** | `await using` yok — `StartAsync` beklenmedik fırlarsa dispose atlanır. | `tests/BuildOrchestrator.Tests/App/AppShutdownTests.cs:101-102` (`var host = new EngineHost(...)` + `await host.StartAsync();`) | **PRE-EXISTING**; B1 artırmadı, yalnızca satıra dokundu. | B1 lens2 L2-2 |
| **T19** | Timeout guard'ı, asıl assert'i **gölgeliyor** (guard önce patlarsa sıralama iddiası hiç ölçülmez). | `tests/BuildOrchestrator.Tests/MsBuild/MsBuildInvokerTests.cs:182` (`invokeTask.WaitAsync(FromSeconds(20))`) ↔ asıl assert `:190` (`Assert.True(grandchildStillAlive, …)`) | Rapor C4'te zaten notlu, bağımsız doğrulandı; lens "aksiyon gerekmiyor" dedi. | B1 lens1 L1-4 |
| **T20** | Aynı IOCP-filtre döngüsü **iki kez kopyalanmış**; extract-method fırsatı kaçmış. | `tests/BuildOrchestrator.Tests/MsBuild/MsBuildInvokerTests.cs:169-180` (`:173` `if (born.MessageId != …) continue;`) ↔ `:247-258` (`:251` aynısı) — **bugün ölçüldü, kaymamış** | Organizasyon bulgusu; B1'in kapsamı sweep'ti. | B1 lens3 L3-1 |
| **T21** | `WideStartupTimeout` takma-ad alanı iki sınıfta var, üçüncüsünde doğrudan referans — üç farklı erişim biçimi. | `tests/BuildOrchestrator.Tests/Supervisor/SupervisorIpcTests.cs:25` (tanım) + iki takma ad | Fonksiyonel değil; DRY. | B1 lens3 L3-2 |
| **T22** | `StartAsync`'in timeout-catch'i `_generation`'ı **artırmadan** `KillCurrent()` çağırıyor → arka plan exit-watcher aynı gen'i geçerli görüp `EngineExited`'i de ateşleyebilir. | `src/BuildOrchestrator.App/Services/EngineHost.cs:103-105` (`catch { if (Volatile.Read(ref _generation) == gen) KillCurrent(); }`) — karşılaştır: framing yolu **artırıyor**, `:132-136` (`Interlocked.Increment(ref _generation); // exit watcher'ı sustur`) | B1'de **değişmeyen satırlar**; lens açıkça "sayılmadı → FINAL REVIEW TRİYAJINA" dedi (`progress.md:371-373`). İki yolun asimetrisi gerçek. | B1 lens2, "B1 DIŞI gözlem" |
| **T23** | Grup 4'ün `<= 2000 ms` kaskat iddiaları **yük altında kırılabilir ve YAMALANAMAZ** — çözüm spec tartışmasıdır, süre büyütmek değil. | `tests/BuildOrchestrator.Tests/Supervisor/CascadeKillTests.cs` (2 sn kaskat ölçütleri) + `tests/BuildOrchestrator.Tests/ProcessControl/JobObjectTests.cs:48` | Süreyi büyütmek testi **yok eder** (timeout'un KENDİSİ spec iddiası). Karar bir spec kararıdır. | B1 fix-1 concern C8 |
| **T24** | Sweep'in G5 grubundaki **67 site ölçülmedi**, gerekçeyle bırakıldı (post-boot / native-hafif child). | Sweep tablosu `task-B1-report.md` (95 site / 14 dosya) | Risk sınıfı farklı (cold-boot değil); ölçüm maliyeti gerekçeyi aşıyor. Re-review "DÜŞÜK risk" dedi. | B1 fix-2 concern C10 |

### 2.E · B2 kalıntıları (Türkçe guard + tokenizer)

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T25** | `"[hata]"` **ölü legacy girdi-token'ı** + bayat yorum; guard'da gerekçeli istisna olarak duruyor. | `src/BuildOrchestrator.App/Console/ConsoleLine.cs:86` (`content.StartsWith("[hata]", …)`) + yorum `:70` | Kaldırmak **DAVRANIŞ DEĞİŞİKLİĞİ** olurdu (B2 saf çeviri turuydu). | `task-B2-report.md` concern 1 |
| **T26** | İki döngü hâlâ neredeyse birebir aynı — B2 yalnız mesaj kopyasını (`DecideSkipped`) kaldırdı, döngüleri değil. | `src/BuildOrchestrator.Supervisor/RunCoordinator.cs:837` (`void DecideSkipped(...)` yerel fonksiyonu) → çağıranlar `:846` ve `:850` | **ÖNCEDEN VAR OLAN** organizasyon kusuru; B2'nin kapsamı çeviriydi. | `task-B2-report.md` concern 3 |
| **T27** | Test ağacında Türkçe exception mesajı (guard `tests/`'i **bilerek** taramıyor). | `tests/BuildOrchestrator.Tests/RepoPaths.cs:76` (`throw new InvalidOperationException(...)`) — lens `:77` demişti, bugün `:76` | B2 fix-1 kararı: guard `tests/`'e bakarsa er geç MEŞRU bir Türkçe dev-script metninde kırmızı verir ve biri **toptan istisna** ekler — It-5 sweep'ini öldüren kalıp buydu. | `task-B2-report.md` concern 4 + lens1 Important (S1) |
| **T28** | `verify-publish.ps1` çevirisi **GERÇEK KOŞUMLA doğrulanmadı** — yalnız string literalleri değişti, kontrol akışına dokunulmadı. | `scripts/verify-publish.ps1` (B2 commit `3702c5a`) | Script'i koşmak gerçek bir publish + Supervisor turu ister; B2 saf metin turuydu. | `task-B2-report.md` concern 5 |
| **T29** | Türkçe **kelime ekseni SEZGİSELDİR**; `.ps1`/`.csproj`'de (zorunlu ASCII, karakter ekseni orada hiç çalışmaz) bilinen sınırı var. | Guard `tests/BuildOrchestrator.Tests/App/NoTurkishUserTextTests.cs` (iki eksen) | Sınır testte AÇIKÇA dokümante edilmiş; daha güçlü hale getirmek yanlış-pozitif üretir. | `task-B2-report.md` concern 2 |
| **T30** | Tokenizer'ın beş kalan Minor'ı: XML karakter entity'leri (`&#xE0;`) çözülmüyor · `--%` ele alınmıyor · XML/PS yorum-elemesi **tüm-metin** regex'i (yapısal kırılgan) · çok satırlı literalde konum yalnız başlangıç satırı · testler arası tekrarlı tam-ağaç tokenize. | `tests/BuildOrchestrator.Tests/App/SourceLiterals.cs` — `FromXml` `:170`, `FromPowerShell` `:197`, `FromCSharp` `:45` | Hiçbiri bugün canlı değil (repoda tetikleyici desen yok); dar kapsam kuralı. | B2 lens2 M1–M5 |
| **T31** | Üç tur sonunda kalan dört **teorik** açık, üçü de bağımsız doğrulandı ve **CANLI DEĞİL**: N3 CDATA yorum-elemesini miras alıyor · N4 PowerShell'in `$( … "iç metin" … )` C1'i · N5 ham string çiti erken kapanış (yanlış-pozitif yönünde) · N6 `ignoredCallers` muafiyeti SATIR METNİNE bakıyor. | `tests/BuildOrchestrator.Tests/App/SourceLiterals.cs` (N3/N4/N5) · `SourceGuard.cs` (N6) | Repoda **sıfır isabet** (grep'lendi: 0 CDATA, 18 `$(...)`'ün hiçbirinde tetikleyici, `""""` yalnız fixture çiti, 4 `Debug.WriteLine`'ın dördü de kendi satırında yalnız). | B2 fix-2 Concerns N3–N6 |
| **T32** | `SourceGuard.Scan` ve `ScanLiterals` neredeyse **birebir kopya** dosya-gezme iskeleti. | `tests/BuildOrchestrator.Tests/App/SourceGuard.cs:44` (`private static Scan`) ↔ `:153` (`private static ScanLiterals`) | Ortak yardımcıya çıkarılabilir; davranış aynı. | B2 lens3 minor/dağınıklık |

### 2.F · B0 kalıntıları (DPI taşınabilirliği) → **A14, ÖNCELİK 5**

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T33** | **DPI-farkında OLMAYAN `precision: 1`** üç dosyada daha duruyor (±0,05 dip toleransı hiçbir DPI'da güvenilir tutmaz). | **Bugün ölçüldü:** `tests/BuildOrchestrator.Tests/App/ConsoleViewTests.cs:488-489` · `tests/BuildOrchestrator.Tests/App/PopoverTests.cs:303` · `tests/BuildOrchestrator.Tests/App/SettingsDialogFocusTests.cs:127` | B0 brief'i sweep'i **bilerek** `MainWindowRealizeTests` ile sınırladı (kapsam taşmasın diye). Ledger `progress.md:255-259` bu sınıfın TÜM süitte triyaj edilmesini istiyor → **ayrı task**. | B0 lens3 minor M1 |
| **T34** | Çıplak eşitlik — standart ölçeklerde güvenli, özel ölçekte kırılır (%111 → 39,64 dip). | `tests/BuildOrchestrator.Tests/App/MainWindowRealizeTests.cs:178` (`Assert.Equal(band, titleBar.ActualHeight)`) — lens `:175` demişti, fix'ten sonra `:178` | B0 fix round'u yalnız docstring çelişkisini aldı; süpürme kapsam dışıydı. T33 ile aynı turda. | B0 implementer concern C3 |
| **T35** | `AlignmentSteps` **her** `FrameworkElement` sıçramasını sayıyor — `VA=Stretch` (yuvarlama riski yok) ile `Center` (gerçek risk) ayrımı yapmıyor; ayrıca `ancestor` gerçekten atası değilse **sessizce** şişik sayı döner (guard yok). | `tests/BuildOrchestrator.Tests/App/MainWindowRealizeTests.cs:221-227`; tek çağrı yeri `:197` (`AlignmentSteps(logo, inner) / 2.0`) | Bugünkü tek çağrı yerinde doğru; ileride `Stretch` bir sarmalayıcı eklenirse bütçeyi gereğinden fazla şişirir. | B0 lens2 minor M1 + M2 |
| **T36** | Yeni yardımcılar, sınıfın diğer static'lerinin aksine **test metotlarının altına** eklenmiş. | `tests/BuildOrchestrator.Tests/App/MainWindowRealizeTests.cs:212` (`InDevicePixels`) · `:221` (`AlignmentSteps`) ↔ diğer static'ler `:34`, `:36` | Yalnızca yerleşim tutarlılığı; aksiyon gerekmiyor. | B0 lens3 minor M2 |
| **T37** | Logonun **KENDİ** `VerticalAlignment`'i pinlenemiyor — `Viewbox` tek başına `Top`'a çevrildiğinde test YEŞİL kalıyor (pinlenen şey konteyner `StackPanel`'in ortalanması). | `tests/BuildOrchestrator.Tests/App/MainWindowRealizeTests.cs:196-197` (drift/budget); üretim `src/BuildOrchestrator.App/MainWindow.xaml:147-163` | Eski assert de yakalamıyordu; ölçülebilir bir ayırt edicilik yolu bulunamadı. | B0 implementer concern C1 |
| **T38** | "İç kutuda ortalı" ile "bantta ortalı" **HİÇBİR DPI'da ayırt edilemez** (fark yarım hairline = 0,4 dip @125%, yuvarlama zemininin ALTINDA). T3 fix-2'nin niyet ettiği iddia **ölçüyle kanıtlanamaz**. | **Bugün ölçüldü:** `tests/BuildOrchestrator.Tests/App/MainWindowRealizeTests.cs:162` (*"**SINIR (bilerek):** 'iç kutuda ortalı' ile '40 bandında ortalı' arasındaki fark yarım…"*) | → **GÖZLE KONTROL KALEMİ**: `visual-check-residue.md`'ye girecek (bkz. T75). Ölçümle kapatılamaz. | B0 implementer concern C2 |

### 2.G · T1 / T2 / T3 kalıntıları

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T39** | `OnMotionChanged` imleç fade-out sürerken `StartBlink` çağırırsa `fade.Completed` ateşle(n)mez → `_cursorFading` **true kalır**, satır commit edilmez (~180 ms pencere, kendiliğinden iyileşir). | `src/BuildOrchestrator.App/Console/ConsoleView.xaml.cs:142-146` (`OnMotionChanged` → `StartBlink`) · `:484-486` (`_cursorFading = true` + `fade.Completed`) · alan `:76` | Kendiliğinden iyileşen dar pencere; T1'in kapsamı motion seam'iydi. | ledger `progress.md:15` (T1 minor deferred) |
| **T40** | `Unloaded` yalnız blink saatlerini bırakıyor; `_typeTimer`/`_cascadeTimer` unload sonrası tıklamaya devam ediyor (sonlu). | `src/BuildOrchestrator.App/Console/ConsoleView.xaml.cs:132` (`Unloaded += (_, _) => { StopBlink(); StopBuildBlink(); };`) · `_typeTimer` `:72` · `_cascadeTimer` `:92` | Fix ÖNCESİ **hiç teardown yoktu**; T1 net iyileştirme yaptı, kalanı sonlu. | ledger `progress.md:16` (T1 minor deferred) |
| **T41** | Realize testi hâlâ **üretim varsayılanı store** ile `MainWindow` kuruyor (bugün Save yolu sürülmüyor, zararsız). | `tests/BuildOrchestrator.Tests/App/MainWindowRealizeTests.cs:34` (`NewMainWindow` → `MainWindowHost.New(temp)`) | Bugün Save yolu sürülmediği için zararsız; T1'in kritiği (gerçek `ui-state.json` yazımı) zaten assertion'la engellendi. | ledger `progress.md:17` (T1 minor deferred) |
| **T42** | Üç test-altyapı kopyası: `NeverTickingBatcher`/`NewVm` (**T1'de 15 dosya; bugün ölçüldü: 27 test dosyası**) · `LatestPillTests.Wiring` ConsoleView kablaj kopyası · `SimulateHover` üretim seam'i artık gereksiz olabilir. | `tests/BuildOrchestrator.Tests/App/AccessibilityTests.cs:32` (ve 26 kardeşi) · `tests/BuildOrchestrator.Tests/App/LatestPillTests.cs:31-58` (`Wiring`, `:48` *"ConsoleView.xaml.cs'teki KABLAJIN BİREBİR AYNISI"*) · `src/BuildOrchestrator.App/Views/ProjectRow.xaml.cs:124` (`internal void SimulateHover`), çağıranlar yalnız `ProjectRowTests.cs:141,146,172,184,185,199` | Kopya **büyüyor** (15 → 27); ortak fixture'a çıkarma ayrı bir temizlik turu. | ledger `progress.md:18` (T1 minor deferred) |
| **T43** | Daktilo imleç konumu **KAYITSIZ SAPMA** — açık borç, dürüst etiketli ve karakterizasyon testiyle pinli. | `src/BuildOrchestrator.App/Console/ConsoleView.xaml.cs:389` (*"UYARI — bu otoriteden KAYITSIZ bir sapmadır"*) · `src/BuildOrchestrator.App/Console/ConsoleView.xaml:24` · test `tests/BuildOrchestrator.Tests/App/ConsoleViewTests.cs:408-410` | Kaydı olmayan bir sapma → **kullanıcı/otorite hakemliği** ister, kod düzeltmesi değil. | ledger `progress.md:140-141` (T3 minor deferred) |
| **T44** | `Resolved(scope,key)` yardımcısı **YARIM uygulandı**: dokuz assert ondan geçiyor, dört kardeşi çıplak `FindResource` kalmış. | **Bugün ölçüldü:** çıplak `tests/BuildOrchestrator.Tests/App/GraphCullTests.cs:272`, `:273`, `:334`, `:465`; yardımcı `:509` (`TryFindResource`) | Zararsız (`FindResource` bulamazsa fırlatır) ama kalem "tutarlı hale getirildi" diye kapanmış görünüyor. | T3 minor + B3 lens1 d2 + lens3 m7 (**üç kayıt**) |
| **T45** | MaxWidth **320/200 sayıları OTORİTESİZ** (sayılar üretimde kaldı; testler artık yalnız DAVRANIŞI pinliyor). | `src/BuildOrchestrator.App/MainWindow.xaml:175` (320) · `:186` (200); gerekçe yorumu `:170-171` | Otorite (design-v1) bu iki sayıyı vermiyor; karar kullanıcı/tasarım hakemliğidir. *Kalemin test yarısı K03'te kapandı.* | ledger `progress.md:144` (T3 minor deferred, b12) |
| **T46** | Ata-yürüyüşünde `includeLogical` farkı **bugün gözlemlenebilir değil** (savunmacı, korundu). | T3 fix-2 kapsamı (`1cc2e66`) | Savunmacı kod; bugün ayırt edici bir test yazılamıyor. | ledger `progress.md:143` (T3 minor deferred) |
| **T47** | **İki paralel `WallClock` seam'i** var: `ConsoleView` kendi property'sini taşıyor, `EventStreamView` VM'inkini okuyor. | `src/BuildOrchestrator.App/Console/ConsoleView.xaml.cs:89` (`internal Func<DateTimeOffset> WallClock`, ctor'da VM'inkine bağlanır — `:85`) ↔ `src/BuildOrchestrator.App/ViewModels/RunViewModel.Stream.cs:28` (aynı imza) ↔ tüketici `src/BuildOrchestrator.App/Views/EventStreamView.xaml.cs:228` | Dağınıklık; ikisi bugün birbirine bağlı olduğu için davranışsal risk yok. *Bu satır, T3'ün "EventStreamView saati hâlâ doğrudan okuyor" kaydının **ölçülmüş** hâlidir — bkz. **G09**.* | ledger `progress.md:142` (T3 minor deferred), **ölçümle yeniden ifade edildi** |
| **T48** | Rebuild'de kart reveal'i artık oynamıyor (prototipte `revealKey` run başında da artar). | `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs` reveal tetiği (`:261` `BeginInvoke(Loaded, PlayRevealStagger)`), yalnız topoloji değişiminde | T2'de bilinçli kapsam dışı. T73 (B3 C8) ile **aynı aileden**: üretim ≠ prototip, karar dönüşü ister. | ledger `progress.md:29` (T2 minor deferred) |
| **T49** | 2.4'ün query varyantı (`No projects match "{query}".`) **UYDURULMADI** — design-v1 yalnız tek metni şart koşuyor. | `src/BuildOrchestrator.App` no-match metni (T2 kalemi 2.4) | Otorite tek metin veriyor; ikinci varyantı uydurmak spec ihlali olurdu. | ledger `progress.md:30` (T2 minor deferred) |

### 2.H · B3 kalıntıları (reveal / motion sinyali)

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T50** | "App kaynağından tek dosya oku" kalıbının **elle kopyaları**; `SourceGuard.ReadApp(relative)` tek-yer önerisi. | `tests/BuildOrchestrator.Tests/App/SystemParametersMotionSignalTests.cs:83-84` ↔ `tests/BuildOrchestrator.Tests/App/ShellLayoutTests.cs:159`; lens'in saydığı diğerleri (`StartupPathTests`, `AntiSlopTests`, `ContrastTests`, `RestoreGlyphTests`, `SuccessFlourishTests`) inline `File.ReadAllText(Path.Combine(RepoPaths.AppSrcRoot, …))` biçiminde | Evi `SourceGuard`'tır ama "tek dosyayı oku" üyesi yok; B3 kapsamı dışı. | B3 lens1 d1 + lens3 D1 (+ fix1-lens3 D1) |
| **T51** | **Sahte vakum guard'ı**: derleme-zamanı boş olamayan dizi literaline `Assert.NotEmpty`. | `tests/BuildOrchestrator.Tests/App/SystemParametersMotionSignalTests.cs:53` (`Assert.NotEmpty(others)`) — karşılaştır: `:110` `Assert.NotEmpty(hits)` **GERÇEK** bir vakum guard'ıdır | Zararsız ama "vakum guard'ı" kalıbını ucuzlatıyor; ayrım yorumlarda yapılmalı. | B3 lens3 m2 |
| **T52** | `const` ↔ `const` assert'leri **derleme-zamanı totolojisi** (derleyici hepsini `420.0` gömer). | `tests/BuildOrchestrator.Tests/App/MotionOwnerHygieneTests.cs:59-61` — yükü taşıyan iki assert `:56` (otorite literali) ve `:64-67` (kaynak guard'ı), ikisi de mutasyonla KIRMIZI doğrulandı | Zararsız; asıl koruma zaten kaynak guard'ında. | B3 lens3 m3 |
| **T53** | Biçime duyarlı **ham metin eşleşmesi** — boşluk/parantez düzenlemesi testi kırar. | `tests/BuildOrchestrator.Tests/App/SystemParametersMotionSignalTests.cs:92` (`Assert.Contains("if (IsMotionProperty(e.PropertyName))", …)`) | Repo deseniyle tutarlı (`StartupPathTests` aynısını yapıyor); yalnız kayıt için. | B3 lens1 m3 + lens3 m4 |
| **T54** | E5 testinin **ön-koşulu, ölçtüğü metodun kendisiyle** ölçülüyor. | `tests/BuildOrchestrator.Tests/App/StickyRevealTriggerTests.cs:155` (`list.RevealRows`) → `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs:359` (`RevealRows => CollectRows()`) | Bağımsız bir ölçüt (`Flow.Items` sayısı / `ItemContainerGenerator` durumu) ön-koşulu gerçek bir çapa yapardı. | B3 lens3 m5 |
| **T55** | Süreç ömrü boyunca yaşayan **statik-event aboneliği** + sınıf `[Collection("Console UI (serial)")]` taşımıyor (karışık konvansiyon). | `tests/BuildOrchestrator.Tests/App/SystemParametersMotionSignalTests.cs:17` (sınıf, collection attribute YOK) · `:70` (`[StaFact]` + `new SystemParametersMotionSignal()`); üretim ctor'u `src/BuildOrchestrator.App/Services/SystemParametersMotionSignal.cs:12-15` (hiç unsubscribe etmiyor, bilerek belgeli) | Tek örnek, ihmal edilebilir — ama tip çoğaltılırsa birikir. C7 flake'i için **aday olarak işaretlendi** (kanıt yok). | B3 lens2 M6 + lens3 m6 (**iki lens**) |
| **T56** | Getter testinin dört kaydı: doc **"Üç assert"** diyor ama **iki** assert var · `Assert.Equal(x, x)` düz totoloji · "OS'a yazılmadı" assert'i tip sisteminin zaten yasakladığını koruyor · aynalama regex'i **ifade-gövdeli** üyeye kilitli. | `tests/BuildOrchestrator.Tests/App/SystemParametersMotionSignalTests.cs:63-64` (doc) · `:77`, `:78` (assert'ler) · `:117` (regex) | Testin bugün pinlediği tek şey "ctor + iki okuma fırlatmıyor" — meşru smoke test; doc daraltılmalı. | B3 fix1-lens3 m2, m3, m4, m5 |
| **T57** | `CollectRows` doc'unun garanti cümlesi **koşulsuz** yazılmış (ağacın o an ölçülebilir olmasına bağlıdır; 2. kademe kaldırıldığı için telafi de yok). | `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs:321-322` (*"…tek bir tur tüm satırları realize eder"*) | Tek kelimelik çare: *"…ağaç ölçülebilir olduğu sürece…"*. Rapor C2 zaten doğru anlatıyor. | B3 fix1-lens1 m2 + fix1-lens3 m1 (**iki lens**) |
| **T58** | E6 "artık" listesi: ctor aboneliği · canlı `Changed` zinciri · unsubscribe yokluğu · **getter'ın HANGİ OS property'sini okuduğu davranışsal olarak pinlenemez** (makine-global ayar çevrilemez). | `src/BuildOrchestrator.App/Services/SystemParametersMotionSignal.cs:12-15`, `:18`; aynalamayı koruyan tek şey `SystemParametersMotionSignalTests.cs:117` kaynak guard'ı | Kullanıcı kararı: makine-global durum değiştirilmeyecek → davranışsal pin **imkânsız**. | `task-B3-report.md` concern C1 |
| **T59** | `CollectRows` hâlâ **kısmi sonuç dönebilir**; "eksikse kabul etme" kuralı ÇAĞIRANDA yaşıyor (tek kapı yok). | `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs:299` (çağıranın layout zorlaması) ↔ `:330` (`CollectRows`) ↔ `:359` (`RevealRows`, kuralı **uygulamıyor**) | İkinci çağıran bugün yalnız test yüzeyi → zararsız; `TryCollectAllRows(out …)` gibi tek kapı öneriliyor. | `task-B3-report.md` concern C2 + lens1 d3/d5 + lens2 D4 + lens3 D3 |
| **T60** | `MainWindowHost.NewWithProjects` motion seam'i için **giriş noktası sunmuyor** (`New`'in `beforeVm`'i var, karşılığı yok). | `tests/BuildOrchestrator.Tests/App/MainWindowHost.cs` (`New` ↔ `NewWithProjects`) | Kapsam dışı; T6'nın eklediği `beforeVm` hook'unun kardeşi henüz gerekmedi. | `task-B3-report.md` concern C4 |
| **T61** | `ProjectRow.PlayReveal`'in **reduced-motion dalı gözlenebilir iz bırakmıyor** → reveal kapsamı o yolda BUGÜN ÖLÇÜLEMEZ. | `src/BuildOrchestrator.App/Views/ProjectRow.xaml.cs` `PlayReveal` (lens koordinatı `:430-456`); ilgili `src/BuildOrchestrator.App/Controls/RevealStagger.cs:76` (`if (_hero is null) return;`) | Bu belirsizlik B3 fix round 1'de **bir kez daha ısırdı** (hatalı S9 okumasının kökü). Gözlenebilir iz eklemek üretim değişikliğidir. | `task-B3-report.md` concern C5 |
| **T62** | Aynı satırın **İKİ KEZ `PlayReveal`** alma olasılığı B3 ile **arttı** (1. kademe realizasyonu zorladığı için artık iki çağrı da oynayabilir). | `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs:299` (`Flow.UpdateLayout()`, yeni) + `:163` (**mevcut kod, değişmez** — iptal edilmeyen ikinci `BeginInvoke`); etkisi `src/BuildOrchestrator.App/Views/ProjectRow.xaml.cs` `PlayReveal` (görünür yeniden-başlatma) | **Canlılığı gösterilemedi**: tek bir `Loaded` penceresinde iki `SetGroups(reveal:true)` gerekir; `_lastTopologySignature` guard'ı aynı topolojiyi zaten eliyor. | B3 lens2 M4 |

### 2.I · T6 / `verify-publish.ps1` kalıntıları

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T63** | Zaman aşımı sonrası `ReadLineAsync()` görevi **iptal edilmiyor**. | `scripts/verify-publish.ps1:225` (`$readTask = $runSup.StandardOutput.ReadLineAsync()`) | Script hijyeni; T6'nın kalemi Sync+Build adımını eklemekti. | T6 lens2 minor M1 |
| **T64** | Geçici klasör temizliği **best-effort**; dosya kilidinde sessizce artık kalabilir. | `scripts/verify-publish.ps1:370-371` (`Remove-Item -Recurse -Force … -ErrorAction SilentlyContinue`) | Mevcut `$OutputDir` deseniyle aynı; yeni risk değil. | T6 lens2 minor M2 |
| **T65** | **Sınırsız bloklayan** `ReadLine()` — T6 diff'inin parçası DEĞİL (mevcut kod), yine de işaretlendi. | `scripts/verify-publish.ps1:131` (`$firstLine = $sup.StandardOutput.ReadLine()`) | Kapsam dışı; T6 yeni adımını `$BuildTimeoutSeconds` ile zaman-sınırlı yaptı ama eskisine dokunmadı. | T6 lens2 kapsam dışı not |
| **T66** | Yeni `SendCommand` yardımcısı, adım 3'ün eski **ham-byte NDJSON** yazımına retrofit edilmemiş → iki paralel yol. | `scripts/verify-publish.ps1:63` (`function SendCommand`) ↔ adım 3'ün ham yazımı (`:127-129` civarı) | Dağınıklık; T6'nın BOM düzeltmesi zaten kapsam sınırındaydı. | T6 lens3 minor 2 |
| **T67** | Script BOM'suz + PS 5.1: **çift tırnaklı** string'te em dash parse'ı bozuyor; kalıcı biçim kararı kullanıcıda. | `scripts/verify-publish.ps1` (BOM düzeltmesi `:87-99`) | Biçim kararı (BOM'lu mu, ASCII-only mi) kullanıcınındır. | T6 concern C4 |
| **T68** | **Makine-global** artık liste: tepsi ikonu kurulumu · `Alt+B` global hotkey kaydı · single-instance mutex · `FontAbWindow.Show()` — dördü de TEST EDİLMEDİ. | `src/BuildOrchestrator.App/MainWindow.xaml.cs:690-714` (`OnSourceInitialized`) · `src/BuildOrchestrator.App/App.xaml.cs` (font-ab dalı) | **Kullanıcı kararı gereği**: testler makine-global durumu (registry/tepsi/mutex/hotkey) değiştirmez. Lens2 bu sınırın korunduğunu bağımsız doğruladı. | T6 concern C2 |
| **T69** | `TokenBrushesTests` **asimetrisi**: `TextFaint` hex'i pinli, `TextDim` değil. | **Bugün ölçüldü:** `tests/BuildOrchestrator.Tests/App/TokenBrushesTests.cs:26` (`Brush.TextFaint` = `#54545c`); `Brush.TextDim` için karşılık **YOK** | T6 kapsamı dışıydı; kontrast oranları ayrıca `ContrastTests`'te pinli (T6 t3). | T6 concern C5 |
| **T70** | Brief'in bazı `dosya:satır` kanıtları **BASELINE DRIFT** nedeniyle eskimiş (t1: `662-663 → 768`, t4: `334-336 → 335-337`). | `task-T6-brief.md` (kontrolcünün brief'i) | **Kod hatası değil** — kontrolcünün brief'inin kanıtı eskimiş. Kayıt için. | T6 lens1 minor |
| **T71** | `StartupPathTests` için ayrı RED kanıtı raporda yok (3 satırlık saf fonksiyon). | `tests/BuildOrchestrator.Tests/App/StartupPathTests.cs:57-68` | Saf fonksiyon; diğer dört kalemin RED'i alınmıştı. | T6 lens3 minor 1 |

### 2.J · Özellik boşlukları — **kullanıcı kararı gerekiyor**

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T72** | Ribbon'daki canlı **"· N warnings"** üretimde KABLOSUZ: sabit `warnings: 0` yazılıyor ve IPC sözleşmesinde derleyici-warning ALANI **hiç yok**. | `src/BuildOrchestrator.App/Views/StickyRibbon.xaml.cs:211` (`… checkDurMs: _vm.ElapsedMs, warnings: 0, …`) + `:213` (`NOT (wire gap)` yorumu); karakterizasyon testi bugünkü dürüst davranışı pinliyor | **TEST boşluğu DEĞİL, ÖZELLİK boşluğu** (log-parse + IPC alanı + VM + kablo). Pozitif iddia bir testle kurulamazdı. Ledger `progress.md:1237-1238`: **A14'e taşınması ÖNERİLİYOR.** | T6 kalem t5 (KAPANAMADI) + kullanıcı kararı bekleyen kalem (b) |
| **T73** | **ÜRETİM ≠ OTORİTE**: prototipte `doSync()` `revealKey`'i HER Sync'te KOŞULSUZ artırır; üretim yalnız topoloji imzası değişince reveal oynatır. | Kayıt `src/BuildOrchestrator.App/ViewModels/RunViewModel.Workspace.cs:83-89`; test doc'u `tests/BuildOrchestrator.Tests/App/ProjectListFilterTests.cs:269-275`; otorite `.claude/outputs/2026-07-15-19-00-design-v1/prototype/app/BuildApp.jsx:1186-1193` (artış `:1190`) | Bilinçli A13.2 (churn) kararı — düzeltme değil, **karar dönüşü** olur. Kullanıcı otorite paritesini isterse açılır. | `task-B3-report.md` concern C8 |

### 2.K · Doküman borcu (merge sonrası)

| ID | Bulgu | `Konum:` | Neden şimdi değil | Kaynak |
|---|---|---|---|---|
| **T74** | `it5-records.md` §2'ye, **6 satırın artık geçersiz** olduğunu söyleyen uyarı bloğu eklenmeli (satırlar SİLİNMEZ — tablo tarihsel kayıttır). | `.claude/outputs/2026-07-26-10-17-it5-records.md:418`'in hemen ARDINA; ilgili satırlar `:423` (#2) · `:428` (#7) · `:435` (#14) · `:436` (#15) · `:438` (#17) · `:439` (#18) | Kapalı bir iterasyonun kabul kaydı; doğru yer bu triyaj belgesiydi (yazıldı) — uyarı bloğu ona atıf verecek. | `final-review-C-docs.md` §2 / **S1** |
| **T75** | `visual-check-residue.md` (yalnız göz-ister kalemler, uygulama gezinti sırasıyla) + "X yeni test, gezintinin Y/228 alt-kalemini pinliyor" raporu **yazılmadı**. | Plan `.claude/outputs/2026-07-30-14-59-visual-check-automation-tdd-plan.md:197-202`; girdi sermayesi `.claude/outputs/2026-07-30-18-45-a13-inventory-appendix.md` §A (10 göz-ister + 4 ek) | Bilinçli borç, unutulmuş değil (`.claude/summaries/2026-07-31-23-02-…:140-146` zaten listeliyor). **T38 bu belgeye girecek.** | `final-review-C-docs.md` §4.3 / **S2** |
| **T76** | `docs/TRUST-BOUNDARY.md`'de üç bayat ref: `App.xaml.cs:55-60` → `:57-63` · `EngineHost.cs:66-78` → `:129-140` · `RunCoordinator.cs:304` → `:307`. | `docs/TRUST-BOUNDARY.md:125` · `:162` · `:96` | İkisi **A13 ÖNCESİNDEN** bayat; belgenin `:15` uyarısı bunları zaten kapsıyor. Merge'ü bloklamaz. | `final-review-C-docs.md` M1/M2/M3 / **S3** |
| **T77** | İki kozmetik doküman kalemi: `README.md:222` dil iddiasına guard atfı (`NoTurkishUserTextTests`) eklenebilir · `a13-inventory-appendix.md:158-159` bayat (ölçüldü: `Ds.Dialog` `surface-raised` **KAYITLI SAPMA**dır). | `README.md:222` · `.claude/outputs/2026-07-30-18-45-a13-inventory-appendix.md:158-159`; kanıt `src/BuildOrchestrator.App/Resources/Controls.xaml:663` (*"SAPMA (T60 brief'inde KAYITLI)"*, otorite `_ds_bundle.js:965`) | Kozmetik. | `final-review-C-docs.md` M4/M6 / **S4** |
| **T78** | `.claude/outputs/2026-07-30-19-37-scrollbar-restyle-plan.md` (33 KB, `DS Scrollbar Restyle` implementation plan) **TAKİPSİZ** — hiçbir commit'te yok. | Repo kökü; `git status` → `?? .claude/outputs/2026-07-30-19-37-scrollbar-restyle-plan.md` | İçeriği **A13 kapsamı dışı, ayrı bir iş kalemi (A14 adayı)**. Ya commit'lensin (`docs(a14): …`) ya bilinçli olarak `.claude/temp/`'e taşınsın — **karar kullanıcınındır**, sessizce bırakılmasın. | `final-review-C-docs.md` M5 / **S5** |
| **T79** | `docs/TRUST-BOUNDARY.md` **TAM TAZELEME**: 53 atıf bu branch'in dokunduğu dosyalara gidiyor ve yeniden doğrulanmadı. | `docs/TRUST-BOUNDARY.md` geneli (137 benzersiz `dosya:satır` refi, 38 dosya; 16'sı bu branch'te değişti). Bilinen bayat örnekler `docs/TRUST-BOUNDARY.md:15`'te yazılı | **ÖLÇÜLDÜ: %76 isabet** (34 örneklenen ref'in 26'sı doğru); hatalar `RunCoordinator.cs` / `RunViewModel*.cs` / `EngineHost.cs` ekseninde yoğun, `Core/*` tarafı örneklemede **%100**. Kontrolcü zaten **ayrı task**'a aldı. | `final-review-C-docs.md` §4.7 / **S6** + `task-B4-report.md` concern 2 |
| **T80** | **ESKİ düz yoldaki başka ledger**, `debugSpawnChildren`'ı hâlâ **açık D2** kalemi diye kaydediyor (A13/B4 kapattı — bkz. **K04**). | `.superpowers/sdd/progress.md:385-387` (*"Task D2: FINAL REVIEW'A TASINDI … `SupervisorHost.cs:73`"*) | Ledger orchestrator'ın sorumluluğu; B4 implementer'ı doğru davranıp dokunmadı. | `task-B4-report.md` concern 3 |

---

## 3. KOVA — **KAPATILDI-GEÇERSİZ** (ölçüldü; gerçek çıkmadı ya da başka bir değişiklik anlamsız kıldı)

| ID | Bulgu (park edildiği hâliyle) | `Konum:` | **Ne ölçüldü** | Kaynak |
|---|---|---|---|---|
| **G01** | Telafi 2. kademesine (`ArmRevealCatchUp` kancası) bağlı **altı ayrı park kalemi**: kancanın maliyeti "layout turu başına, süresiz" (M2) · `_revealCatchUp` alanı metotların ortasında (D2) · kancanın test süreçlerine sızması (M3) · "…ya da HİÇ tüketilmez" garantisinin iki düşürme yolu (Ö1 2. yarısı) · telafi yaşam döngüsünün üç yere dağılması (d4) · `RowEntryCount()`'un ikinci O(n) turu (D1'in yarısı). | Kaldırılan üyeler: `ArmRevealCatchUp` · `DisarmRevealCatchUp` · `_revealCatchUp` · `RowEntryCount()` · `LayoutUpdated` aboneliği — bugün `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs`'te **hiçbiri yok** | **2. kademe SIFIR KAPSAMLIYDI:** gövdeye `throw` konup TAM SÜİT koşuldu → **1643/1643 YEŞİL** (hiçbir test o dalı sürmüyordu). Bunun üzerine mekanizma tümüyle **SİLİNDİ** (`8628886`); söküm sonrası `git grep` src+tests → **0 eşleşme**, kalıntı yok. Altı kalem de **KONUSUZ** kaldı. | B3 lens3 K2 · lens2 M2/D2/M3 · lens1 Ö1/d4 · lens2 D1 (**üç lens**) |
| **G02** | k2'nin premisi: `GraphCullTests`'te sessiz `Assert.Equal(null, null)` **vakumu** olduğu varsayılıyordu. | `tests/BuildOrchestrator.Tests/App/GraphCullTests.cs` (baseline `e2e50aa`) | **VAKUM HİÇ MEVCUT DEĞİLDİ:** `git show e2e50aa:…/GraphCullTests.cs` içinde `TryFindResource` **çağrısı yok** (yalnız `:448`'de bir yorum geçiyor); tüm kaynak assert'leri `FindResource` ile ve o bulamazsa **FIRLATIR**. İki lens bağımsız doğruladı. Kazanç yalnız **hata mesajı**. *Dönüşümün yarım kalması ayrı kalem → **T44**.* | B3 lens1 m4 + `task-B3-report.md` concern C6 |
| **G03** | `Flow.UpdateLayout()`'un maliyeti 500+ satırda **ölçülmemişti** (C3). | `src/BuildOrchestrator.App/Controls/StickyLayerList.xaml.cs:299` (çağrı) + gerekçe yorumu `:296-298` | **ÖLÇÜLDÜ** (izole net10.0-windows WPF probe, gerçek HWND): n=50 → **0,012 ms** · n=200 → **0,052 ms** · n=500 → **0,225 ms** (`CollectRows` yürüyüşü dâhil) — `ListRealizationPerfTests`'in 191-satır/400 ms bütçesinin **~3 büyüklük altında**. Gerekçe de düzeltildi: "Flow'un layout'u ucuz" değil, *"zaten temiz bir ağaçta context turu ≈ 0"* — koda işlendi. | `task-B3-report.md` concern C3 (lens2 P10 ölçümü) |
| **G04** | İlk tam koşumda **kimliği belirlenemeyen 1 FLAKE** (C7). | `tests/BuildOrchestrator.Tests/App/MotionOwnerHygieneTests.cs:64-67` · `tests/BuildOrchestrator.Tests/App/SystemParametersMotionSignalTests.cs:110`, `:117` (iki **kaynak-tarayan** guard) | **AÇIKLANDI:** bu iki guard, koşum sırasında kaynak dosyalar geçici olarak mutasyonluysa kırmızı verir. lens1 bunu **bizzat gözledi** (paralel bir lens'in geçici mutasyonu ikisini birden düşürdü); lens3 **üç tam koşumda** (1643/1643 ×3) tekrarlatamadı. Raporun tek izi (`MethodBaseInvoker.InvokeWithNoArgs`) **ayırt edici değil** — kasten kırılan üç farklı testte de aynı frame çıktı. Ders kayda geçti: ayırt edicilik denemeleri sırasında paralel tam-süit koşulmaz. | `task-B3-report.md` concern C7 |
| **G05** | Şüphe: `ProjectRow`'un automation peer'i **olmayabilir** (n1'in kök nedeninin, "adı var, bozma" listesindeki bir yüzeyde tekrarı) → C5 olarak açılacaktı. | `src/BuildOrchestrator.App/Views/ProjectRow.xaml.cs` | **ÖLÇÜMLE ÇÜRÜTÜLDÜ:** peer **VAR** (`UserControlAutomationPeer`), `peer.GetName()` = `"OSYS.Base"`, controlType `Custom`. Kök neden burada tekrarlanmıyor → **üretim kusuru YOK, C5 hiç açılmadı**. Yan ürün: kapsam testinin `ItemsControl` barındıran yüzeyler için **kısmen vakum** olduğu bulundu ve düzeltildi (`bd60378`, veri-sonrası layout turu). | T5 fix-1 concern C5 (AÇILMADI) |
| **G06** | `cmd.exe` bölgesi için belgede **üç farklı aralık** veriliyor (`:250-251` / `:250-252` / `:250-256`) — tek gerçeğe üç numara. | `docs/TRUST-BOUNDARY.md:310` · `:190` · `:109` | **ÜÇÜ DE DOĞRU ve FARKLI aralıkları tarif ediyor:** yalnız komut satırı / +`Launch` / +`catch (Win32Exception)` (breakaway probe). Re-review: *"Kusur değil, kapatıyorum"*. | B4 lensA M3 → `review-B4-fix1-lensA.md:267` |
| **G07** | It-5 park listesindeki "BİR KEZ" guard'ının referansı `SuccessFlourishTests.cs:295-297`. | `.claude/outputs/2026-07-26-10-17-it5-records.md:436` (§2 #15) | **REFERANS GEÇERSİZ:** guard bugün `tests/BuildOrchestrator.Tests/App/SuccessFlourishTests.cs:322-326` (`new Regex("RepeatBehavior|AutoReverse", …)`, çağrı `:326`). Ledger `progress.md:589` bu referansı zaten "yanlış" diye işaretlemişti; A13/T4 `d855664` dosyayı büyütünce daha da kaydı. **Altındaki DÜRÜST SINIR (guard bir kaynak-deseni denetimidir, ispat değil) hâlâ geçerlidir** — geçersiz olan yalnız koordinat. | `it5-records.md §2 #15` · `final-review-C-docs.md:105` |
| **G08** | "Tüm GÖZLE KONTROL borcu — tek yürünebilir liste `visual-check-walkthrough.md`" (It-5'in en büyük park kalemi). | `.claude/outputs/2026-07-26-10-17-it5-records.md:439` (§2 #18) | **BÜYÜK ÖLÇÜDE GEÇERSİZ — A13'ün TAMAMI bu satırı hedefledi.** Envanter (`.claude/outputs/2026-07-30-18-45-a13-inventory-appendix.md:12-17`) 228 alt-kalem ölçtü: 162 pinli + 56 testsiz + 10 göz-ister. T1 12 tetikleyici kalemini (`ae2c832..214e290`) · T3a/T3b/T3c kopya+ölçü · T4 motion+negatif guard · T5 a11y · T6 kalanlar kapattı. Bugün geriye **yalnız 10 "göz ister" + 4 ek** kalıyor (appendix §A) → `visual-check-residue.md` (**T75**). | `it5-records.md §2 #18` · `final-review-C-docs.md:107` |
| **G09** | "`EventStreamView` saati hâlâ **doğrudan okuyor** (`WallClock` seam'ini kullanmıyor)". | `src/BuildOrchestrator.App/Views/EventStreamView.xaml.cs:228` | **BUGÜN DOĞRU DEĞİL — ölçüldü:** satır `Console.WallClockFormat.Of(_vm.WallClock())`; saat `src/BuildOrchestrator.App/ViewModels/RunViewModel.Stream.cs:28` (`internal Func<DateTimeOffset> WallClock`) seam'inden geliyor ve testte deterministik. `git show 8e6ebbe:…` — **BASE'de de `_vm.WallClock()` okuyordu**; T3'ün değiştirdiği yalnız FORMAT'tı (`WallClockFormat.Of`). Kalan gerçek kalem **iki paralel seam** → **T47**. | ledger `progress.md:142` (T3 minor deferred) |
| **G10** | E4 karakterizasyon testinin "topoloji gerçekten tüketildi" ön-koşulu **ayrıştırıcı değil** (fixture zaten o satırları üretiyor). | `tests/BuildOrchestrator.Tests/App/ProjectListFilterTests.cs:279` içindeki `Assert.Equal(new[] { "Alpha", "Beta", "Gamma" }, VisibleRowNames(list))` | **CANLI DEĞİL — ölçüldü:** `RunViewModel.OnEvent` (`src/BuildOrchestrator.App/ViewModels/RunViewModel.cs:744`) senkron bir `switch`'tir, **event düşürülemez**. Gerçek vakum guard'ı `afterTopology > 0` zaten var ve mutasyonla (M-C) KIRMIZI doğrulandı. Kalan yalnız raporun ifadesinin olduğundan güçlü olması. | B3 lens2 M5 |
| **G11** | T4 brief'inin iddiası: *"mevcut testler `ShakeTranslate`'in YANLIŞ eksenini (Y) okuyor"*. | `src/BuildOrchestrator.App/Views/ProjectRow.xaml.cs` `BuildShakeAnimation` / `PART_ShakeTranslate` | **BULGU GERÇEK ÇIKMADI — kontrolcünün hatası:** lens1 ölçtü, implementer'ın teşhisi (**aynı transform reveal'de Y, shake'te X taşır**) DOĞRUydu. Brief hatası kayda geçirildi. | ledger `progress.md:162-164` (T4 BRIEF HATASI) |
| **G12** | B1 lens1 Important 1: *"3 ardışık koşum hepsi 0 failed" literal karşılanmadı* (koşum 2'de kapsam-dışı bir Osys acceptance testi kırmızı verdi). | `tests/BuildOrchestrator.Tests/Integration/OsysIncrementalAcceptanceTests.cs:62` (`Category=Acceptance`) | **KRİTER YANLIŞ EKSENDE ÖLÇÜLMÜŞTÜ.** Kontrolcü, repo'nun **kendi yazılı politikasıyla** (`README.md:83`, `--filter "Category!=Acceptance"`) yeniden koştu: **üç ardışık filtreli koşum, üçü de 0 failed**, üçüncüsü 8 CPU spinner altında (+%93 wall). Kırmızı veren test B1'in ailesinden değil, izole koşumda geçiyor ve repo politikası gereği normal süitin **dışında**. | B1 lens1 Important 1 → kontrolcü adjudikasyonu (`progress.md:393-397`) |

---

## 4. Commit'le doğrulanamayan kapanış beyanları

Aşağıdaki iki kalem **kapandı diye beyan edildi** ama kapanışın ürünü `.superpowers/` altındaki bir rapor
dosyasıdır ve **`.gitignore` nedeniyle git'e girmez** — dolayısıyla bir commit hash'iyle doğrulanamaz.
Kural gereği KAPANDI kovasına **yazılmadı**.

| Kalem | Beyan | Durum |
|---|---|---|
| **B1 concern C7** — sweep'in iki kez eksik çıkmasının kök nedeni YÖNTEMSELDİ (API ekseninde arandı, DAVRANIŞ ekseninde değil). | "EYLEME DÖNÜŞTÜ: kullanılan tüm tarama eksenleri raporda listeli; kapsam dışı her dosya isim+sayıyla düşüldü" (`progress.md:483`). | **DOĞRULANAMADI (commit yok)** — B1 fix round 2 açıkça *"KOD DEĞİŞMEDİ (commit yok; rapor `.superpowers/.gitignore` altında)"* diyor (`progress.md:459`). Re-review APPROVE verdi ve 95 siteyi **iki bağımsız toplama yöntemiyle** yeniden saydı. |
| **B1 concern C9** — sayım iki turda da bulgu bazında yapıldı, dosya bazında değil (yöntem tur içinde değişti). | Rapor düzeltme notuyla işaretlendi; eski yanlış sayılar (`45` → gerçeği `95`) **silinmedi**. | **DOĞRULANAMADI (commit yok)** — aynı gerekçe. Süreç dersi olarak kayıtlı. |

---

## 5. Bu triyajın DIŞINDA: merge öncesi 4 doküman kalemi

Bunlar **park edilmiş borç değil**, `final-review-C-docs.md`'nin merge'i BLOKLAYAN olarak işaretlediği
kalemlerdir (toplam ~8 satırlık saf metin değişikliği, süit koşmayı gerektirmez). Kaybolmasınlar diye
buraya yalnız **işaret** konuyor; karar ve reçete o belgededir.

| # | Kalem | `Konum:` | Kaynak |
|---|---|---|---|
| **B1** | Süit komutu filtresi — README kendi içinde de tutarsız (`:77` filtresiz komut ↔ `:82-83` "filtreyle hariç tutuluyor"); `CLAUDE.md` de filtresiz. Filtresiz koşum kullanıcının GERÇEK OSYS reposunu derliyor (~3 dk 30 sn ↔ 1 dk 33 sn). | `README.md:77` · `README.md:82-83` · `CLAUDE.md:45` | `final-review-C-docs.md` §1 (**KRİTİK**) |
| **B2** | `README.md`'deki **"runs 16 checks"** artık yanlış (bugün 23 `Check` çağrı yeri, yeşil koşumda **22**); ayrıca A13/T6'nın eklediği en anlamlı check (publish edilmiş ikiliyle Sync+Build) anlatıda hiç yok. | `README.md:113-119` (sayı `:114`); script `scripts/verify-publish.ps1` | `final-review-C-docs.md` §4.1 |
| **B3** | **Bu branch'in kendi kırdığı** 3 referans (B4'ün sweep'i yalnız `SupervisorHost`/`Program`/`EngineHost` ekseninde yapıldı). Biri bir **güvenlik argümanının** iki ayağını taşıyor. | `docs/TRUST-BOUNDARY.md:300` ve `:395` → `RunViewModel.ActionBar.cs:45-47` **→ `:104-106`** · `:298` → `:125-137` **→ `:188-200`** · `:395` → `MainWindow.xaml.cs:102` **→ `:119`** | `final-review-C-docs.md` §4.2 |
| **B4** | Provenance çıpası yanlış commit'i gösteriyor: `3243d4a`'da o tablo **yoktu** ve doğrulamanın ürünü olan 7 satır düzeltmesi de `0b72803`'te land etti. | `docs/TRUST-BOUNDARY.md:14` — `3243d4a` **→ `0b72803`** | `final-review-C-docs.md` §3 (B4 fix1-lensA m2'den yükseltildi) |

---

## 6. Kapsam notu

- `it5-records.md` §2'nin **18 satırından 6'sı** bu belgede karara bağlandı (K04, K05, K13, K14, G07, G08).
  **Kalan 12 satır** (P1 · L1 ×2 · G1 ×2 · G2'nin diğer ~16 Minor'ı · W1 · W2 ×2 · D1 · T1 ×2 · V1/V2) It-5'in
  kabul kaydında **olduğu gibi geçerlidir**; A13 onlara dokunmadı ve bu triyajın kapsamı dışındadır.
  Yolları: `.claude/outputs/2026-07-26-10-17-it5-records.md:422-439`.
- A13 boyunca **yükseltilen** (Kritik/Önemli) ve fix round'larında kapatılan bulgular bu belgede **yoktur** —
  onlar park edilmedi, kapandı; kayıtları ledger'ın task `complete` satırlarındadır.
- Bu belge **hiçbir dosyayı değiştirmedi**; salt okuma + `git log`/`git show`/`git merge-base` doğrulamaları
  ve `HEAD` üzerinden satır ölçümleriyle üretildi.
