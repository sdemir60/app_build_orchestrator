# It-3 Records — Incremental Build (canlı OSYS kabul kanıtı)

> Task 19 capstone. Tüm sayılar **ölçülen** (canlı OSYS koşusu), uydurma yok. Kanıt dosyaları:
> `.claude/temp/bo-it3-evidence.md` (incremental) · `.claude/temp/bo-it2-evidence.md` (rebuild/ordering).
> OSYS: `D:\Projects\Delta\OSYS` · HEAD `6b4ecba` · branch `feature/service-invoice-changes-reflect-work-order-refactor`
> · Parallelism 6 · Configuration Debug.

---

## 1. Ne wire edildi (incremental Build artık GERÇEK, uçtan uca)

It-3'ün bileşenleri (Task 1–18) unit-kanıtlıydı ama kompozisyon kökü (`Program.cs`) incremental planner'ı
gerçek bir koşuya BAĞLAMIYORDU — `Build` modu `Rebuild` gibi davranıyordu. Task 19 bunu bağladı:

- **`Program.cs` `BuildRunPlan` → incremental pass** (`ComputeIncremental`): fresh (Rebuild/Build) modda
  `GitService` (HEAD / branch / dirty paths / `GetTrackedBlobHashesAsync`) **SALT-OKUR** okunur, `BuildStateStore.Load()`
  ile state alınır, `IncrementalRunBinder.Bind` her düğüm için `WillBuild` + byte-stable `BuildSignature` üretir.
  Deferred **Task 7b** burada tamamlandı: her projenin `EvaluatedProject.CompileFiles` (+ `.csproj`) →
  `Path.GetRelativePath(repoRoot, f)` + `/`-normalize → `git ls-tree` haritasıyla kesişerek per-project committed
  fingerprint (`IncrementalRunBinder.ToRepoRelativeNormalized` / `RepoRelativeBuildFiles`).
- **`RunCoordinator` — Build pre-skip:** `WillBuild==false` (cycle DIŞI) projeler scheduler'a `Skipped` tohumlanır
  (dependent'ları için resolved), dispatch EDİLMEZ, `ProjectSkippedEvent` reason `"skipped — up to date"` ile
  raporlanır. Rebuild HER ŞEYİ derler (değişmez). `IncrementalPlan.SignatureById` + HEAD + branch coordinator'a taşınır.
- **`RunCoordinator` — success persist:** bir proje `projectSucceeded` olunca `BuildState`
  (`BuiltSignature`=hesaplanan imza, `BuiltCommit`=HEAD, `LastResult`=Succeeded, `LastBranch`, `LastDurationMs`)
  `BuildStateStore.Upsert` ile yazılır → BİR SONRAKİ Build incremental olur.
- **`buildPreview` artık GERÇEK `willBuild` taşır** (planner koştuğu için null değil).
- **Çapraz bug fix (`ProcessRunner`):** child process'e stdin de yönlendirilip HEMEN kapatılır — aksi halde
  Supervisor'ın App'ten gelen NDJSON pipe'ını miras alan `git.exe` o pipe'ta EOF beklerken sonsuza dek asılı
  kalıyordu (conhost tahsis + block). Bu olmadan incremental planning her koşuda 30s hang'e giriyordu.

Korunan It-2 invaryantları: deterministik ready-set scheduler, iki-katmanlı Stop, stdout YALNIZ NDJSON,
per-run disk log, shell-out per project.

---

## 2. Incremental all-skipped (EN GÜÇLÜ gösterim) — CANLI

İki `Build` AYNI `build-state.json`'ı paylaşır (aynı cacheRoot). Kaynak İKİSİ ARASINDA DEĞİŞMEZ.

| | TotalProjects | Succeeded | Failed | Skipped | Süre |
|---|---|---|---|---|---|
| **Run 1** (state YOK → hepsi derlenir) | 177 | **122** | 23 | 32 (cycle) | 18 985 ms |
| **Run 2** (kaynak değişmeden → incremental) | 177 | **0** | 23 | **154** | **5 704 ms** |

- `build-state.json` Run 1 sonrası **122 kayıt** (= her başarılı proje persist edildi).
- Run 2'de **`"skipped — up to date"` = 122** → Run 1'de başarılı olan projelerin **HEPSİ** skip.
- **Run 1 başarılı (122) → Run 2'de up-to-date SKIP edilmeyen: 0.** (Assert: `notSkipped` boş.)
- Run 2'de gerçekten dispatch edilen (derlenen) proje = **23** = Run 1'de FAILED olanlar (repo-kaynaklı bozuk
  projeler; state kurulamadığı için yeniden denenir). Başarılı 122'nin hiçbiri yeniden derlenmedi.
- Süre 18 985 ms → **5 704 ms** (yalnız 23 bozuk proje + cycle skip'leri): incremental kazanç ölçülür.

---

## 3. Minimal rebuild — tek proje dirty (in-process, gerçek OSYS grafı, SALT-OKUR)

Kurulu state üstünde TEK bir projenin bir compile dosyası "dirty" simüle edildi (OSYS working tree'ye
DOKUNULMADAN — sentetik dirty path; içerik gerçek dosyadan OKUNUR, YAZILMAZ):

- **Hedef:** `OSYS.Types.Accounting.Print` (dirty: `Accounting/.../Print/AccountDetailContract.cs`).
- Hedef `WillBuild = **True**` (imza değişti).
- **Doğrudan (cycle-dışı) dependent: 3 · flip=true olmayan (İHLAL): 0** → garantili cascade (topological
  build-order'da hedef onlardan önce → memo taze imzayı görür).
- Transitive (cycle-dışı) dependent: 24 · flip=true: **17** (çoğunluk). Kalan birkaçı yalnız cycle-tangled
  path üzerinden erişilir; topological-memo o path'lerde imzayı yaymayabilir — `IncrementalPlanner`
  tasarımının bilinen/documented sınırı; garanti DOĞRUDAN dependent'lardadır.
- **İlgisiz + skip (false) kalan proje: 100** → over-build YOK (dirty olmayan projeler dokunulmadan skip).

---

## 4. Ordering assert (Part 2 · resolved-gate, it2-records §8 carry) — CANLI

Gerçek OSYS Rebuild koşusunun kendi olay akışı üzerinden, `RunCoordinatorTests`'ten **BAĞIMSIZ**: bağımlılık
grafı in-process (Core `BuildPlanBuilder`) çıkarılır; olay zaman çizelgesinde her `ProjectStartedEvent`,
o projenin plandaki TÜM bağımlılıkları terminal (succeeded|failed|skipped) OLDUKTAN SONRA geldiği assert edilir.

- Rebuild: 177 proje, **145 ProjectStarted**, Completed, max in-flight 6/6, Succeeded 122 / Failed 23 / Skipped 32.
- **Sıralama ihlali: 0** (`Assert.Empty(orderingViolations)` yeşil).

---

## 5. Kabul kriteri → kanıt haritası

| It-3 kriteri | Kanıt | Sonuç |
|---|---|---|
| **Incremental all-skipped** | Real OSYS: 2× Build, `notSkipped=0`, up-to-date=122 (§2) | ✅ canlı |
| **Minimal rebuild (L1→L3 dirty)** | Real OSYS grafı + sentetik dirty: hedef+doğrudan dependent flip, ilgisiz skip (§3) | ✅ canlı |
| **Ordering / resolved-gate** | Real OSYS Rebuild event akışı, 0 ihlal (§4) | ✅ canlı |
| **Branch-bounce (A→B→A)** | `WorktreeManager.PlanWorktree` gerçek aktif branch ile: A→InPlace, B→Worktree+K3, A→InPlace (git-no-op, read-only) | ✅ canlı (component) |
| **buildPreview gerçek willBuild** | Planner koşuyor → `BuildPreviewEvent` non-null (§1) | ✅ canlı |
| config-switch all-dirty | `IncrementalPlannerTests.configuration_switch_marks_every_project_dirty_in_{safe,fast}_mode` | ✅ unit |
| worktree matrisi + K3 intent | `WorktreeManagerTests.PlanWorktree_*` (3-durum + K3 + `never_invokes_any_git_command` + Build-anı add) | ✅ unit |
| succeeded→clean canlı geçiş (VM) | `RunViewModelTests.ProjectSucceeded_flips_WillBuild_to_clean_immediately` + `BuildPreviewEvent_pre_populates_rows_...` | ✅ unit |
| fetch + offline degrade (K1) | `SyncFetchTests.FetchRefOnlyAsync_*` (ref-only args, HEAD/tree değişmez, offline→yerel HEAD) | ✅ unit |
| depIssue zinciri (CS0006 kök taşıma) | `DepIssueTrackerTests.*` + `RunCoordinatorTests` (depIssues emit) | ✅ unit |
| Retry failed kümesi | `RetryFailedTests.retry_set_is_failed_projects_plus_their_transitive_dependents_...` | ✅ unit |
| ETA formülü | `EtaCalculatorTests.*` (raw + EMA 0.75/0.25 + 5s yuvarlama + almost-done eşiği + fallback) | ✅ unit |
| path-mapping (Task 7b) | `IncrementalRunBinderTests.*` + `IncrementalPlannerTests.ComputeCommittedFingerprint_*` | ✅ unit |

Non-acceptance suite: **474 test, 473 geçti, 1 atlandı** (Task 19'da eklenen `IncrementalRunBinderTests` dahil).

---

## 6. K1 — read-only garanti (OSYS asla checkout/pull/reset edilmedi)

Her iki acceptance testinde OSYS HEAD + aktif branch koşu **öncesi ve sonrası** okunup eşit assert edildi:

- HEAD: `6b4ecba43e7d7d1232c33c957be36b7c0097aaed` → değişmedi.
- Branch: `feature/service-invoice-changes-reflect-work-order-refactor` → değişmedi.

Planlamadaki git tamamen SALT-OKUR: `rev-parse HEAD`, `symbolic-ref`, `status --porcelain`, `ls-tree -r HEAD`.
Checkout/switch/pull/fetch/reset/commit/stash YOK. Branch karşılaştırma committed fingerprint / `ls-tree` ile.

---

## 7. It-4 deferred backlog (wired-vs-deferred)

**Wired (Task 19):** incremental Build pre-skip + success persist; committed-fingerprint path resolution (7b);
buildPreview gerçek willBuild; ordering assert; ProcessRunner stdin-close fix.

**Deferred → It-4:**
- **Worktree uçtan uca BUILD:** `WorktreeManager.PrepareWorktreeAsync` + obj-izolasyon (Task 10 seam) gerçek
  run akışına bağlı DEĞİL — `Program.cs` hâlâ in-place (`inPlace = !UseWorktree`, worktree kökü hazırlanmıyor).
  `RunCoordinator.worktreeObjRootResolver` seam hazır ama beslenmiyor. Branch-farklı Build şu an in-place derler.
- **Sync-workspace IPC/UI:** `SyncWorkspaceCommand` + fetch akışı GitService'te var, Supervisor host'a/UI'a
  bağlı değil (`SyncStarted/Progress/Completed`, `BranchListEvent` emit edilmiyor). ListBranches/ListWorktrees/
  deleteWorktree IPC yüzeyi minimal.
- **Pixel/kart/çip/graf render:** VM-state (willBuild dots, depIssue ▲, ETA text) gözlemlenebilir; görsel
  render It-4.
- **ETA canlı tick:** `EtaCalculator` pure + VM'de tüketiliyor ama Supervisor progress event'ine bağlı canlı
  periyodik ETA yayını yok.
- **StaleObj çift-Evaluate perf:** `ComputeIncremental` cache'i sıcak re-kullanır (mtime+size fast-path) ama
  ayrı bir StaleObj Inspect geçişi + evaluation birleştirme optimize edilmedi.
- **repoRoot = git toplevel:** committed fingerprint workspace root == git toplevel varsayar (OSYS için doğru).
  Alt-dizin workspace'te fingerprint boşa düşer → over-build (güvenli) → It-4'te `rev-parse --show-toplevel`.
- **Transitive cascade cycle-tangled path'ler:** SCC üzerinden geçen bazı transitive dependent'lar topological-memo
  ile flip olmayabilir (§3) — garanti doğrudan dependent'larda; It-4'te SCC-aware propagation değerlendirilebilir.
