# It-3 TDD Plan — Incremental + Git + depIssue + Retry + ETA

> **Execution:** `superpowers:subagent-driven-development`, task-by-task. Otorite: v7 plan
> [.claude/outputs/2026-07-16-08-39-build-orchestrator-plan-v7-implementation.md](2026-07-16-08-39-build-orchestrator-plan-v7-implementation.md) Part C It-3.
> Devir: [it2-records.md](2026-07-17-21-01-it2-records.md) §8 + `.superpowers/sdd/progress.md` It-3 backlog / minor roll-up.

**Kapsam (v7 Part C It-3):** T25 (signature+propagation), T27 (build-state persist), T11 (git edge input),
T13 (path sanitize), T14 (worktree pool), T29 (branch-driven worktree + K3 niyet satırı), T15 (layer pattern),
T53 (will-build UI — VM seviyesi), T69 (Sync-fetch ref-only + offline degrade — K1), T54 (depIssue motoru),
T55 (Retry failed), T70 (ETA + lastDurationMs) **+ It-2 devir girdileri** (mojibake, EngineExited→VM,
depIssue etiketleme, obj-izolasyon worktree yolunda, sync-I/O kilit dışı, ucuz sertleştirmeler).

**Karar (bu iterasyon, kullanıcı onaylı 2026-07-18):**
- **UI = VM-seviyesi + testler.** Motor + Contracts + Supervisor tam; App tarafı RunViewModel'de gözlemlenebilir
  state (willBuild / depIssue / ETA) + VM testleri. Pixel-perfect kart/çip/graf render **It-4**.
- **Git testleri = ephemeral temp repo + read-only OSYS.** Unit/integration izole geçici git repo'larında;
  acceptance gerçek OSYS'te ama **SALT-OKUR**: aktif branch ASLA checkout/pull edilmez (K1); worktree
  `%LOCALAPPDATA%\BuildOrchestrator\worktrees\` havuzunda; offline degrade ulaşılamaz-remote ile.

---

## Global Constraints (her task için bağlayıcı — reviewer'a verilecek)

Plan v7 Global Constraints + It-2 kararları:

- **Ortak OutDir'e DOKUNULMAZ/OKUNMAZ.** "Değişti mi" yalnız kaynak sinyalinden (commit + git diff). DLL/bin
  timestamp asla okunmaz. `[§4]`
- **Signature = tek Core `BuildSignature.Compute`, byte-stable, determinism testli:** `config + HEAD commit +
  (in-place'de) local-diff hash + transitive upstream producer signatures`. `[D6]`
- **build-state GLOBAL** (projectId anahtarlı), single-writer serialized + atomik temp+rename, her proje
  bitiminde persist. `[§4/§6 · D2]`
- **K1 — Sync başında `git fetch origin <branch>` (ref-only):** checkout/pull **ASLA** (aktif branch değişmez);
  hedef SHA = remote-tracking ref; ağ/remote yoksa **warn + yerel HEAD ile degrade**.
- **K3 — Branch seçimi = niyet:** seçimde git'te işlem yok; konsola niyet satırı
  `branch target: <name> (<sha>) — worktree will be used at Build`; gerçek `git worktree add --detach` **yalnız
  Build anında** loglanır. `git switch --detach` YASAK.
- **obj-izolasyon yalnız worktree yolunda (I2-K2):** in-place build → `BaseIntermediateOutputPath = null`
  (VS-parity); worktree build → proje **Id anahtarlı izole** path. Plumbing hazır (MsBuildInvokeRequest /
  MsBuildArguments) — worktree yolunda null yerine izole path geç.
- **depIssue:** resolved = `succeeded | failed | skipped`; hatalı bağımlılık alt projeleri **BLOKLAMAZ**;
  bağımlılar son başarılı çıktıyla derlenir; kök hata adları zincir boyunca taşınır (doğrudan + miras birleşir,
  dedup). Contracts: `ProjectSucceeded/FailedEvent.depIssues[]` + `runCompleted.depIssueCount` — A9'a birebir.
- **ETA formülü (A6/Δ8 birebir):** `(queued süre tahminleri toplamı + building kalanları) / paralellik +
  (building varsa) 400ms`; üstel yumuşatma `0.75·eski + 0.25·yeni`; gösterim 5s'e yuvarlanır; `<4s → · almost done`.
  Süre tahmini girdisi = `BuildState.LastDurationMs`.
- **It-2 kararları KORUNUR:** I2-K1 iki-katmanlı Stop (graceful copy-aware / hard TerminateJobObject);
  ready-set ileri-atlamalı **deterministik** scheduler (K2 — rastgele/hash YOK); per-run disk log; **stdout
  YALNIZ NDJSON** (tüm log stderr/dosya); **D8** (deterministik test, sleep-poll YASAK); v1 flag'leri sabit
  (`-p:UseSharedCompilation=false -nodeReuse:false`); shell-out per project (in-process MSBuild yok).
- **Tüm sayı/tarih formatlaması `InvariantCulture`.**
- **Test hijyeni:** her test bir şey assert etmeli (tautoloji yasak); production kodu YAGNI (plan dışı alan/flag
  eklenmez); mevcut metot varken inline kopya yasak (CLAUDE.md review kuralı).

**Mevcut kod anchor'ları (haritalama sonucu — implementer'lara verilecek):**
- Contracts: `src/BuildOrchestrator.Contracts/Ipc/IpcMessages.cs` (RunMode:32, StartRunCommand:34, events:52-70),
  `src/.../Model/ProjectModels.cs` (ProjectNode:16, BuildPlan:61, BuildState:66 — `LastDurationMs` :73 hazır).
- Scheduler: `ReadySetScheduler.cs` — resolved semantiği `IsResolvedLocked`:209, `IsReadyLocked`:204,
  `TryDispatch`:138, `Complete`:160, `IsDone`:123, fresh ctor:36 / resume ctor:73 (dedup hedefi:84-92),
  `TakeSnapshot`:193.
- Coordinator: `RunCoordinator.cs` — dispatch `WorkerAsync`:404/wake:413, succeeded emit:449, failed emit:456/463/470,
  skip emit:320, `BaseIntermediateOutputPath` null kaynağı:433, log-snapshot kilit-altı disk okuma:110-115 (:114),
  RunSegment finally tally:358-366.
- Planning: `BuildPlanBuilder.cs` — layer slot `LayerIndex/LayerName: null`:37, buildOrder loop:30-39;
  `WillBuildEvaluator.Evaluate(inCycle, currentSignature, state)`; `BuildPreview.ComputeWillBuild(plan,
  currentSignatureFunc, stateLookupFunc)` — T25/T27 injection seam.
- Discovery: `CsprojEvaluator.EvaluatedProject{ Path, AssemblyName, CompileFiles, HintPaths, ProjectReferences,
  IsSdkStyle }`; `StaleObjDetector.Inspect(csprojPath, expectedTfm)` — **hiçbir yerde invoke edilmiyor**.
- Graph: `GraphBuilder.ProjectEdges{ ProjectId, Dependencies }` (Dependencies = üreticiler/upstream);
  `TopoSort.Compute` (Tarjan SCC + Kahn), reverse-edge modeli `dependents[]` içeride.
- App: `RunViewModel.cs` — IsRunning:115 / IsStarting:126 / CanContinue:130, `OnEvent` switch:221-236,
  `RunEndingErrorCodes`:67 (engine-death YOK); `EngineHost.EngineExited`:17 (subscriber yalnız MainWindow:31-35,
  VM'e bağlı DEĞİL); `MsBuildOutputEncoding.cs:25` (`GetEncoding(GetACP())` — mojibake noktası);
  `NativeMethods.cs:122-126` (ölü STARTUPINFOW overload); `RunLogWriter.cs:7/49/65` (XML-doc CS1570/1574),
  `:73` (sync `File.ReadAllText`); `SupervisorHost.cs:14` (ölü `_stopRequested`); `Program.cs:50` (çift scan).

---

## Task 1: Contracts — It-3 IPC/DTO yüzeyi

**Amaç:** It-3 motorunun ihtiyacı olan sözleşme alanlarını sabitle (önce gelir — hepsi buna bağlı).

**TDD (Ipc/Model round-trip testleri, `IpcMessagesTests`/`ProjectModelsTests` desenine):**
- `RunMode` polimorfik round-trip: `build` ve `retryFailed` yeni değerleri camelCase serialize/deserialize.
- `StartRunCommand` yeni alanlarıyla round-trip; `DependentMode` enum (`safe`/`fast`) camelCase.
- `ProjectSucceededEvent`/`ProjectFailedEvent` `depIssues` null iken emit edilmez (WhenWritingNull), dolu iken sıra korunur.
- `RunCompletedEvent.depIssueCount` round-trip.
- Yeni git komut/eventleri round-trip (aşağıdaki).

**Production (`IpcMessages.cs` + `ProjectModels.cs`):**
- `RunMode` → `{ Rebuild, Build, Continue, RetryFailed }`. (`Build` = incremental; `RetryFailed` = failed+dependents.)
- `StartRunCommand` alanları: mevcut `(RunId, Mode, RootPath, Configuration, Parallelism)` + `Branch` (string),
  `UseWorktree` (bool), `WorktreeName` (string?), `DependentMode` (yeni enum Safe/Fast, varsayılan Safe).
- `ProjectSucceededEvent` + `IReadOnlyList<string>? DepIssues`; `ProjectFailedEvent` + `IReadOnlyList<string>? DepIssues`.
- `RunCompletedEvent` + `int DepIssueCount`.
- Git IPC (motor akışı için): `SyncWorkspaceCommand(RootPath, Branch)` komut; eventler
  `SyncStartedEvent(RootPath, Branch)`, `SyncProgressEvent(string Line, string Level)` (dim/info/warn),
  `SyncCompletedEvent(string Branch, string? TargetSha, bool FetchDegraded, int ProjectCount, int CycleCount)`,
  `BranchListEvent(IReadOnlyList<BranchRef>)`. Yeni DTO'lar Contracts.Model'de: `BranchRef(string Name, string Sha,
  bool IsActive, bool IsRemoteTracking)`, `Worktree(string Name, string Branch, string Path, bool IsActive,
  long? DiskSizeBytes)`. (`ListBranches`/`ListWorktrees`/`deleteWorktree` tam IPC → It-4 UI; It-3'te GitService
  metotları + testleriyle kanıtlanır, IPC yüzeyi minimal.)
- `[JsonDerivedType]` kayıtlarını her yeni komut/event için ekle.

**Done:** round-trip testleri yeşil; `DefaultIgnoreCondition.WhenWritingNull` depIssues=null'da alan yazmıyor;
build 0/0.

---

## Task 2: `BuildStateStore` — global build-state persist (T27)

**Amaç:** `BuildState` (projectId anahtarlı) diske single-writer + atomik yaz/oku.

**TDD (`BuildStateStoreTests`, yeni):**
- Boş/olmayan dosya → boş state (throw yok).
- Save+Load round-trip: N proje, tüm alanlar (`BuiltSignature/BuiltCommit/LastResult/LastRunAt/LastBranch/
  LastDurationMs`) InvariantCulture ile korunur.
- Atomiklik: yazma sırasında process ölse hedef dosya ya eski ya yeni tam içerik (temp+rename) — reader asla
  yarım JSON görmez (D8: Barrier/TCS ile deterministik, sleep yok).
- Single-writer: eşzamanlı `Upsert` çağrıları serialize; son yazılan kazanır, kayıp yok.
- Bozuk JSON → warn/boş dön (never-throw), StaleObjDetector deseni.
- `Upsert(projectId, BuildState)` per-project persist; global dosya `build-state.json`.

**Production (`src/BuildOrchestrator.Core/State/BuildStateStore.cs`, yeni):**
- Konum: `<cacheRoot>\build-state.json` (Program.cs'deki cache klasörü yanında).
- `Load(): IReadOnlyDictionary<string,BuildState>`; `Upsert(BuildState)` (lock + atomik temp+rename);
  serializer = `IpcJson.Options` (camelCase) veya kendi options (InvariantCulture).
- Tek writer semaforu (`SemaphoreSlim`/lock); yazım = temp dosyaya yaz → `File.Move(overwrite:true)`.

**Done:** tüm testler yeşil; atomiklik+single-writer deterministik.

---

## Task 3: Path sanitization (T13)

**Amaç:** worktree adı + branch slug güvenli türetimi (path traversal / geçersiz karakter yok).

**TDD (`PathSanitizerTests`, yeni):**
- Branch slug: `release/2026.06` → `release-2026.06` (`/`→`-`); `feature/x\y` içindeki `\` da normalize.
- Auto worktree ad: slug + aynı prefix'li mevcut worktree sayısı+1 (v7 A6: `main` → `main-2`).
- Reddedilen girdi: `..`, mutlak yol, geçersiz Windows karakterleri (`:*?"<>|`) → temizlenir/rejekte.
- Boş/whitespace branch → hata veya güvenli fallback (tanımlı davranış).
- Deterministik, pure (I/O yok — mevcut adlar parametre olarak verilir).

**Production (`src/BuildOrchestrator.Core/Git/PathSanitizer.cs`, yeni):**
- `SanitizeBranchSlug(string branch): string`; `NextWorktreeName(string branch, IEnumerable<string> existing): string`;
  `IsSafeSegment(string): bool`.

**Done:** testler yeşil; pure.

---

## Task 4: `GitService` çekirdek + edge input (T11)

**Amaç:** ProcessRunner tabanlı git wrapper + patolojik repo durumları.

**TDD (`GitServiceTests`, yeni — ephemeral temp repo helper ile):**
- Test helper: temp dizinde `git init` + commit üreten fixture (D8: gerçek git.exe, sleep yok).
- `GetHeadCommit`: normal repo → 40-hex SHA; **no-commits** (fresh init) → null + `treat-as-dirty` warn.
- `GetCurrentBranch`: normal → ad; **detached HEAD** → null/`(detached)` + warn.
- `IsDirty`/`GetDirtyPaths`: working-tree değişikliği doğru raporlanır; temiz repo → boş.
- **Shallow** repo (`--depth 1`) → treat-as-dirty + warn (edge).
- `ListBranches`: yerel + remote-tracking ayrımı, `IsActive` doğru.
- Git yoksa/komut hata → `error` sinyali (exception değil, tanımlı sonuç).

**Production (`src/BuildOrchestrator.Core/Git/GitService.cs` + `GitStatus` DTO, yeni):**
- `GitService(ProcessRunner runner, string repoRoot)` (mevcut `ProcessRunner` — exit-code+stderr+timeout).
- `GetHeadCommit()`, `GetCurrentBranch()`, `GetDirtyPaths()`, `ListBranches()`, edge tespiti (`no-commits`,
  `detached`, `shallow`) → `GitRepoState { HeadCommit?, Branch?, IsDetached, HasNoCommits, IsShallow, TreatAsDirty }`.
- Uyarılar konsola dim/warn satırı olarak yansıtılabilir (Supervisor `console` callback'i — Task 12/19'da bağlanır).

**Done:** temp-repo testleri yeşil; edge durumlar treat-as-dirty + warn.

---

## Task 5: Sync-fetch ref-only + offline degrade (T69 · K1)

**Amaç:** Sync başında `git fetch origin <branch>` (yalnız ref); hedef SHA = remote-tracking; offline degrade.

**TDD (`SyncFetchTests`, yeni — temp repo + yerel "remote" repo ile):**
- İki temp repo: `origin` (bare/normal) + klon; remote'a yeni commit → `FetchRefOnly(branch)` → remote-tracking
  ref güncellenir ama **çalışma ağacı/aktif branch DEĞİŞMEZ** (HEAD aynı commit, dosyalar aynı) — checkout/pull yok.
- `GetTargetSha(branch)` = fetch sonrası `origin/<branch>` SHA'sı (curSha'dan farklı olabilir).
- **Offline degrade:** ulaşılamaz remote (`origin` = geçersiz URL) → `FetchRefOnly` hata yutar, `FetchDegraded=true`
  + warn, hedef SHA = **yerel HEAD** (K1). Throw yok.
- Fetch komutu argümanları: `fetch origin <branch> --no-tags` (checkout/merge argümanı YOK) — assert edilir.

**Production (`GitService`'e ekleme):**
- `FetchRefOnly(string branch): FetchResult { bool Degraded, string? TargetSha }`; `GetRemoteTrackingSha(branch)`.
- **checkout/pull/merge/switch çağrısı YOK** (kod incelemesiyle + arg assert'iyle garanti).

**Done:** ref-only fetch + degrade testli; aktif branch değişmezliği pinlenir.

---

## Task 6: `BuildSignature.Compute` (T25 signature)

**Amaç:** Byte-stable proje imzası (incremental kararın çekirdeği).

**TDD (`BuildSignatureTests`, yeni):**
- Determinism: aynı girdi (config + commit + compile-file hash'leri + upstream imzaları) → **byte-identik** imza
  (iki çağrı eşit); girdi sırası karıştırılınca imza aynı (deterministik sıralama).
- Config değişince imza değişir (Debug↔Release).
- HEAD commit değişince imza değişir.
- In-place local-diff: bir compile dosyası dirty iken imza değişir; temizken sabit.
- Transitive upstream: upstream producer imzası değişince downstream imzası değişir (GLOBAL propagation girdisi).
- Worktree (committed) modunda local-diff hash **dahil edilmez** (yalnız commit + upstream), in-place'de dahil.

**Production (`src/BuildOrchestrator.Core/Incremental/BuildSignature.cs`, yeni):**
- `Compute(ProjectNode node, string configuration, string? headCommit, IReadOnlyList<string> dirtyCompileFilesHashInput,
  Func<string,string?> upstreamSignature, bool inPlace): string` — SHA256 hex, byte-stable, tüm parça
  `InvariantCulture` + deterministik ayraç. Compile dosya hash girdisi GitService.GetDirtyPaths + dosya içerik
  hash'i (yalnız build-etkileyen uzantı: `.cs .xaml .resx .csproj .props .targets`).
- `EvaluatedProject.CompileFiles` kullanılır (file→project); OutDir/bin/obj **okunmaz** (§4).

**Done:** determinism + tüm sinyal testleri yeşil.

---

## Task 7: Downstream propagation + skip gate (T25 propagation)

**Amaç:** GLOBAL graf propagation — Safe (dirty + transitive dependents) vs Fast (yalnız dirty); WillBuild seam.

**TDD (`IncrementalPlannerTests`, yeni + `WillBuildTests` genişletme):**
- **L1→L3 dirty:** L1 (kök) dirty → Safe modda tüm transitive dependent'lar (L2, L3) willBuild=true;
  Fast modda yalnız L1.
- **config-switch all-dirty:** config değişimi → TÜM projeler willBuild=true.
- Temiz + state.Succeeded + imza eşit → willBuild=false (skip); state yok → true; inCycle → false.
- Dalga dalga skip: bağımlılık çözülünce downstream skip (tek seferde hepsi değil) — sıra korunur.
- `BuildPreview.ComputeWillBuild` seam'ine signature (Task 6) + state (Task 2) Func'ları bağlanır.

**Production (`src/BuildOrchestrator.Core/Incremental/IncrementalPlanner.cs`, yeni):**
- `Func<ProjectNode,string?> currentSignatureFunc` üretici (signature + state + git HEAD birleştirir).
- Downstream propagation GLOBAL graf üzerinden (transitive dependents = `Dependencies` ters yönü); Safe/Fast bayrağı.
- `WillBuildEvaluator.Evaluate` mevcut (değişmez) — Planner girdi sağlar.

**Done:** L1→L3 + config-switch + skip dalga testleri yeşil.

---

## Task 8: Layer pattern engine (T15)

**Amaç:** Sıralı regex+isim katman ataması + sert faz bariyeri + reverse-dep warn-only.

**TDD (`LayerEngineTests`, yeni):**
- Sıralı regex, ilk eşleşen kazanır; eşleşmeyen → `Other`.
- Katman index/ad atanır; katman-içi topo sıra korunur; sert faz bariyeri (Layer N tümü, Layer N+1 başlamadan).
- **Ters katman bağımlılığı** (üst katman alt katmana bağımlı) → **warn-only** (bloklamaz), tespit edilir.
- Boş pattern listesi → başlıksız tek liste (tüm projeler LayerIndex=null).
- Determinism.

**Production (`src/BuildOrchestrator.Core/Planning/LayerEngine.cs` + `LayerPattern` DTO, yeni):**
- `LayerPattern(int Order, string Regex, string Name)`; `AssignLayers(IReadOnlyList<ProjectNode>,
  IReadOnlyList<LayerPattern>): (layerIndex, layerName) map + reverse-dep warns`.
- `BuildPlanBuilder.cs:37` slot'una bağla (LayerIndex/LayerName null yerine). BuildPlanBuilder imzası pattern listesi
  alacak şekilde genişler (varsayılan boş → mevcut davranış korunur).

**Done:** katman + bariyer + reverse-dep-warn testleri yeşil; boş pattern regresyon yok.

---

## Task 9: Branch-driven worktree + pool (T29 · T14 · K3)

**Amaç:** 3-durum matrisi + niyet satırı + Build-anı `worktree add` + pool cap/LRU.

**TDD (`WorktreeManagerTests`, yeni — temp repo ile):**
- **3-durum matrisi:** aktif+OFF → in-place+local (worktree yok); aktif+ON → committed worktree; farklı branch →
  **zorunlu** worktree. Her durum doğru `WorktreePlan` üretir.
- **K3 niyet satırı:** farklı branch seçiminde konsol satırı **birebir**
  `branch target: <name> (<sha>) — worktree will be used at Build` + `Branch changed: <name> — Sync required`;
  seçim anında **git worktree add ÇAĞRILMAZ** (mock/arg gözlemiyle).
- **Build anı:** `PrepareWorktree` → `git worktree add --detach <path> <sha>` çağrılır (yalnız burada);
  `git switch`/checkout YOK.
- **Pool (T14):** per-worktree disk boyutu; cap aşılınca LRU prune; aktif worktree asla prune edilmez; per-worktree
  Sil.
- Concurrent-VS guard + tek-run kilidi korunur (mevcut davranış).

**Production (`src/BuildOrchestrator.Core/Git/WorktreeManager.cs` + `WorktreePlan` DTO, yeni):**
- `PlanWorktree(activeBranch, selectedBranch, useWorktreeToggle): WorktreePlan { Mode(InPlace/Worktree),
  Path?, IntentLine }`; `PrepareWorktree(plan): path` (Build anı, `worktree add --detach`); pool: `ListWorktrees`,
  `PruneToCap(maxBytes)`, `Delete(name)`. Naming = Task 3 `NextWorktreeName`. Havuz kökü
  `%LOCALAPPDATA%\BuildOrchestrator\worktrees\`.

**Done:** matris + niyet satırı (seçimde git-no-op) + Build-anı add + pool testleri yeşil.

---

## Task 10: obj-izolasyon worktree yolunda (I2-K2 devir)

**Amaç:** Worktree build'lerinde `BaseIntermediateOutputPath` proje-Id anahtarlı izole; in-place null kalır.

**TDD (`MsBuildArgumentsTests` genişletme + `RunCoordinator` seviyesinde):**
- Worktree modunda `MsBuildInvokeRequest.BaseIntermediateOutputPath` non-null, proje **Id** ile deterministik
  benzersiz (iki farklı proje → farklı path); arg `-p:BaseIntermediateOutputPath=<path>\` üretir (trailing `\`).
- In-place modunda null → arg **yok** (mevcut davranış, VS-parity).
- Path şeması: `<worktree>\_obj\<projectId-hash>\` gibi çakışmasız; proje kendi obj'ini paylaşmaz.

**Production:**
- `RunCoordinator.cs:433` null kaynağı → worktree modunda izole path üretici (proje Id → stable hash klasörü).
  In-place'de null. `MsBuildArguments.cs:14-15` plumbing zaten hazır (değişmez).
- Bayat-obj senaryosu (OSYS.Types.NewSales.Print vakası) izole path ile çözülür — testte pinlenir (mümkünse).

**Done:** worktree izole / in-place null testli.

---

## Task 11: ETA + `lastDurationMs` persist (T70)

**Amaç:** v7 A6 ETA formülü + süre tahmini girdisi persist.

**TDD (`EtaCalculatorTests`, yeni — formül birebir):**
- `(queued tahminleri toplamı + building kalanları)/paralellik + (building varsa) 400ms` — sabit girdilerle
  beklenen ms.
- EMA: `0.75·eski + 0.25·yeni` — iki adımda beklenen değer.
- Gösterim 5s'e yuvarlanır; sonuç `<4s → "· almost done"`; aksi `~Ns left` / `~Nm left` (fmtDur InvariantCulture).
- **İlk koşu fallback:** tahmin yok (LastDurationMs null) → `X/N` + geçen süre (ETA yok).
- `LastDurationMs` persist: proje succeeded → BuildStateStore.Upsert ile süre yazılır (Task 2 store).

**Production (`src/BuildOrchestrator.Core/Incremental/EtaCalculator.cs`, yeni):**
- `EtaCalculator` (pure); girdi = queued proje LastDurationMs'leri + building elapsed + paralellik + önceki EMA.
- Supervisor projectSucceeded'da `BuildState.LastDurationMs` günceller (Task 12/13 wiring ile).

**Done:** formül + EMA + eşik + fallback testleri yeşil; persist testli.

---

## Task 12: depIssue propagation motoru (T54)

**Amaç:** resolved={succ,fail,skip}; kök hata adları zincir boyunca taşınır; Contracts alanları + warn satırları.

**TDD (`DepIssueTests`, yeni + `RunCoordinatorTests` genişletme):**
- **CS0006 zinciri:** A fails → B (A'ya bağımlı) ve C (B'ye bağımlı) **BLOKLANMAZ**, derlenir, `depIssues=[A]`
  taşır (B doğrudan, C miras). Kök adlar dedup + sıralı.
- Doğrudan vs dolaylı: B.depIssues=[A] (doğrudan failed dep); C.depIssues=[A] (miras kök) — birleşim doğru.
- skipped dep de resolved sayılır (bloklamaz) ama depIssue üretmez (yalnız failed kökler taşınır) — v7 A6.
- `runCompleted.depIssueCount` = benzersiz dependency-affected proje sayısı.
- Warn satırları (Supervisor log başı): doğrudan → `warning: <root> failed in this run — last successful output
  referenced (…)`; dolaylı → `warning: failure in dependency chain (…) — referenced outputs may be stale`.

**Production:**
- Core: `DepIssueTracker` — scheduler `Completed` (Failed köklerini) okuyup her dispatch edilen projeye
  `depIssues[]` hesaplar (upstream Dependencies üzerinden transitive kök birleşimi). `ReadySetScheduler.cs:12-13`
  stub'ı gerçeklenir; resolved semantiği (`IsResolvedLocked`:209) değişmez.
- Contracts alanları (Task 1) doldurulur: `RunCoordinator.cs:449/456` emit noktalarında `DepIssues` eklenir;
  finally tally:358-366'da `depIssueCount` hesaplanır → `RunCompletedEvent`.
- Supervisor: `BuildProjectAsync` log açılışında (`:438-442` civarı) warn satırları yazılır.

**Done:** zincir + doğrudan/dolaylı + count + warn testleri yeşil; determinizm korunur.

---

## Task 13: Retry failed + Continue re-queue stopped (T55 + devir)

**Amaç:** `RunMode.RetryFailed` kümesi; Continue'da reason=stopped Failed → Queued (torn-DLL guard).

**TDD (`RetryFailedTests` + `ContinueRunTests` genişletme):**
- **Retry kümesi:** failed + transitive dependents = yeni willBuild; succeeded/skipped dahil edilmez; konsol/stream
  **SIFIRLANMAZ** (mevcut plan üstünden yeni segment).
- Retry sonrası succeeded olan eski-failed → temiz; hâlâ failed → tekrar failed.
- **Continue re-queue:** önceki segmentte `reason="stopped"` ile Failed'a düşen projeler Continue'da **Queued**'a
  geri döner (yeniden derlenir) — torn-DLL'e referans önlenir; `reason` başka olan Failed'lar Failed kalır.
- Elapsed korunur (RunClock snapshot — mevcut).

**Production:**
- `RunCoordinator.RunSegmentAsync` Continue branch (`:254-263`): resume scheduler kurulurken
  `snapshot.Completed`'daki `Failed(reason=stopped)` → Queued'a çevir (yeni yardımcı; RunSnapshot/scheduler resume
  ctor girdisi). RetryFailed branch: failed+dependents willBuild kümesi hesaplayıp yeni segment.
- `RunMode.RetryFailed` (Task 1) dispatch: `SupervisorHost.DispatchAsync` / `StartAsync` mode ayrımı.

**Done:** retry kümesi + stopped re-queue + elapsed testleri yeşil.

---

## Task 14: StaleObjDetector wiring (T72 It-3 wiring)

**Amaç:** In-place build öncesi bayat-obj tespiti → **dokunmadan** konsola warn (mevcut `StaleObjDetector` bağlanır).

**TDD (`StaleObjWiringTests` + mevcut `StaleObjDetectorTests` yeterli çekirdek):**
- Run başında in-place projeler için `Inspect` çağrılır; bayat obj → tek warn satırı (dosyaya dokunulmaz — assert).
- Worktree modunda (izole obj) warn üretilmez.
- `expectedTfm` girdisi: `EvaluatedProject`'e TargetFramework alanı eklenir veya csproj'dan türetilir (moniker).

**Production:**
- `CsprojEvaluator` → `EvaluatedProject`'e `TargetFrameworkMoniker` (veya raw TFV) ekle (YAGNI sınırında — yalnız
  StaleObj için gerekli minimum).
- Supervisor run başında (RunSegment fresh branch) in-place projeler için `StaleObjDetector.Inspect` → `console` warn.

**Done:** wiring warn testli; dosya-dokunmama pinlenir.

---

## Task 15: MsBuildOutputEncoding UTF-8 fix (devir §5)

**Amaç:** VS18/Roslyn redirected UTF-8 çıktıyı doğru decode et (mojibake giderilir).

**TDD (`MsBuildOutputEncodingTests`, yeni):**
- UTF-8 baytları (`ı`=0xC4 0xB1, `ş`, `ğ`) → doğru Türkçe string (`başlatıldı`, `dosyası`) decode edilir.
- ASCII satırlar (MSB302x, `error CS`) bozulmadan geçer (retry/sınıflandırma etkilenmez).
- BOM'lu UTF-8 → BOM yutulur.
- (Opsiyonel) heuristik: `Ã/Ä±/ÅŸ` bigram imzası görülürse UTF-8'e düş — detektör testi.

**Production (`MsBuildOutputEncoding.cs:25`):**
- `Value` → UTF-8 decode (redirected pipe UTF-8 yazıyor). En sağlam: pipe'ı UTF-8 oku; ANSI fallback yalnız
  gerçek ANSI tespitinde. `GetACP()` bağımlılığı (NativeMethods) kaldırılabilir/izole edilir (Task5 minor).

**Done:** UTF-8 + ASCII + BOM testleri yeşil; ASCII token'lar değişmez.

---

## Task 16: EngineExited → RunViewModel run-state reset (devir)

**Amaç:** Engine ölürse (startRun sonrası runStarted öncesi ya da run ortasında) IsStarting/IsRunning sıfırlansın.

**TDD (`RunViewModelTests` genişletme):**
- `IsStarting=true` iken engine-died sinyali → IsStarting/IsRunning/CanContinue sıfırlanır, komutlar tekrar enable.
- `IsRunning=true` iken engine-died → sıfırlanır; "Restart Engine" sonrası temiz.
- runStarted/runCompleted normal akışını bozmaz (idempotent).

**Production:**
- `RunViewModel`'e `OnEngineExited(int? code)` handler: run-state reset + konsola/duruma engine-died yansıması
  (sticky şerit kalıcı hata modu VM-state'i — pixel It-4).
- `EngineHost.EngineExited` (:17) → VM handler'a bağlanır (MainWindow:31-35 subscriber VM'i de çağırır ya da VM
  doğrudan subscribe). Mevcut `RunEndingErrorCodes` deseniyle tutarlı.

**Done:** engine-died reset testleri yeşil; normal akış regresyon yok.

---

## Task 17: RunViewModel It-3 state surface (T53 VM + ETA/depIssue VM)

**Amaç:** willBuild / depIssue / ETA gözlemlenebilir VM state (pixel It-4).

**TDD (`RunViewModelTests` genişletme):**
- **will-build:** buildPreview/willBuild bilgisi proje VM'lerine yansır (dirty=true/clean=false/hollow=null).
- **succeeded→clean canlı geçiş:** proje succeeded event'i → o projenin willBuild anında false (clean) olur.
- **depIssue ▲:** projectSucceeded/Failed `depIssues` dolu → VM'de `HasDepIssue=true` + kök adlar; boşsa false.
- **ETA:** progress state'inde ETA metni (Task 11 EtaCalculator VM'de tüketilir); ilk-koşu fallback X/N.
- `depIssueCount` runCompleted'dan özet state'e.

**Production:**
- `RunViewModel` (+ proje-satır VM'i varsa) observable alanlar: `WillBuild`, `HasDepIssue`, `DepIssueRoots`,
  `EtaText`. Event handler'ları (`OnProjectSucceeded/Failed/RunCompleted`, buildPreview) doldurur. YAGNI: pixel yok.

**Done:** will-build + succeeded→clean + depIssue + ETA VM testleri yeşil.

---

## Task 18: Cleanup + ucuz sertleştirmeler (devir minor roll-up)

**Amaç:** Kilit-dışı I/O + ölü kod + XML-doc + scheduler sağlamlaştırma (davranış-nötr veya küçük düzeltme).

**TDD / doğrulama (her değişen davranış için test; nötr temizlik için build+mevcut suite):**
- **TryGetProjectLogSnapshot** disk okuması (`RunCoordinator.cs:110-115`, `:114`) `_gate` **dışına** taşınır
  (async/kilitsiz); büyük log okuması stopRun/startRun'ı bloklamaz. Mevcut stitch/log testleri yeşil kalır.
- **ReadySetScheduler.IsDone** (`:123`) `queued empty` → `nothing ready` reformülasyonu (self-loop güvenliği);
  yeni test: hiç ready olamayan düğüm senaryosu terminal olur.
- **Resume-ctor dedup** (`:73-93` ≈ `:36-56`): `ReadySetScheduler(plan) : this(plan, EmptySnapshot)` — davranış-nötr;
  mevcut testler yeşil.
- **Ölü kod sil:** `SupervisorHost._stopRequested` (:14/:16/:68 — okunmuyor), `NativeMethods` plain STARTUPINFOW
  overload (:122-126 — caller yok). Build 0/0.
- **RunLogWriter XML-doc** (:7 `<logsRoot>`/`<ts>` raw → CS1570; :49/:65 `AppendLine` unqualified cref → CS1574)
  düzeltilir (0-warning politikası doc-gen açılırsa kırılmasın).
- **Program.cs:50** çift `scanner.Scan` → tek geçiş (It-3 consolidation).

**Production:** yukarıdaki noktalar. Riskli olan yalnız sync-I/O taşıma ve IsDone — test-first.

**Done:** değişen davranış testli; ölü kod yok; build 0 warning; mevcut 214 suite yeşil.

---

## Task 19: Acceptance ordering assert + It-3 kabul koşusu + `it3-records.md`

**Amaç:** Bağımsız sıralama assert'i + It-3 acceptance kanıtı + kayıt.

**TDD / acceptance:**
- **Ordering assert** (`OsysRebuildAcceptanceTests` genişletme): gerçek OSYS koşusunda her `ProjectStartedEvent`,
  o projenin tüm bağımlılıkları **terminal** duruma ulaştıktan SONRA geldiğini event zaman damgalarıyla assert eder
  (resolved-gate bağımsız pinlenir — it2-records §8).
- **It-3 acceptance** (yeni `[Category=Acceptance]` testler, temp repo + read-only OSYS):
  - **branch-bounce:** branch A→B→A seçimi doğru worktree/in-place matrisi + niyet satırı üretir.
  - **L1→L3 dirty** + **config-switch all-dirty:** incremental willBuild kümesi doğru.
  - **worktree matrisi + niyet satırı:** 3 durum + K3 satırı; git worktree add yalnız Build anı.
  - **will-build dot'lar + succeeded→clean canlı geçiş** (VM state).
  - **fetch'li Sync + offline degrade** (K1): ref-only, aktif branch değişmez, offline→yerel HEAD.
  - **depIssue zinciri** (CS0006 kök taşıma) testli.
  - **Retry failed kümesi** doğru.
  - **ETA formülü** testli.
- OSYS koşusunda **K1 ihlali yok** (aktif branch checkout/pull edilmedi — assert/log).

**Production:** yalnız test + `it3-records.md` (canlı sayılar, uydurma yok). Production kodu YOK.

**Done:** ordering assert + It-3 acceptance yeşil; `it3-records.md` yazıldı (kabul kanıtı + It-4 devir backlog).

---

## Execution notes

- **Branch:** `it3-incremental` (main'den). It-2 deseni: task-başı WIP commit; main'e merge/push **kullanıcı onayıyla**.
- **Ledger:** `.superpowers/sdd/progress.md`'ye It-3 bölümü; her temiz task `Task N: complete` satırı.
- **Sıra/bağımlılık:** T1 (contracts) önce. T2/T3/T4 bağımsız. T5←T4; T6←T4; T7←T6+T2; T8 bağımsız; T9←T3+T4;
  T10←T9; T11←T2; T12←T1; T13←T1; T14←evaluator; T15/T16 bağımsız (App/encoding); T17←T1+T7+T11+T12; T18 nötr;
  T19 son.
- **Model seçimi:** mekanik/tek-dosya (T3, T15, T18 parçaları) ucuz tier; entegrasyon (T9, T12, T13, T19) standart;
  final whole-branch review en yetenekli tier.
- Bitince **R promptu (Fable)** — final whole-branch review + aşamayı kaydet.
