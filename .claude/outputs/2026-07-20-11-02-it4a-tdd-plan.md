# It-4a TDD Plan — ZOR-CUSTOM UI altyapı paketi (+ devir FAIL fix)

> **Execution:** `superpowers:subagent-driven-development`, task-by-task (fresh implementer → task review → fix → sonda whole-branch review).
> **Otorite zinciri:** davranış/semantik → v7 plan [.claude/outputs/2026-07-16-08-39-build-orchestrator-plan-v7-implementation.md](2026-07-16-08-39-build-orchestrator-plan-v7-implementation.md) (A7/A13); **görsel kesin değerler → design-v1** [README](2026-07-15-19-00-design-v1/README.md) §1–§3 + `prototype/app/BuildApp.jsx` + `prototype/_ds/…/tokens/*.css` (+ Ek A: [feasibility §Ek A](2026-07-15-23-34-design-wpf-feasibility-analysis.md) 25 davranış); **WPF teknik çözümler → feasibility §3-§5**. Çelişkide: görsel=design-v1, teknik=A13.2/feasibility, davranış=v7.
> **Branch:** `it4a-ui-infra` (BASE `3d28f88`, main'den). Per-task commit implementer'da; **merge/push main+origin'e YALNIZ kullanıcı onayıyla** (it2/it3 deseni + CLAUDE.md).

**Kapsam (kullanıcı "yalnız bunlar" dedi):** T56 (AvalonEdit konsolun kalanı), T57 (TrackedTextBlock), T58 (sticky overlay + LayoutMetrics), T59 (ScrollAnimator/BottomAnchor/Follow + latest pill), T62 (pencere kabuğu paketi), T63 (graf hibrit render — Shapes yolu). **+ İLK task** (T65'ten devreden bilinen FAIL) **+ Foundation** (bu 6 task'ın yaslandığı motion/token ResourceDictionary — reduced-motion kuralı zorunlu kılar).

**Bu pakette OLMAYAN (It-4b motor/wiring'e ait — karıştırma):** T35 (2×2 layout + görünüm modları), T50 (graf paneli veri wiring), T49 (tam token çevirisi — burada YALNIZ 6 task'ın tükettiği alt küme), T60 (DS kontrolleri), T61 (tooltip), T64 (tam ikon/ICO hattı — T62 yalnız tray ICO'sunu minimal üretir), T66/67/68, kart/çip render, ETA live-tick. **MUST-DO-FIRST Core/motor kalemleri** (depIssue-persist stale-skip, pre-skip deterministik test, LayerEngine inert, inPlace-from-resolved-worktree, worktree e2e, Sync IPC/UI) → **hepsi It-4b**, bu pakette DEĞİL. Bu paketten düşen TEK It-4 backlog kalemi: **console re-seed dup residual** (Task12 It-4 minor) → T56'ya katlanır.

**Görsel doğrulama vehiculü:** final 2×2 layout It-4b olduğundan, T57/T58/T59/T63 primitifleri **dev-only lab harness** penceresinde (`--it4a-lab` bayrağı, `--font-ab` gibi) temsili veriyle (build-data.js'in 36-proje örnek OSYS grafı → küçük C# sample-data provider) host edilir; kullanıcı prototiple yan yana karşılaştırır. Konsol (T56) + pencere kabuğu (T62) gerçek `MainWindow`/`ConsoleView`'a wire edilir (zaten oradalar). Lab, FontAbWindow gibi referans kalır (It-4b'de gerçek layout gelince kaldırılır).

---

## Global Constraints (her task için BAĞLAYICI — reviewer'a birebir verilecek)

**Mimari/motor invaryantları (dokunma):**
- **Kökteki font ayarına DOKUNMA (T65 kararı):** `MainWindow` kökünde `TextOptions.TextFormattingMode="Display"` + `TextOptions.TextRenderingMode="Grayscale"` SABİT. Graf etiketleri **Ideal** = LOKAL override (yalnız o elemanlarda), kökü değiştirmeden.
- **FontAbWindow (`Spikes/FontAbWindow.*`, `--font-ab`) repoda referans KALIR** — It-4a silmez/taşımaz/bozmaz.
- **A13.2 zorunlu teknik kararlar (İHLAL = plan ihlali):**
  - `DragDrop.DoDragDrop` YASAK (bu pakette drag yok ama ilke geçerli).
  - Akan dash kenarları **HER ZAMAN UIElement Path** — `DrawingContext` içinde `Pen.DashStyle.Offset` animasyonu güvenilmez.
  - **Dash birimi = StrokeThickness ÇARPANI** (px değil): 1px'te birebir, 1.6px kenarda değerler bölünür.
  - `ContainerVisual.Opacity` **animate edilemez** (DP değil, düz CLR) — DrawingVisual katmanları için katman host'ları UIElement olmalı.
  - **Koleksiyon reset YASAK** (typewriter'da yalnız son `Run.Text` güncellenir; ObservableCollection tam reset yapılmaz).
  - **`px→DIP 1:1`**; `PerMonitorV2` + kökte `UseLayoutRounding=True` (hairline'lar).
  - Tüm sayı/tarih formatlaması **`InvariantCulture`** (VM'de).
- **Reduced-motion:** tüm süre/eğri **tek ResourceDictionary'den** (Foundation); algılama `SystemParameters.ClientAreaAnimation` + `StaticPropertyChanged` **canlı takip** (uygulama-içi toggle YOK); reduced iken tüm süreler 0'a düşer.
- **Motion:** yalnız transform+opacity; bounce/overshoot YOK; aynı anda ≤1 hero; dekoratif sonsuz animasyonlara `Timeline.DesiredFrameRate=30`; typewriter/kaskat **Stopwatch-bazlı** elapsed (DispatcherTimer ~15.6ms çözünürlük, 11ms tick değil).
- **Konsol yüksek-frekans satır akışı:** satır başına `Dispatcher.Invoke` YASAK; batch başına TEK marshal (mevcut ConsoleBatcher/MainWindow deseni korunur).

**Kesin görsel değerler (design-v1 — birebir, uydurma yok):**
- **Renkler (`tokens/colors.css`):** `console-bg #060608`; yüzeyler `surface-sunken #0a0a0c`/`surface-base #0e0e10`/`surface #141417`/`surface-raised #1a1a1e`/`surface-overlay #202024`; border `border-subtle #1c1c20`/`border #2a2a30`/`border-strong #3a3a42`; metin `text-primary #ededee`/`text-secondary #a9a9b0`/`text-dim #76767e`/`text-faint #54545c`/`text-on-accent #1c1304`; amber `#eda10f`/`amber-bright #ffb52e`/`amber-dim #b87a0b`/`amber-text #f1ab2e`/`amber-soft rgba(237,161,15,.12)`/`amber-soft-hover .18`/`amber-border .32`; statü success `#43b16b`/text`#58cb80`/soft`.12`/border`.30`, fail `#ee5a52`/text`#ff706a`/soft`.12`/border`.32`, skipped `#6a6a73`/text`#888890`/soft`.10`/border`.24`, cycle(warn) `#df6f2b`/text`#f0853f`, queued `#7c7c84`/text`#9a9aa2`; will-dot dirty=amber / clean=`#3a3a42` / unknown=transparent+1px`#1c1c20` halka (8px); focus-ring `rgba(237,161,15,.50)` 2px offset 1.
- **Motion (`tokens/effects.css`):** süreler instant **80ms** / fast **120ms** / base **180ms** / slow **280ms**; eğriler ease-out `cubic-bezier(.22,1,.36,1)` [giriş], ease-standard `cubic-bezier(.4,0,.2,1)` [durum değişimi], ease-in-out `cubic-bezier(.65,0,.35,1)` [yer değiştirme] — üçü de KeySpline ile matematiksel birebir. Gölge yalnız floating overlay: overlay `0 10px 28px -10px rgba(0,0,0,.66), 0 2px 6px -2px rgba(0,0,0,.5)`, popover `0 6px 18px -8px rgba(0,0,0,.6)`.
- **Tipografi:** UI=Geist, makine çıktısı=Geist Mono (tabular). Ölçek 2xs 11 / xs 12 / **sm 13 base** / md 14 / lg 16. Ağırlık başlık 600 / vurgu 500 / gövde 400. Caps etiket: **11px, 500, letter-spacing 0.07em, uppercase, text-faint**. Satır yük.: tight 1.2 / snug 1.35 / normal 1.5 / **mono 1.55**.

**Test hijyeni:** her test bir şey assert etmeli (tautoloji yasak); WPF görselleri `[StaFact]` (Xunit.StaFact mevcut); saf mantık (colorizer offset, advance-width, LayoutMetrics aritmetiği, EdgeStyleResolver, kamera scale) normal `[Fact]` + saf fonksiyon; production kodu YAGNI (plan dışı alan/flag yok); mevcut metot varken inline kopya yasak.

---

## Mevcut kod anchor'ları (haritalama — implementer'lara verilecek)

- **Konsol (T56 tabanı):** `src/BuildOrchestrator.App/Console/ConsoleView.xaml(.cs)` — `AvalonEdit:TextEditor Editor` (IsReadOnly, WordWrap=False, FontSize=13, ConsoleFontFamily=`./#Geist Mono Console` pack URI), `AppendBatch(text)` = `BeginUpdate→Insert(TextLength)→EndUpdate` + `StickToBottom`→`ScrollToEnd`, `Document` get/set. `Console/ConsoleBatcher.cs` — `Channel<string>` (SingleReader=false), `Post`/`Complete`/`DiscardPending`/`PumpAsync(flush, ct)` (tick→drain→tek flush). `Fonts/GeistMonoConsole.CompositeFont` (It-0 spike, line 1.55 sarmalama). Testler: `tests/.../App/ConsoleViewTests.cs`, `ConsoleBatcherTests.cs`, `CompositeFontSpikeTests.cs`.
- **VM state (hazır — bu paket TÜKETİR, değiştirmez):** `App/ViewModels/RunViewModel.cs` — `Projects` (`ProjectRowViewModel{ Id, Name, State(Pending/Started/Succeeded/Failed/Skipped), DurationMs, WillBuild:bool?, DepIssues, HasDepIssue }`), `EtaText`, `DepIssueCount`, `ActiveProjectId`, `SeedRunDocument()/SeedProjectDocument(id)`, `GetRunDocumentText()`. Console dikişi `_gate` kilitli.
- **Pencere kabuğu (T62 tabanı):** `App/MainWindow.xaml(.cs)` — WindowChrome (CaptionHeight 40, ResizeBorderThickness 6, UseAeroCaptionButtons=False), min 1240×620, min/max/close butonları (`OnMinimize`/`OnMaximizeRestore`/`OnClose`), `OnSourceInitialized` (DWM dark/corner/border), `OnStateChanged`→`MaximizeFix.PaddingFor`. `App/Shell/MaximizeFix.cs` (`PaddingFor`), `App/Shell/Dwm.cs` (DWM + GetSystemMetricsForDpi P/Invoke). Testler: `App/MaximizeFixTests.cs`.
- **DI/başlangıç:** `App/App.xaml`(BOŞ — token dictionary YOK), `App/App.xaml.cs` (`OnStartup`: `--font-ab`→FontAbWindow; else DI: EngineHost/ConsoleBatcher/RunViewModel/MainWindow). `App/Services/EngineHost.cs` (Supervisor spawn + `EventReceived`/`EngineExited`).
- **Discovery (İLK task):** `Core/Discovery/WorkspaceScanner.cs` (`Walk` → `.csproj`/`.sln`, Ignored={`.git bin obj node_modules .vs`}), `Core/Discovery/EvaluationCache.cs` (`GetOrEvaluate(csprojPath, evaluate)`: `new FileInfo(csprojPath).Length` — satır 23-25 FAIL noktası), `Core/Planning/BuildPlanBuilder.cs:26` (`scan.CsprojPaths.Select(p => cache.GetOrEvaluate(p, evaluator.Evaluate))`). Testler: `Discovery/WorkspaceScannerTests.cs`, `Discovery/EvaluationCacheTests.cs`. Acceptance: `tests/.../Integration/OsysRebuildAcceptanceTests.cs`.
- **Test csproj:** `net10.0-windows`, `UseWPF=true`, App referanslı; `AvalonEdit 6.3.1.120`, `Xunit.StaFact`, `Xunit.SkippableFact`; Fonts TestAssets'e kopyalanır.

---

## Task 0 (İLK — devir FAIL): wpftmp scanner dışlaması + EvaluationCache FileNotFound toleransı

**Amaç:** OsysRebuildAcceptance canlı koşusunda WPF markup derlemesi proje klasöründe (obj DEĞİL) geçici `<Ad>_<hash>_wpftmp.csproj` üretir; scanner bunları yakalar, `EvaluationCache.GetOrEvaluate` `FileInfo.Length` okurken dosya silinmiş olur → `FileNotFoundException`. Canlı build ↔ project scan yarış durumu. İki savunma: (a) scanner geçici WPF temp proje dosyalarını DIŞLAR; (b) EvaluationCache kaybolan/erişilemeyen dosyaya toleranslı olur.

**TDD (önce failing test):**
- `WorkspaceScannerTests`: yeni test — kökteki `Foo_abc123_wpftmp.csproj` (ve `Foo.csproj_wpftmp.csproj` varyantı) tarama sonucunda YER ALMAZ; normal `Foo.csproj` alınır. RED (şu an `.csproj` uzantı testi geçici dosyayı da alır) → GREEN.
- `EvaluationCacheTests`: yeni test — `GetOrEvaluate` çağrısı sırasında var olmayan/silinmiş bir csproj yoluyla çağrıldığında **throw ETMEZ**; `evaluate` çağrılmaz (skip) VEYA girdi güvenle atlanır; cache bozulmaz, sonraki mevcut dosyalar normal işlenir. RED (şu an `FileNotFoundException`) → GREEN. (Deterministik: dosyayı sil, sonra çağır — D8 sleep yok.)
- Regresyon: mevcut 4 EvaluationCache testi + WorkspaceScanner testi yeşil kalır.

**Production:**
- `WorkspaceScanner.Walk`: `.csproj` eklerken, dosya adı geçici WPF temp desenine uyuyorsa atla. Desen: `*_wpftmp.csproj` (case-insensitive; MSBuild WPF MarkupCompilePass'ın ürettiği `<ProjectName>_<8hex>_wpftmp.csproj`). Basit, muhafazakâr son-ek kontrolü (`.EndsWith("_wpftmp.csproj", OrdinalIgnoreCase)`). Yorum: kalıcı proje değil, canlı build artefaktı.
- `EvaluationCache.GetOrEvaluate`: dosya erişimini toleranslı yap. `FileInfo` oluştur; `info.Exists` false ise (ya da `Length`/`LastWriteTimeUtc` erişimi `FileNotFoundException`/`DirectoryNotFoundException`/`IOException` fırlatırsa) → **cache'te varsa mevcut girdiyi dön, yoksa `evaluate`'i çağırMA ve girdiyi atla** (throw YOK). Karar: BuildPlanBuilder tarafı bu projeyi listede tutmayacaksa (scanner dışlaması sayesinde normalde hiç gelmez) yine de savunmacı ikinci kat. Not: bu, "kaybolan dosya = yeniden değerlendir" DEĞİL, "kaybolan geçici dosya = güvenle skip"tir; kalıcı bir dosya gerçekten silinmişse bir sonraki Sync onu zaten görmez.
- `BuildPlanBuilder.Build(ScanResult…)` satır 26: `GetOrEvaluate` artık null/skip dönebilirse (tasarım kararı) — EN TEMİZİ: `GetOrEvaluate` imzasını değiştirme; skip'i cache içinde ele al ve **var olmayan dosya için `evaluate`'i çağırmadan** çağıranın beklediği davranışı koru. Eğer skip bir projenin plandan düşmesini gerektiriyorsa (savunmacı yol), `BuildPlanBuilder`'da `.Select(...).Where(x => x is not null)` benzeri bir filtre eklenebilir — ANCAK yalnız gerçekten gerekirse; scanner dışlaması birincil savunma olduğundan çift-Evaluate/çift-tarama eklenmez. Implementer en küçük, en az invaziv çözümü seçer ve raporunda gerekçelendirir.

**Gözle doğrulama adımı:** `dotnet build BuildOrchestrator.slnx` (0/0). `dotnet test --filter "FullyQualifiedName~Discovery"` → yeni + mevcut Discovery testleri yeşil. (Tam OsysRebuildAcceptance canlı koşusu ~30dk/opsiyonel — kullanıcı isterse `--filter FullyQualifiedName~OsysRebuildAcceptance`; yarış artık tekrarlanmaz.)

---

## Task 1 (Foundation): Motion + token ResourceDictionary + ReducedMotion servisi + lab harness kabuğu

**Amaç:** Bu 6 UI task'ının yaslandığı TEK motion/token kaynağını kur (reduced-motion kuralı "tüm süre/eğri tek ResourceDictionary'den" zorunlu kılar). Bu, tam T49 DEĞİL — yalnız bu paketin tükettiği alt küme (renk brush'ları + süre/KeySpline + reduced-motion mekanizması). Ayrıca primitifleri gözle doğrulamak için minimal dev-only lab penceresi kabuğu.

**TDD:**
- `[Fact]` `ReducedMotionTests`: `SystemParameters.ClientAreaAnimation` sinyalini soyutlayan bir servis (`IMotionSettings` + izlenebilir `AnimationsEnabled`) — kaynak bayrağı false iken efektif süre 0, true iken token süresi; `StaticPropertyChanged` benzeri değişimde canlı güncellenir (test enjekte edilen sinyalle deterministik, D8 — gerçek SystemParameters'a bağlanmadan).
- `[StaFact]` `MotionResourcesTests`: uygulama ResourceDictionary'sinde `Duration.Instant/Fast/Base/Slow` = 80/120/180/280ms; `KeySpline` kaynakları ease-out/standard/in-out tam kontrol noktalarıyla (.22,1,.36,1 / .4,0,.2,1 / .65,0,.35,1); reduced iken süre kaynakları 0.
- `[StaFact]` `TokenBrushesTests`: birkaç kritik brush (`console-bg #060608`, `amber #eda10f`, `status-fail-text #ff706a`, `border #2a2a30`, `text-faint #54545c`) doğru renkte çözülür.

**Production:**
- `App/Resources/Motion.xaml` (ResourceDictionary): `Duration` + `KeySpline` kaynakları (yukarıdaki kesin değerler). `App/Resources/Tokens.xaml`: `SolidColorBrush` kaynakları (colors.css alt kümesi — bu 6 task'ın kullandıkları). App.xaml `MergedDictionaries`'e ekle (App.xaml şu an boş). **Anahtar adları** kararlı ve design token adıyla eşlensin (ör. `Brush.ConsoleBg`, `Brush.Amber`, `Brush.StatusFailText`, `Duration.Base`, `KeySpline.EaseOut`).
- `App/Services/ReducedMotion.cs` (veya `MotionSettings`): `SystemParameters.ClientAreaAnimation` oku + `SystemParameters.StaticPropertyChanged` abone → `AnimationsEnabled` yay; süre kaynaklarını 0'a çevirme mekanizması (uygulama düzeyinde toplu). Test için sinyal enjekte edilebilir olsun.
- `App/Spikes/It4aLabWindow.xaml(.cs)` + `App/App.xaml.cs`'te `--it4a-lab` bayrağı (FontAbWindow deseni): DI/EngineHost kurulmaz; boş bir sekme/panel iskelesi ("primitifler buraya eklenecek"). Sample-data provider iskeleti: `App/Spikes/SampleGraphData.cs` — build-data.js'in 36-proje OSYS grafını (node id/ad/layer/deps/status + edge) statik C# olarak port et (sonraki task'lar bunu tüketir). **build-data.js'ten birebir** node/edge kümesi.

**Gözle doğrulama adımı:** `dotnet run --project src/BuildOrchestrator.App -- --it4a-lab` → boş lab penceresi açılır (dark, token'lı zemin). Reduced-motion doğrulama: Windows "Show animations" kapat/aç → (sonraki task'larda animasyonların anlık olup olmadığı gözlenir; bu task'ta yalnız servis + kaynaklar).

---

## Task 2 (T57): TrackedTextBlock — GlyphRun 0.07em advance + uppercase caps etiketi

**Amaç:** WPF'te letter-spacing YOK (dotnet/wpf#293). Tasarımın en yaygın tipografik detayı (panel/popover başlıkları + katman başlıkları: `DEPENDENCY GRAPH`, `PROJECTS`, `EVENT STREAM`…). GlyphRun tabanlı özel kontrol: karakter başına `FontSize×0.07` advance ekler; uppercase dönüşümü gömülü. Hair-space ekleme YASAK.

**TDD:**
- `[StaFact]` `TrackedTextBlockTests`: `Text="dependency graph"` + `TrackingEm=0.07` → render edilen GlyphRun'ın `AdvanceWidths` toplamı = Σ(glyphAdvance) + (N)·FontSize·0.07 (karakter başına ek); `MeasureOverride` sonucu bu toplamı yansıtır. Uppercase: görünen glyph'ler "DEPENDENCY GRAPH". Boş metin → 0 genişlik, çökme yok.
- `[StaFact]`: `GlyphTypeface.CharacterToGlyphMap` ile gömülü Geist'te caps + boşluk glyph kapsamı doğrulanır (eksik glyph → fallback riski test edilir).
- `[Fact]` (saf yardımcı varsa): advance hesabı saf fonksiyon olarak da test edilebilir.

**Production:**
- `App/Controls/TrackedTextBlock.cs`: `FrameworkElement` türevi; `Text`, `TrackingEm` (default 0.07), `FontSize`(default 11), `FontFamily`(Geist), `FontWeight`(500 Medium), `Foreground`(text-faint brush), `Uppercase`(default true) DP'leri. `OnRender`: `GlyphTypeface` → `GlyphRun` (her karakter advance = glyphAdvance + FontSize·TrackingEm). `MeasureOverride` toplam genişliği döner. `TextFormattingMode` kökten Display miras (değiştirme). InvariantCulture.
- design-v1 caps etiketleri buna geçmeli (kullanım noktaları T58/T63'te + lab'da örnek). Bu task kontrolü + testi + lab'da bir örnek satır ekler.

**Gözle doğrulama adımı:** `--it4a-lab` → caps etiket örnekleri (`DEPENDENCY GRAPH`, `PROJECTS`) 11px/500/0.07em uppercase text-faint; prototipteki panel başlıklarıyla harf aralığı yan yana karşılaştır.

---

## Task 3 (T56): AvalonEdit konsolun kalanı — colorizer + hibrit typewriter + kaskat + chunk loader

**Amaç:** Mevcut batch-append iskeletinin (ConsoleView/ConsoleBatcher) üstüne design-v1 §2.5/§3.6 birebir: renkli satır + hibrit aktif-satır typewriter + proje-log kaskatı (tempo+fade) + chunk loader. AvalonEdit tek yol (metin seçimi kutsal + renk + hacim). **Bu paketten düşen It-4 backlog kalemi:** console re-seed dup residual → reseed'i pump'ın tek okuyucusundan geçir.

**TDD (mümkün olduğunca saf + `[StaFact]`):**
- `[StaFact]` `ConsoleColorizerTests`: `DocumentColorizingTransformer` bir satırı offset-bazlı boyar — `HH:MM:SS` bölümü text-faint, `▸` amber, gerisi satır tipine göre (cmd=text-primary, info=text-secondary, dim=text-faint, success/warn/error=ilgili `-text`). Satır düz metin kalır (kopyalanınca anlamlı — renk yalnız görsel katman). Assert: verilen satır için üretilen renk aralıkları (offset→brush) beklenen.
- `[Fact]` `TypewriterSchedulerTests` (saf, Stopwatch-enjekte): "satır ≤~250ms" temposu — verilen elapsed'lerde açığa çıkan karakter sayısı monoton, ≤250ms'de tamamlanır; DispatcherTimer değil enjekte saat (D8).
- `[Fact]` `CascadeSchedulerTests` (saf): proje-log kaskatı **26ms'de 3 satır** temposu (satır başına 140ms fade); açığa çıkma sırası doğru; flash yok (başlangıç opacity 0).
- `[Fact]` `ChunkStitchTests` (saf): chunk loader — son 128-256KB'lik parçalar sequence-id ile dikilir; tepeye yaklaşınca önceki chunk prepend + **scroll telafisi** offset hesabı doğru (prepend edilen yükseklik kadar VerticalOffset artışı); tekrar/kayıp yok.
- `[StaFact]` re-seed: mod değişiminde (run↔proje) reseed pump'ın tek okuyucusundan geçince aynı satır iki dokümana düşmez (dup residual kapanır).

**Production (`App/Console/…`):**
- `ConsoleColorizer.cs` (`DocumentColorizingTransformer`): satır tipini tespit (VM satır tipini taşımıyorsa, satır metninden/prefix'ten türet ya da `ConsoleBatcher.Post`'a satır-tipi meta eklenir — implementer en temiz yolu seçer, kopyalanan metni düz tutarak). `ConsoleView`'a bağla.
- **Hibrit aktif satır:** en yeni satır dokümana girmeden altındaki `TextBlock`'ta daktilolanır (Stopwatch-bazlı, `DesiredFrameRate=30`); bitince `Document.Insert` ile commit — aktif satır ~250ms seçilemez (bilinçli). İmleç = **7×13px Rectangle** (font glyph'i DEĞİL), 1.1s blink; yazım bitince ~420ms sonra söner. Idle: `HH:MM:SS ▮ ready` (dim).
- **Kaskat:** proje-log moduna geçişte satırlar remount ile 26ms'de 3 satır, satır başına 140ms **opacity-fade** (translateY+scale AvalonEdit'te YAPILAMAZ → A13.1 kabul; tempo birebir). Building sonda amber `build in progress ▮`. Boş-durum metinleri (§2.5): skipped/queued/diğer birebir.
- **Chunk loader:** `getProjectLog` chunk geçmişi + scroll-telafili prepend + sequence-id dikişi (VM `_gate`/`LoadProjectLogAsync` deseniyle uyumlu). Render dilimi: konsol son 200 satır; `N lines` = TAM tampon (Ek A-16/23). Copy log butonu (konsol başlığı): satırlar `\n` ile panoya, **Clipboard retry sarmalayıcı** (CLIPBRD_E_CANT_OPEN), ikon 1400ms ✓ + "Copied" tooltip (Ek A-3).
- Alta-yapışık + `⌄ latest` pill T59'da (burada `StickToBottom` mevcut davranış korunur; pill T59'da eklenir — çift iş yok).
- **Renkler/süreler Foundation kaynaklarından** (hardcode YASAK).

**Gözle doğrulama adımı:** gerçek app: `dotnet run --project src/BuildOrchestrator.App` → repo kökü ver, Rebuild → konsolda canlı MSBuild akışı renkli; en yeni satır daktilo + blink imleç; bir karta tıkla → proje logu kaskatla açılır (26ms/3 satır), building'de amber `build in progress ▮`; metin seçilebilir + Copy log çalışır. Prototiple yan yana his karşılaştır.

---

## Task 4 (T58): Sticky overlay + LayoutMetrics — birikimli yapışan katman başlıkları

**Amaç:** WPF'te `position:sticky` yok. Overlay mimarisi: liste ScrollViewer üstünde ItemsControl; yapışık küme salt aritmetikle (36px satır + 24px başlık kümülatif tablosu). `LayoutMetrics` = TEK servis (follow-mode T59 ile PAYLAŞILIR). Virtualization KAPALI başlar (OSYS 191 bandı; §4.1 drift'i böyle sıfırlanır).

**TDD (aritmetik saf → `[Fact]`, overlay `[StaFact]`):**
- `[Fact]` `LayoutMetricsTests`: satır 36px + başlık 24px kümülatif offset tablosu — verilen (katman→satır sayısı) yapısından her satırın/başlığın mutlak Y'si doğru; verilen VerticalOffset'te **i'inci görünür başlık `top = i×24px`**'e yapışır (birikimli: alttakiler kaydıkça üsttekiler asılı). Seçim/follow scroll hedefi offset tablosundan doğru üretilir.
- `[Fact]`: katman YOK (varsayılan) → başlıksız tek liste, uniform 36px; sticky devrede değil.
- `[StaFact]` `StickyOverlayTests`: overlay ItemsControl gerçek başlıklarla aynı DataTemplate + opak zemin (geçiş görünmez); scroll'da yapışık başlık doğru başlığa döner.

**Production (`App/Controls/…`):**
- `LayoutMetrics.cs` (saf servis): satır/başlık yükseklik sabitleri + kümülatif offset tablosu; sorgular: `OffsetOfRow(i)`, `StickyHeadersAt(verticalOffset)`, `ScrollTargetForRow(i)`. Follow-mode T59 ile ORTAK (aynı instance).
- Sticky overlay: liste ScrollViewer (`VirtualizingPanel.IsVirtualizing=False` başlangıç; `ScrollUnit=Pixel`) üstünde `ItemsControl` overlay; katman başlıkları TrackedTextBlock (caps 11px) + mono satır sayısı; opak `surface`/`surface-base` zemin. Katman verisi VM `Projects`'ten grupla (varsayılan boş → tek liste).
- **Virtualization KAPALI** (yorum: §4.1 drift; 500+ hedefi T51/It-5 drift kalibrasyonuyla açar — burada DEĞİL).

**Gözle doğrulama adımı:** `--it4a-lab` → 36 örnek proje 6 örnek katmanla (Load sample layers karşılığı); kaydır → katman başlıkları birikimli yapışır (i×24px), alttaki başlık üsttekini yukarı iter. Prototipteki sticky davranışıyla yan yana.

---

## Task 5 (T59): ScrollAnimator + BottomAnchor + FollowScroll + ⌄ latest pill

**Amaç:** WPF'te native smooth scroll YOK (`VerticalOffset` read-only). Attached DP + animasyon; alta-yapışık scroll; follow-mode (frontier takibi); `⌄ latest` pill. LayoutMetrics'i (T58) PAYLAŞIR.

**TDD:**
- `[StaFact]` `ScrollAnimatorTests`: attached DP `ScrollAnimator.VerticalOffset` + `DoubleAnimation` bir ScrollViewer'ı hedefe kaydırır; wheel/kullanıcı scroll gelince animasyon **iptal** (`BeginAnimation(prop, null)`) + suppress bayrağı; `ScrollUnit=Pixel` önkoşulu.
- `[Fact]` `BottomAnchorTests` (saf): `ExtentHeightChange`>0 (içerik) vs kullanıcı scroll ayrımı; **48px eşik** — dipten ≤48px iken içerik büyümesi dibe yapışır, >48px iken serbest; `jumping` bayrağı.
- `[Fact]` `FollowScrollTests` (saf): follow-mode **550ms throttle + 54px dead-band**; ilk building satırını görünür tutar; hedef sapması <54px ise dokunmaz; LayoutMetrics offset tablosundan hedef.
- `[StaFact]` pill: dipten ≥48px iken `⌄ latest` pill görünür (560ms jumping penceresi, Ek A-15); tıkla → yumuşak dibe; dibe dönünce kaybolur.

**Production (`App/Controls/…`):**
- `ScrollAnimator.cs` (attached DP): `VerticalOffset` attached property + `DoubleAnimation` (süre Foundation'dan; ease Foundation KeySpline); wheel iptali + suppress. `ScrollUnit=Pixel`.
- `BottomAnchorBehavior.cs`: 48px eşik + jumping (design-v1 §2.5/§2.6 birebir).
- `FollowScrollController.cs`: 550ms/54px, LayoutMetrics (T58 instance) paylaşımlı; seçimde durur, seçim kalkınca sürer.
- `⌄ latest` pill: surface-overlay zemin, border-strong, radius-md (chip/kbd 3? — pill radius-md=overlay 8), popover gölgesi; pill hover'da yüzey/metin bir adım açılır (Ek A-15). Konsol + stream ORTAK kural.
- Auto-scroll arbitration tam servisi T48 (paket dışı) — burada primitifler + suppress/handoff bayrağı sağlanır (yo-yo yasağı için altyapı).

**Gözle doğrulama adımı:** `--it4a-lab` (liste + konsol) → koşu simülasyonunda liste frontier'i yumuşak takip eder; elle yukarı kaydır → takip durur + `⌄ latest` pill çıkar; pill'e tıkla → yumuşak dibe. Konsolda da aynı pill. Prototiple karşılaştır.

---

## Task 6 (T63): Graf hibrit render — Shapes yolu + kamera + dash-flow tek clock + EdgeStyleResolver

**Amaç:** design-v1 §2.3 birebir dependency graf: 26px 4px-radius KARE düğüm, katmanlı DAG, kübik bezier kenar, dash-flow, kamera, seçim sönmesi, dep-hata rozeti. ≤~150 düğüm **Shapes yolu** (tasarımın 36'sı bu bantta); T51 (500-1000, DrawingVisual katmanları) It-5 perf'i — burada mimari tasarlanır ama Shapes yolu ship+verify edilir. **Graf etiketleri `TextFormattingMode=Ideal`** = LOKAL override (kök Display'e dokunma).

**TDD (saf çekirdek → `[Fact]`, render `[StaFact]`):**
- `[Fact]` `EdgeStyleResolverTests` (saf, prototip if-zinciri portu): (kaynak durumu, hedef durumu, seçili mi, hata dalı mı) → (renk, thickness, dash pattern, akan mı). Tüm dallar: default border 1px; hedef building→amber akan dash `4 7`; succeeded→yeşil; failed→kırmızı; hata dalı (kaynak failed/depIssue)→kırmızı statik dash `3 4`; seçiliye değen→amber(veya hata dalıysa kırmızı) 1.6px opak. **Dash birimi StrokeThickness çarpanı** (1.6px'te bölünmüş).
- `[Fact]` `GraphCameraTests` (saf): scale panele sığar + **0.68–1.08** kıstırma; hedef = seçili düğüm / building frontier ağırlık merkezi / done'da merkez; frontier küçük sapma eşiği (<~8px retarget etme); merkez y=H×0.3, pan 12px, tx/ty Math.round (Ek A-10).
- `[StaFact]` `GraphRenderTests`: 26px 4px-radius kare düğüm; discovered kesikli çerçeve (`Rectangle.StrokeDashArray`); dep-hata rozeti 13px daire + kırmızı üçgen; ilk açılış katman stagger (55ms/katman, başlangıç Opacity=0 flash yok); akan kenar TEK paylaşımlı clock (UIElement Path).
- `[StaFact]`: reduced-motion → statik dash + renk güncellemesi (Foundation servisi).

**Production (`App/Graph/…`):**
- `EdgeStyleResolver.cs` (saf fonksiyon). `GraphLayout.cs` (katman → yatay sıra, satır aralığı 96px, düğüm aralığı ≤96px; kenar kübik bezier). `GraphCamera.cs` (TransformGroup Scale+Translate; From'suz To-animasyonu + SnapshotAndReplace; 460ms ease-in-out).
- `GraphView` (Shapes yolu ≤150): düğüm = 26px `Rectangle`/`Border` RadiusX/Y=4 + 13px paket ikonu (SVG→XAML Geometry veya basit placeholder — tam ikon T64) + altında **mono 10px** kısa ad (`OSYS.` öneki atılır), `TextFormattingMode=Ideal` LOKAL. Kenarlar frozen `StreamGeometry` bezier. **Akan dash HER ZAMAN UIElement Path**, TEK paylaşımlı clock; dash `4 7` To=-22 0.9s. Seçim: seçili+komşu normal, gerisi %25 opak, kenarlar %16. Kamera GraphCamera'dan. Panel başlığı 28px `surface` alt border-subtle: `DEPENDENCY GRAPH` (TrackedTextBlock) + sağda mono 11px `N projects · M dependencies`.
- **T51 3-katman mimarisi (EdgeLayer/NodeLayer DrawingVisual/FlowOverlay)** yalnız TASARIM notu + genişleme kancası (It-5); bu task Shapes yolunu ship eder — over-engineering YASAK.
- Renk/süre Foundation kaynaklarından; sample-data (Task 1) tüketir.

**Gözle doğrulama adımı:** `--it4a-lab` (graf paneli) → 36-düğüm örnek graf; koşu simülasyonunda building frontier akan amber dash ile grafta aşağı akar; düğüme tıkla → kamera 460ms kayar + komşu-dışı sönme; failed düğümde kırmızı statik dash + ▲ rozet. Prototipteki graf canlı animasyonuyla yan yana his karşılaştır.

---

## Task 7 (T62): Pencere kabuğu paketi — Snap Layouts + restore glyph + tray/balloon + single-instance + Alt+B

**Amaç:** Mevcut WindowChrome/maximize-fix/DWM tabanı üstüne pencere kabuğunun kalanı: Snap Layouts, restore glyph swap (K8), tray + ilk-X balloon (K5), single-instance (AllowSetForegroundWindow), Alt+B global hotkey.

**TDD (P/Invoke ağırlıklı — saf yardımcılar test edilir, entegrasyon manuel):**
- `[Fact]` `SnapLayoutHitTestTests` (saf): WM_NCHITTEST için maximize buton rect'i + DPI → HTMAXBUTTON dönme kararı; buton dışında normal hit-test; DPI-ölçekli rect hesabı doğru.
- `[StaFact]` `RestoreGlyphTests`: `WindowState=Maximized` → glyph "restore" (iç içe iki kare); Normal → tek kare; swap tetiklenir.
- `[Fact]` `SingleInstanceTests` (saf/mockable): ikinci instance mutex ile tespit + ilk instance'a sinyal; `AllowSetForegroundWindow(pid)` sinyalden ÖNCE çağrılır (sıra assert). (Named-mutex + pipe; gerçek çok-process manuel.)
- `[Fact]` `HotkeyTests` (saf): Alt+B → RegisterHotKey modifier/vk hesabı; çakışmada sessiz devre dışı (kayıt başarısız → hata değil).
- `[Fact]` `TrayBalloonOnceTests` (saf): ilk X kapatmada balloon gösterilir + bayrak persist → ikinci X'te gösterilmez.

**Production (`App/Shell/…` + `MainWindow`):**
- Snap Layouts: `WndProc` hook — `WM_NCHITTEST`'te maximize buton alanı→`HTMAXBUTTON`, `WM_NCLBUTTONDOWN/UP` işleme, hover görselini elle sür; DPI-hassas rect. (dotnet/wpf#4825 — WindowChrome vermiyor.)
- Restore glyph: MaxButton içeriği `WindowState`'e göre swap (Maximized→iki-kare geometry). K8.
- Tray: `H.NotifyIcon.Wpf` NuGet (App.csproj'a ekle) — tray ikonu (16px; SVG oto-küçültme "D"yi bozar → minimal elle ICO/raster üret; TAM ikon hattı T64). `X`→`Close()` yerine tray'e küçült; **ilk X'te OS tray balloon** (K5, tek sefer, bayrak persist). Tray menü: Exit (→cascade-kill: App exit → EngineHost dispose → outer Job), Stop. `OnClose` mevcut davranışını (Close) tray-minimize ile değiştir.
- Single-instance: named Mutex + ikinci instance'tan ilk instance'a sinyal (pipe/message); ilk instance `AllowSetForegroundWindow` + `Activate` (§4.3 — yoksa yalnız taskbar flash). `AllowSetForegroundWindow(pid)` sinyalden ÖNCE.
- Alt+B: `RegisterHotKey` (ayarlanabilir; çakışmada sessiz devre dışı) → tray'den pencereyi getir (animasyon reduced-motion'a tabi — Foundation).
- **Kökteki font ayarına + FontAbWindow'a DOKUNMA.**

**Gözle doğrulama adımı:** gerçek app: `dotnet run --project src/BuildOrchestrator.App` → (1) maximize butonunun üstüne gel → Win11 Snap Layouts uçbirimi çıkar; (2) maximize et → buton glyph'i restore (iki kare) olur; (3) X → pencere tray'e küçülür + ilk seferde balloon; tray ikonuna tıkla → geri gelir; (4) ikinci instance başlat → ilk pencere öne gelir; (5) Alt+B → tray'den pencere gelir; (6) tray Exit → uygulama + supervisor kaskat kapanır.

---

## Sonda: whole-branch review (en güçlü model) → aşamamızı kaydet

Tüm task'lar bitince `scripts/review-package $(git merge-base main HEAD) HEAD` → final whole-branch review (superpowers:requesting-code-review, en capable model): A13.2 invaryantları, design-v1 birebirlik, Minor roll-up triyajı. Ardından **aşamamızı kaydet** (summary + handoff). **Merge/push kullanıcı onayında.**
