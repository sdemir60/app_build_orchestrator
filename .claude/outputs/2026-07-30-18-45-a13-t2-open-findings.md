# A13 / T2 — AÇIK BULGULAR (fix round 1, yarım kaldı)

**Neden bu dosya var:** T2'nin 3-lens review'u yapıldı ve fix dalgası dispatch edildi, ama implementer
oturum limiti nedeniyle **kesildi ve commit atmadı**. Çalışma ağacında 18 değişmiş dosya var. Bulgular review
ajanlarının çıktısındaydı (dosya yazmaları istenmemişti) — kaybolmasın diye buraya alındı.

**Ağaç durumu:** branch `a13-visual-debt-automation`, HEAD `0ed106c`. T2 fix round 1 için **commit YOK**.
Değişmiş dosyalar: `Controls/FollowScrollController.cs` · `Controls/IconVisual.cs` ·
`Controls/StickyLayerList.xaml.cs` · `MainWindow.xaml` · `MainWindow.xaml.cs` · `ShellRoot.xaml.cs` ·
`ViewModels/RunViewModel.ActionBar.cs` · `ViewModels/RunViewModel.Workspace.cs` · `ViewModels/RunViewModel.cs` ·
`Views/ActionBar.xaml.cs` · `Views/WorktreePopover.xaml.cs` · testler: `BranchInventoryTests.cs` ·
`FollowScrollControllerTests.cs` · `MainWindowHost.cs` · `NoFilterMatchTests.cs` · `ProjectFilterChipTests.cs` ·
`ProjectListFilterTests.cs` · `RunViewModelStateTests.cs`

> **Implementer kesildiğinde** "OSYS entegrasyon testini izole koşup değişikliklerle ilgisiz olduğunu
> doğrulayayım" adımındaydı — yani fix'leri yazmış, tam süiti koşmuş ve **bir test kırmızısını**
> araştırıyordu. Bu iş **yarım sayılmalı**; ağaç sıfırdan doğrulanmalı.

---

## CRITICAL

### C1 · Branch seed `Branch`'i KALICI ÇİVİLİYOR — harici `git checkout` sonrası bayat branch derleniyor

**Konum (T2'nin getirdiği kod):** `src/.../ViewModels/RunViewModel.Workspace.cs:279`
`if (Branch.Length == 0 && ActiveBranchName is { } active) Branch = active;`

**Kanıt zinciri (mevcut kod, değişmez):** `MainWindow.xaml.cs:128` (`saved.Branch` seed) ·
`RunViewModel.ActionBar.cs:35-36` (`IsWorktreeForced`) · `Views/WorktreePopover.xaml.cs:88-89, 93-97` ·
`Views/ActionBar.xaml.cs:363-364` · `Supervisor/Program.cs:183, 207-208, 215-216`

1. T2 öncesi `Branch` çoğu kullanıcıda sonsuza dek `""`; `Program.cs:183` tek git çağrısı yapmadan in-place
   dönüyordu, `:214` "branch boş ⇒ niyet AKTİF branch" diyor → **her build hep doğru branch'i hedefliyordu.**
2. İlk Sync `Branch = "main"` yazıyor, değer **diske persist ediliyor**.
3. Kullanıcı terminalde `git checkout feature/y` yapıyor.
4. Seed YALNIZ `Branch` boşken koştuğu için `Branch` `"main"` kalıyor — uygulama kendini ASLA düzeltemiyor.
5. `IsWorktreeForced == true` ama `UseWorktree == false` (kullanıcı popover'a hiç girmedi).
6. Build: `Program.cs:216` `!mandatory && !cmd.UseWorktree` sağlanmıyor → `mandatory=true` →
   **worktree ZORUNLU açılıyor, `main`'in committed HEAD'i derleniyor**, kullanıcı `feature/y`'deyken.
7. UI: chip **"off"** (`ActionBar.xaml.cs:364`), popover switch **işaretsiz + DISABLED** (`:88-89`),
   source satırı "working directory — local changes included".

**Testte YOK:** `forced + UseWorktree=false` kombinasyonu hiçbir testte yok.
`A_non_active_branch_makes_the_worktree_switch_forced_on_and_disabled` forced'a `SelectBranch` ile giriyor, o da
`UseWorktree=true` set ediyor (`ActionBar.cs:51`). `The_inventory_never_overwrites_a_branch_that_was_already_chosen`
`IsWorktreeForced==true` assert ediyor ama `UseWorktree`'ye bakmıyor.

**I4 (C1'in ikizi, birlikte çöz):** `Program.cs:207-208` — `Branch` dolu olduğu için "aktif branch çözülemedi"
(detached HEAD / bozuk git) durumu artık `warn + in-place` yerine **`planFailed` → run HİÇ BAŞLAMAZ**'a düşüyor.
Daha önce çalışan in-place Build kırmızıya dönüyor.

**FIX (üçü birlikte):**
- Diskten gelen bayat seed ile kullanıcının popover'dan yaptığı AÇIK seçimi AYIR (`_branchChosenByUser` biti).
  Envanter geldiğinde: `Branch` envanterde yoksa/aktif değilse **tazele**, açık seçimi KORU.
- Forced iken `UseWorktree` zorla `true` — **tek yerde**, `IsWorktreeForced` türetimiyle aynı sınıfta.
  Bugün bunu yalnız `SelectBranch` yapıyor, seed yolu yapmıyor.
- `StartRunCommand.Branch`'e giden değerin semantiğine karar ver ve gerekçelendir.
- **Regresyon testleri:** (a) bayat `Branch` + farklı aktif branch → chip `"off"` DEĞİL,
  `StartRunCommand.UseWorktree == true`; (b) `forced + UseWorktree=false` artık üretilemiyor;
  (c) detached HEAD'de in-place Build hâlâ koşuyor.

---

## IMPORTANT

### I-A · Filtre chip'i ilk gerçek tıklamadan sonra KALICI pasif-griye düşüyor
`ShellRoot.xaml.cs:132` `chip.IsChecked = true` yalnız ctor'da BİR KEZ; `SetFilterChip` (`:152-156`) yalnız
`Text`/`Visibility` yazıyor. Chip bir `ToggleButton` — gerçek tıklamada `ButtonBase.OnClick` →
`ToggleButton.OnToggle` `IsChecked`'i **false** yapıyor, bir daha kimse `true` yapmıyor. `Ds.Chip`'in amber
görünümü tamamen `IsChecked=True` trigger'ına bağlı (`Resources/Controls.xaml:328-332`); design-v1 §2.4/§3.4 ve
prototip (`BuildApp.jsx:1492` `DS.Chip active`) amber şart koşuyor.
**Test göremiyor:** `ProjectFilterChipTests.cs:109` sentetik `chip.RaiseEvent(new RoutedEventArgs(ButtonBase.ClickEvent))`
kullanıyor — `OnClick`/`OnToggle` HİÇ koşmuyor. Amber assert'i (`:120-131`) hiç tıklamadan koşuyor.
**FIX:** `SetFilterChip` içinde `IsChecked = true` (durum "her zaman aktif"), veya chip'i `Button` + kalıcı amber
stiliyle kur. Test: `ButtonAutomationPeer.Invoke`/`Toggle` ile gerçek tıklama + **tıklama sonrası yeniden filtre
uygulayıp** `IsChecked`/`Background` tekrar assert.

### I-B · ✕ chip'in aktif rengini izlemiyor + `IconVisual.Make` inline kopyalanmış
`ShellRoot.xaml.cs:111-129` (`BuildFilterChip`) `Controls/IconVisual.cs:16-23`'ün beş adımını yeniden yazmış —
o dosyanın XML doc'u *"tek yerde toplanır (kopya YASAK)"* diyor. `:114-116`'daki üç linecap ataması ÖLÜ
(`IconPaint.Apply` aynı üçünü `IconPaint.cs:49-51`'de kuruyor).
✕ `Path`'i `SetResourceReference(Stroke, "Brush.TextDim")` ile SABİTLENMİŞ → chip'in `IsChecked=True`
trigger'ının set ettiği `AnimatedForeground = Brush.AmberText`'i miras almıyor. Doğru desen:
`Views/ActionBar.xaml.cs:336-344` `BoundChipIcon`
(`path.SetBinding(Shape.StrokeProperty, new Binding(nameof(Control.Foreground)) { Source = chip })`).
**FIX:** `IconVisual.Make` + `BoundChipIcon` desenini uygula; iki çağıranı olduğu için `BoundChipIcon`'u ortak
yere çıkar. Test: `Assert.NotNull(glyph.Data)` (`:148`) yetmez — kaynağın `Icon.ChipRemove` olduğunu VE
`Stroke`'un amber'ı izlediğini pinle.
**İlgili (m4):** `IconPaint.Apply` (`IconPaint.cs:42-43`) kalınlığı ANINDA çözüyor; `ShellRoot` ctor'unda öğe
ağaçta değil → `Application` yoksa `null` dönüp **erken çıkıyor**, `Stroke` hiç bağlanmıyor. Üretimde
`Application.Resources` sayesinde çalışıyor (T49 sınıfının kardeşi). Kalınlığı da `SetResourceReference` ile
ertele veya `Loaded`'da boya; test `StrokeThickness > 0` pinlesin.

### I-C · Her topoloji değişiminde liste İKİ KEZ tam reset ediliyor
`RunViewModel.Workspace.cs:228` `RefreshRunSurface()` **`TopologyChanged`'den (`:237`) ÖNCE** çağrılıyor.
Zincir: `RefreshRunSurface` → `OnPropertyChanged(VisibleProjects)` → `RefreshVisibleRows`
(`MainWindow.xaml.cs:420`) imza değişti → guard tutmuyor → `ApplyProjectGroups(reveal:false)` **tam reset**;
sonra `TopologyChanged` → `RefreshProjectGroups` → `ApplyProjectGroups(reveal:true)` **guard'sız, ikinci tam
reset**. Birinci geçiş çöp; 191 satırlık realize maliyeti iki kat. "Churn imza guard'ıyla kesiliyor" savunması
bu yol için geçerli DEĞİL — guard yalnız `reveal:false` dalında.
**FIX:** sırayı çevir (`TopologyChanged` önce, `RefreshRunSurface` sonra) ya da `reveal:true` dalını da imza +
"reveal borcu" ayrımıyla koru. Sıra değişiminin `Counters`/graf beslemesine etkisini AYRICA doğrula.

### I-D · `SetGroups` her çağrıda `FollowScrollController`'ı yeniden kuruyor → 550ms throttle sıfırlanıyor
`StickyLayerList.xaml.cs:127` `_follow = new FollowScrollController(Metrics, …)`.
Kanıt: `FollowScrollController.cs:26` `_lastMoveAtMs = long.MinValue` · `:65-66` (`elapsed = MaxValue` →
`ShouldMove` hep true). Eskiden `SetGroups` yalnız topoloji değişiminde koşuyordu; artık görünür küme her
değiştiğinde koşuyor → koşarken `Building`/`Failed` filtresi açıkken her `projectStarted`/`projectSucceeded`
yeni controller yaratıyor → throttle atlanıyor, takip 200ms tick'te hareket ediyor. design-v1 §3.3'ün throttle
sabiti filtre altında etkisizleşiyor. `_hasSelection` da düşüyor (`ScrollArbiter` maskeliyor; throttle'ın
arbiter'da karşılığı YOK).
**FIX:** `_follow` yeniden yaratılmasın; `FollowScrollController`'a `Rebind(LayoutMetrics)` ver.

### I-E · A12 guard'ının TABAN ÇİZGİSİ assert'siz — sessiz vakum riski
`ProjectListFilterTests.cs:169-178`: `PumpUntil(() => list.RevealGeneration > 0, 3s)` timeout'ta hata VERMEZ;
`afterTopology` 0 kalabilir ve `Assert.Equal(afterTopology, list.RevealGeneration)` `0 == 0` ile trivial yeşil
olur. Kardeş test `AnimationsEnabledProvider = () => true` diyor (`:188`), bu test demiyor.
**FIX:** `:170` sonrası `Assert.True(afterTopology > 0, "topoloji reveal'i hiç oynamadı — taban çizgisi yok")`
+ motion ön-koşulunu iki yarıda eşitle.

### I-F · Test fixture'ları kopyalanmış ve ŞİMDİDEN ayrışmış
`Node(string,int)` birebir aynı: `NoFilterMatchTests.cs:28`, `ProjectFilterChipTests.cs:28`;
`ProjectListFilterTests.cs:26` üst kümesi. `NewShell(temp)` üç yerde. **Ayrışma:**
`ProjectFilterChipTests.NewShell` `RootPath`'i set etmiyor → o kabuk `HasWorkspace == false` ile, PickRepository
overlay'i AÇIKKEN koşuyor; diğer ikisi öyle değil.
**FIX:** `MainWindowHost.NewWithProjects(temp, params (string Name, string? Layer)[] nodes)` → `(window, vm, list)`.

### I-G · `ListWorktreesCommand` de App'ten HİÇ gönderilmiyor (6. üretim boşluğu, kapsama alındı)
Worktree havuzu boş kaldığı için `AutoWorktreeName` mevcutları hep 0 sayıp **çakışan ad üretebiliyor**.
İlgili iki minor bununla kapanır: `EffectiveWorktreeName`'in `Worktrees` bağımlılığı title bar aboneliğinde
dinlenmiyor (`MainWindow.xaml.cs:145-148`); `IsWorktreeForced` değişim bildirimi taşımadığı için AÇIK bir
worktree popover'ı envanter gelince tazelenmiyor (`RunViewModel.ActionBar.cs:35-36`,
`WorktreePopover.xaml.cs:60-70` `Branches.CollectionChanged` dinlemiyor).

---

## DOKÜMAN / İDDİA DÜZELTMELERİ (kod değil, doğruluk)

- **m1:** `StickyLayerList.xaml.cs:144-147` yorumu "bekleyen bir reveal İPTAL EDİLİR" diyor — bayrak
  TÜKETİLDİKTEN sonra (`:238-242` `DispatcherPriority.Loaded` kuyruğu) bu YANLIŞ; kuyruktaki çağrı iptal
  edilmiyor. İptal isteniyorsa `PlayRevealStagger` generation-guard'lı olsun (`RevealStagger.Generation` deseni
  mevcut), istenmiyorsa yorumu düzelt.
- **m9:** `task-T2-report.md`'deki "filtre altında scroll başa döner (doğru davranış)" iddiası ÖLÇÜLMEMİŞ ve
  büyük olasılıkla yanlış — WPF `ScrollViewer` `ItemsSource` değişiminde `VerticalOffset`'i KORUR ve yeni
  extent'e clamp eder. Ya ölç ya gerekçeyi düzelt.
- **m5:** `MainWindow.xaml:168-169` `TextTrimming="CharacterEllipsis"` NO-OP — yatay `StackPanel` (`:142`)
  çocuklarını sonsuz genişlikle ölçer; uzun repo/branch adında metin caption butonlarının altına sert kesilir.
  `MaxWidth`/`Grid` ile sınırla. Ayrıca ek `Margin="8,0,0,0"` taşırken gövde ayracı `" · "` tek boşluk.

---

## LEDGER'A DÜŞEN MINOR'LAR (fix döngüsüne girmez)

Envanterin Sync komutundan SONRA gitmesi (ilk Sync hâlâ boş branch ile) — C1 fix'i kapatabilir ·
prototipin `No projects match "{query}".` varyantı (triyaja) · filtre altında follow-mode davranışı pinsiz ·
`ProjectListFilterTests`'in saf `ProjectFilterTests` semantiğini realize maliyetiyle tekrarlaması ·
`SetGroups_plays_the_reveal_only_when_it_is_asked_to`'nun pozitif yarısının `StickyRevealTriggerTests` kopyası ·
`ChipContentGap=6` üçüncü kopya · `MainWindow` ctor'unda 8 anonim `PropertyChanged` aboneliği ·
`BranchInventoryTests` `EngineHost`'u dispose etmiyor · `VisibleProjects` bildirimi başına 3-4 tam filtre
değerlendirmesi + ~15KB imza dizesi.

---

## KOŞUM ÖLÇÜTÜ

`dotnet build BuildOrchestrator.slnx` → 0/0 · tam süit (baseline **1509 passed / 2 skipped / 0 failed**) ·
guard'lar `--filter "NoHardcodedColor|NoHardcodedMotion|NoSleepPoll|TokenBrushes|TokenRealizeCoverage"` → 69/69.
C1 fix'i `IsWorktreeForced`/`UseWorktree` yollarını değiştirdiği için `ActionBarTests`, `WorktreePopover` ve
`SettingsDialogTests` AYRICA sürülmeli.
