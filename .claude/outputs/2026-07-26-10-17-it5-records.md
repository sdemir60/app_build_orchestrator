# It-5 — kabul kaydı (acceptance records)

**Branch:** `it5-perf-dist` @ `f65b2b3` (BASE `1b63720` = `main` = `origin/main`; merge/push YAPILMADI).
**Kapsam:** v7 Part C It-5 — **SON iterasyon**. P1·P2·DRAG·P3 · L1 · G1·G2 · W1·W2 · D1·D2·D3 · T1 · V1·V2·V3.
**Kanıt kaynağı:** `.superpowers/sdd/progress.md` (It-5 ledger) + kodun kendisi + bu belgede kaydedilen
**gerçek koşumlar** (aşağıdaki §0). Ledger ile bir başka belge çelişirse **ledger kazanır**.

> **Belgenin kuralı:** her satır bir **kanıta** bağlıdır — commit sha, `dosya:satır`, ölçüm sayısı, test adı
> ya da script çıktısı. Kanıtı olmayan hiçbir iddia yazılmadı; kanıtı zayıf olanlar **nitelemesiyle** yazıldı.

---

## 0. Koşum kayıtları (bu belge yazılırken, bu makinede)

| Ne | Komut | Sonuç |
|---|---|---|
| Build | `dotnet build BuildOrchestrator.slnx` | **0 Uyarı / 0 Hata** (2,57 sn) |
| **Tam süit** (acceptance DAHİL) | `dotnet test tests/BuildOrchestrator.Tests/BuildOrchestrator.Tests.csproj` | **1428 passed · 2 skipped · 0 failed** (toplam 1430, 2 dk 38 sn) |
| Canlı OSYS acceptance | `dotnet test --filter "Category=Acceptance"` | **3 passed / 0 failed / 0 skipped** (1 dk 22 sn) |
| Non-acceptance (çıkarma) | — | **1425 passed · 2 skipped · 0 failed** |
| Publish doğrulaması | `powershell -File scripts\verify-publish.ps1` | **RESULT: PASS**, exit 0, **17/17** (16 `Check` + 1 ön koşul) |

> **Koşum anı:** yukarıdaki sayılar `b59851b` ağacında alındı. HEAD sonradan **`f65b2b3`**'e ilerledi
> (V1/V2 fix round 2 — **yalnız test dosyaları**, üretim kodu değişmedi); süit **1428/2/0** ve build **0/0**
> **değişmedi** (kontrolcü doğruladı).

**2 skipped nedir (ikisi de bilinçli, görünür atlama):**
- `App.CompositeFontSpikeTests.CompositeFont_LineSpacing_155_holds_in_AvalonEdit` — It-0'dan beri spike skip'i.
- `App.DragReorderTests.Reorder_uses_mouse_capture_and_never_calls_the_ole_drag_drop_api` —
  `Skip.If(Mouse.PrimaryDevice.ActiveSource is null)`; **oturuma gerçek mouse trafiği akmadığında** atlanır
  (kök neden DRAG task'ında tanı testiyle kanıtlandı). ⚠️ Bu skip **yalnız davranışsal ayağı** kapsar;
  A13.2'nin "`DragDrop.DoDragDrop` çağrılmaz" yasağı **KOŞULSUZ** bir kaynak-tarama guard'ıyla ayrıca korunur
  (DRAG, `b5a8f83`).

**Koşum hijyeni:** `verify-publish.ps1` sonrası makinede kalan `BuildOrchestrator.App` /
`BuildOrchestrator.Supervisor` process'i **0**, geçici publish klasörü **silinmiş** (doğrulandı).

---

## 1. It-5 kabul kalemleri — kanıt tablosu

### 1.1 CPU cap tavanı tutuyor ✅ *(iki nitelemeyle)*

**Ölçüm (P1, `81eda1f`..`874af76`):** 8 core, **10 busy-loop child**, cap **%40** →
**capsiz 0,822 · capli 0,399** (ramp-in penceresi ölçümden atlanıyor).
**Otomatik kanıt:** `tests/BuildOrchestrator.Tests/ProcessControl/JobCpuRateTests.cs:232`
`Cpu_hard_cap_holds_under_a_saturating_child`.
**Mekanizma pinleri (mutasyonla kanıtlı, P1 fix round 1):**
- Ham alan round-trip'ten BAĞIMSIZ assert'lenir: `CpuRate == percent*100`, `ControlFlags == ENABLE|HARD_CAP`
  (`JobCpuRateTests.cs:93` `Cpu_cap_is_written_as_hundredths_of_a_percent_with_a_hard_cap`).
  Mutasyon: `(uint)percent` → **4 dal RED**; `HARD_CAP` düşürülünce → **5 test RED**.
- `QueryCpuRate` HARD_CAP'siz rate control'ü **cap saymaz** (`JobCpuRateTests.cs:119`).
- Priority 3 dal `[Theory]` (`JobCpuRateTests.cs:150`); `Normal↔BelowNormal` takası → **2 dal RED** (32↔16384).
- Cap+priority yazımı `KILL_ON_JOB_CLOSE`'u korur (Query→OR→Set) ve cascade hâlâ ≤2s
  (`JobCpuRateTests.cs:170` `Cascade_kill_still_lands_within_2s_after_a_cap_and_priority_write`).

> ### ⚠️ NİTELEME 1 — tavan **mutlak değildir**: belgelenmiş, ref-count'lu **copy-contention penceresi hariç**
> Post-build copy sıkıştığında (MSB302x) inner Job'un cap'i **bilinçli olarak taban'a** yükseltilir:
> **%70 + `BelowNormal`** (= `Balanced`'ın değerleri). Yani bir `Light` (%40) koşumunda cap, o pencere boyunca
> **%40 değil %70**'tir.
> **Kod:** `src/BuildOrchestrator.Core/ProcessControl/PerfProfile.cs:44` (`CopyPhaseFloorPercent`) ve `:52`
> (`CopyPhaseFloorPriority`) — ikisi de **tesadüfen değil kasıtlı olarak** `Balanced`'dan türetilir;
> `src/BuildOrchestrator.Supervisor/RunCoordinator.cs:406-424` (etkin cap/priority hesabı — `:406`
> `EffectiveCapLocked`, `:422` `EffectivePriorityLocked`),
> `:439-445` (ref-count `_copyFloorDepth++`, ilkte yaz), `:465-466` (son çıkışta run profiline dön).
> **Testler:** `RunCoordinatorTests.cs:1755` (yükselt + geri koy) · `:1832` (pencere içindeyken canlı
> `setPerfMode` cap'i **tabanın altına itemez**) · `:1864` (iki worker → **bir kez** yükselir, **son** çıkanla
> geri döner) · `:1773`/`:1790` (capsiz `Full` ve perf-modsuz run'da governor'a **hiç dokunulmaz**).
> Kaynak kayıt: ledger D2 bulgu 2 + P3.

> ### ⚠️ NİTELEME 2 — cap **yalnız inner Job'a** yazılır → **Sync/IPC aç kalır** (T20'nin çekirdek cevabı)
> `git` ve `vswhere`, `ProcessRunner` üzerinden **düz `Process.Start`** ile koşar
> (`src/BuildOrchestrator.Core/Processes/ProcessRunner.cs:42`) ve **inner Job'a hiç girmez**. Inner Job üyeliği
> yalnız tek kapıdan doğar: `JobProcessLauncher` ile **suspended doğum → `AssignProcessToJobObject` → resume**
> (`Core/ProcessControl/JobProcessLauncher.cs:64` `CreateProcessW` SUSPENDED, `:73` `job.Assign`, `:83`
> `ResumeThread`; `Core/ProcessControl/JobObject.cs:42`) ve o kapının tek çağıranı
> `Core/MsBuild/MsBuildInvoker.cs:68`'dir.
> Sonuç: cap **MSBuild'i** kısar, **Sync/IPC'yi kısmaz**. (git/vswhere Supervisor'ın child'ı olduğu için
> **outer** job'a miras yoluyla dahildir — kaskat garantisi bozulmaz.)

**İlgili tasarım kararları (P2, `d32056f`..`bbdc43c`):** canlı değişen şey **cap + priority**'dir,
**paralellik DEĞİL** (worker'lar run başında bir kez yaratılır). Planlama penceresi deliği kapatıldı
(`_pendingPerf`: `IsStarting` sırasındaki niyet run başında komuttakini ezer) ve `_pendingPerf`/`_perfApplied`
run kapanışının **son kilit bloğunda** (`_runActive=false` ile aynı kritik bölge) sıfırlanır — sızıntı
penceresi `PumpGateStream` test seam'iyle **gerçekten açılarak** pinlendi.

---

### 1.2 Copy fazı aç kalmıyor ✅ (P3)

**Commit:** `fbf7db6` + fix `69b0ae3` · re-review **PASS / 0 Critical / 0 Important**.
**Ne yapılır:** copy-contention penceresinde cap **ve** priority tabana gevşer (yukarıdaki Niteleme 1),
ref-count'ludur, pencere kapanınca run profiline dönülür; ayrıca **cap-farkındalı backoff (×1.5)**.
**Testler:** `RunCoordinatorTests.cs:1755` · `:1804` (backoff canlı run profiline bağlı) · `:1864` (ref-count) ·
`:1907` (graceful stop, uçuştaki post-build copy'nin drain olması için cap kalkar) · `:1948` · `:1978`.

**Review'ın bulduğu Critical (iki lens bağımsız buldu) ve kökü — kayda değer:** `DrainCapLocked` kararı
**capsiz (Full) profilde hiç kaydedilmiyordu** → graceful-stop drain'i sırasında gelen canlı
`setPerfMode("Light")` cap:40 yazıp §3'ün "torn DLL yok" penceresini geri açıyordu. Kök neden, aynı
"cap aktif mi" kuralının **dört ayrı yerde üç farklı biçimde** yazılmış olmasıydı → kural tek predicate'e
(`RunCoordinator.cs:347` `CapWritableLocked`) ve yazım tek noktaya (`:392` `WritePerfLocked`) toplandı.
**Mutasyon doğrulaması:** guard geri alınınca **1 test**, priority atlanınca **4 test** kırmızı.

**Bilinçli kalan (deferred, zararsız doğrulandı):** `RunCoordinator.cs:420-423` `EffectivePriorityLocked`,
`EffectiveCapLocked`'in simetriği diye tanıtılmasına rağmen **drain durumunu kontrol etmez**; drain sırasında
canlı `setPerfMode` priority'yi hâlâ değiştirebilir. Kod okumasıyla zararsız (yalnız `BelowNormal↔Idle` arası,
asla baseline'dan kötü değil) — eksik olan yalnız doc'ta asimetrinin belirtilmesi.

---

### 1.3 Graf 500–1000 düğümde akıcı + cull ✅ *(iki rakam da bağlayıcı)*

**Commit:** G1 `3229144`+`490d1fa`+`6de03fc` · G2 `5d48331`+`ed13070`+`49dd14d`.

> **⚠️ Başlığın tek başına "6,9×" olması YANILTICIDIR** — o yalnız **ilk görünür alanı** ölçer.
> Kabul kaydına **iki rakam birden** girer (fix1 sonrası yeniden ölçüm, 6 tur dönüşümlü A/B):

| | ilk görünür alan | tüm graf gezildiğinde |
|---|---|---|
| **500 düğüm** | 394,1 → **91,5 ms** (**4,3×**) | 394,1 → **206,2 ms** (**1,9×**) |
| **1000 düğüm** | 934,8 → **136,0 ms** (**6,9×**) | 934,8 → **469,1 ms** (**2,0×**) |

**Düğüm başına görsel nesne: 17 → 9** (test tavanı 18 → 10). Kırılım: tembel rozet alt-ağacı **−6**
(düğümlerin %97'sinde ölü duruyordu), ikon `Viewbox` → paylaşılan **donmuş** `ScaleTransform` **−2**.
Cull ile materyalize düğüm: **500'de 202/500**, **1000'de 206/1000**.

**Otomatik kanıt:** `App/GraphRealizationPerfTests.cs:60` (design boyutu bütçe + 500/1000 kaydı) ·
`:109` `A_graph_node_builds_no_more_than_the_per_node_object_ceiling` · `:132` (500 düğüm **gerçek pencerede**,
App resource zinciri üzerinden) · `App/GraphCullTests.cs:179` (yalnız kameranın gördüğü düğümler kurulur) ·
`:195` · `:237` (seçili düğüm + komşuları **asla** cull'lanmaz) · `:295`/`:341` (uzağa atlama ↔ animasyonlu
pan ayrımı, iki yol da pinli).

**Etiket LOD (G2 fix round 1'in en ağır bloğu — ilk teslim gerçekten kırıktı):** eşik artık etiketin
**çizilen genişliğinden** türetilir (`spacing >= widestLabelWidth`, yeni `GraphLabelMetrics`, T57 advance-width
matematiği yeniden kullanılıyor, **katman başına tek ölçüm**) ve LOD cull'ın küçük-graf kapısına bağlandı:
`src/BuildOrchestrator.App/Graph/GraphView.xaml.cs:50` **`FullDetailMaxNodes = 150`** — **150 ve altında LOD
kodu hiç koşmaz** (küçük graf değişmezliği **yapısal**). Etiketi düşen düğüm tam proje adını veren **tembel
tooltip** taşır. Testler: `GraphCullTests.cs:112` (kalabalık ama küçük grafta **her etiket kalır**) · `:130`
(kapının üstünde kısa etiketler sağ kalır, uzunlar düşer) · `:155` (etiketi düşen düğüm **tooltip** taşır,
nesne kurmadan).
> Bu düzeltme olmasaydı **177 düğümlük gerçek OSYS grafı anonim karelere dönüyordu** (`Graph/` altında tek
> `ToolTip` yoktu).

**G1'in dürüst okuması (implementer'ın kendi ifadesi, review doğruladı): G1 PERF AÇISINDAN NÖTRDÜR.**
500: 418,1 → 400,7 ms · 1000: 864,3 → 814,6 ms · 36: 20,8 → 20,3 ms — fark ölçüm gürültüsünün içinde.
G1'in teslim ettiği şey **doğruluk** (tuval sabit 880'den düğüm sayısına türetiliyor: 4.303/8.553 px; 26px
kareler artık üst üste binmiyor; 36 düğümde tuval **tam 880**'de kalıyor) ve **tekrarlanabilir ölçüm zemini**.

> ### >>> STOP GATE — `DrawingVisual` katman göçü **YAPILMADI** (kontrolcü kararı, ölçüme dayalı)
> G1'in maliyet kırılımı: `SetGraph` **görsel-ağaç kurulumu %64-72** · WPF `Measure/Arrange` **%28-36** ·
> saf layout aritmetiği **%0,03**. Ölçekleme **lineer**. Yani kaldıraç algoritma değil **nesne sayısı**.
> `DrawingVisual` göçü **çizim** maliyetini hedefler, oysa ölçülen darboğaz çizim değil **nesne kurulumu** —
> cull + tembel rozet aynı kazancı, tüm graf render yolunu yeniden yazma riskine girmeden verdi. It-5 son
> iterasyon; plan §244'ün eşiği (ilk realize > 2000ms) hiçbir senaryoda aşılmadı.

**Kapsam dışı ama değerli yakalama (`ed13070` içinde, re-review doğruladı):** `AppFonts` type initializer'ı
`pack://` şemasını kayıt altına almadan `Uri` kuruyordu → WPF'ten önce initialize edilirse şema **kalıcı olarak
zehirleniyordu**. Tek satırlık standart idiom düzeltmesi; **launch-fatal aday** (`c6e9a21` sınıfı).

---

### 1.4 Publish çalışan exe üretiyor ✅ (D1 + `scripts/verify-publish.ps1`)

**Commit:** `23db009` + `39a6e2c` + `d1b7803`.
**Çekirdek kusur (keşifte bulunmuştu):** `CopySupervisorOutput` yalnız `$(OutDir)`'e kopyalayan çıplak bir
`<Copy>`'ydi, `ResolvedFileToPublish`'e hiçbir şey eklemiyordu → **publish edilen uygulamada `supervisor\`
klasörü YOKTU**, engine hiç başlamıyordu ve kullanıcıya **hata bile verilmiyordu**. Review ayrıca sessiz
kırılmanın kalan üç kapısını kapattı: `_SupervisorFiles` boşken **fail-fast guard yoktu** · preflight yalnız
`File.Exists`'i yakalıyordu (`CreateProcessW`'nin senkron hataları generic catch'e düşüyordu) · engine yokken
Sync/Build/Rebuild **aktif kalıyordu** ve konsola **Türkçe** metin sızıyordu (uygulama İngilizce-only).
Aynı tur **77 Türkçe metin hit'i** süpürüldü; `ErrorEvent` tüketicileri yalnız `Code`'a baktığı için
**IPC sözleşmesi kırılmadı**, hiçbir test gevşetilmedi.

> **Kanıt tekrarlanabilir bir script'tir, elle gözlem DEĞİL.** İlk teslimde kanıt tek seferlik elle bir
> gözlemdi (lens 3 bunu 3 Important olarak işaretledi). Şimdi repoda `scripts/verify-publish.ps1` var:
> publish → yerleşim → publish edilmiş supervisor ile NDJSON round-trip → App.exe başlat → supervisor child'ını
> **WMI olayıyla** bekle (poll YOK, D8 uyumlu) → çalışan pencereden **UI Automation** ile konsol boot satırı +
> şeritte hata modu olmaması → kapat/temizle. Re-review hükmü: **yanlış-yeşil VEREMEZ** — her koşullu Check,
> atlanmasına yol açan durumda fail eden bir üst Check'e bağlı ve `catch` her beklenmeyen hatayı FAIL'e yazıyor.

**BU BELGE İÇİN KOŞULDU (2026-07-26, bu makine) — çıktının tamamı:**

```
==> on kosul: acik bir Build Orchestrator ornegi yok
    [PASS] acik ornek yok
==> publish -> C:\Users\Delta\AppData\Local\Temp\bo-verify-publish-5cfa08ad
    [PASS] dotnet publish exit code 0 (exit 0)
==> publish yerlesimi
    [PASS] BuildOrchestrator.App.exe
    [PASS] supervisor\BuildOrchestrator.Supervisor.exe
    [PASS] Assets\GEIST-LICENSE.txt (OFL)
    supervisor\ icerigi: 11 dosya
==> publish edilen supervisor ile NDJSON round-trip
    [PASS] engineReady stdout NDJSON -> {"type":"engineReady","pid":30676,"engineVersion":"1.0.0\u002Bit5"}
    [PASS] supervisor exit code 0 (exit 0)
    [PASS] engineVersion Directory.Build.props degeri -> 1.0.0+it5
==> publish edilen App.exe baslatiliyor
    [PASS] supervisor child process dogdu (pid 34644, parent 28948)
    [PASS] child parent = App process
    [PASS] child publish klasorunden calisiyor -> C:\...\bo-verify-publish-5cfa08ad\supervisor\BuildOrchestrator.Supervisor.exe
==> calisan pencereden UI Automation ile dogrulama
    [PASS] konsol boot satiri "Engine ready - v<surum>" -> 10:23:35 Engine ready — v1.0.0+it5
    [PASS] boot satirindaki surum = engineReady surumu
    [PASS] seritte engine hata modu YOK
    [PASS] pencere gorunur (UIA agaci okundu) (25 metin elemani)
==> kapanis: cascade dogrulamasi + temizlik
    [PASS] App process kapandi (pid 28948)
    [PASS] supervisor CASCADE ile kendiliginden oldu (child pid oldurulmedi) (pid 34644)

RESULT: PASS
```

**16 `Check` + 1 ön koşul satırı = 17/17 PASS**, exit 0. Kanıt satırı: `Engine ready — v1.0.0+it5`.
Geride process/pencere/klasör kalmadı (ayrıca doğrulandı).

> #### §3 cascade artık **ölçülmüş kanıt** (round 2 düzeltmesi)
> İlk hâlde "geride process kalmadı" Check'i **kendi force-kill süpürmesinden SONRA** koşuyordu → **totolojikti,
> hiçbir koşulda fail edemezdi** (ve isme göre makinedeki diğer instance'ları öldürüyordu). Şimdi: yalnız
> **kendi App pid'i** sonlandırılıyor, **supervisor child pid'ine HİÇ DOKUNULMADAN** `WaitForExit(15 sn)` ile
> kendiliğinden ölmesi bekleniyor — cascade kırık olsa Check timeout'a düşüp **FAIL** verirdi. Force-kill yalnız
> güvenlik ağı ve **PID tabanlı**. Yukarıdaki koşumda cascade **gerçekten gözlendi** (pid 34644 dokunulmadan öldü).
>
> **D2'nin nitelemesiyle BERABER okunmalı:** kaskat, **App'in doğurduğu zincir** için tutar. App outer job'ın
> **SAHİBİ ama ÜYESİ DEĞİL** (`App/Services/EngineHost.cs:42` job'u yaratır —
> `JobObject.CreateKillOnClose()`;
> `Core/ProcessControl/JobProcessLauncher.cs:73` yalnız child'ı assign eder) → App'ten **doğrudan** açılan
> `devenv`/`explorer` **hiçbir job'da değildir** ve App ölünce yaşamaya devam eder. (Visual Studio için bu zaten
> istenen davranıştır, ama §6.1'in kaskat garantisi bu niteleme olmadan yazılamaz.)

---

### 1.5 Flourish yalnız stream satırında parlar ✅ (V1, `78f42a3` + fix1 `11f7aa9` + **fix2 `f65b2b3`**)

**T44 kodu DEĞİŞMEDİ** (It-4b/D3'te teslim edilmişti); üç iddiası doğrulandı ve **anti-drift testleriyle
kilitlendi** (`tests/BuildOrchestrator.Tests/App/SuccessFlourishTests.cs`, **12 test**).
**Review turları:** review **PASS** (0 Critical / 6 Important, hepsi kapatıldı) → fix1 re-review **PASS**
(0 Critical / **3 Important AÇIK** → fix round 2 gerekiyordu) → **fix round 2 `f65b2b3`, scoped re-review
PASS / 0 Critical / 0 Important / 1 Minor.** Bu belge ilk yazıldığında (`b59851b`) fix round 2 **henüz
açıktı** ve bu bölüm nitelemesiz ✅ yazılmıştı — **düzeltildi**.

| İddia | Kanıt |
|---|---|
| **Tek satır** parlar | Uygunluk TEK yerde: `App/ViewModels/StreamEventViewModel.cs:63` → `GlowEligible = kind == StreamKind.Done && !anyFailed`. Koşumda `StreamKind.Done` satırı **bir** tanedir (`ViewModels/RunViewModel.Stream.cs:108-113`; `Stopped` → `Info`, parıltı yok). Parıltıyı çalıştıran tek yer `Views/EventStreamView.xaml.cs:504-519`. |
| **Bir kez** oynar | Guard **VM'de**: `EventStreamView.xaml.cs:517` `_vm.GlowPlayed = true` (`StreamEventViewModel.cs:30`) → container recycle / `RebuildRows` taze satır üretse bile `:506`'dan döner. Animasyon `FillBehavior.Stop` + tek `ColorAnimationUsingKeyFrames`, `RepeatBehavior` **yok**. |
| **Liste/grafta dalga YOK** | `Glow\|Ripple\|Confetti\|Celebrat\|Flourish\|Sparkle\|Firework` TÜM `src/` ağacında yalnız iki dosyada: `EventStreamView.xaml.cs` + `StreamEventViewModel.cs`. `ProjectRow`/`GraphView`'de başarıya bağlı **hiçbir animasyon yok**. |

**Test isimleri (davranış pinlenir, API şekli değil):**
`A_clean_run_flourishes_exactly_one_row_and_that_row_is_the_done_row` (ikinci bir kutlama eklenirse toplam 2 →
RED) · `Rebuilding_the_stream_rows_never_replays_the_flourish` (guard view-lokale taşınırsa RED) ·
`A_failed_run_never_flourishes_any_row` (non-vacuous: done satırı VAR) ·
`Reduced_motion_marks_the_flourish_played_without_ever_starting_a_clock` (sinyal sonradan açılınca da
oynatmaz — W2 sözleşmesi) · `A_succeeded_list_row_holds_no_clock_while_a_building_row_breathes` **(non-vacuous
negatif kanıt: building satırı GERÇEKTEN nefes alır)** ·
`Graph_nodes_release_their_clocks_when_they_succeed_instead_of_celebrating` (non-vacuous: building düğüm nabzı
döner) · iki **kaynak guard'ı** + bir **meta-test** (`…go_red_on_a_drifted_owner_but_not_on_a_distant_legitimate_use`).

**Fix round 1'in kapattığı 4 Important** (kaynak: `.superpowers/sdd/2026-07-25-13-40-it5-tdd-plan/task-V1-report.md`
→ "## Fix round 1 (review 4 Important)"; V2'nin 2'siyle birlikte ledger'ın saydığı **6 Important**'ı verir):
guard yalnız WPF API'sini arıyordu → projenin **kendi** animasyon kapıları (`MotionTokens.`,
`TransitionColor`, `SplineTo`, `PopIn.Play`, `PlayReveal`, …) da tarama kümesine girdi · headless'ta
**yapısal olarak hep doğru** olan bir assert kaldırıldı · iki "sıfır parıltı" testine **pozitif kontrol**
eklendi (`Assert.All(rows, r => r.IsLoaded)` — tetikleyicinin gerçekten ateşlendiği kanıtlanıyor) ·
"BİR KEZ"in **animasyon şekli** hem çalışma-zamanı hem kaynak tarafından pinlendi
(`RepeatBehavior.Forever` eklense eskiden 10 test de yeşil kalırdı).

**Fix round 2'nin kapattığı 3 Important (`f65b2b3`, re-review PASS / 0 bulgu):**
- **(I-A)** `SuccessFlourishTests.cs:85-87` — "liste/grafta dalga yok" kaleminin **iki sitesinden biri hâlâ
  pozitif kontrolsüzdü**; kapatıldı.
- **(I-B)** `SuccessFlourishTests.cs:93-96` + `:114-116` — *"`FillBehavior.HoldEnd` runtime'da yakalanır"*
  iddiası **YANLIŞTI**: son keyframe zaten `Colors.Transparent` olduğu için `Stop` ile `HoldEnd`
  **gözlemsel olarak aynıdır** (`EventStreamView.xaml.cs:513-514`). Davranış değil **iddia** düzeltildi.
- **(I-C)** `MsBuildArgumentsTests.cs:65-79` — bkz. §1.8 (tarama kökü `src/` → **repo kökü**).
- Ayrıca **`AutoReverse` guard'a eklendi.**

> #### ⚠️ DÜRÜST SINIR (ledger'a geçti, buraya da geçiyor)
> *"Biri yarın `RepeatBehavior.Forever` eklese testler kırmızı verir mi?"* → **EVET — ama tek testle ve tek
> koşulla** (`SuccessFlourishTests.cs:295-297`; literal `RepeatBehavior`, `EventStreamView.xaml.cs` içinde
> yorum-olmayan bir satırda geçerse). **Tekrar bir yardımcının içine saklanırsa ya da `AutoReverse` dışında
> bir yolla yapılırsa hiçbir test kırmızı vermeyebilir.** Yani "BİR KEZ" iddiası **pinli ama tam kapalı
> değil**; guard bir kaynak-deseni denetimidir, bir ispat değil.
>
> **Kalan tek Minor (fix round 2 sonrası, davranışsal risk YOK):** `*.targets` kolunun ikinci-seviye sağlık
> ağı (`Assert.NotEmpty`) hiç etkinleşmiyor; offender toplama satırı koşulsuz çalıştığı için yarın bir
> `.targets` dosyasına yasaklı literal eklenirse test yine **RED** verir.

---

### 1.6 README + trust-boundary teslim edildi ✅ (D2 `7587349` · D3 `22471ad`+`537f080`)

- **`README.md`** — repoda **İLK KEZ**, İngilizce (D3).
- **`docs/TRUST-BOUNDARY.md`** — v7 T17 kalemi (D2). İlk yazıldığı yer `.claude/outputs/` idi (kontrolcü
  hatası); D3'te `git mv` ile kalıcı yerine taşındı, **rename `similarity index 100%`**, geçmiş korundu.

**Doküman review'ının tek kriteri DOĞRULUKTU ve işini gördü:** 60+ iddia kodda birebir doğrulandı, **7 iddia
kodla uyuşmuyordu ve düzeltildi** —
`Alt+B` için "configurable in Settings" (Settings'te hotkey arayüzü YOK) · konsol notunun **yalnız mid-run**
yazıldığı ve önünde `HH:mm:ss` damgası olduğu atlanmış · "commit + `git diff`" (kod `git diff`i **hiç**
çalıştırmıyor: `rev-parse` / `status --porcelain` / `ls-tree -r HEAD`) · "17 checks" (16 `Check` + 1 ön koşul) ·
"Sync required" metninin yüzeyi karıştırılmış · **"Malformed commands never take the Supervisor down" YANLIŞ**
(framing hatası **exit 2** ile öldürüyor, `Supervisor/SupervisorHost.cs:56-57`; `:58-59`'daki `badCommand`
catch'i ise `continue` eder, öldürmez — ilginç olan, README'nin kendi
linklediği `docs/TRUST-BOUNDARY.md` bunu DOĞRU yazıyordu) · İngilizce README'nin tamamen Türkçe belgeye uyarısız
link vermesi.

**Doğru çıkanlar (kayda değer):** K11 perf tablosunun 9 hücresi (`Core/ProcessControl/PerfProfile.cs:32-34`) ·
konsol notu gövdesi **byte düzeyinde birebir** (ayırıcı `0xC2 0xB7` = **U+00B7**) · üç dürüst niteleme de
yerinde (cap Sync/IPC'yi sınırlamaz · copy penceresinde Balanced tabanına yükselir · parallelism run başında
sabitlenir) · publish bölümü · lisans dürüst (repoda `LICENSE` yok, uydurma lisans yok).

---

### 1.7 It-4 kabulünün **#5 kalemi KAPANDI** — ama bütçe TUTMADI ✅⚠️ (L1, `3c69d78`)

It-4 acceptance matrisi (`.claude/outputs/2026-07-25-04-12-it4-records.md` §1) satır **#5**:
*"500–1000 kart+node akıcı"* — tek **AUTO** ama **"henüz ölçülmedi"** işaretli kalemdi. It-5'te **ölçüldü** ve
**karar kayda geçti**.

**ÖLÇÜM (aynı makine, aynı yöntem, medyan):** 191 satırın ilk realize'i
**787,3 ms → 487,5 ms** (**%38 iyileşme**).
- Öncesi 3 koşum: 779,4 / 807,9 / 787,3
- Sonrası 4 koşum: 474,8 / 481,5 / 493,5 / 538,9
- Satır başına görsel nesne: **55 → 39**.

**Ne yapıldı:** proje listesi satır kartında hover ikonları + VS-chooser popup'ı artık **ilk hover'da lazy**
kuruluyor (tek kurulum noktası `App/Views/ProjectRow.xaml.cs::EnsureActions`, yeni XAML kökü
`ProjectRowActions.xaml`). Sticky/follow/arbiter dosyalarında **0 değişiklik** (kullanıcının şart koştuğu sınır).
**Testler:** `App/ListRealizationPerfTests.cs:43`
`Realizing_191_project_rows_measure_and_arrange_stays_under_the_400ms_budget` (yumuşak bütçe + sanity ceiling
deseni) · `:61` `An_unhovered_project_row_builds_no_more_than_the_per_row_object_ceiling`.

> ### ❌ **BÜTÇE (400 ms) TUTMADI — 87 ms açık.** Gizlenmiyor.
> Kalan maliyetin **~%65'i** kartın XAML instantiation'ı (191 satır için **276 ms**). Görsel risk almadan
> çıkarılabilecek ~5 nesne daha ≈ **35–60 ms**, yani **en iyi ihtimalle ~430 ms** — bütçe kart sadeleştirmesiyle
> tek başına kapanmıyor.

> ### >>> KULLANICI KARARI (2026-07-25, karar kapısı): **"487 ms'i kabul et, virtualization'a girme."**
> **L2 (virtualization) AÇILMADI ve It-5'te AÇILMAYACAK.** Gerekçe (ledger'dan birebir): gerçek senaryo
> **191 satır** ve bu **tek seferlik** ilk-realize maliyeti; virtualization ise `StickyLayerList` sticky
> overlay + `LayoutMetrics` kümülatif offset tablosu + `FollowScrollController.ScrollTargetForRow` +
> `ScrollArbiter`'ı **aynı anda** riske atıyor ve **It-5 SON iterasyon** — regresyonu yakalayacak bir sonraki
> tur yok.
>
> → **It-4 kabulünün #5 kalemi BURADA KAPANDI:** ölçüldü, karar kayda geçti. Graf ayağı (500–1000 düğüm)
> §1.3'te **bütçesiz ama ölçülmüş** olarak kapandı; **liste ayağı bütçeyi tutmadan, kararla** kapandı.

---

### 1.8 T33 kararı: shared compilation **KAPALI** kalıyor ✅ (V2, `e4904bd` + fix1 `11f7aa9` + **fix2 `f65b2b3`**)

**Karar kaydı:** `.claude/outputs/2026-07-26-07-38-t33-decision.md`.
`-p:UseSharedCompilation=false` ve `-nodeReuse:false` **KORUNUR**.
**Reddedilen şey küçük bir optimizasyon değil:** spike ölçümü (`.claude/outputs/2026-07-16-10-20-spike-results.md:13`,
birebir alıntı) — flag'ler kapalıyken **47–50 sn**, açıkken **16–21 sn** → **≈2,5–3×**; kazancın tamamı shared
compilation'dan. Takas bilinçli: **doğruluk > hız**.
**Gerekçe tek mantığa indirgenmiş:** Job üyeliği **yalnız** "biz doğurduk → suspended → assign → resume"
zincirinden doğar (tek çağıran `Core/MsBuild/MsBuildInvoker.cs:68`); `VBCSCompiler`'ı biz doğurmayız →
üyeliği **garanti edilemez** (üç olası durum da tek tek çürütüldü) → kaskattan sağ çıkan bir yazıcı = **torn DLL**.
**Pinler:** `tests/BuildOrchestrator.Tests/MsBuild/MsBuildArgumentsTests.cs` →
`Shared_compilation_and_node_reuse_can_not_be_re_enabled_on_the_build_path` ·
`The_restore_path_compiles_nothing_which_is_why_it_carries_no_compiler_flags` (restore hedef kümesi TAM olarak
`["-t:restore"]`) · `The_compiler_server_switches_have_exactly_one_source_in_the_production_tree`
(literaller üretim ağacında yalnız `Core/MsBuild/MsBuildArguments.cs:11`'de geçebilir).

> #### ⚠️ Bu son pinin fix round 1'de **bilinen bir boşluğu** vardı — fix round 2'de (`f65b2b3`) kapatıldı
> Fix round 1'de "TEK KAYNAK" pininin tarama kökü `<repo>/src` idi (`MsBuildArgumentsTests.cs:65-79`). Oysa
> `src/` altında **hiç** `*.props` / `*.targets` dosyası yok — tek `Directory.Build.props` **repo kökünde**.
> Yani döngünün props/targets kolu **sıfır dosya tarıyordu**: flag oraya eklenirse pin fark etmezdi.
> **Fix round 2:** tarama kökü **repo köküne** çıkarıldı (kökteki `Directory.Build.props` artık gerçekten
> taranıyor) ve **sıfır-dosya durumu artık RED veriyor**. Bu belge ilk yazıldığında (`b59851b`) bu boşluk
> **açıktı ve belgede yazılmamıştı** — düzeltildi. (Kalan tek Minor için bkz. §1.5 sonundaki not.)

**Açık sınırlama kayda geçti:** child'lar parent ortamını **miras alır** (`JobProcessLauncher.cs:64-66`,
`lpEnvironment = NULL`); ana risk kapalı (komut satırındaki `-p:` global property env'i ezer), kalan yüzey
(miras gelen ortam, belgelenmemiş knob'lar) **pinsiz** olarak yazıldı.
**Yeniden açılma koşulları** (üçünden biri): üyelik kanıtı (gerçek kill testi, N koşumda 0 orphan/0 torn DLL) ·
emit'i `MSBuild.exe`'nin kendi process'inde bırakan bir seçenek · koşuma özel staging + atomik yayımlama.

---

### 1.9 Ek: It-5'in diğer teslimatları (kabul maddesi değil, kayda değer)

- **W1 (`4be8b0f`, review 0 Critical / 0 Important — fix wave GEREKMEDİ):** proje başına commit sha'sı uçtan uca
  bağlandı. Kapatılan 3 üretim kusuru: `TargetSha` 40 haneli ham hex olarak 118px slot'a basılıyordu →
  **7 haneye** kısaltılıyor · sha slot'u ilk Sync'ten sonra **kalıcı boş** kalıyordu · `BuiltCommit` **hiç
  taşınmıyordu**. Çözümün şekli: sıra hatası yamayla değil **veri yönü tersine çevrilerek** kapatıldı
  (`TargetSha` artık kart tarafından ata ağaçtan çekilmiyor, `RunViewModel`'den satır VM'ine **itiliyor**).
  118px kanıtı somut: `ProjectRowTests.The_rendered_sha_pair_fits_inside_the_118px_right_block` kartı **gerçek
  pencerede** kurup **gerçek Geist Mono** ile ölçüyor + kontrol grubu (ham 40-hex çift > 118px). Sıra sorunu
  testle gerçekten kilitli: `RunViewModelTests.cs:680-705` ve `ProjectRowTests.cs:575-591` olayları **TERS**
  sırayla tetikliyor. Kısaltma tek kaynakta: `App/ViewModels/RunViewModel.ActionBar.cs:63` `Short7`.
  **Hiç derlenmemiş proje kararı:** sol yarı boş bırakılır, çift yerine **yalnız hedef sha** basılır — yer
  tutucu **UYDURULMAZ** (design-v1'de bu durumun karşılığı yok; prototip her projeye sentetik curSha üretir).
- **W2 (`804da6b` + `e9a3c7b`):** motion seam'leri ortak tiplere katlandı (`Controls/MotionGate.cs`,
  `Controls/RevealStagger.cs`, `Views/PopoverBase.cs`). Ölçek: `App.Motion?.AnimationsEnabled ?? false`
  **9 kopya → 1** · subscribe-once kablajı **5 → 1** · reveal hero/kuşak/release muhasebesi **2 → 1** ·
  popover iskeleti **2 → 1** (8 sahip + 2 popover, 12 dosya). **Yöntem doğruydu:** fold'dan ÖNCE davranışı
  pinleyen 4 test yazıldı ve **eski kodda yeşil** olduğu doğrulandı; fold sonrası aynı testler DEĞİŞMEDEN yeşil
  kaldı. Hüküm: *"Fold davranış-nötrdür; G2'nin geç-katılan-düğüm yolu BASE ile **BYTE-AYNI**."*
  Fix round 1 bir **LATENT BUG** kapattı: `PopoverBase` ctor'u türevin `InitializeComponent()`'inden ÖNCE
  `DataContextChanged`/`Loaded` bağlıyordu → kablaj `OnInitialized`'a taşındı; XAML yükleme sırasını taklit eden
  `XamlLikePopover` testi eski kablajda **gerçekten RED** veriyor.
- **T1 (`163fce5`+`4281a81`+`a086c1a`+`a5a5053`+`31a3940`):** T49 token **FINAL PASS**. **Gerçek drift = 1 adet**
  (`Spikes/FontAbWindow.xaml.cs:34` `BorderStrong = "#2a2a30"` → `Brush.BorderStrong` `#3a3a42`; aynı dosyadaki
  12 ham hex de token'a bağlandı). Token katmanı otoriteyle **birebirdi** (Tokens.xaml 56/56 brush, Motion.xaml
  7/7, 108 `*.cs` tarandı) — kusur **değerlerde değil, drift'in GÖRÜNMEMESİNDEydi** (renk taraması yalnız
  `*.xaml`'e bakıyordu). Kalıcı ürün: renk taraması `*.cs`'e genişletildi · ham-ms motion guard'ı ·
  **D8 sleep-poll guard'ı** (izin listesi 6 yol, her biri adet-pinli ve gerekçeli) · çok-satırlı ihlalleri de
  yakalayan tarama (mutasyonla kanıtlandı). **W1'den devreden D8 ihlali kapatıldı:**
  `Core/State/BuildStateStore.cs:136` `Thread.Sleep(5)` → enjekte edilmiş gecikme seam'i (semantik birebir
  korundu, seam `internal` + IVT yalnız test projesine, üretimde hiçbir yerde set edilmiyor → **no-op'a düşemez**).

---

## 2. Kapanmayan / bilinçli park edilen kalemler (tek liste)

Aşağıdakiler It-5'te **bilerek** kapatılmadı. Liste, ledger'daki (`.superpowers/sdd/progress.md`, It-5
bölümü) **tüm** `minor (deferred)` / "bilinçli park" / "bilerek düzeltilmeyen" kayıtları taranarak kuruldu —
**eksiksiz olması bu listenin tek değeridir**. Her satır: **ne** + **neden**. Sıra task sırasıdır.

| # | Task | Kalem | Gerekçe / durum |
|---|---|---|---|
| 1 | P1 | **Bilerek düzeltilmeyen 3 kalem:** `ClearPriorityClass` yokluğu · `Dispose`↔`Set` **TOCTOU** · `PerfProfileParityTests`'te `Category` trait yokluğu | `Normal` zaten "kısıtlama yok" demek olduğu için ayrı bir clear'a gerek görülmedi; TOCTOU **mevcut `JobObject` deseninin tamamında** var (P1'in getirdiği bir şey değil); trait yokluğu testin önceki hâlinde de vardı. Ledger `progress.md:39-41` |
| 2 | P3 | `RunCoordinator.cs:422-424` `EffectivePriorityLocked`, `EffectiveCapLocked`'in simetriği diye tanıtılmasına rağmen **drain durumunu kontrol etmez** | Kod okumasıyla **zararsız** doğrulandı (yalnız `BelowNormal↔Idle` arası, asla baseline'dan kötü değil); eksik olan yalnız doc'ta asimetrinin belirtilmesi (§1.2) |
| 3 | L1 | **L2 (liste virtualization) AÇILMADI** — bütçe 400 ms tutmadı (487,5 ms) | **Kullanıcı kararı** (2026-07-25): gerçek senaryo 191 satır, maliyet tek seferlik; virtualization sticky overlay + `LayoutMetrics` + follow + arbiter'ı **aynı anda** riske atar ve It-5 son iterasyon (§1.7) |
| 4 | L1 | **4 Minor:** `ProjectRowTests.cs:179-180,188` zayıf `Assert.NotNull(...Style)` · `ListRealizationPerfTests.cs:37` dar tavan (40) · `ProjectRow.xaml.cs:151` `OnLoaded` savunma dalı testsiz · `ProjectRow.xaml.cs:165-173` `OnDataContextChanged` `_hover`/`_actions`'ı sıfırlamıyor | Sonuncusu **L2 açılırsa CANLANIR — açılmadı**; bugün ölü yol, çünkü `StickyLayerList.xaml.cs:109` her `SetGroups`'ta `ItemsSource`'u tam resetliyor |
| 5 | G1 | **`DrawingVisual` katman göçü YAPILMADI** | **STOP GATE, ölçüme dayalı**: darboğaz çizim değil **nesne kurulumu** (%0,03 layout aritmetiği ↔ %64-72 görsel-ağaç kurulumu); cull + tembel rozet aynı kazancı, tüm render yolunu yeniden yazma riskine girmeden verdi (§1.3) |
| 6 | G1 | **3 Minor:** 1000 düğümlük en geniş senaryo yalnız **headless** `Measure/Arrange` ile ölçüldü (gerçek-HWND realize testi yalnız 500'ü kapsıyor, `GraphRealizationPerfTests.cs:326-347`) · compute **çift** çalıştırılıyor, faz-medyan toplamı headline medyanla **%1,4-2,7** örtüşmüyor (`GraphRealizationPerfTests.cs:397-410` + `GraphView.xaml.cs:218`) · perf testlerinin süite eklediği süre **ölçülmedi** (tahmin ~5,5-6 s) | Birincisinde risk düşük (Effect/CacheMode/ScrollViewer yok); ikincisi istatistiksel olarak beklenen ve sonucu değiştirmiyor. Ledger `progress.md:178-184` |
| 7 | G2 | **~17 Minor:** stale `_neighbourSet` · layout-pass içi senkron materyalizasyon / `SizeChanged` O(N+E) · 0×0 `_nodeLayer` altında hit-test'in testsiz olması · `IconPaint` self-heal turunun fast-path'le kalkması · `NodeBounds`'un halka+rozet taşmasını içermemesi · longest-seçiminin iki yerde olması · ölçüm başarısızlığında fail-closed · 0px etiket boşluğu · B2 testinin duvar saatine bağlılığı · hero release'in geç katılımı kapsamaması · C2 tooltip ayağının yalnız string doğrulaması · **`AutomationProperties.Name` eksiği (a11y)** · `[G2 full]` ölçümünün süit maliyeti | Hiçbiri Critical/Important değil; It-5 son iterasyon, motion/graf koduna ek dokunuş riski. Ledger `progress.md:228-233` |
| 8 | W1 | **4 Minor:** `task-W1-report.md:90` gerekçe metnindeki abartı ("amber dot zaten ayırt ediyor" — oysa `WillBuildEvaluator.cs:16` never-built ile dirty-ama-eskiden-derlenmişi AYNI `WillBuild=true` ile işaretliyor) · `ProjectRow.xaml.cs:327` simetrik olmayan "yalın-ok" (sağ yarı boşken `"a3f81c2 → "`) · `SyncWorkspaceService.cs:387-390` imza genişlemesi · `:114-140` teorik exception penceresi | Bloklayıcı değil; "yalın-ok" pratikte yalnız tek bir Sync'in `buildPreview↔syncCompleted` arası **anlık (I/O'suz)** penceresinde oluşur ve bilerek testle pinlenmiş |
| 9 | W2 | **Guard'ın 4 kopyası + "gecikmeli yükseliş" primitifinin 3 kopyası KATLANMADI** | **Kontrolcü kararı:** motion kodunda son iterasyonda alınacak regresyon riski, kazancı (yalnız temizlik) haklı çıkarmıyor |
| 10 | W2 | **7 Minor:** `PlayRevealStagger` değerlendirme sırası (`GraphView.xaml.cs:997`, `StickyLayerList.xaml.cs:236-237`; `MotionCoordinator.EndHero` callback'siz olduğu için nötr) · latch-first kipe eklenen `-=` (`MotionGate.cs:70`, guard nedeniyle kanıtlanabilir no-op) · motion-changed `sender` kimliğinin `IMotionSettings` → `MotionGate` olması · StickyRibbon için ayrı latch pin'i yok (dolaylı `ReducedMotionCoverageTests` kapsıyor) · latch'siz kipte kaynak canlıyken değişirse eski aboneliğin bırakılmaması (BASE davranışı, artık pinli) · `StatusGlyph` seam'inin iç `BuildingSpinner`'a yayılmaması · `EventStreamView` canlı-abonelik testinin yalnız "sinyal KAPANIYOR" yönünü pinlemesi | Hiçbiri davranış değiştirmiyor; çoğu **kanıtlanabilir no-op** ya da BASE'den devralınan ve artık pinlenmiş durum. Ledger `progress.md:309-317` |
| 11 | D1 | **8 Minor:** Restart'ta sürüm satırı yok (`RunViewModel.cs:539-550`) · `+it5` etiketi ürün kimliğinde kalıcı · `FileWrites` çıktısı muhtemelen ölü (`csproj:96-102`) · PS 5.1'de `2>&1`+`Stop` kullanımı · temp klasör silme doğrulanmıyor · `ContinueOnError` yorumunun yarı doğruluğu · `MSBuild` task'ının `ProjectReference` metadata'sını taşımaması · `AddSupervisorToPublishList` guard testinin yalnız **XML-yapısal** olması | Bloklayıcı değil. Ledger `progress.md:386-390` |
| 12 | T1 | **4 Minor:** `GraphView.xaml`'de **ÖNCEDEN var olan** ve T1'in ifşa ettiği merge-zinciri boşluğu · gecikme seam'i varsayılanının `attempt` parametresini kullanmaması · guard/iskeletin klasör-namespace yeri · **"33/103 kullanılmayan token" rakamı** (lens **doğrulayamadı**, yalnız bilgi amaçlı) | Hiçbiri T1'in getirdiği bir kusur değil; sonuncusu doğrulanmamış bir sayı olarak işaretli. Ledger `progress.md:440-442` |
| 13 | T1 | **`Show()` başlatma yolu realize kapsamı DIŞINDA** | Re-review hükmü: `MainWindow`'un **ölçülen içerik ağacı** kapalı (M3 en az iki mekanizmayla, M1/M2 iki bağımsız ağla), ama `Show()` başlatma yolu (tepsi/hotkey/motor) ve Collapsed dalların şablonla doğacak içeriği hâlâ kapsam dışı. Ayrıca `Spikes/FontAbWindow.xaml` realize-testsiz kalan tek XAML kökü (bilinçli; gerçek `Application` gerektiriyor) |
| 14 | D2 | **`debugSpawnChildren` üretim derlemesinde de dinleniyor** | `Contracts/Ipc/IpcMessages.cs:22` (discriminator) + `Supervisor/SupervisorHost.cs:80` (dispatch). Saldırı yüzeyi dar (stdio, App'in kendi child'ı) ama üretimde duran bir debug hook'u — final review'a taşındı |
| 15 | V1/V2 | **DÜRÜST SINIR:** "BİR KEZ" iddiası pinli ama **tam kapalı değil** — `RepeatBehavior.Forever` **tek testle ve tek koşulla** yakalanır (`SuccessFlourishTests.cs:295-297`); tekrar bir **yardımcının içine saklanırsa** ya da **`AutoReverse` dışında** bir yolla yapılırsa yakalanmayabilir | Guard bir **kaynak-deseni denetimi**dir, ispat değil. Ledger'da kayıtlı (§1.5) |
| 16 | V1/V2 | **Kalan tek Minor (fix round 2 sonrası):** `*.targets` kolunun **ikinci-seviye sağlık ağı** (`Assert.NotEmpty`) hiç etkinleşmiyor | **Davranışsal risk YOK:** offender toplama satırı koşulsuz çalışıyor; yarın bir `.targets` dosyasına yasaklı literal eklenirse test yine **RED** verir (§1.8) |
| 17 | D3 | **`CLAUDE.md`'de 3 bayat ifade düzeltilmedi** | **Kullanıcı kararına bırakıldı**; playbook **`A11`** bölümünde tablolaştırıldı: (a) "her projeyi `dotnet build` ile shell-out" ↔ kod `MSBuild.exe` çalıştırıyor · (b) test projesi `net10.0` ↔ gerçekte `net10.0-windows` + `UseWPF` · (c) "Kod henüz yoktur" ↔ It-0→It-5 tamam, süit yeşil |
| 18 | V3 | **Tüm GÖZLE KONTROL borcu** (It-4b'nin ~81 kalemi + E6 Adım 3 + It-5'in kendi görsel kalemleri) | Harness ekran görüntüsü alamaz → **kullanıcı pası**. Tek yürünebilir liste: `.claude/outputs/2026-07-26-10-17-visual-check-walkthrough.md` (D4 **ZORUNLU** işaretli) |

**Ayrıca — kayıt doğruluğu (kontrolcü düzeltmesi bekliyor, bu belge kapsamı dışı):**
`.superpowers/sdd/2026-07-25-13-40-it5-tdd-plan/task-D2-report.md:3` hâlâ `docs/TRUST-BOUNDARY.md`'nin
**eski** `.claude/outputs/…` yolunu veriyor. Başka bir task'ın artefaktı olduğu için D3 implementer'ı
dokunmadı. *(Ledger `progress.md:320` bu listede DEĞİL: o satır doğru yolu — `docs/TRUST-BOUNDARY.md` —
veriyor, `.claude/outputs/` orada yalnız tarihsel bir not olarak geçiyor.)*

---

## 3. Süit ve build durumu (final)

- **Build:** `dotnet build BuildOrchestrator.slnx` → **0 Uyarı / 0 Hata**.
- **Tam süit (acceptance dahil):** **1428 passed · 2 skipped · 0 failed** (toplam **1430**).
- **Canlı OSYS acceptance:** **3 / 3** (`Category=Acceptance`, gerçek Supervisor + gerçek MSBuild, 1 dk 22 sn).
- **Non-acceptance:** **1425 passed · 2 skipped · 0 failed**.
- **Publish:** `scripts/verify-publish.ps1` → **PASS, 17/17**, exit 0.

**Süitin It-5 boyunca büyümesi (ledger'dan):**
1203 (baseline) → P1 **1223** → P2 **1247 / 1 / 1** ⚠️ → DRAG **1248** → P3 **1266** → L1 **1273** →
G1 **1280** → G2 **1307** → W1 **1318** → W2 **1330** → D1 **1408** → T1 **1413** → D3 (baseline sayısı
karışıklığı, aşağıda) → V1+V2 **1428**.

⚠️ **"Her task sonunda 0 failed" DOĞRU DEĞİL:** **P2** `1247 passed / 1 skipped / **1 failed**` ile kapandı
(ledger `progress.md:83-84`). O tek kırmızı bir ortam flake'iydi (`DragReorderTests`) ve **DRAG task'ıyla**
kapatıldı — kök neden tanı testiyle kanıtlandı (`Mouse.PrimaryDevice.ActiveSource == null`), test görünür
`Skip.If`'e alındı, üretim kodu değişmedi. P2 dışındaki tüm task'lar **0 failed** ile kapandı.

**D3 turundaki baseline sayısı karışıklığı:** `Category=Acceptance` trait'li **3 test** filtreli koşumun
dışında kalıyordu; kontrolcü çözdü — **1421 + 3 = 1424** (ledger `progress.md:463-464`). Bu belgedeki sayılar
o karışıklıktan **bağımsızdır**: **filtresiz tam koşumdan** ve **acceptance-only koşumdan** doğrudan
alınmıştır.

---

## 4. Bu belgenin kapsamadığı tek şey

**Görsel doğrulama.** It-5'in ve It-4b'nin tüm görsel kabul borcu tek yürünebilir listeye dönüştürüldü:

→ **`.claude/outputs/2026-07-26-10-17-visual-check-walkthrough.md`**
  (BÖLÜM 1: It-4b'nin ~81 kalemi panel panel · BÖLÜM 2: prototiple yan yana, design-v1 §2.1–§2.9 ·
   BÖLÜM 3: It-5'in kendi görsel kalemleri — perf chip/konsol notu, 500+ graf + LOD + tooltip, publish edilen
   exe'nin ilk açılışı, W1 sha göstergesi, W2/G2 geç-materyalize reveal, L1 ilk-hover)

O liste **kullanıcı tarafından** yürünecek; çıkan sapmalar ya **fix wave**'e ya **A13.1 "algısal eşdeğer"**
sınıfına gider (belgenin sonunda her ikisinin de nereye yazılacağı yazılı).
